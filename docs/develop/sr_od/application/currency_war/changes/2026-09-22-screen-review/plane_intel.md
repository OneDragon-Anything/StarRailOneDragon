# T-25 位面情报 修法设计(plane_intel)

## 0. 元信息

- 迭代目标:货币战争全画面规范符合性审查(2026-09-22-screen-review)·只设计不落码,待用户裁决
- 状态:对抗收敛待用户裁决+新规范增补待裁决(对抗轨迹:r1 未收敛 4 条→修订→r2 未收敛 1 条行级→修订→r3 定点复核收敛 0 条;新规范增补(2026-09-22 两条款)= §3,重审输入 = `.debug/progress/2026-09-22-cw-screen-review/reports/respec-T16-T25.md`)
- 审查输入:`.debug/progress/2026-09-22-cw-screen-review/reports/T-25-r1.md`(发现 F-1..F-5;总判定 = 有问题,高 0 中 0 低 5)
- 真值基线:本稿全部「现状」陈述以审查时点工作树代码为准,落地时若代码已再变,以落地时点代码重新对账后再动笔。符号锚 = `文件::符号名`,行号仅作本稿定位辅助。文档路径根 = `docs/develop/sr_od/application/currency_war/`,代码路径根 = `src/sr_od/application/currency_war/`(下文反引号短路径均相对此两根)。
- 修法性质:F-1..F-5 全部为注释清理与画面篇格式修正,**零行为变化**(无任何逻辑/签名/常量/建档/测试改动)。
- 清理准则:F-1..F-3 注释清理按 T-3 稿([battle_wait.md](battle_wait.md))§2.6 五条准则机械执行(禁形一·会话局部标识符 / 禁形二·变更史叙述 / 改写方向 = 结论→出处→边界 / 保留豁免 / 跨修法交叠一次成文),本屏特有豁免面见 1.1–1.3「明确不解决」与各验证门。

## 1. 问题与动机

### 1.1 F-1 采集 op 模块 docstring 含变更史(旧裁决退役叙事)(低)

- **现状症状**(审查发现 F-1):`operations/cw_screen/cw_screen_plane_intel.py` 模块 docstring 职责首句「职责:在「货币战争-位面详情」屏恒全采三位面情报(用户裁定"先按能采处理",原"只采当前及之后"裁决退役):」——「原 X 裁决退役」= 从什么改成什么的变更叙事(源码跨两行折行)。
- **根因归层**(根源两问):①根在哪层 = **约定层**——AGENTS.md §8「变更史不进注释」为后立规范,存量注释未回溯清理;恒全采裁决变更时,正本 `screens/plane_intel.md` §3 的同语义句在册、模块头同款措辞漏扫;②修的是根还是症状 = 删变更史括注、当前语义「恒全采」自足成立即治本;『全仓存量注释卫生』的流程级根已由 T-3 稿 §2.6 登记的全仓注释卫生批立案(跨件半问:与 T-3 F-6 / T-22 F-3 同根,共用同一登记,不另立第二套机制),本稿只清审查对象。
- **解决到哪**:模块 docstring 该括注整段删除(§2.1);审查检查⑥仅列此三处注释命中,同文件其余注释面核对通过,不动。
- **明确不解决**:同文件「识别失败响亮暴露」「兜底字面量(1080p 实测值)」「纹章风头像识别能力缺口」等现役语义注释(禁借清理之名改写);`kernel/cw_screen_report/plane_intel.py` 与编排 op 注释(F-2/F-3 点位外零命中);全仓注释卫生(卫生批);「先按能采处理」在他处的引文——`changes/2026-09-20-takeover-intel-pipeline/design.md` 内的同款裁决句属迭代过程区自辖(引用寿命 = 迭代),不属本批。

### 1.2 F-2 kernel 屏文件注释引用 changes/ 迭代目录(低)

- **现状症状**(审查发现 F-2):`kernel/cw_screen_report/plane_intel.py` 模块 docstring「写门渠道签名按迭代设计(design §2.3,2026-09-20-takeover-intel-pipeline)定死:…」——代码注释长期引用 `changes/` 迭代目录,命中 AGENTS.md §9 铁律「代码与正本文档禁引其内容(长期引用),正本必须自足」。
- **根因归层**:①根在哪层 = **约定层**——该引用写于迭代落地期(迭代内工件引用总纲合法,寿命 = 迭代,iteration-design.md §1 引用寿命分层),迭代收尾正本更新后注释指针未随寿命到期清出;②修的是根还是症状 = 指针删除即治本:签名四元组的单一源本就住本文件 `_WRITE_SIG` 常量(紧随其后的 inline 注释逐位重述)与正本(`screens/plane_intel.md` §6、`../game_state/fields.md` §3.1.3,均在档自足),迭代目录指针零导航增量,删后不留第二源。
- **解决到哪**:括注清出,句收敛为「按设计定死」+ 现役语义逐字保留(§2.2)。
- **明确不解决**:同文件「对账网已随死机制退役,不在本写门」句(审查核对通过,未列禁形;若用户裁决按严格禁形二连带清扫此类退役申报句,归全仓注释卫生批统一口径,不混入本批);`changes/2026-09-20-takeover-intel-pipeline/` 目录本体(过程区自辖);`../game_state/fields.md` §3.1.3(自足,零改动)。

### 1.3 F-3 注释持久索引锚停粗节(§7.3 → 最近锚 §7.6)(低)

- **现状症状**(审查发现 F-3;依据按权威源实文核正):`operations/cw_screen/cw_screen_plane_intel.py::CwScreenPlaneIntel.report` 词缀登记注释「登记面纪律 = effect-domain.md §7.3」——锚停在**粗锚**:§7.3 = 「驱动事件映射」节,节内实有通用「登记挂点纪律」段(登记面 best-effort 不阻塞主链;「词缀源登记挂点 = `cw_affix_effects.register_affixes_from_names`(简报屏/位面详情屏两链,§7.6)」,自带通往 §7.6 的交叉指针)——按之查通用纪律不落空;但本注释语境是词缀登记专属语义(命中结构化注册 / 在册条目幂等 / 不双登记),该语义住 **§7.6**「词缀源(source='affix')」,最近锚应指 §7.6。
- **根因归层**:①根在哪层 = **表示层**——索引锚可解析但非语义最近锚(通用挂点纪律粗锚 vs 词缀源专属节),词缀语境的注释停在粗锚、未随精度归位;②修的是根还是症状 = 精度归位至最近锚 + 节名兜底即治本,非语义/流程层问题。
- **解决到哪**:§7.3 → §7.6,附节名「词缀源节」双定位(§2.3)。
- **明确不解决**:①他处 §7.3 引用按各自指向语义成立,**不因本修法连带失效,落地批禁连带清扫**——`kernel/cw_affix_effects.py:409`(「§7.3『登记挂点纪律』」,该段实存)、`kernel/cw_effect_inventory.py:287`(登记面纪律)与 `:1161`(「禁 logic 直写防双计」= §7.3 实存语义)均合法;②`operations/cw_screen/cw_screen_briefing.py:104` 同语境同句式「登记面纪律 = effect-domain.md §7.3」(词缀登记,简报屏读链产出点)——按本稿同判应改指 §7.6,**归简报屏稿([briefing.md](briefing.md))辖域;该稿现未列此句,随汇总批并入登记,不扩本稿文件面**;③`../game_state/fields.md` §3.1.3 的「见 effect-domain.md §7 词缀源节」(章级指针可解析,原样认可);④`effect-domain.md` 本体与节号稳定性(若后续重构致节号再漂移,随彼时收尾清查回查,本稿不为节号漂移立机制)。

### 1.4 F-4 画面篇两处裁定日期入正文(低)

- **现状症状**(审查发现 F-4):`screens/plane_intel.md` §2「(6 node,**用户裁定 2026-09-20** 豁免 [op-layer.md](op-layer.md) §1.1 两 node)」、§3「**恒全采位面1→2→3**(**用户裁定 2026-09-20**「先按能采处理」):」——裁定日期两处入画面文档正文,命中 `screens/README.md` §2 纪律行「as-built 无状态(事故史/裁定日期不进正文)」。
- **根因归层**:①根在哪层 = **约定层**——豁免申报与恒全采语义本有规范出处(`screens/op-layer.md` §1.1/§3 多屏管线行、本篇 §3/§7),日期属过程信息,成文时随叙事带入正本;②修的是根还是症状 = 语义留、日期去,删后各句自足可解析,零信息损失;与 F-1 同根家族(裁定叙事入记载面),载体不同(正本文档 vs 代码注释),修法同向一次定死。
- **解决到哪**:两处「用户裁定 2026-09-20」清出(§2.4);豁免事实、出处指针与恒全采语义逐字保留。
- **明确不解决**:两处既有指针指向形式(`screens/op-layer.md` §1.1 = 被豁免的两 node 规则本体,持久可解析,不换指);正文其余句(审查核一致);兄弟画面篇同类日期残留(归各屏稿)。

### 1.5 F-5 画面篇 7 处坐标裸 area 名(缺「画面名.area名」全形)(低)

- **现状症状**(审查发现 F-5):`screens/plane_intel.md` §3(L36 boss 条「按钮-位面卡1..3」与「区域-boss大图标」两处、L38 词缀条「区域-词缀横条」)与 §4 动作锚表(L59「按钮-位面卡1/2/3」;L61「标识-位面详情标题」;L62「按钮-关闭位面详情」与「标识-位面详情标题」两处)共 **7 处**裸 area 名;其中「区域-boss大图标」「区域-词缀横条」「标识-位面详情标题」「按钮-关闭位面详情」全篇无全形首注;同篇 §4 对照表 3 处已用全形,两种格式并存。
- **根因归层**:①根在哪层 = **表示层**——`screens/README.md` §2 纪律行「坐标一律 `画面名.area名`」执行不一致(§4 对照表按全形写、§3 与锚表按裸名写);②修的是根还是症状 = 所涉 area 均在档无歧义(建档对账 = 审查 §3 第 11 条),一次统一全形即治本,零语义变化。
- **解决到哪**:7 处统一全形 `货币战争-位面详情.<area名>`,四处「无全形首注」area 随之补齐首注(§2.5)。
- **明确不解决**:§9「备战档节点条 area」(备战档 `assets/game_data/screen_info/currency_war_battle_prep.yml` 的跨文件指称,非本屏 area 引用,审查未列);兄弟文档同类格式(各屏稿辖域);建档 yml 本体(零改动)。

- **在册核对一致项**:审查报告 §3 已确认一致的面(6 node 结构与重试预算、report 写门唯一性与写门语义三方一致、零策略器问询、编排 `CwEntryPlaneIntel` 四 node 与 prep 委派面、建档 yml 对齐、识别失败响亮语义、测试锁与遥测申报等)本稿不立修法,仅作为各修法不得触碰的现状边界——落地时禁借清理之名改动这些在码/在册语义。

## 2. 方案

### 2.1 F-1 修法:模块 docstring 变更史括注整段删除

| # | 位置 | 现状(病句) | 目标 |
|---|---|---|---|
| 1 | `operations/cw_screen/cw_screen_plane_intel.py` 模块 docstring 职责首句 | 「职责:在「货币战争-位面详情」屏恒全采三位面情报(用户裁定"先按能采处理",原"只采当前及之后"裁决退役):」 | 「职责:在「货币战争-位面详情」屏恒全采三位面情报:」——括注(裁定日期 + 新旧裁决对照)整段删除;冒号后三位面 boss / 敌人词缀 / 节点带与难度参考值列举逐字不动,折行由实现者按 docstring 行宽重排 |

依据:AGENTS.md §8(变更史归 git 历史,注释只留当前值成立的理由 + 指针;「恒全采」当前语义自足)+ battle_wait.md §2.6 准则 2/3;恒全采语义的规范承载 = `screens/plane_intel.md` §3(修后见 §2.4,含 §7「恒全采语义 = 不按变暗裁剪跳位面」边界),代码注释无须重复出处。

**验证门**:`grep -nE "先按能采|只采当前及之后|裁决退役" operations/cw_screen/cw_screen_plane_intel.py` 零命中;正向核对模块 docstring 仍含「恒全采三位面情报」(语义句在)。

**取舍**:备选 = 保留「先按能采处理」作裁定语义引文(仅去日期)——放弃:该短语是「恒全采」的裁定口语版,留之即同义第二抄本;正本 §3 已承载语义与边界,注释复述即漂移面,删括注零损失。

### 2.2 F-2 修法:kernel 屏文件 changes/ 指针清出

| # | 位置 | 现状(病句) | 目标 |
|---|---|---|---|
| 1 | `kernel/cw_screen_report/plane_intel.py` 模块 docstring 写门签名段首句 | 「写门渠道签名按迭代设计(design §2.3,2026-09-20-takeover-intel-pipeline)定死:采集真值落账属 logic_action 族(同简报写点族),screen = 实际采集画面(本写点有明确宿主屏,非简报写点的空 screen),actor = 写者本体;对账网已随死机制退役,不在本写门。」 | 「写门渠道签名按设计定死:采集真值落账属 logic_action 族(同简报写点族),screen = 实际采集画面(本写点有明确宿主屏,非简报写点的空 screen),actor = 写者本体;对账网已随死机制退役,不在本写门。」——仅删「迭代」二字与括注「(design §2.3,2026-09-20-takeover-intel-pipeline)」,其余逐字不动;原文该句跨两行(「按设计」居 L8 行尾、「定死」居 L9 行首),删括注后折行由实现者按 docstring 行宽重排(同 §2.1 惯例),修后「按设计定死」为单行 |

依据:AGENTS.md §9 铁律(代码与正本文档禁引 changes/ 内容)+ iteration-design.md §1 引用寿命分层(迭代内引用寿命 = 迭代,收尾失效);签名真值单一源 = 本文件 `_WRITE_SIG` 常量(family/actor/screen/mode 四元组 inline 注释逐位在)+ 正本 `screens/plane_intel.md` §6 与 `../game_state/fields.md` §3.1.3(均在档自足),迭代件指针零导航增量。

**验证门**:`grep -nE "takeover-intel-pipeline|design §|按迭代设计" kernel/cw_screen_report/plane_intel.py` 零命中;正向核对模块 docstring 仍含「按设计定死」(按目标列折行重排声明,修后为单行)、「logic_action 族」「对账网已随死机制退役」(现役语义句逐条在)。

**取舍**:备选 A = 指针改指正本(`screens/plane_intel.md` §6)——放弃:签名定义就在本文件常量与同段注释 inline 重述,跨文件指针零增量,反增一处可失效引用;备选 B = 首句整句删除——放弃:「logic_action 族(同简报写点族)/ screen = 实际采集画面 / actor = 写者本体」是签名逐位的 why(为何归采集族、为何 screen 非空、为何 actor = 写者),删句即丢设计动机,收敛指针而非删义。

### 2.3 F-3 修法:词缀登记面指针精度归位 §7.3 → §7.6(节号 + 节名双定位)

| # | 位置 | 现状(错节) | 目标 |
|---|---|---|---|
| 1 | `operations/cw_screen/cw_screen_plane_intel.py::CwScreenPlaneIntel.report` 词缀登记注释括注 | 「(补采重跑不双登记;登记面纪律 = effect-domain.md §7.3)。」 | 「(补采重跑不双登记;登记面纪律 = effect-domain.md §7.6 词缀源节)。」——节号改正 + 节名兜底,其余逐字不动 |

依据:目标锚 = `../game_state/effect-domain.md` §7.6「词缀源(source='affix')」——词缀登记面专属语义(规格注册表/登记端/在册条目幂等)的最近锚;§7.6 登记端在册符号 = `register_affixes_from_names`(即本点调用的登记函数,「简报屏/位面详情屏两词缀读链产出点」同条在册,本屏位面详情链覆盖在内)。原锚 §7.3 实文 = 「驱动事件映射」节,节内(L235-242)通用「登记挂点纪律」段含「词缀源登记挂点 = register_affixes_from_names(…,§7.6)」交叉指针——原锚可解析、非最近锚,修法性质 = **精度归位**,非纠死指针。佐证链 = `../game_state/fields.md` §3.1.3「见 effect-domain.md §7 词缀源节」(章级在册)。他处 §7.3 引用按各自指向语义成立(`kernel/cw_affix_effects.py:409` / `kernel/cw_effect_inventory.py:287` / `:1161`),不因本修法连带失效,落地批禁连带清扫。

**验证门**:`grep -n "effect-domain.md §7.3" operations/cw_screen/cw_screen_plane_intel.py` 零命中;正向核对同注含「effect-domain.md §7.6 词缀源节」。

**取舍**:备选 = 仿 `fields.md` §3.1.3 只留章级称谓「effect-domain.md 词缀源节」——放弃:同文精确到节的指针可用即用,精度不倒退;节名随行兜底后,节号若再漂移仍可按名解析,不为本稿引入防漂移机制。

### 2.4 F-4 修法:两处裁定日期清出(语义与指针逐字保留)

| # | 位置 | 现状(病句) | 目标 |
|---|---|---|---|
| 1 | `screens/plane_intel.md` §2 首句 | 「**多屏管线形态**(6 node,用户裁定 2026-09-20 豁免 [op-layer.md](op-layer.md) §1.1 两 node):识别/点开子步各自独立 node 与重试预算,逐子步验收;开/关转场(进屏/出屏)归编排 op,采集 op 不开屏不关屏。」 | 「**多屏管线形态**(6 node,豁免 [op-layer.md](op-layer.md) §1.1 两 node):识别/点开子步各自独立 node 与重试预算,逐子步验收;开/关转场(进屏/出屏)归编排 op,采集 op 不开屏不关屏。」——删「用户裁定 2026-09-20 」,其余逐字不动 |
| 2 | `screens/plane_intel.md` §3 恒全采条首 | 「**恒全采位面1→2→3**(用户裁定 2026-09-20「先按能采处理」):」 | 「**恒全采位面1→2→3**:」——括注整段删除,语义主句自足(§7「恒全采语义 = 不按变暗裁剪跳位面」边界同文) |

依据:`screens/README.md` §2 纪律行「as-built 无状态(事故史/裁定日期不进正文)」;6 node 豁免的规范出处 = `screens/op-layer.md` §1.1(两 node 总则,被豁免规则本体)+ `screens/op-layer.md` §3 多屏管线行(本屏豁免申报与 6 node 序列在册行);恒全采语义 = 本篇 §3 全节 + §7 边界句,均不依赖日期成立;日期考古归 git 历史。

**验证门**:`grep -nE "用户裁定|2026-09-20" screens/plane_intel.md` 零命中;正向核对 §2 仍含「豁免 [op-layer.md](op-layer.md) §1.1 两 node」、§3 仍以「恒全采位面1→2→3」起句。

**取舍**:备选 = #2 保留「先按能采处理」仅去日期——放弃:与 §2.1 取舍同型(同义第二抄本);两处是同一裁决在两载体的残留,目标语义一次定死,禁两种清理口径并存。

### 2.5 F-5 修法:7 处裸 area 名统一全形

| # | 位置 | 现状(裸名) | 目标(全形) |
|---|---|---|---|
| 1 | §3 三位面 boss 条「点该位面卡」括注 | 「按钮-位面卡1..3」 | 「货币战争-位面详情.按钮-位面卡1..3」 |
| 2 | §3 三位面 boss 条「大图标 SIFT」括注 | 「区域-boss大图标」~107px | 「货币战争-位面详情.区域-boss大图标」~107px |
| 3 | §3 敌人词缀条 | 「区域-词缀横条」 | 「货币战争-位面详情.区域-词缀横条」 |
| 4 | §4 锚表「点位面卡切选中位面」行·锚列 | 「按钮-位面卡1/2/3」 | 「货币战争-位面详情.按钮-位面卡1/2/3」 |
| 5 | §4 锚表「点备战节点图标开详情」行·证据列 | 「标识-位面详情标题」出现 | 「货币战争-位面详情.标识-位面详情标题」出现 |
| 6 | §4 锚表「点 X 关闭位面详情」行·锚列 | 「按钮-关闭位面详情」 | 「货币战争-位面详情.按钮-关闭位面详情」 |
| 7 | §4 锚表「点 X 关闭位面详情」行·证据列 | 「标识-位面详情标题」消失 | 「货币战争-位面详情.标识-位面详情标题」消失 |

其余逐字不动;#2/#3/#5/#6/#7 落位后,四枚「全篇无全形首注」area(区域-boss大图标 / 区域-词缀横条 / 标识-位面详情标题 / 按钮-关闭位面详情)在修后文档内均有全形在场,首注缺口补齐。

依据:`screens/README.md` §2 纪律行「坐标一律 `画面名.area名`」;同目录兄弟文档(briefing / boss_briefing / plane_detail 等)动作表全形通例;全形 screen 名 = 采集 op `_PD_SCREEN` 常量值「货币战争-位面详情」(建档 `assets/game_data/screen_info/currency_war_plane_detail.yml` screen_name 同名,审查 §3 第 11 条建档对账在案)。

**验证门**:①负向:`grep -nE "「(按钮|区域|标识|文本)-" screens/plane_intel.md` 零命中(全形串以「货币战争-位面详情.」前缀起头,不落模式);②正向:上表 7 个目标串各 ≥1 命中。

**取舍**:备选 = 仅补四处无全形首注 area、已有全形上下文处的裸名留短——放弃:同篇两种格式并存正是 F-5 病灶,一次统一全形归一;全形行长代价可接受(兄弟文档同式),且 README 无裸名豁免条款,禁自行创设短式约定。

### 2.6 落地文件面总表与统一验收

| 文件 | 修法点位 | 性质 |
|---|---|---|
| `operations/cw_screen/cw_screen_plane_intel.py` | 模块 docstring 变更史括注删除(F-1);report 词缀登记注 §7.3→§7.6(F-3) | 注释 |
| `kernel/cw_screen_report/plane_intel.py` | 模块 docstring changes/ 指针清出(F-2) | 注释 |
| `screens/plane_intel.md` | §2/§3 裁定日期清出(F-4);7 处裸 area 名全形化(F-5) | 画面篇 as-built 格式修正 |

统一验收:§2.1–§2.5 五验证门各自通过;两 py 文件 `uv run ruff check` 通过;`git diff` 确认零逻辑改动(注释/文档单面);修后 `screens/plane_intel.md` 与 `screens/op-layer.md` §3 多屏管线行(6 node 序列与豁免申报)、`../game_state/fields.md` §3.1.3(写门签名)、`../game_state/effect-domain.md` §7.6(词缀登记面)口径互查一致。

**交叠注(battle_wait.md §2.6 准则 5)**:F-1 与 F-3 同在 `cw_screen_plane_intel.py` 但不同段(模块头 vs `report` 方法体内注释),无同段交叠,一次成文落地无冲突;F-4 与 F-5 同在 `screens/plane_intel.md`,建议一次成文(同文件两处触碰,分批无收益)。

## 3. 新规范增补(2026-09-22 两条款)

> **背景**:正本 `screens/op-layer.md` §1.1 新入两条款(commit 2f35d4011,用户裁定 2026-09-22)——**条款①观察标准化门**(:34)/ **条款②画面 op 不支持局外单独调用**(:36)。新规范符合性重审(G2)= `.debug/progress/2026-09-22-cw-screen-review/reports/respec-T16-T25.md` §2「T-25 位面情报」节,总判定 = **需增补 1 点**(词缀容器域标准化欠账登记)+ 2 条注记(局外出口形态差候裁;MCP 单独入口边界同裁)。本节只追加,不改 §0-§2 任何已收敛内容;既有修法(F-1..F-5)全部维持。
>
> **时点差与辖域申报**:本稿对抗收敛成文在先、两条款入正本在后,属时点差非有意违范;本节与既有修法零冲突——F-1..F-5(注释/指针/日期/坐标)与新两条款零涉及、零登记冲突(重审增补点 D1 明示「设计稿现修法(F-1..F-5 纯注释/日期/坐标)零涉及、零登记」),故本节无 §2 目标文本连带改写站点(正本屏契约随批义务见 §3.3)。§0「修法性质:零行为变化」辖域不变:增补 1 = 登记级(欠账实现归标准化收敛批),本节全部为申报面,零行为变更。
>
> **修订轨迹(增补节 D 批定点对抗,就地修订;§0 状态行维持原样,§0-§2 已收敛正文零改动)**:本节经定点对抗审判(D 批 4 条:中 1 低 3;`.debug/progress/2026-09-22-cw-screen-review/reviews/respec-attack-D.md`),涉本稿仅 D-3(低)已修:§3.1 修法 1 同构参照 `_standardize_options` 行号锚(:130-154,沿自 respec 转写)与现工作树(:138-162)不符,删行号留符号锚(briefing.md §3.2 同批同修);其余 3 条(D-1/D-2/D-4)均落 briefing.md §3,本稿无涉。

### 3.1 增补 1(条款①·登记级):词缀容器域标准化欠账登记 + 开放注册表采集附加边界

**域核对(两域分工,依据就地标注)**:

- **boss 域(`plane_bosses`)已合规,零增补**(给依据,重审明示):读数 = 「区域-boss大图标」SIFT 对拍 `boss_avatar` 模板库(`match_boss_sift`)→ 身份 = 模板库标准身份(注册数据的视觉形态,条款①辖面的视觉域等价形态);识别失败(节点条读不出/非首领/SIFT 未命中)统一 `round_retry` → 耗尽 op fail 响亮暴露、零写(T-25-r1 §3.13 核「无 None/占位/降级分支」)——与条款①「转换失败 = 观察失败零写」失败语义同型。
- **词缀域(`enemy_affixes`)零转换欠账(本增补点)**:读链 = 「区域-词缀横条」OCR(`read_detail_affixes`)→ `_affixes` → `obs.enemy_affixes=list(self._affixes)`(`operations/cw_screen/cw_screen_plane_intel.py:313`,**OCR 原名**)→ `report_screen_plane_intel_obs` 写容器(`kernel/cw_screen_report/plane_intel.py:64-67`,幂等门仅过滤「容器已有/空读数」,不过滤未注册名)——**零形变归一、零 LCS、零注册表比对**,条款①「观察时转换成标准注册数据再上报」未达成;定性 = 存量欠账(逐批收敛,禁新增未标准化直报),非本批违例。
- **登记面辨析(防误申报)**:登记挂点 `register_affixes_from_names`(`cw_screen_plane_intel.py:344-351` 调用;`kernel/cw_affix_effects.py:410` 本体)的「命中结构化注册才入账本」= 效果**账本**的登记过滤,**不是容器标准化**——未命中注册的 OCR 原名照样写 `enemy_affixes`、上报 node 照样 `round_success`(`:352`);接管局「词缀登记唯一活源」申报(`screens/plane_intel.md` §6)不构成容器面合规。
- 注册数据在册(条款①辖面成立)= 同 briefing(T-20) 词缀域:**开放集合** `data/affix_effects_data.py::AFFIX_EFFECTS`(词缀名 → 效果原文,运行时采集面自写)+ 结构化 `kernel/cw_affix_effects.py::AFFIX_EFFECT_SPECS`。

**修法 = 登记欠账(本稿不实施;归标准化收敛批,与 briefing(T-20) 词缀域同域同方向同批;开放集合豁免判语义先裁后行,裁定前禁落码)**:

1. **两段转换**:形变归一(OCR 形变族)后精确命中注册表 → 不中再 `one_dragon.utils.str_utils::find_best_match_by_lcs`(阈值常量住代码);同构参照 = 投资环境首个落地 `cw_screen_invest_env.py::_standardize_options`。转换住采集 op「上报」node 组装 obs 之前(观察侧,op-layer §2.2「生产半住观察侧」),落容器半照旧 `report_screen_plane_intel_obs`(幂等门不动)。
2. **失败语义(已有词缀)**:任一候选转换失败 = 观察失败——上报 node round_fail 零写零上报(与 boss 域 SIFT 失败出口同型:响亮暴露零写)。
3. **真新词缀附加边界(采集面豁免)**:两段皆不中且属注册表外新词缀 → 不判观察失败,走采集登记面(截图收集 → 人工入册 → 下次起可归一)——开放集合,机械套「不在册 = 失败」会杀死采集面;「新词缀」判据机械形态归 T-37/收敛批先裁(与 briefing(T-20) 同一裁决一并);屏契约落点 = `screens/plane_intel.md` §3 词缀条 + §6 容器写门条(目标语义文本见 §3.3 站点表,随收敛批施工)。

**T-37 登记行原文**(逐字,交 T-37 汇总对账):

> - [T-37 登记][欠账] plane_intel(T-25)::34 词缀容器域标准化欠账(登记级)——OCR 原名直写容器 `enemy_affixes` 零转换(`cw_screen_plane_intel.py:313` → `report_screen_plane_intel_obs` 幂等门,`kernel/cw_screen_report/plane_intel.py:64-67`);`register_affixes_from_names`「命中结构化注册才入账本」(`:344-351`)只是账本登记过滤非容器标准化;注册数据在册 = 开放集合 `data/affix_effects_data.py::AFFIX_EFFECTS` + 结构化 `kernel/cw_affix_effects.py::AFFIX_EFFECT_SPECS`。收敛 = 两段转换(上报 node 组装 obs 前,观察侧)+ 失败语义(已有词缀转换失败 = round_fail 零写零上报)+ 真新词缀采集附加边界(开放集合;边界落 `screens/plane_intel.md` 屏契约;新词缀判据先裁后行);与 briefing(T-20) 词缀域欠账同域同方向同批;boss 域 SIFT 模板身份 + 失败零写已合规零触;禁新增未标准化直报。

**验收锚(登记级)**:

1. 本节 + T-37 登记行在册(汇总清单可查),欠账认定有代码锚可复核。
2. 行为面零变化(本批):`enemy_affixes` 容器写端唯一 = `report_screen_plane_intel_obs` 幂等门零改动;本批零半截转换、零豁免判落码。
3. 收敛批开工入口 = 本节 §3.1 + T-37 登记行,不重做认定;新词缀豁免判语义未裁前禁落码。

### 3.2 注记(候裁,非增补点):局外出口形态差(success 终结 vs 响亮 fail)+ MCP 单独入口边界

注记一(出口形态差,重审 R3):

- **编排门(拒绝形态,合规面)**:编排 op `CwEntryPlaneIntel.gate` 的 `_session` 缺席(cw_match None)= `round_fail('ctx.cw_match 无对局 session(不在对局中),无法补采')`(`cw_entry_plane_intel.py:81-84`)——拒绝服务门(拒绝对局外跑,非支持代码),条款②精神合规。
- **采集 op 上报 node(出口不同型)**:`sess` 缺席 = save_screenshot + `round_fail('上报时无对局 session(不在对局中),真值无处落')`(`cw_screen_plane_intel.py:307-310`)——与条款②「无 match = 零决策零点击 round_success 终结交回」出口不同型(fail vs success);缓解事实:该路径生产不可达(编排门已挡),且发生在识别完成后(非决策/点击前的兜底)。
- **多屏管线豁免措辞边界**:多屏管线形态被用户裁定豁免两 node(op-layer §1.1/§3 在册),条款②未给多屏管线豁免措辞——本屏局外出口形态是否沿用豁免口径 = 同域待裁。

注记二(MCP 单独入口边界):`CwEntryPlaneIntel` 的 MCP `run_operation` 单独入口为在册面(T-25-r1 §4),条款②字面辖画面 op,编排 op 的局外单独入口是否随条款②收紧 = 同一候裁。

归口 = T-37 候裁(重审报告 §3 序 5「裁决时顺带」),不阻塞本稿、不列硬增补;裁前现役代码零改动。

### 3.3 与既有修法的咬合申报(随批义务;本稿 §2 零连带改写站点)

- **零改写站点声明**:F-1..F-5 修法面(模块 docstring 变更史/changes/ 指针/§7.6 指针/裁定日期/裸 area 名)与两条款零涉及——本稿 §2 各目标文本无需连带改写,落地批按 §2 原文施工即可。
- **随批义务(正本屏契约登记点,归收敛批施工,非本批)**:

| # | 宿主 | 站点现文(= 既有修法落笔后形态) | 目标语义文本(逐字,随收敛批) | 缘由 |
|---|---|---|---|---|
| 1 | 正本 `screens/plane_intel.md` §3 敌人词缀条 | 「- **敌人词缀**:「货币战争-位面详情.区域-词缀横条」(`read_detail_affixes`,位面1 同帧读一次;词缀不随位面卡切换变;空 = 无词缀,上报走幂等门)。」(本稿 §2.5 F-5 全形化修法后形态) | 条尾追加:「词缀读数经词缀注册两段转换后上报(已有词缀转换失败 = 观察失败,round_fail 零写零上报交回重采);真新词缀(两段皆不中)不判观察失败,走采集登记面(截图收集 → 人工入册 → 下次起可归一)——开放注册表附加边界,规范 = op-layer.md §1.1 观察标准化门『各屏转换成功性的附加边界由屏契约登记』;账本登记过滤(`register_affixes_from_names` 命中注册才入账本)≠ 容器标准化。」 | 增补 1 屏契约(与 briefing(T-20) 词缀边界同款同批,同族措辞) |
| 2 | 正本 `screens/plane_intel.md` §6 容器写门条 | 「`enemy_affixes` **幂等门**(仅容器空时写;空读数 = 无词缀,不落写——空表覆写会把已有真值抹成「无数据」)。」 | 句尾追加半句:「(幂等门过滤的是写时点非名字域;标准化收敛后本写门输入 = 标准注册名,转换失败不进本写点,欠账登记见 §3 观察面词缀条)」 | 增补 1 屏契约随批(防「幂等门在册 = 容器面合规」误读) |

**一致面非冲突申报**:§1 末「在册核对一致项」中「report 写门唯一性与写门语义三方一致」「识别失败响亮语义」两结论——增补 1 为登记级(欠账实现归收敛批),本批不动任何写门/失败出口代码,一致面零触碰;§3.1 boss 域「已合规」认定与「识别失败响亮语义」结论同源互证。
