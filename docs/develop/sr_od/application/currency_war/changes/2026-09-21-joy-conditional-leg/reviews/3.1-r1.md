# 阶段 3.1 交付验收 review(r1·重派复核)

- **验收对象**:主仓 `14dd40977`、测试仓 `d3f5788e`(固定快照,全程 `git show <commit>` 审,不受工作区在飞影响)。
- **验收依据**:design.md §2.1-§2.6、landing.md §3.1。
- **审查有效性说明**:主仓工作区在飞改动仅 docs(他批设计目录,未触 src);`14dd40977..HEAD` 之间零提交触及四个入库文件;测试仓 HEAD 即 `d3f5788e`、工作区干净——本文行为抽测结果对快照有效。
- **证据坐标约定**:代码行号 = 各自 commit 快照内行号;测试行号 = `d3f5788e` 内行号。

---

## 核查 1:两 commit 全 diff 实读与入库面 —— ✅ 通过

- 主仓 `git show 14dd40977 --stat`:恰 4 文件——
  `kernel/cw_action_report/pick_planner.py`(+72 行含 rider)、`kernel/cw_events.py`、`kernel/cw_gain_chain.py`、`operations/cw_op/cw_overlay_pick_action.py`,与 landing §3.1 文件面逐一对应,**零卷入**(staged 集合 = 申报集合,无他批文件混入)。
- 测试仓 `git show d3f5788e --stat`:恰 2 文件——`test_cw_gain_chain.py`(+228)、`test_cw_yinlang_phase32.py`;rider 组归并 `test_cw_gain_chain.py` 已在 commit message 申报,符合 landing「归并或新文件,随批申报」两可口径。
- landing §3.1 范围语句逐条对照:两相形态、rider 落点、撤闩面、weaken 退役四处、「不含」三 negatives(两选项本体/sim 接线/comp 消费)——diff 内均无越界改动。✅
- commit message 注明「按『测试过时』判定改写」,满足 design §2.6 随迁纪律。✅

## 核查 2:rider 对照 design §2.2-1/§2.2-2 —— ✅ 通过

`_grant_joy_conditional`(pick_planner.py:107-142):

| design 条目 | 实码证据 | 结论 |
|---|---|---|
| 落地相内、腿型分派**之前** | :94 `_grant_joy_conditional(gs, sig, rng)` 位于分派分支(:95-104)之前,且在发射相早退(:86-91)之后 | ✅ |
| 在册判定 normalize 两侧 | :127 `normalize_invest_name(gs.active_env.value or '') != '欢愉契约'` → return;`normalize_invest_name` 对『欢愉契约』为恒等(仅分隔符形变映射,cw_investments.py:691-701),右字面即规范形;`active_env` 为 `Field[str | None]`,`.value or ''` 覆盖 None | ✅ |
| 候选集 = ENV_GIFTS 名集,零字面硬编码 | :129-130 `[name for name, _text in ENV_GIFTS['欢愉契约'].chars_conditional]`;在册数据 = cw_investments.py:1575-1581(('火花',…),('开拓者·欢愉',…)),函数体内无第二抄本 | ✅ |
| `gain_character(unit, 1, rand=True, evidence='joy_conditional:<单位名>')` | :134-136,star=1、producer=`_PLANNER_PRODUCER`(design §2.2-1「沿用」) | ✅ |
| rng 注入 | :70 `rng: random.Random | None = None`(keyword-only);:131 `rng if rng is not None else random.Random()`,缺省未播种 = 实机猜测语义,与 `on_strategy_gained` 的 `roll_hacker_mod` 先例同形态(cw_gain_chain.py:676) | ✅ |
| best-effort = `except Exception`(noqa BLE001) | :137-142;形态与先例 cw_gain_chain.py:466/:581(design 引 :470/:585,常量删除后行号前移)同款 | ✅ |
| 缺陷行四字段 | kind=`joy_conditional_grant_failed` / expected=`条件腿授予成功` / actual=`授予异常:{e}` / evidence=`joy_conditional:{unit}`(:139-142),行形状与 `_emit_defect` 台账 schema(kind/field/expected/actual/observed_evidence…,cw_game_state.py:891-896)对齐 | ✅ |
| 不阻塞分派 | except 块仅 log+留证后自然返回,分派在函数返回后照常执行 | ✅ |
| import 方向零循环 | cw_gain_chain.py 对 `cw_action_report` 仅 docstring 提及(:5-7/:24/:611),无 import;pick_planner 单向 import kernel 链 | ✅ |

## 核查 3:通道语义(rand=True 恒 logic_rand)—— ✅ 通过

- **继承路径实读**:rider `rand=True` → `gain_character`(:477)→ `_gain_character_core`(:363)→ `write = _select_write(gs, rand)`(:384)→ `anchor_aware_write(gs, rand)`(:247)→ `if rand or not prep_anchored_of(gs): return gs.write_logic_rand`(cw_game_state.py:1805-1806)——**rand 形参短路优先于锚定闩**,锚定前后同为 logic_rand;合成步(`_write_merge_step` :429)与递归产物(:436)均透传同一 rand,全链同通道。与 design §2.2-3 口径逐字一致。✅
- **测试双臂**(test_cw_gain_chain.py:602-634):未闩臂授予落 bench `source == 'logic_rand'` + 观察覆盖落 `logic_rand_outcome` 行、零 `observe_vs_logic_mismatch`;置闩臂(`gs2.prep_anchored = True`,测试/直构域专用置闩口,cw_game_state.py:1787)同路径 `source` 仍 = `logic_rand`——与 design §2.6 修正后口径一致,**无「置闩=logic」残留**。授予到位由 `granted == [cands[0]]` 锁定,通道断言非平凡(source 由末次写决定,若链走 logic 通道该断言即红)。✅

## 核查 4:撤闩完备性 —— ✅ 通过

- `git grep -n 'JOY_PROVISIONAL_KIND\|joy_contract_provisional\|provisional_mark' 14dd40977 -- src/` → **零命中**(exit 1)。测试仓同词表仅剩三类合法残留:test_cw_dead_pair_exit.py 的 `provisional`(= `strategies.impl.mandate_v1.audit` 校准注入模块,异名同形无关)、新退役锁测试名/注释、portal 零发射断言(:504)。✅
- `on_env_gained` 欢愉契约仅 chars_immediate:翻来源分支与标记行整段删除(cw_gain_chain.py 快照 :607-629 区),docstring 补「欢愉契约条件腿不在本枚举:其正式模型 = … rider」指针,自述与实码一致;模块头失败安全段新增「唯一调用点级显式例外」申报(:24-25)。✅
- 撤闩行为锁 = portal 用例断言零翻面零 provisional 行(见核查 6-③),即 design §2.6「grep 断言的等价形式」。✅

## 核查 5:weaken 退役完备性 —— ✅ 通过

- `git grep -nE 'weaken|弱化档' 14dd40977 -- 'src/*.py'` → **零命中**(exit 1)——全 src 含 sim 一并清零,严于任务口径的「排除 sim」。✅
- `classify_planner_leg`:降级分支(「弱化/降低敌人」→ weaken)删除,判定序改为 锚匹配 → 相似救援 → 升费关键词 → unknown(:1023-1026 区),含弱化词未知名卡文落 unknown(留证行);`PLANNER_LEG_WEAKEN` 常量与词表注释同步退役,「破解芯片碰撞例 = 装备域锚先行消解」注释保留(:898)。✅
- `decide_planner`:55 分弱化档分支删除(:877-880 区),docstring 三层定序改两档制(升费档 > 装备档,含弱化词未知名卡文按装备档回落);design 点名的两处散布弱化引用已清除——「落到弱化档之下」→「投资无处兑现」、「不跨域压弱化档」→「只在装备档内排前」。✅
- pick_planner:weaken 派发分支删除,终态兜底 `reason='unrouted_leg_zero_write'`(:104);模块 docstring(:20-22)与函数 docstring(:78)的 weaken 字样同步退役。`cw_overlay_pick_action.py` 腿型注释 `upgrade|weaken|equip|unknown` → `upgrade|equip|unknown`(:126)。✅
- 正本文档(planner.md / pick-planner.md)在快照仍持 weaken 字样——属 landing 正本更新清单(末阶段,依赖 3.1)的申报任务面,design §2.8「随末阶段改写」明定,**不构成 3.1 缺陷**。✅

## 核查 6:test_cw_yinlang_phase32.py 四处改写正当性(重点攻击面)—— ✅ 通过

逐 hunk 判定,四改写均为「条件腿建模 + weaken 退役」的必然结果,无改期望掩盖回归:

1. **文件头(:8-10、:24-26)**:分类自述「关键词降级」→「升费关键词/unknown+退役申报」;pick_invest 迁移段「provisional 闩随 on_env_gained 迁 kernel」→「仅 immediate 腿+正式模型指针」。与 design §2.6 预申报的 :23-24 改写面一致。✅
2. **分类锁(:143-178)**:`classify_planner_leg('使后续节点【弱化】')` 期望 `('weaken','')` → `('unknown','')`——分类分支删除的直接必然;**破解芯片 → ('equip','破解芯片') 断言原样保留**(碰撞治理不回退,design §2.6 明文),退役面与保留面同测,无偷减。✅
3. **unknown 用例(:297-315,更名 `test_unknown_leg_evidenced_zero_write`)**:原 `leg_type='weaken'` 入参随腿型退役失义,改 `leg_type=''`(未路由兜底形态)并新增 `reason == 'unrouted_leg_zero_write'` 断言——把 reason 更名从「无锁」变「有锁」,语义只强不弱;零容器写/零留证断言保留。用 `''` 而非保留 `'weaken'` 字串作入参是正确选择(后者会为新分类器不可能产出的载荷立锁)。✅
4. **portal 用例(:480-504)**:撤闩后旧行为(bench/equips 值不变翻来源 + provisional 行)已不存在,期望改写为——`units == [('希儿',1),('银狼LV.999',1)]` 精确等值(immediate 唯一腿,**强于**旧 `in` 断言)、bench `source == 'logic_rand'` 保留但归因改为「送卡链写未锚定期改道」(通道规则,与撤闩无关)、equips `source == 'observation'` 且值不变(**新增**零翻面锁)、`kinds == []`(**新增**零发射锁)。「零 provisional 行」不会因链静默坏而假绿——immediate 腿入席断言证明链确实执行。✅
- 另有**新增**(非改写)`test_decide_planner_weaken_tier_retired`(:171-190):两选项臂(4 分轮滑鞋胜出,退役前 55 分弱化档占优)+ 单选项臂(reason 以 `装备(` 回落、无「全场弱化」词),补齐 design §2.6「decide_planner 零弱化档分」退役锁,落位 yinlang 文件与 commit message 申报一致。✅
- 完整性反查:测试仓快照 grep `weaken|provisional|joy_contract` 确认 joy 临时闩断言唯一面确为该文件(design §2.6「唯一面」申报属实),无漏改的他文件断言。✅

## 核查 7:行为抽测(自跑)—— ✅ 通过

- `uv run pytest test_cw_gain_chain.py test_cw_yinlang_phase32.py -v` → **48 passed**(3.40s),含 rider 六条全绿:`grant_channel_rand_anchor_and_coverage`(:602)/`registry_gate`(:637)/`rng_injection_deterministic_and_reaches_both`(:671)/`fires_once_per_landing_leg_not_emission`(:691)/`failure_best_effort`(:740)/`grant_chain_timing_merge`(:771)。
- `uv run pytest sr-od-test/test/sr_od/application/currency_war -m "not slow"` → **931 passed**(27.40s,exit 0),与 worker 自述 L1 931 一致。✅

## 核查 8:注释与规范 —— ✅ 通过

- 中文注释/docstring;新增文案无会话局部标识(对 `14dd40977` 全部 `+` 行扫 `r[0-9]/W轮/批N/[N]/changes/` 零命中);持久索引仅 ADR-0524(既有);出处均为语义描述(「与骇客改件采样同约定」「gain-chain.md §6」正本指针)。
- 类型注解齐备:`rng: random.Random | None`、`_grant_joy_conditional(...) -> None`、测试侧 `_joy_candidates() -> list[str]` 等。
- `except Exception as e:  # noqa: BLE001` 形态与 cw_gain_chain.py:466/:581 同款,注释就地申报例外理由(design §2.2-1「随正本更新在 gain-chain.md §6 申报」属末阶段面)。
- ruff check 四文件 → All checks passed。
- 「均匀分布未实测,候校准」披露就地落在 `_grant_joy_conditional` docstring(:120),符合 §2.2-2「披露键注释就地申报」。

---

## 发现清单

**阻断:无。**

**应修:无。**

**建议(均不阻断,不要求本批处理)**:

1. test_cw_yinlang_phase32.py:312 `out2` 调用的续行缩进与首行参数列未对齐(改写时仅动了行内字串所致)——纯外观,ruff 不辖,下次触及该文件时顺手即可。
2. rider 失败注入桩 `_boom(gs, name, star, **kw)` 用 `**kw` 吸收调用侧 kwargs——系测试替身的合理形态(非构造函数,不违项目「构造函数显式声明参数」约束),记录备查。

## 最终判定:**验收通过**

八项核查全过:入库面 = landing 申报面零卷入;rider 七要素与 design §2.2 逐条对上;通道语义经 `_select_write`→`anchor_aware_write` 继承路径实码证实(rand 短路优先于闩)并有双臂测试锁定;撤闩与 weaken 退役在快照 src 全域(含 sim)grep 双语清零;四处测试改写均系申报行为的必然结果且锁力只增不减(重点攻击面未发现掩盖回归);48 + 931 两级测试全绿。遗留面(正本 weaken 字样)属末阶段申报任务,不影响本判定。
