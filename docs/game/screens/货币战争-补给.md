# 货币战争-补给(currency_war_supply)

> source_image: sr-od-test/screens/货币战争-补给/default.webp(2026-08-15 live 建档,1-5 首遇);
> 另有 08-13 早期 fixture:未选择/已选择(id_mark 测用,test_currency_war_supply_screen.py)。

## 何时出现

补给节点(位面内固定出现;对局内**独立屏**,非 overlay —— 右上「返回备战界面」按钮。
2026-08-15 live 实锤,同投资策略屏模式)。

## 状态流转

```
进入(5 列补给选项:角色+装备对)→ 点列卡身选中(已选择 n/1;可换选)→ 点确认
→ 返回备战(节点完成,下轮 nodeseq current 前进)
```

## 识别特征

- id_mark「标识-补给阶段」[893,120,1027,230](顶部标题;live precise 实证)

## 可交互元素(live 验证 2026-08-15)

| 元素 | 坐标(中心) | 行为 |
|---|---|---|
| 补给列 x5 | 列卡身 y~550(列中心 x~250/610/970/1330/1690) | 点列选中(该列角色+装备整列取得;live 点列3 已选择 0/1→1/1) |
| 确认 | (1708,980) | 确认选择 → 返回备战(不可逆出口;选中后才亮) |
| 返回备战界面 | 右上 (~1790,57) | 回备战屏(选择待定) |

## automation 要读信息

- 每列(角色名 y~548 + 装备名 y~680)→ read_supply_options(cw_node_obs)→ decide_supply
  (target_comp.key_equips 契合 + 装备通用价值;钻判定待补)
- 「文本-已选择」/「文本-剩余次数」(本节点 1 选;多选变体待遇)

## 注意

- 列数 **5**(live 实证;v1 文档「3 选 1」过期 —— 列数随节点变,reader 动态读不硬编码)
- 「剩余次数:0」在选择后出现(剩余可选数);decide_supply refresh_used=True(supply 无刷新按钮)
