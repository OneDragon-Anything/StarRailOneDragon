---
screen_name: 货币战争-保存阵容弹窗
appears_in: [currency_war]
last_updated: 2026-08-15
source_image: screens/货币战争-保存阵容弹窗/保存阵容弹窗.webp
---

# 货币战争-保存阵容弹窗(模态)

攻略列表卡片「保存阵容」打开:把攻略阵容存进「我的阵容」(0/10)。

## 何时出现 + 状态流转

- **入口**:卡片「保存阵容」(1610,b+116)。**出口**:「保存并收藏」(1468,852)提交;ESC 取消(实测关闭回列表,未保存)。

## 识别特征(稳定锚点)

- id_mark(组合):标题「保存阵容」+「保存并收藏」。

## 可交互元素

- 「存为新阵容」×3(820-970, y≈397/574/756):选保存槽位(未实测选中态,属低频路径)。
- 「保存并收藏」:提交并加入收藏。

## 识别快照

- fixture:保存阵容弹窗.webp(测试仓 `screens/货币战争-保存阵容弹窗/`)。

## 备注

- 槽位选中态未实测(点击会实际写入用户阵容,验证时主动跳过);bot 无当前需求,「待现场快照」。
