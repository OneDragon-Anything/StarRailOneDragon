# 存量开关盘点 + 吸收核对边界外候选核对报告

> 盘点员:策略审查(干净上下文)。判据单一源 = `sr-od-currency-war-dev` skill references/strategy-work.md「策略开关的生命周期」(2026-08-31 用户裁定版):**开关唯一合法存在理由 = 行为改动输入未就绪 + 开臂判据挂账;「A/B 已判决却默认关挂起」在任何档都禁;且必须带裁决期限**。
> 范围:`src/sr_od/application/currency_war/kernel/cw_registry.py` 全部默认关(`= False`)开关 + `REVIEW_ABSORPTION_CHECK.md` 边界外三候选。文件面 = 只读 + 本报告。

## ⚠️ 2026-09-02 清退批重对账(本节 supersede 下方 §1 全部清单;策略审查 R5 轮验收条件)

用户裁定「旧方案彻底出局」后,旧方案清退批(dd-009)已把 §1 全部默认关开关族**整批删除**(字段+接线+分支+专属遥测键,grep 零活残留,CW 快速集 2920 绿):

- §1.A A1-A15 全族 + §1.C C1(spend_gate 三字段)→ **已删除**。C1 的处置走的是原二选一中的「关死」分支:dd-009-legacy-mechanism-eviction(用户裁定级决策)推翻 ADR-0499「禁第四态删码」禁令,合法非静默。
- 删除后 `cw_registry.py` 默认关布尔开关 = **0 个**(现存 30 个布尔字段全部默认开,第 3 态已判开,回退通道=git revert,不属悬置纪律辖域)。
- **「重开判据+裁决期限」两栏(生命周期纪律:不允许无期限悬置)**:当前存活默认关族 = 空,两栏在当前时点无可填对象;纪律对**未来复活**生效——任一已删族要重开,须按生命周期第 1 态重新立项:①预注册判据(写明主判/机制判与样本量);②裁决期限锚「实机对局周期」(建议 N≤2 个周期:每个周期约 10 局,sim 批可折半计),到期未判读 = 删码留 ADR,禁再造无期限挂账。重开时本表恢复登记并逐族填两栏。
- §1.B 死开关清理项维持「空」;§2 边界外候选核对不受本批影响,结论仍有效。

以下 §1/§2/§3 为 2026-09-02 盘点时点的历史快照(其开关字段引用已随清退批物理删除,引用仅循墓碑注释/归档 ADR 可解析)。

## 0. 盘点前置发现(影响全部条目)

1. **ADR 指针已悬空**:注册表与决策代码注释引用的 ADR-NNNN 原址 `docs/develop/currency_war/decisions/` 在当前树**不存在**;全部 ADR 实体已归档至 `.debug/progress/2026-08-31-currency-war-redesign/支撑/adr_archive_20260901/`(2026-09-01 用户裁决删除重构代码、旧树归档时一并迁移)。注释里的 ADR 编号仍可循归档路径解析,但「持久索引」规范要求的可检索性已破。**处置建议**:在归档目录补一个 README 索引(或恢复 decisions/ 目录为指归档的指针页);共享文档改动需用户确认,本报告只登记不动手。
2. **裁决期限普遍未落地**:裁定要求挂账必须带期限(超过一个对局周期未决=违规)。现存挂账的「期限」几乎全部是「排进 sim A/B 批」「A/B 数据齐即跑」这类条件指针,无任何日期/对局周期锚。这是**批量轻度违规**,逐条见下表「期限」列。
3. **历史死开关已清干净**:注册表内多处墓碑注记(ADR-0487 五开关、ADR-0492 p2_spend_auth、ADR-0496/0497 件价值、ADR-0507 R-B 定价、ADR-0509 转型臂、ADR-0444 资产臂、ADR-0427 评分偏置两字段等)证明既往清理批运转正常——**现存字段中无「判据永不满足或概念已定谳」的死开关**,死开关清理项 = 空(见 §2)。

## 1. 任务 1:默认关开关三分清单(历史快照:A1-A15 与 C1 已随清退批删除,见文首重对账节)

盘点对象 = cw_registry.py 中默认值为 False 的行为开关(共 22 个字段,归 14 组)。`director_v2_shadow_compare` 为纯诊断工具(不改游戏动作,采集完成即回 False,随迁移批 3 退役),不属策略开关生命周期辖域,列为豁免不计入。(**清退批后注:该豁免项亦已删除——ADR-0465 承诺的删除在本批兑现。**)

### 1.A 合法挂账(输入未就绪 / 判据预注册未执行)——**全族已随清退批删除**

| # | 开关(组) | 引用依据(归档状态) | 开臂判据现状 | 期限 | 备注 |
|---|---|---|---|---|---|
| A1 | `crisis_fallback_enabled` | W956 死亡分配器;设计+PREREG 在 `docs/develop/currency_war/prereg/w956_death_allocator/`(**未归档,现行树在**) | 预注册判据已写(PREG.md),sim A/B 未跑;病灶输入已证(g_20260831_082322 p2r2「5 卡全拒 0 执行」) | 无日期,仅「判正后翻默认开」 | 与 082322 候选#2(危机兜底集)同源,见 §3.3 |
| A2 | `line_switch_survival_gate_enabled` | ADR-0429(已归档);W665 DESIGN v2 §4 判据锐化终版 | 输入已就绪(p_win_p2_by_rung/损血表已注入);主判+机制判+G4 锚全预注册,A/B(W638 批,三窗 330/臂)未跑 | 有排期指针(W638 修复 A/B 批),无日期 | W683→v3 改道终态已并入判据(门闩 R-A) |
| A3 | `rounds_two_state_enabled` | 同 ADR-0429 家族 | 同上,与 A2 正交、同臂组排期 | 同上 | 与 A2 禁 2×2 耦合臂 |
| A4 | `c1_directed_spend_enabled` | ADR-0443(已归档;破息分支部分已概念定谳否决——但那半边**从未落码**,定谳注记即清理物) | 主通道(溢余段定向收窄)两臂 A/B 判据预注册(ADR-0443 Decision 3),未跑;姊妹资产臂已定谳删除(ADR-0444) | 退役批(ADR-0466/0467/0469)补「排进 sim A/B 批」,无日期 | |
| A5 | `realization_chain_enabled` + 7 子旗标 | W802 实施批;PREREG_兑现链A_B v3(`docs/develop/currency_war/prereg/w795_realization_design/`,现行树在) | 占位参数已 sim 标定(κ=0.5/Δp_tier=0.75/γ=0.95/β=0.05 有扫描结论,注释在案);主判 M1-M4+机制判 G1-G7(n≥500/臂)未跑 | 「A/B 数据齐即跑」,无日期 | 观测硬依赖归 W793 后继批,不阻塞开臂 |
| A6 | `p1_tier_push_enabled` + 3 子旗标 | ADR-0494(已归档);PREREG_tier_push_AB v2(`docs/.../w803_tier_push_design/`,现行树在) | 占位参数组未标定(设计 §6/§8 挂账);判据 M1-M3+G0-G5 预注册,未跑 | 同上,无日期 | |
| A7 | `recipe_fence_enabled` | ADR-0432(已归档) | 判据可判(sim 触发面在);排期声明=本组**第一个跑**(形态达标率是当期头号缺项);未跑 | 退役批补排期,无日期 | |
| A8 | `form_break_sell_blocked_enabled` | ADR-0433(已归档) | sim 判据**结构性永不满足**已证(触发面=0,off 逐位一致);有效性归实机判读(拆队笔数 before-after),可验时点=实机回归战役(前置=sim 说服包) | 排期=蓝图 §8 武装序,无日期 | 不是死开关:实机判据真实存在且可判 |
| A9 | `below_floor_spend_gate_enabled` | ADR-0434(已归档) | 同 A8 形态:sim 触发面=0 已证,实机 r9 破息分账判读,基线(w42 26.3%/w43 5.5%)在案 | 同上,无日期 | |
| A10 | `junk_first_sacrifice_enabled` | 机制真值=affix_effects_data 实采;设计件 w880 族前序 | 双钥匙实机判据(≥2 局词缀零漏采 + ≥2 次牺牲对识别误判 0);实机样本未攒齐 | 无 | 与 A13 的 synth_equip_dep 有 `__post_init__` 联动禁独立开臂(构造期校验在) |
| A11 | `equip_env_fill3_enabled` | w880 DESIGN §4.1(`docs/.../w880_equip_env_design/`,现行树在) | 三钥匙(环境通道/行为对/sim A/B);未执行 | 无 | |
| A12 | `w875_energy_leak_enabled` / `w875_sync_action_enabled` | W875 环境B类评分补全包 | sim A/B 同池配对判据;死映射防线已核查(counter 值域有载体) | 无 | |
| A13 | `w878_mono/formed_bond/slow_burn/synth_equip_dep_enabled` | W878 死 tag 复活批 | 样本 ≥10 局判读方向一致;**注释自述「当前无模拟/实机输入,悬置默认关」**——按裁定重新核读:悬的是「输入」(词缀局样本未恢复)而非「判决」,属合法第 1 态;但「悬置」措辞系裁定前旧话术,且无期限 | 无 | formed_bond 另有判非 3 套补验前置 |
| A14 | `longterm_refresh_discount_enabled` | 机制原文 cw_invest_data id=120 | 输入缺:局内累计付费刷新计数**现无观测,缺省 0 → 折后价构造性永不生效** | 无 | 挂账理由最弱的一档——缺的是待开发接线而非外部采集在途;建议本批给它定「接线或删码」的二选一期限 |
| A15 | `megastar_enhance_enabled` | 巨星强化维度 | 输入缺:执行面未接(画面未建档 + 机制效果无文档真值);决策层意向已落遥测 | 无 | 半接线态(决策层活/执行面死),若长期无建档计划应考虑砍执行半边 |

**A 组共性处置建议**:清理批统一补裁决期限(建议锚「对局周期」:每个挂账开关给出「N 个实机对局周期内必须出 A/B 判读,不过则删码留 ADR」的具体 N);A14/A15 因输入属自家开发欠账而非外部在途,优先催办或收敛。

### 1.B 死开关清理项

**空**。现存默认关字段逐一核对后,无「判据永不满足」(A8/A9 的 sim 判据不可满足但实机判据在)或「概念已定谳仍留码」(定谳者均已在前批删码留墓碑)的条目。历史清理机制(第 4 态删码留 ADR)运转正常。

### 1.C 悬置违规项(已判决却默认关挂起)——**C1 已随清退批删除(走「关死」分支,dd-009 裁决)**

| # | 开关 | 违规判定 | 处置建议 |
|---|---|---|---|
| C1 | `spend_gate_enabled` + `spend_gate_interest_enabled` + `spend_gate_bench_enabled` | **唯一实质违规**。ADR-0499 三轮 A/B(W840/W848)已终局定性:清账门绿 ∧ 传导格三度双败 → 裁决「维持关保留」。这是「已判决」——按 2026-08-31 裁定第 3 态禁令(已判决却默认关挂起,任何档都禁),现状不合法;且其「恢复开臂前置=实机锚量化判据」**未定**(无定义、无期限)= 挂起无出口 | 二选一,不能维持现状:①**补判据**:实机锚量化判据立项 + 裁决期限(把它升回合法挂账);②**关死**:按第 4 态清理。注意 ADR-0499 明言「禁第四态删码」——若走 ② 须先回该 ADR 出增补裁决推翻此禁令(归档 ADR 在 adr_archive_20260901/0499-*,增补需走决策流程),不是静默删码 |
| C2 | (系统性)A 组全部 15 组 | 轻度违规:挂账无裁决期限(裁定明文「必须带裁决期限,超过一个对局周期未决=违规」) | 清理批统一补期限(见 §1.A 处置建议) |

## 2. 任务 2:吸收核对边界外候选核对

输入:`REVIEW_ABSORPTION_CHECK.md` §四列明不辖的三项——101653 #3/#4、082322 #1。核对基准 = redesign 设计件(IMPL_DESIGN.md + IMPL_FIX_LEMMAS.md R26-R42 链,即登记表)+ 生产代码现状。

### 2.1 082322 候选#1:危机带 R* 降档(门控缺口)→ **已被生产代码接管**(给指针)

- **接管证据(代码)**:`decision_v2/posture_release.py` 危机金出口臂——溢余基降档 **R*_crisis≡0**(P36-a′/ADR-0503;`crisis_overflow`=g,注释明引 match4 复盘 p2r1 hp3/金89/R*90 病灶,即 082322 本件)。总开关 `registry.crisis_release_enabled = True`(默认开,开关生命周期第 3 态,ADR-0503 §开臂判据尾注挂实机确认门)。
- **接管证据(实机)**:更晚的对局复盘 `g_20260831_101653.md` 专项④逐帧确认「R* 降档工作正常」:p2r1 cap=90 → p2r2 回落 50;p3r1 hp=1 开臂 ovf=13/bud=12/实花 36——082322 的「gold<R* 臂不开」门控缺口**未复现**。
- **语义对账**:082322 原诉求「hp≤emergency 带内 R* 不含排程升级费、hp≤10 时趋 0」;落地形态=危机臂辖域内溢余基整体改用 R*_crisis≡0(比诉求更彻底),全局 R*(储备线)不动。吸收完成,无需立项。

### 2.2 101653 候选#3:boss 帧备战核查清单(空槽上阵/idle 装备上身)→ **已吸收(结构层,更强)**

- **部署半边(明确)**:新设计不设「boss 帧专项核查」——部署是**每备战期无条件义务**:骨架序 M1(同帧部署)→ M1′(升级后新空位迭代补部署至不动点),依据=P24「零支出补部署严格支配」+ [33] 人口位当轮兑现(IMPL_DESIGN §3.1 M1/M1′ 行)。boss 帧是全帧义务的真子集,旧病灶「备战件躺 bench 不上阵」在新语义下构造性不可达。
- **装备半边(模块位在,建议补一条吸收证据锁)**:M7 装备转移在执行器序内(§3.1 序 M5→M2→M4→M1→M3→M1′→M6→M7),装备模块=§2.6 equipment.py(P42/P50/P34);另总纲 §3.4 #4「无用件聚拢穿到工具人」被 R32-中⑤ 引用为既定条款。本核对未逐款比对「idle 装备必穿」字面(装备分配判据细节属 equipment.py 设计件内部)。
- **处置**:不需立项修正项;建议落码批在 §6.4 测试位补一条行为锁「备战期结束时 board 空槽=0 ∧ 可穿 idle 装备=0」作为本候选的吸收实证(轻量补强,防实现拆装时静默脱落)。

### 2.3 101653 候选#4:强敌带(词缀强化遭遇/boss)战力预估缺位 → **已吸收(新语义处理,同 101653 #2 模式)**

复盘建议的两半分别有更强承载:

1. **「停手线放宽」半边——旧判据对象被架构性移除**:新设计 stop_flag=**分类谓词输入,非拦截门**(R5-6/§3.2:线成型只把非线内件重分类、落出 M2 序 1/2 进序 3-5 EV 域,受 ev_buy_veto 正常管辖仍可买)。「强敌前帧放宽停手线」在新设计里没有宿主对象——停手根本不构成消费封锁(与已核对过的 101653 根因 2 同一模式)。
2. **「强敌带战力预估」半边——入 λ 键 + 顾问架构**:λ_death 分层表键=**难度带 × 血带 × 位面 × 节点类型**(R26-H1 PL 键),难度维=局内旗牌读取真值优先(语料覆盖 90%)+公式 interim 兜底+持续对账漂移哨兵;节点类型维区分遭遇/boss。高危险格由 λ 顾问(相对分位触发,R28-1/R34-2)与血线硬地板承载「下一场强 → 收紧/转化」;equipment 侧另有 boss 掉落流例外(`boss_stream_exception()`,§2.6)。强敌帧的「提前转化」由 M6 溢余转压库 + 危机不变式(P36-a)在 executor 结构位承接(R1-11)。
- **如实申报的退化通道**(判读时须分键,非缺口):λ 顾问 p=None 期不消费(R29-1)、血线阈值 None 期恒 0 空窗(R36-2)——标定期前强敌预估面部分休眠,与 REVIEW_ABSORPTION_CHECK 对根因 1 申报的 θ fail-closed 退化同族;已进 §5.2 激活率门/影子键观测设计,首批 sim 按分键归因,禁读作证伪。
- **处置**:不需立项修正项;退化通道观测建议随根因 1 的「窗口期 K 冻结 × P2 星级停滞」联合观测一并在首批 sim 判读清单挂账(同一张清单可复用)。

### 2.4 核对汇总

| 候选 | 判定 | 指针 |
|---|---|---|
| 082322 #1 危机带 R* 降档 | **已被生产接管**(非 redesign 吸收) | `decision_v2/posture_release.py`(R*_crisis≡0,ADR-0503);实机验证=g_20260831_101653 专项④ |
| 101653 #3 boss 备战核查 | **已吸收(结构层,更强)** | IMPL_DESIGN §3.1 M1/M1′/M7、§2.6;建议 §6.4 补行为锁(见 2.2) |
| 101653 #4 强敌带战力预估 | **已吸收(新语义)** | IMPL_DESIGN §1 lambda_death 行(PL 键)/§2.0-3 规格①②/R5-6/§3.2;退化通道随首批 sim 分键判读(见 2.3) |

## 3. 处置建议汇总(给编排者)(**清退批后注:第 1/2 条已随 dd-009 清退批一并解决——全部默认关开关删除,无悬置对象;第 3/5 条仍开放**)

1. ~~唯一实质悬置违规 = spend_gate 三字段(C1)~~ → 已删(关死分支,dd-009)。
2. ~~批量补期限:A 组 15 组~~ → 已删,无存活默认关族;复活纪律见文首重对账节。
3. **ADR 指针修复**:归档目录补 README 索引或恢复 decisions/ 指针页(共享文档,先经用户确认)。
4. 死开关清理项 = 空,无需清理动作。
5. 边界外三候选均不需立修正项;两条轻量补强(§2.2 行为锁、§2.3 退化通道观测挂首批 sim 判读清单)建议随落码/判读批顺带兑现。
