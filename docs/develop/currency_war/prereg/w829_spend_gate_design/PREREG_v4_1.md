# PREREG v4.1 · 支出门 E-g 低位豁免修订版判前锁(重跑前冻结)

> 独立成文,不修改 v4 原文件(`PREREG_v4.md` 保持冻结原貌)。本文件 = v4 全文
> 语义继承 + 下述修订面的替换;未列条目一律以 v4 原文为准。修订程序依据 =
> v4 §3 Goodhart 条款原文(「>30% 即门判据过宽,回豁免面重审,不调阈值」)
> 与 v4 §4 越权修订显式声明同款治理要求(修订先声明、重跑前冻结)。

## 0. 修订声明(判前性质)

- **依据**:
  1. W847 豁免面重审报告(`../w847_goodhart_review/REPORT.md`):拦截构成
     解剖(62.3% 被拦 P1 帧中 89.0% 归 d1_interest 单臂;65.4% 的 d1 拦截
     落在帧初金 <40 深低位;重拦层 ≥9 帧 76 局 M1 改善:恶化=22:20,门收益
     归零)+ G2 沉积实证(被拦局末金 51.8 vs 未被拦 46.0,囤金随被拦帧数
     单调上升 = d1 深低位拦截的沉积,官方 P2 口径 +9.6pp)+ 数学先行论证
     (息保护价值 ∝ 保有档位可续收息,保有 <2 档时 50 封顶息不可达,保护
     的只是 1-2 金/轮残息,买入战力期望已由 piece_value 评分层计价)。
  2. 本仓 Goodhart 警报触发记录 = W840 报告 §6(主臂 62.3% / gate-only
     61.0% P1 帧被门拦 > 30% 线,v4 §3 条款触发)。
  3. 设计参照 = W829 v3 §4 豁免面与 `../w840_spend_gate_ab/REPORT.md`
     三臂基线(对照)。
- **变更面(仅此一项,其余逐字继承 v4)**:D1 息线臂增加 **E-g 低位豁免**
  ——仅当花后金仍保有 ≥2 个息档(即 ⌊花后金/10⌋ ≥ 2,等价花后金 ≥20)才
  拦;花后金 <2 息档的下穿买入放行。E-g 定义逐字:拦截条件由
  「花后金 < 息线 ∧ ⌊gold/10⌋ 下降」修订为
  「花后金 < 息线 ∧ ⌊gold/10⌋ 下降 ∧ **⌊花后金/10⌋ ≥ 2**」。
  豁免面其余(d2/d3 判据、E-a/E-b/E-c、压库谓词三条件、让位序)零改动;
  伞与子旗标开关、W820 标定值在库均零改动。
- **判前性质**:本修订在重跑批开跑前冻结;开跑后零改动、执行零偏离。
  Goodhart 达标线 30% 不变(不调阈值,仅扩豁免面);G7 带格维持 v4 §3
  U 推导带机制,W840 批 U 复算值 U_spec=4.51pp ≥ 1.9pp → **带 = ≤+1pp
  收紧格**(预期 E-g 后残差轻升,但仍须 ≤ 带内——放行的是保护价值 ≤1-2
  金/轮的残息段,泄露须实测确认可承受)。
- **本批主门(v4 §3 继承 + 新增核心指标)**:清账门(G4 ≤+2pp ∧ G7 ≤+1pp
  收紧带)∧ 传导格(M1-L ∨ 阶跃兑现率,至少一肢正证据,肢定义逐字
  v4 §0-1)∧ M4 方向非负 ∧ G2 不升 ∧ gate-only 选择性恒等 ∧ 分层警报
  零触发 ∧ **拦截率 <30%**(新增核心指标,容差带;分子分母口径 = v4 §3
  Goodhart 同款:P1 帧被门拦占比,拒因枚举逐轮计数)。
- **判定兑换(三选一,沿 v4 §3 失败分流)**:四门全绿 → 开臂建议呈编排者
  终裁(生命周期第 3 态);E-g 后传导格仍双败(即使拦截率达标)→ 门维持
  关 + 传导归实机锚(W840 同款第 1 分支变体);清账门红 → 判据回炉非删伞。
  G2 与拦截率任一红即预声明面红,如实记录并回登记(禁只治一头)。
