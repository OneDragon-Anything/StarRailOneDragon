# 0297 - 决策框架 v2 刷新×追级并存仲裁批(评分侧联动采纳+约束侧备选)

- **Status**: accepted(2026-08-25;**平局线 ±3.3 未达,如实报**——
  n=100 配对差 -5.76;但通道病治愈:lvl 2.07→6.27(≈v1 6.83),
  refr 10.3→2.91,且为全部并存变体中唯一双窗一致臂)
- **Context**:ADR-0295 残差重定位:刷新 re-decide 链抽干金→[12]
  追级息引擎门(levelup_interest_engine_gate)锁死升级通道
  (v2 lvl 2/局 vs v1 7;refr 10-13 vs v1 4);refresh_ev=1.5 臂
  证明关掉刷新即解锁 lvl 8-9/局但损失早刷找件净收益——需要的是
  **两通道并存**而非二选一。本批=仲裁层(层4)+评分刷新门
  (层3)的资源分配机制,ADR-0290 约束收口一处(constraints
  注册表)辖下。
- **Decision**:
  1. **两机制都实装,评分侧联动(方案 b)采纳为默认**:
     - 方案 a(约束侧,`refresh_budget` 约束):每局刷新预算
       `refresh_game_cap`(v1 量级 4-6)+追级保留金
       `levelup_reserve_gold`(等级未满时刷后金低于此值即让位);
       **诊断否决为默认**(双窗不稳),保留为注册 A/B 通道
       (默认 0=关闭),局刷新计数 `session.v2_refresh_used`
       由 arbiter 回写(数据源)。
     - 方案 b(评分侧联动,采纳):`refresh_starve_discount`
       ×`refresh_starve_gold`——金<40 时 refresh_ev×0.6
       (EV=1.5-2=-0.5 恒负分不刷),≥40 全额。**初值 0.6/40**
       由双窗+终验定。
  2. 回归锁:test_cw_adr0297_coexist_arbitration.py(饥饿折扣
     恒负分/阈值上全额 EV/预算上限触发/保留金触发+等级封顶豁免/
     两通道同局共存+计数回写/初值锁)+0293 hash 锁更新
     (16858a49→57450124,标定五参与 0295 两参值不变)。
- **Considered Options**(全部 sim 配对,snapshot 池,单进程串行;
  v1 臂每窗同进程重跑——ADR-0295 基线漂移教训):
  1. **20 局诊断(900000-900009)选机制方向**:base(改前)
     23.5/refr 10.3/lvl 2.2;v1 25.6/3.7/6.1;a1(cap=5)
     26.0/5.0/4.5;a2(cap5+reserve30)28.4/3.5/4.8;b(0.6/40)
     28.9/2.1/5.9;b50(0.6/50)20.9(refr 0.2 刷死);a2r25
     23.5(reserve 25 劣于 30)。b 的 mean 略高但单窗无分辨率,
     机制裁决交给双窗。
  2. **双窗 30+30(900000-900029/900030-900059,v1 臂重跑)**:
     a2 -3.27(SE 5.15)/-11.70(验证窗爆);a2b(cap5+res30+
     disc0.8/45)-4.37/-11.80;a3(cap6+res40)-6.20/-9.50;
     **b -5.23/-4.80(唯一双窗一致)**;b80(0.8/45)-3.93/-9.57;
     b85(0.85/40)-1.53/-9.97(折扣不破零→刷链不死 refr 10.2,
     原窗好运验证窗爆);b70(0.7/35)-6.87/-7.30。**无任何变体
     双窗同时进 ±3.3**;方案 a 族双窗全不稳(约束侧硬门在验证窗
     系统性劣于评分侧软联动),方案 b 采纳。
  3. **n=100 终验(900000-900099,b 采纳参数)**:v1 32.37/团灭 6/
     ge60 10;v2 26.61/团灭 8/ge60 5;配对差 **-5.76(SE 2.62)**。
- **Consequences**:
  - 正:刷新×追级通道病治愈(lvl 2.07→6.27≈v1 6.83,refr
    10.3→2.91,两通道同局共存被单帧锁钉死);并存机制参数化
    (四参进 registry+hash 锁),方案 a 约束侧留作 A/B 通道
    (约束收口一处不破);双窗一致臂入选(单窗最优臂 b85 的
    原窗 -1.53 是过拟合陷阱,验证窗 -9.97 实证)。
  - 负/风险:**平局线 ±3.3 未达**(-5.76 vs ADR-0295 的 -5.99,
    噪声带内无 sim 可见增益)。残差定位下移:**买入通道
    (buys 9.9/局 vs v1 15.0)是新的主导残差**——lvl 解锁臂
    (a2b lvl 7.5)仍 -4~-12,证明追级批已收口;买入饥饿在
    候选生成/目标件显影层(candidates.py,在飞 worker 域,
    本批不碰)。v1 臂跨进程波动 ~1.4(33.74→32.37)在判读
    配对差时须核同进程臂。
  - 教训入档:①「机制选型」与「参数标定」分两步——20 局诊断只
    判机制方向(a/b 谁能双窗一致),参数值必须直接上双窗
    (ADR-0295 教训复现);②折扣必须破零(EV<0)才断刷链,
    b85(0.85)留正 EV → re-decide 链照跑(refr 10.2),
    「排序让位」在不破零时退化为无效联动。
