---
screen_name: 货币战争-遭遇节点
appears_in: [currency_war]
last_updated: 2026-08-11
source_image: screens/货币战争-遭遇节点/default.webp
---

# 货币战争-遭遇节点(二选一遭遇选择)

## 何时出现 + 状态流转

对局内**位面推进中**随机出现的「遭遇节点」选择屏(非每轮必有;出现时机由肉鸽地图节点决定)。玩家从两张遭遇卡中**二选一**,确认后进入该遭遇的战斗(或效果),打完回备战。

- **入口**:备战 → (地图节点为遭遇时) → 本屏;或从备战点「返回遭遇选择」(右上,遭遇未结算时出现)回本屏。
- **出口**:点一张卡选中 → 点「选择」→ 进入对应遭遇(战斗)→ 结算 → 回备战。
- **放弃(可逆,实测 2026-08-11)**:「返回备战界面」(右上)退回备战 —— **不扣「剩余次数」、不放弃遭遇**:备战屏右上出现「返回遭遇选择」,可随时回到本屏继续选。故本屏与备战间可往返,只有「选择」才真正消耗本次遭遇进入战斗。
- **查看详情(子屏)**:「查看详情」(上中)→ 弹「敌方信息」全屏覆盖层(见下「敌方信息覆盖层」)。

## 识别特征(稳定锚点)

- **标题「遭遇节点」**(画面正上方,x≈900 y80)—— 画面独有,可作 id_mark。
- 「遭遇其一」「遭遇其四」(两张卡标题)—— 出现哪几个「其X」视当前遭遇池而定(不一定固定是一/四;检测用「遭遇其」前缀更稳)。
- 底部「选择」按钮 + 「剩余次数：N」。
- ⚠️ 勿用「难度」「奖励预览」等作特征(多屏共有 / 易变)。

## 可交互元素

| 元素 | 位置(中心,1080p) | 说明 |
|---|---|---|
| 遭遇卡·左(如遭遇其一) | (622, 530) 卡身(`遭遇卡-其一` pc_rect [445,340,800,720] 中心) | 点**卡身**选中(选中态 = 蓝色发光边框;未选中 = 棕色边框)。左卡通常难度低 |
| 遭遇卡·右(如遭遇其二/其四) | (1257, 530) 卡身(`遭遇卡-其二` pc_rect [1080,340,1435,720] 中心) | 点卡身选中(选中态切换,可反复点两卡改选)。右卡通常难度高、奖励更好 |
| 「查看详情」 | (960, 278) 上中 | 弹「敌方信息」覆盖层(敌波次/等级/词缀/难度分;可切「遭遇其一/其二」页签 + 「详情/技能」页签)。X 关闭(右上 ~1862,65) |
| 「选择」按钮 | (1082, 898) | **不可逆出口**:选中一张卡后点此 → 进入该遭遇战斗 → 结算回备战。消耗本次遭遇(只剩「返回」不消耗) |
| 「返回备战界面」 | (1790, 80) 右上 | 退回备战(**可逆**:备战「返回遭遇选择」可回本屏,不扣剩余次数) |

**难度标记(实测)**:每张卡标题下「-难度-」旁有 **V 形图标**,数量 = 难度等级。颜色随难度档变(低档蓝 V / 高档红 V,实测:1V 蓝 / 2V 蓝 / 4V 红)。⚠️ **OCR 读 V 不稳**(常误读成 M)→ 难度计数靠视觉大模型 / CV,不靠 OCR。

**两张卡内容(实测两例,随遭遇池变化)**:
- 例 1(2026-08-04):左「遭遇其一」1V 蓝 + 金币×2;右「遭遇其四」4V 红 + 随机 4 费角色×3。
- 例 2(2026-08-11):左「遭遇其一」1V 蓝 + 金币×2;右「遭遇其二」2V 蓝 + 员工投影仪×1。

(难度越高敌人越强、奖励越好,「难度越高敌人越强大,请谨慎选择」。)

## 交互模型(实测 2026-08-04,**踩坑固化**)

正确序列 = **① 点卡身(选中)→ ② 点「选择」(确认)**。

⚠️ **两步之间不要插任何点击**:旧实现插了一个「空白点击 (960,540)」想关详情,实际会**取消选中** → 「选择」时无选中 → 不推进 → 死循环(plane1 round6 后卡死 7 分钟,OCR 反复见同一屏,根因)。点卡身是「选中」不是「开详情」,无需关。

bug#1 缓解:关键 click 前 `mouse_move`(零移动 click 不被框架 before_screenshot 判成拖拽)。

### 穷举交互验证(2026-08-11,补建档漏的 onboarding §4)

逐个交互确认每个元素行为(可逆先、不可逆出口最后):

| 交互 | 行为(实测) | 可逆? |
|---|---|---|
| 点左卡身 (622,530) | 选中左卡(选中态从右→左,蓝发光边框移动) | 可逆(再点右卡即改选) |
| 点右卡身 (1257,530) | 选中右卡(选中态→右) | 可逆 |
| 点「查看详情」 (960,278) | 弹「敌方信息」覆盖层(见下节) | 可逆(X 关闭回本屏) |
| 点「返回备战界面」 (1790,80) | 回备战,右上出现「返回遭遇选择」可回本屏;**不扣剩余次数** | 可逆 |
| 点「选择」 (1082,898) | 消耗本次遭遇、推进对局(2026-08-11 测:选其一→遭遇选择消失、回备战;遭遇节点为战斗类,选择后进战斗流程) | **不可逆**(出口) |

选中态 = **视觉(蓝色发光边框),非 OCR**;默认进屏时已有一张卡选中(非无选中)。「选择」未选中时灰、选中后亮 —— 但因默认总有选中,实战少见灰态。

## 敌方信息覆盖层(查看详情子屏,待建档)

「查看详情」弹出的全屏覆盖层(`screens=[]` 未建档),展示选中遭遇的敌方信息 —— **策略化选卡的关键数据源**(难度分 + 词缀决定是否打得过)。

- **结构**:标题「敌方信息」(左上);顶部「遭遇其一/其二」页签(切两卡的敌方信息,当前卡高亮);右上「详情/技能」页签(详情 = 敌人列表,技能 = 敌人技能);右上 X 关闭(~1862,65)。
- **左侧主区**:敌人列表(按波次「·第1波」,每敌 等级90 + 星级;两行排列)。
- **左下红条**:「敌人难度 NNN+N」(如 108+3,该遭遇综合难度分)+ 一排**词缀图标**(实测 4 个:随从强化/冷冻冬眠/净化身心/火之熄火)。
- **待建档**:本覆盖层未建 screen_info(`screens=[]`)。bot 要做策略化选卡(按难度分 + 词缀评估两卡)需先建档读这些 —— 列入后续画面建档(task)。

## 识别快照

`analyze_screen`(基线图 `screenshot_20260804_111031_308056.png`):
- 匹配画面:**货币战争-遭遇节点 `is_precise=True`**(2026-08-10 建档后;标识-遭遇节点 conf 0.9994 + 按钮-选择 + 按钮-返回备战界面)。建档前为 `screens=[]` 纯 `round_by_ocr('遭遇其一')` 检测。
- 全量 OCR:`返回备战界面` / `遭遇节点` / `遭遇其一` / `遭遇其四` / `-难度-` / `M` / `奖励预览`×2 / `金币` / `随机4费角色` / `2` / `3` / `难度越高敌人越强大，请谨慎选择` / `剩余次数：1` / `选择`。
- 视觉大模型(智谱 GLM-4.5V):两张六边形卡左右并列;左卡难度 1 蓝 V + 金币×2;右卡难度 4 红 V + 随机4费角色×3;初始无卡选中;「选择」未选中时灰边。

## 备注 / 待查

- **screen_info 现状**:**已建(2026-08-10)** —— id_mark `标识-遭遇节点`(「遭遇节点」)+ `遭遇卡-其一`/`遭遇卡-其二`(卡身点击选中)+ `按钮-选择` + `按钮-返回备战界面`。穷举交互验证(2026-08-11)已补:onboarding §4 全元素行为实测过。handler(`battle_loop.py`)现仍 OCR 判定 + 硬编码坐标,后续可改用 area 坐标(单一真相源)。
- **策略化选卡(待做)**:当前默认选左卡(难度低、稳)。应按「敌方信息覆盖层」的难度分 + 词缀 + 奖励评估两卡价值 —— 属节点决策(见 `docs/develop/currency_war/strategy/08_node_decisions.md`);**前提是先建「敌方信息覆盖层」screen_info**(本 doc 上节)。
- 「遭遇其X」的 X 不固定(其一/其二/其四实测)→ 检测用前缀「遭遇其」+「选择」+「遭遇节点」组合更稳。
- **「返回备战界面」不扣「剩余次数」**(2026-08-11 实测确认:回备战后右上出现「返回遭遇选择」可回本屏,遭遇保留)。
- fixture 已归档 `sr-od-test/screens/货币战争-遭遇节点/default.webp`(2026-08-10)。
