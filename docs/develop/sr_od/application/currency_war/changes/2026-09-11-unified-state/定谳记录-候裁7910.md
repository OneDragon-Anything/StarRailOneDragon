# 候裁 7/9/10 对抗定谳记录(账本 T-94;T-7 开工前置硬门)

> **修订注(2026-09-12,编排者转达用户裁定,第三版)**:
> ①候裁 7 **用户已直接定谳 = 改**(模块文件名随正名改)——本记录候裁 7 节的对抗过程
> 降为**对照验证**(结论与用户定谳一致,攻击过程保留作证据底稿);
> ②候裁 9 **用户已直接定谳 = B 案(打散归位)+ 有用性前置**——公共零件先盘点有用性
> (全仓引用实数),死零件清理申报,活零件各自迁入 kernel 语义相关的既有模块;
> 「留原文件改名」案被否。本记录候裁 9 节原对抗结论(A survived)降为**对照记录**,
> 执行口径已按用户裁改写为:零件有用性盘点表 → 清理申报 → 打散归位映射表(逐零件
> 唯一答案);其中动作契约族的归宿含**一处对「既有模块」字面的偏离申报**(新设
> cw_actions.py,理由与证据在案,候编排者/用户追认);
> ③候裁 10 用户尚未裁决——对抗结论暂作 T-7 执行规格与呈报建议,用户裁决后以其为准。
>
> **卷首·自主定谳声明**:本记录由 T-94 候裁 7/9/10 对抗定谳批作出(干净上下文 worker)。
> 总授权依据 = AGENTS.local.md「自主推进模式」条款(状态 ON:「可自主决策一切内容,不要咨询
> 用户;对不确定的内容,发起子 agent 对抗直到找到治本方案」)。三候裁出自
> `game_state/r5-migration-plan.md` §7 条 7/9/10,是账本 T-7(W8 本体切割删除+正名)的
> 开工前置硬门。每条候裁执行了两段式攻击——①**无前提攻击**(假设计划倾向有错,主动找
> 反例/被忽略影响面/与正本或既有裁定的冲突);②**治本核验**(根源两问:根在哪一层、修的
> 是根还是症状)。攻击 survived → 定谳 = 计划倾向;被破 → 定谳 = 攻击揭示的修正案。全部
> 证据为 2026-09-12 工作树亲读(文件:行号在案;行号会漂移,复核以符号名/常量名为准)。
>
> **定谳权声明**:定谳权形式上保留用户——**用户对任一条不同意即翻案重开,翻案走对抗批**
> (重新发起对该条的无前提攻击,不沿用本记录结论)。候裁 7/9 已由用户直接定谳;候裁 10
> 各节「翻案条件」列明何种新证据会推翻本定谳,即翻案的受理门槛。
>
> **材料底稿**(全只读;本批入库文件 = 本记录):r5 计划 =
> `game_state/r5-migration-plan.md`(§2 W8 行/§3/§7 条 7/9/10);正名依据 =
> `changes/2026-09-11-unified-state/landing.md` §3.4 + ADR-0630(正名计划锚);
> W5 前案 = `changes/2026-09-06-redesign/w5-透传域建模方案.md`「BenchChar 居所定谳」节;
> pin_scope 生命周期正本 = `design/决策行文件schema设计.md` §8-2;代码 = 现树亲读(锚点逐条在案)。
> 本批零代码/零测试/零账本直改(账本仅按交付契约跑 dag.py 状态行)。

---

## 候裁 7:模块文件名随正名改(cw_board_state.py→cw_game_state.py)vs 只改类名留旧文件名

> **状态:用户已定谳 = 改(2026-09-12,经编排者转达);本节对抗结论降为对照验证——
> 攻击独立得出同一答案,以下内容保留作证据底稿。**

### ① 候选案

- **案 A(计划倾向)= 文件名随正名改**:类名 BoardState→GameState(正名,W8 名段既定)
  的同时,`kernel/cw_board_state.py` 更名 `kernel/cw_game_state.py`(git mv)。
- **案 B = 只改类名,留旧文件名**:GameState 类住在 cw_board_state.py 里。

### ② 攻击过程与结果:案 A survived(对照验证)

**攻击线一:审查噪声——改名 diff 会混进删除波污染逐 hunk 归属。**
量了:改名机械面 = src 130 行/55 文件 + 测试仓 116 行/48 文件 + docs 133 行/26 文件,
约 380 行。攻击是否成立,取决于它是否**新增量级**——对照同段既定对象:类名正名
BoardState 符号本身 = src 269 行/52 文件 + 测试仓 287 行(git grep -w / Select-String
现读)。模块名面与类名面**同量级、同性质**(纯机械字符串替换,零行为),且 r5 §3 已把
W8 定为「先删后名」两段——两段结构就是为隔离这类机械大 diff 设计的,改名归「名段」,
与删除段审查隔离。攻击提出的风险已被既有波次结构吸收,**未破**。

**攻击线二:在飞批冲突——T-56 测试仓归并批、T-86/T-90 前置设计与 W8 同文件域。**
属实但被时序门挡住:W8 进入门 = T-6 验收绿 + 全仓 GameState 零引用锁(r5 §2 W8 行),
后者只有在 W6 尾波(决策面切换)落地后才可能绿——即 W8 开工时同文件域在飞批已落定。
残余的时序冲突只有一处:测试仓文件名改名(batch3/batch4 是 T-56 归并批对象)→ 不阻塞
W8 主体(文件名≠运行时语义),执行口径给顺延规则(见⑥)。**未破,转化为执行口径。**

**攻击线三:git 考古断裂。** 同目录改名用 git mv,历史随文件走(rename detection);
import 行的 blame 噪声一次性付清。反向核:留旧名不是没有代价,而是把代价改成年金——
每个未来读者「搜 GameState 定义 → 跳到 cw_board_state.py」的错位税,永久收。**未破。**

**攻击线四:「名字不算语义,不值得 380 行」。** 语义检查:仓内既有约定 = 文件名随主类名
(cw_state.py↔GameState、cw_board_state.py↔BoardState、cw_effect_inventory.py↔
EffectLedger 等,全仓一致)。T-7 正名的语义动机 = GameState 成为唯一正名(r5 §3 论证
三条;ADR-0630 后果节逐字锚「迁移完之后将 BoardState 改名成 GameState」)。类名改而
文件名留 = 正名做一半,且**主动破仓内命名约定**——这不是审美,是可检索性:日后按文件
名找类、按类名猜文件的全部直觉都失效。治本核验:错位是约定层根,改名是治本;留旧名
是用「省一次机械改动」换永久错位。**未破。**

**攻击线五:命名撞车。** `cw_game_state` 全仓零占用(git ls-files + src 树 glob 实测);
`game_state/` 是 docs 目录名,不是 Python 包,无模块名冲突。**未破。**

**案 B 死因(对照记录)**:全部收益 = 省 ~380 行机械 diff(一次性);代价 = 符号/文件
永久错位 + 破命名约定(年金)。攻击未找到案 B 的隐藏收益(无工具链/外部契约依赖旧
文件名:grep 全仓,cw_board_state 无 config/脚本/screen_info 引用;测试三处字符串锚
随批更新即可)。

### ③ 定谳结论

**用户定谳 = 案 A:文件名随正名改,cw_board_state.py → cw_game_state.py(git mv,历史保留)。**

### ④ 代价/影响面实数(2026-09-12 树,复核命令在案)

| 面 | 实数 | 口径/命令 |
|---|---|---|
| src 模块名出现 | 130 行/55 文件 | `git grep -n "cw_board_state" -- "src/*.py"` |
| 其中 import 语句 | 102 行/52 文件 | 同上 + `cw_board_state import|import …cw_board_state` 过滤 |
| src 非 import 行(注释/docstring/字符串) | 28 行 | 同上取补集;含 `_BS_ATTR = '_cw_board_state'`(:1492,内存态 session 属性键,仅 :1508/:1512 同模块读写,非持久,随批改 `'_cw_game_state'` 零行为) |
| 测试仓 | 116 行/48 文件;import 88 行 | Select-String 两层深目录口径 |
| 测试仓字符串锚 | 3 处 | test_cw_state_write_discipline.py:50(D1 锁常量)/test_cw_board_state_batch4.py:262/test_cw_w5_passthrough_adoption.py:574 |
| D1 锁常量 | 1 处 | `_BOARD_STATE_MODULE = 'cw_board_state.py'`(discipline 锁 :50;sentinel :92 同源引用,改常量即全生效) |
| 测试文件名 | 4 个含 cw_board_state | test_cw_board_state{,_batch3,_batch4,_consume}.py;cw_core.txt/slow_marks.txt 对之零引用(实测) |
| docs | 133 行/26 文件 | 正本区 12 文件(decisions 0630/0651、design 2 件、game_state/ 8 件)+ changes/ 14 文件 |
| 计划原估对照 | 计划「32 导入面」→ 实测 52 文件 | 树在 W5/W6 波1/决策行落地后长大了,数字以本表为准 |

### ⑤ T-7 执行口径(唯一答案)

1. 名段执行 `git mv src/sr_od/application/currency_war/kernel/cw_board_state.py
   → kernel/cw_game_state.py`,与类名正名同段;import 路径全仓机械替换
   `kernel.cw_board_state` → `kernel.cw_game_state`(含函数内 lazy import,102 行/52 文件现口径)。
2. D1 锁常量改 `'cw_game_state.py'`(test_cw_state_write_discipline.py:50 一行;哨兵断言
   :92 自动跟随——不改则锁红,锁红即正确拦住改名漏项,不是机械跟绿对象)。
3. 测试字符串锚两处随改:batch4:262 路径、w5 采纳锁 :574 豁免名(锁语义重推:两处
   锚的都是「容器模块文件」这个对象,对象换了名,锚跟随,锁语义不变)。
4. `_BS_ATTR` 改 `'_cw_game_state'`(内存键,零持久化,零行为)。
5. 4 个测试文件名 git mv 随批改(纯改名零内容);**若执行时 T-56 归并批仍在飞且文件面
   相撞,文件名改名顺延至该批落地后同窗补做**——记 W8 报告尾项,不阻塞主体。
6. docs 正本区 12 文件的模块路径锚随批更新;**changes/ 14 文件不改**(迭代区寿命有限,
   铁律禁长期引用,不投入)。
7. 改名后交付「`cw_board_state` 全仓零引用 grep 锁(src+测试仓;docs changes/ 豁免)」,
   入 W8 交付清单(r5 §2 W8 行「grep 锁全绿」的落地形态)。
8. 随裁义务(修订批/落码批承接):r5 §7 条 7 加一行收口注指本记录;landing.md §3.4
   文件面行「kernel/cw_board_state.py(正名)」表述随批对齐。

**翻案条件**:已闭环(用户定谳);出现依赖旧文件名的外部契约时由执行批按事实申报。

---

## 候裁 9:cw_state.py 残余共享词汇类型的居所

> **状态:用户已定谳 = B 案(打散归位)+ 有用性前置(2026-09-12,经编排者转达);
> 「留原文件改名」案被否。本节原对抗结论(A survived)降为对照记录;执行口径已按
> 用户裁改写为 ⑥ 的三表:零件有用性盘点表 → 清理申报表 → 打散归位映射表。**

### ① 候选案(原候裁文本)

- **案 A = 留原文件,文件随批更名**(r5 §7 条 9 示例名 `cw_vocab.py`;W5 前案对
  BenchChar 建议「留 cw_state.py 原地随 P8 更名定居,不迁 kernel」)。
- **案 B = 迁 kernel 既有模块**(打散归位;用户追加前置 = 先盘点零件有用性,
  死零件清理申报,活零件逐个归位)。**用户定谳 = B。**

### ② 底稿:cw_state.py 顶层符号全量 = 82 个(python 正则提取,含类/函数/模块级赋值)

分三册对账:**活零件 64**(有外部 src 消费,逐一归位)/ **清理申报 7**(零或近零消费)
/ **切割面 11**(GameState 本体族,随 W8 删除;W6 前过渡引用数在案)。63+7+11+1
(ShopCard 旧版计入清理)= 82 闭合。

### ③ 对照记录:原对抗过程(A survived)与对 B 的两条实质反对

原攻击(对 A):分域成本(6-8 簇 × 67 文件消费面)、动作族无宿主、grab-bag 内聚性
病真实但非本裁的病、W5 BenchChar 隔离前案——四条攻击线当时均未破,详见本记录第二版
(git 历史 commit 49a7dac00/6a659bc79)。**用户裁定推翻该结论,以其为准。** 对 B 的
两条实质反对,在 ⑥ 执行规格中的处置:

1. **「动作族无既有宿主」** → 处置:动作契约族 14 符号**整体一个家**,不切碎;唯一天然
   候选 cw_prep_actions.py 存在**同名类硬撞**(SellBench:85/SellDeployed:129/
   DeployMove:136/LevelUp:144 与族 A 同名,同模块不可共存)→ 该族归宿为**新设
   kernel/cw_actions.py**,作为对「既有模块」字面的**一处显式偏离申报**(见映射表注)。
2. **「分域成本大」** → 处置:有用性前置压缩成本——7 个死零件先清理申报(不搬),
   64 个活零件逐个给唯一归宿;且打散暴露出一个**净收益**:合成规则族与
   cw_merge_simulate(合成引擎)语义双源收敛(该模块 :35 现 import cw_state 即证据)。

### ④ 用户定谳

**B 案(打散归位)+ 有用性前置**:零件有用性盘点(全仓引用实数)→ 死零件清理申报 →
活零件各自迁入 kernel 语义相关的既有模块。执行规格 = ⑥ 三表。

### ⑤ 零件有用性盘点表(2026-09-12 树;格式 = 外部 src 文件数/处数 | 测试仓文件数/处数)

**第 1 归宿 kernel/cw_exec_state.py(执行层状态载体)——席位/槽位跟踪域 + 节点台账 + 布局转发,26 符号**
宿主证据:ExecState 自申报 tracked_bench_chars/tracked_deployed(:151-156,pad 态定长槽表契约)与 plane_node_ledger(:232)——席位坐标系与台账宿主都在执行侧。

| 符号 | 外部 src | 测试仓 |
|---|---|---|
| BenchChar | 31 文件/215 处 | 71/402 |
| BENCH_CAPACITY | 18/65 | 23/71 |
| DEPLOYED_FRONT_CAPACITY | 8/20 | 3/13 |
| DEPLOYED_CAPACITY | 6/18 | 3/10 |
| DEPLOYED_BACK_CAPACITY | 1/3 | 0/0 |
| iter_occupied_deployed | 8/46 | 3/26 |
| deployed_occupied | 8/28 | 4/12 |
| bench_occupied | 10/26 | 3/14 |
| bench_from_compact | 6/18 | 1/5 |
| deployed_from_compact | 5/12 | 1/1 |
| bench_place | 5/12 | 4/7 |
| deployed_place | 4/9 | 1/7 |
| pad_bench | 3/8 | 2/8 |
| pad_deployed | 2/3 | 1/2 |
| iter_occupied | 2/8 | 2/6 |
| deployed_slot_no | 2/5 | 1/3 |
| snapshot_copy | 3/8 | 1/3 |
| rebuild_deployed_from_board | 1/3 | 1/4 |
| _apply_row_to_char | 1/1(=cw_board_by_row) | 0/0 |
| ledger_node_type | 6/11 | 7/36 |
| get_node_ledger | 5/11 | 2/11 |
| ledger_update_plane | 4/12 | 4/16 |
| PlaneNodeLedger | 2/4 | 3/3 |
| fill_boss_by_position | 1/3(=cw_screen_plane_intel) | 1/5 |
| reset_layout_unknown_state | 2/6 | 1/4 |
| _layout_unknown_reset | 1/1(=cw_back_layout 注册槽) | 0/0 |

**第 2 归宿 kernel/cw_merge_simulate.py(合成引擎)——升星合成规则族,9 符号**
宿主证据:该模块头自申报「同名同星计数 bench+deployed 全场/满 3 升星连锁/满栏自动多买」(:1-10)——与本族语义同域;迁移同时收敛双源(其 :35 现 import cw_state)。

| 符号 | 外部 src | 测试仓 |
|---|---|---|
| same_star_count | 5/15 | 2/5 |
| _merge_bench | 4/6 | 3/4 |
| count_merge_material_blocked | 4/11 | 1/4 |
| merge_material_reject_reason | 3/9 | 1/6 |
| merge_buy_completes | 3/9 | 1/3 |
| merge_buy_k | 2/7 | 1/1 |
| _apply_full_bench_merge_buy | 2/2 | 1/1 |
| will_merge_on_buy | 1/2(=mandate economy_cycle) | 0/0 |
| merge_material_stale_names | 1/3(=mandate shop) | 2/10 |

**第 3 归宿 kernel/cw_economy.py(经济/等级/节奏骨架)——金/经验/刷新/卖出回金/血线,12 符号**
宿主证据:模块头自申报「金/经验/息/刷新成本…ADR-0129 单击经验模型」——XP 族与刷新成本语义已在此;血线族 = W6 波2「经济血线门」落点同域。

| 符号 | 外部 src | 测试仓 |
|---|---|---|
| sell_refund | 12/48 | 9/49 |
| bench_char_cost | 8/22 | 3/5 |
| XP_TO_NEXT_LEVEL | 9/38 | 6/18 |
| XP_PER_BUY | 6/24 | 2/6 |
| REFRESH_COST_BASE | 6/18 | 3/8 |
| xp_apply_clicks | 5/8 | 1/10 |
| XP_CLICK_COST_FALLBACK | 2/4 | 1/2 |
| xp_clicks_to_level | 1/2(=cw_screen_prep) | 2/8 |
| card_cost | 4/9 | 2/6 |
| effective_hp_threshold ⚠ | 5/9 | 3/15 |
| HP_SAFE_THRESHOLD | 1/1(=currency_war_config 缺省源) | 1/3 |
| DIFFICULTY_HP_TABLE | 2/2(=config+cw_observation) | 0/0 |

⚠ effective_hp_threshold 签名含旧 GameState → W8 前由 W6 波2(经济血线门)先行重家/改签名;
若 W8 开工时仍带 GameState 签名,按切割面随本体删除并以 W6 波2 载体为准,不得原样搬入。
_SELL_MULT(sell_refund 私有倍率表,内部 2 处)随 sell_refund 同迁,不单列。

**第 4 归宿 kernel/cw_bond_equips.py(羁绊标签单一源)——板面计数派生,1 符号**
| 符号 | 外部 src | 测试仓 |
|---|---|---|
| _recount_board | 6/15(含 cw_battle_calib 2 处) | 3/18 |

宿主证据:_recount_board 构建于 unit_bond_tags(本模块单一源)之上,board 计数派生与标签函数同域。

**第 5 归宿 kernel/cw_deploy_logic.py(部署选人围栏纯逻辑)——板上唯一性守卫,1 符号**
| 符号 | 外部 src | 测试仓 |
|---|---|---|
| board_unique_key | 1/2(=cw_op_deploy 同名唯一性守卫) | 1/4 |

宿主证据:唯一活消费方 = 部署 op 围栏;本模块已消费 DEPLOYED_BACK_CAPACITY,同域。

**第 6 归宿 kernel/cw_run_allocator.py(跨局分配层)——局终结算,1 符号**
| 符号 | 外部 src | 测试仓 |
|---|---|---|
| MatchOutcome | 1/4(=cw_loop 局终分支) | 0/0 |

宿主证据:MatchOutcome docstring 点名消费语义 =「跨局分配器/runs summary」= 本模块。

**第 7 归宿 kernel/cw_actions.py(新设)——动作契约族,14 符号(偏离申报见注)**

| 符号 | 外部 src | 测试仓 |
|---|---|---|
| BuyCard | 31/118 | 54/294 |
| LevelUp | 29/137 | 27/99 |
| SellBench | 27/109 | 32/160 |
| RefreshShop | 21/74 | 26/79 |
| SellDeployed | 18/60 | 4/12 |
| DeployMove | 18/44 | 5/13 |
| CompTransaction | 12/39 | 2/21 |
| LevelUpShop | 11/27 | 12/44 |
| CloseShop | 9/32 | 10/36 |
| Action(联合类型) | 10/33 | 0/0 |
| SwapDeploy | 5/11 | 1/3 |
| SELL_BENCH_CONVERT_REASONS | 4/12 | 3/9 |
| PickEvent | 6/10 | 3/21 |
| FillSpec | 1/9(=cw_evolution) | 0/0 |

> **偏离申报(唯一一处)**:动作契约族**不可拆**(Action 联合类型引用全体成员、sim 按
> isinstance 链分发、telemetry 按类名序列化、双族对照表文档块随族走),必须整体一个家;
> 唯一天然语义候选 cw_prep_actions.py 存在同名类硬撞(SellBench:85/SellDeployed:129/
> DeployMove:136/LevelUp:144——同模块同名类不可共存),kernel 其余既有模块无「决策→
> 执行动作契约」语义承载。故本族归宿 = **新设 kernel/cw_actions.py**:这是 B 案精神
> (语义归位、禁 grab-bag)对该族的唯一忠实执行,偏离「既有模块」字面,候编排者/用户
> 追认;若坚持既有模块,退路 = 整族并入 cw_exec_state.py(执行域聚合,次优)。

### ⑥ 清理申报表(死零件候选;W8 批首 grep 复核,零消费即删,有消费回映射表)

| # | 符号 | 有用性实测 | 申报处置 |
|---|---|---|---|
| 1 | deployed_to_compact | 外部 0/内部仅 def 自身/测试 0——**现值即死**(docstring 声明的 sim/遥测序列化消费方不存在) | 删除 |
| 2 | bench_clear | 外部 0(内部消费全在切割面 simulate/mutate)/测试 1 文件 | W8 删除(测试锁随批重推) |
| 3 | deployed_clear | 外部 0/内部同上/测试 0 | W8 删除 |
| 4 | iter_deployed_slots | 外部 0/测试 0(⚠ ADR-0392 文档提名其为「deployed 迭代一律」——删前按文档锁重推,确认无消费即删) | W8 删除(文档锚随批) |
| 5 | _card_to_bench | 外部 0(内部消费全在切割面)/测试 1 文件 | W8 删除 |
| 6 | _bench_char_cost(公共名别名) | 外部 2 文件/7 处(sim engine_p1、telemetry schema) | 两消费点改指公共名 bench_char_cost 后删别名(docstring 自申报「接缝面批后新代码一律用公共名」) |
| 7 | ShopCard(旧版,:111) | 外部 12 文件(多数与容器版同名消费重合)/唯一直接 import 1 文件(contracts.py:34) | 随本体切割删除;唯一直接消费点改指 cw_game_state.ShopCard(容器版,:477) |

### ⑦ 切割面(11 符号,随本体删除;W6 前过渡引用在案,由 W6 尾波清零)

GameState(外部 56 文件/400 处 | 测试 99/538)、simulate(15/49 | 19/85)、
mutate_bench_deployed(6/8 | 4/28)、_resolve_comp_transaction(0 | 1/1)、
_remove_by_identity(外部仅 cw_evolution 注释提及,非导入)、_apply_comp_transaction、
_tx_state_view、_log_action、_bench_clear_by_identity、_deployed_clear_by_identity
(后五者外部 0)。GameState/simulate/mutate 三项的清零 = W6 尾波(决策面切换+sim 引擎
切换)的既定交付,W8 进入门(全仓 GameState 零引用)已辖。

### ⑧ T-7 执行口径(唯一答案,按用户裁)

1. 按 ⑤ 映射表逐簇迁移:git mv 不适用(分散归位),采用「符号搬家 + 消费面 import 改指」;
   64 活零件一批迁完,禁按簇多批拆分(每多一批=一次全仓 import 面重开)。
2. 迁移顺序建议(依赖单向化):先迁无下游依赖的叶子簇(bond_equips/deploy_logic/
   run_allocator/economy)→ 合成族(merge_simulate,同批做双源收敛:其引擎内自实现
   语义与迁入函数对齐,禁两套并存)→ 席位/台账域(exec_state)→ 动作契约族
   (cw_actions 新设,双族对照表文档块随迁)→ 最后删 cw_state.py 空壳与切割面。
3. 清理申报表 7 项按 ⑥ 处置;批首 grep 复核记录进 W8 报告。
4. 每个目的地迁入后:逐目的地 import 环检查(`python -c "import …"`)+ ruff +
   受影响测试;槽位语义锁(ADR-0316/0392 系)与 D1 锁不动容器,预期不受影响,锁红
   按「先重推锁语义」处理。
5. cw_actions.py 新设为**偏离申报项**:W8 派单任务书须显式带上本申报,编排者/用户
   未追认前该簇不得开工;追认或改判(退路 = 并入 cw_exec_state.py)后按表执行。
6. 随裁义务:r5 §7 条 9 加收口注指本记录(注明用户裁 B 案+有用性前置);W5 透传域
   建模方案「BenchChar 居所定谳」节加用户裁更正注(原建议留原文件,已被 B 案取代,
   BenchChar 归宿 = cw_exec_state.py——坐标系隔离论仍成立,只是家从「独立词汇文件」
   改为「执行态模块」)。

**翻案条件**:已闭环(用户定谳);映射表内单个归宿的翻案 = 对该簇提新证据走对抗批
(如某既有模块被证实语义相容且无撞名)。

---

## 候裁 10:pin_scope='board_state' 标记值随正名改写 vs 保留旧值

> **状态:用户尚未裁决——对抗结论暂作 T-7 执行规格与呈报建议;用户裁决后以其为准。**

### ① 候选案与生命周期底稿

决策行钉面标记 `pin_scope`(定义 schema.py:562,语义注释 :557-561):消费切换完成前,
行带 `'board_state'` 标记 = 「本钉解析出的是 BoardState 面,非决策实际消费面」,
防无标记的对账假结论;切换落地后撤销(回空串)——正本 =
`design/决策行文件schema设计.md` §8-2(:383-388,两种取值共存合法,旧读面按值分型)。

- **案 A = 保留旧值(数据值兼容,词汇冻结)**
- **案 B = 随正名改写(断代:标记值词汇跟类名走)**

### ② 攻击过程与结果:案 A survived(且发现前提已空心化)

**攻击线一(对 A 的最强反对):「旧值词汇过时,正名后留着误导读者」。** 攻破分两层:
(i) 语义层——标记值是历史窗口(M4→W6)的事实记录,其语义由生命周期定义,不由当前
类名定义;改写成 'game_state' 反而制造「旧行 board_state/新行 game_state」的**伪代际**,
读者必须额外学一条「game_state 值=改名前的 board_state 值」的换算——断代方案自己
制造了它声称要避免的混乱;(ii) 事实层(本批新证据)——**该标记从未在任何存档数据里
存在过**:实测 `.debug/currency_war/telemetry` 全部 31 个 decisions.jsonl,
`"pin_scope"` 行数 = 0(Select-String 全扫;含 live/sim/freeze 全部档案)。旧生产写入面
已随删除波 1 整段退场(recorder.py 现 93 行残壳,头注自证「策略源收编 9 流写入端已删」;
r5 :51 的「pin_scope 在产 recorder.py:223-231」是 2026-09-11 旧树锚,现树不成立);
新决策行写入面(cw_decision_trace.py:326)按 C6 定谳恒写空串。**改写方案没有数据对象;
案 A 的「兼容」成本实测为零。未破,且前提空心化使本裁从「两害相权」降为「无对象不改」。**

**攻击线二:「那干脆把字段也删了」。** 出题外:决策行类字段只追加不删除(设计稿 §2.2
契约),字段存废不在候裁 10 辖;且测试锁明确锁着该字段的解析兼容
(test_cw_decision_trace_r4.py:105/120-127:可选末尾字段缺省 '' + 历史值 'board_state'
可解析回来)。**未破。**

**案 B 死因记录**:无数据对象可改写(0 行);能改的只有注释词汇与测试锁——改锁 = 为
跟名破坏「历史值解析兼容」的锁语义(锁红≠改动错,此锁语义未被任何新裁定取代);
改注释词汇 = 把历史标记值改成现行名,伪造代际。**死因:唯一真实代价(读者需知道
'board_state' 是历史值)不因改写消失,反而因伪代际翻倍。**

### ③ 定谳结论(建议,待用户裁决)

**建议案 A:标记值 `'board_state'` 保留(词汇冻结);W8 不改写任何数据值、不动解析兼容锁。**

### ④ 代价/影响面实数

| 面 | 实数 | 口径 |
|---|---|---|
| 生产写入 | 恒 ''(cw_decision_trace.py:326;字段封闭清单 :167/:306 同步申报) | 亲读 |
| 旧档案携带 pin_scope 的行 | **0**(31 个 decisions.jsonl 全扫) | Select-String '"pin_scope"' 全扫 .debug/currency_war/telemetry |
| 测试锁 | 2 处不改动 | test_cw_decision_trace_r4.py:124(缺省 '')/:126-127(历史值解析) |
| 注释正名对照 | 1 处可选补注 | schema.py:557-561 |
| docs 锚 | 正本 2 处不动(§8-2 生命周期/r5 :217);changes/ recovered 若干不投入 | 亲读 |

### ⑤ T-7 执行口径(唯一答案,随用户裁决生效)

1. W8 的「pin_scope 标记值核对」= 三步,全部只读/一行注:①grep 锁确认全仓无
   `'board_state'` 值的新写入(现态即满足);②schema.py pin_scope 注释块(:557-561)
   末尾补一句正名对照(「标记值按历史窗口语义保留;BoardState 符号已于 W8 正名
   GameState」);③r4 解析兼容锁保持不动。
2. 不改写任何档案数据(归档只读铁律);不新增 'game_state' 标记值词表。
3. 随裁义务:r5 §7 条 10 加收口注指本记录。

**翻案条件**:发现任何真实数据行携带 pin_scope(现 0)且其消费读面因正名产生歧义
——届时按「读面按值分型」契约走翻案对抗批,而非简单改写。

---

## 卷尾·定谳总表

| # | 候裁 | 状态 | 定谳 | T-7 执行口径核心 |
|---|---|---|---|---|
| 7 | 模块文件名 | **用户已定谳** | **改**(cw_board_state.py→cw_game_state.py,git mv) | import 面 52 文件+D1 锁常量 1 行+测试锚 2 处+_BS_ATTR+docs 正本 12 文件;零引用 grep 锁交付;测试文件名 4 个随批改(与 T-56 冲突则顺延补);对抗结论作对照 |
| 9 | 残余词汇居所 | **用户已定谳** | **B 案打散归位+有用性前置**(留原文件案被否) | 82 符号三册对账:64 活零件七簇归位(cw_exec_state 26/cw_merge_simulate 9/cw_economy 12/cw_bond_equips 1/cw_deploy_logic 1/cw_run_allocator 1/cw_actions 新设 14)+清理申报 7+切割面 11;**cw_actions 新设=一处偏离申报候追认**;对抗结论作对照 |
| 10 | pin_scope 标记值 | **待用户裁决**(建议 A) | **保留旧值 'board_state'(词汇冻结)** | 核对三步(零写入 grep 锁+注释对照一行+锁不动);档案 0 行实测,无改写对象 |

**实测贡献汇总**:候裁 7 导入面计划估值 32→实测 52 文件;候裁 9 完成用户前置要求的
全量有用性盘点(82 符号三册对账闭合)并暴露合成语义双源收敛机会;候裁 10 实测档案
0 行,裁決从「数据兼容权衡」降为「无对象不改写」。

*用户翻案权保留:候裁 7/9 已由用户定谳;候裁 10 不同意即翻案,翻案走对抗批(重新无
前提攻击,不沿用本记录结论);各节翻案条件即受理门槛。T-94 对抗定谳批,2026-09-12
工作树快照。*
