# P43 逐件证明验证报告:连胜-息取舍 EV

> 验证对象:`docs/game/currency_war/research/proofs/p43-streak-interest-tradeoff-ev.md`(单篇+索引行,只读未改)
> 验证人:数学验证工程师(逐件证明验证批);日期:2026-09-08
> 方法:阅读门(全量 docs/game/currency_war 索引+注册表直调+06_input_model §3 导航+公理栈)+ 五门逐门;数值自检脚本亲跑重跑+独立重实现 DP 对拍。
> 阅读门声明:①`docs/game/currency_war/` 索引与证明集已过(重点 economy §10.1 / combat §4 / user_playstyle [19][39] / math_proofs 索引行);②注册表直调:`cw_economy.py`(STREAK_GOLD_TABLE / LOSS_GOLD_BY_NODE / WIN_STREAK_BREAK_INTEREST / streak_gold)+ `engine_p1.py` 收入段(L731-757);③06_input_model §3 注册表地图已读;④**跳过声明**:p47/p39/p40/p48 证明正文未读——P43 对它们是纯接口引用(只消费 L 函数/支出侧 EV 结论),验证以脚本内自含复刻的 p47 L 递推+引擎口径交叉核验代替;p03/p13 连胜旧树证明未读——P43 第 9 行自含声明「未读禁 anchor」,无依赖即无验证义务;AB_* 账本做字段级抽样(门④)。

## 五门判定

### 门① 内部推导 —— ✅ 通过

- **DP 递推 vs 引擎口径**:逐条对上 `engine_p1.py` 收入段(L741-749):轮首入账、连胜金=`streak_gold(进轮连胜)`(胜轮发 T[s] 非 T[s+1],增量只影响下一轮)、败轮金=`LOSS_GOLD_BY_NODE[败掉轮节点]` 次轮轮首补发且仅当下一轮是战斗类节点(`_prev_node in LOSS_GOLD_BY_NODE` 分支)、奖励轮照发 `streak_gold(streak)` 计数不动、补给轮零发——证明 A1 的口径描述与代码逐位一致。齐次模型忽略的「败轮后跟奖励/补给轮漏发 loss 金」分支已在 A4 显式声明(方向: 高估连胜账的败侧对冲,量级 ~1/3×2-4 金/败)。
- **独立重实现对拍**(验证者自己的 DP,与 p43_check.py 不同代码):V(0,0,2)=3−p 解析锚逐位复现;ΔV(2,9,0.1)=2.09 / ΔV(6,9)=2.76 / ΔV(0,9)=1.32 / ΔV(4,6)=1.60 / D(s=4,battle)=5.41(次轮金差+1、续期差 4.41)/ 上限 12.4 / ΔV(0,9,0.15,p=0.55)=0.85(P3 翻案格)——全部与文档表格一致。
- **脚本亲跑**:`uv run python tools/cw/proofs/p43_check.py` 全部断言通过,七段输出与文档数值逐位吻合(P4 锚 79.0/38.7/40.3/36.3、P6 尾段 0.20/0.40/0.60、P1b 粗式比 1.4×/4.8×/11.2×、R=8 与 R=40 续期差逐位相等)。
- **iff 判据**:破息保连胜 iff ΔV_streak ≥ L——定义式判据,L 纯引 p47(脚本内自含复刻对拍 L(40,20,9,I=8)=L(I=9)=5,带差 0),无重推越界。重建闭式「任何视界禁用」有断言锁(精确≤粗式+ε;饱和检验),推导成立。

### 门② 锚点直调 —— ✅ 通过

| 锚点 | 注册表直调值 | 证明引用 | 判定 |
|---|---|---|---|
| STREAK_GOLD_TABLE | `(1,1,2,2,2,3,4)`(cw_economy L43;49/49 弹窗实测,economy §10.1) | 同值,索引=进轮连胜越界取尾 | ✅ |
| LOSS_GOLD_BY_NODE | `{battle:2, encounter:4, boss:4}`(L70;实机 108 局/767 轮差分,boss 8/22 散布大置信中) | 同值,boss 置信已在待标定①如实分级 | ✅ |
| interest cap=5 / 金位 50 | engine `min(_icap, gold//10)`、GOLD_CAP=50 | 脚本 GOLD_CAP=50/`min(g//10,5)` 同构 | ✅ |
| 奖励轮照发/计数不动 | engine L743-744 + 85/85 | A4 同口径 | ✅ |
| 连胜跨位面 | combat.md §4:用户裁定 2026-08-31「连胜会跨位面继承」 | A4 收口、位面内=下界 | ✅(见门③ 小瑕疵) |

脚本常量为注册表忠实转写(非 import 但逐值核对无漂移)。

### 门③ 建模对象 vs 玩法文档 —— ✅ 通过(1 处状态不一致小瑕疵)

- 收入三通道(基础+息+连胜)、战败无连胜成分、败轮实发=基础+息(用户 2026-08-31 裁定,economy §10.1 收口行)→ A1/A2 与机制文档一致;A2「败轮也拿基础奖励→差分消去」有实机残差口径支撑。
- combat §4:无递增连败金(单边)、败轮底金 2/4、奖励关不计连胜数、连胜≈100 金(攻略口径,已挂账待标定④)——建模面全部对上。
- [19] 三态谱:原文三变量=连胜在手/**血量安全边际**/来牌;P43 按 [39]② 收口(血量变量退役,裁决变量=连胜在手/来牌/息差)数学化——与 user_playstyle L267 收口推论逐字一致,收口语境已在头部声明,非偷换建模对象。
- **瑕疵**:` 待标定清单` 行②「连胜是否跨位面 🟡 待实机」与 A4「已裁定(2026-08-31 用户)……待标定③ 收口」自相矛盾(且 A4 引的编号③在表里是「胜率 p 与 d→Δp」——编号错位;combat.md 侧也写「p43 待标定③ 收口」,同源错位)。实质不影响结论(两处都取位面内=下界口径,方向保守),属登记状态滞后。

### 门④ 实证 vs 账本 —— ⚠️ 挂账如实(可达性核验通过,清单未执行)

- sim 对拍清单六项均未执行——证明自身如实标注(结论强度表逐命题给最弱环节),索引行同口径挂账,无虚报。
- **可达性亲核**:AB 数据集在位(`redesign/AB_cw3_n40_{s0,r2,r3}`、`AB_legacy_v2_n40_*`,各含 decisions.jsonl);字段级抽样:`sim.node / sim.gold_before / sim.income{base,interest,streak,event} / sim.p2_win_p` 均在——连胜账分布(清单1)与破息事件 A/B(清单2)在现有账本上**可重建**。
- **必须修正的坑**:清单1 未指定测度通道,而 `income.streak` 字段已被 P51 v3 专核 T5 坐实噪声性错位(胜轮 38/58、全轮 202/490 与注册表不符,无系统可校准结构,已弃用定谳)——若对拍走该字段会复踩 P51 同坑,必须按 P51 J3 的「胜负旗 × 注册表锚」重建。

### 门⑤ 参数溯源 + 底线形态(超额收益隔离)—— ✅ 通过

- **零调参**:本件零新拟合常数——全部数值由注册表两表+p47 L 递推导出;p/Δp 显式外生(A3/A7,粗模型声明);数值带以 Δp 为轴给出而非点值授权,符合「禁点值当闸门」。
- **CI/稳健性**:T 表 49/49 零散布;loss boss 8/22 置信中已分级(待标定①);p 时变→带平移结构不变(A3 声明);短半衰期→R 扫描带覆盖(A6)。s0 敏感性(79.0 vs 75.0,差 40.3 vs 36.3)>30 门双口径均过。
- **底线形态检验(连胜置零)**:①「默认姿态=保息、破息是例外」在连胜=0 底线上成立——s=0 行全表 ΔV 最小(1.32@Δp=0.1),引擎口径翻案格(0.85<L=1)给「未连胜不为单场破息」数值形态;②「连胜账单独从不证成深破息」(L=14 在 p=0.7 数学不可达,上限 12.7)与连胜无关侧亦稳;③**只属上行层的结论已正确分层**:浅破可证成(Δp*≈0.05-0.34)、s≥5 饱和、位面末熄火——全部以 ΔV_streak>0 为载体,消费形态=花侧加性外差项(§⑤),不进底线。
- **与 P51 的分层定位(本件核心检验)**:**不冲突,分层正确且互补**。P51 推论4 明确 W_flow「只计当轮连胜表差,不计连胜链未来成长期权与断连胜重爬成本——下界注记形态;若入账只增 W」——P43 的 ΔV_streak 恰是 P51 排除项的独立定价(续期差主导正是「重爬成本+维持态溢价」),两量不同源不双计;量级同带互证(ΔV_streak 2.8 金/9 轮 ≈ 0.3 金/轮 vs W_flow 0-0.5 金/轮、多桶 CI 触零);P51「W_flow 单通道不支撑任何转化判据」与 P43「连胜账单独从不证成深破息」同向收口。**缺口**:两侧目前只有隐式一致——P51 §9 联动清单未列 P43,P43 §⑤ 也未点名 P51 W_flow,互指缺位(见修正建议3)。
- **旧常量冲突状态**:`WIN_STREAK_BREAK_INTEREST=2` 仍在代码(cw_economy L98,plan 层 `_should_save_for_interest` 消费)——与 P43 逐案判据冲突,证明 §⑤ 已登记处死名单+「重构批联动、本篇不删」;索引行同口径。状态:如实挂账,非证明缺陷。

## 最终判决

**通过(可消费为「已证」)**。五门:①✅ ②✅ ③✅(1 处登记状态小瑕疵)④⚠️ 挂账如实(清单未执行+测度通道须修正)⑤✅。索引行「R2 无阻断,对抗收口」与逐门复核结果一致——按用户裁定收口不豁免五门,本次全门执行,**无新阻断**;核心数值(独立重实现逐格对拍)与锚点(注册表直调)双重复核通过。

## 修正建议(按优先级)

1. **(小修,文档内)待标定表状态对齐**:行②「连胜跨位面」由「🟡 待实机」改为「已裁定(用户 2026-08-31 继承)/计数保留待实机核验」,并修 A4「待标定③ 收口」的编号错位(该事项在表内是②);combat.md §4 的「p43 待标定③ 收口」同源错位一并修。
2. **(对拍前置)清单1 测度通道改注册表锚**:补注「连胜账分布对拍弃用 `income.streak` 字段(P51 v3 T5 坐实噪声错位),按胜负旗×注册表锚重建」——防对拍执行时复踩 P51 已定谳的坑。
3. **(分层互指)P43↔P51 显式挂钩**:在 P43 §⑤ 或 P51 §9 联动清单补一行——「ΔV_streak = P51 W_flow 下界注记显式排除的连胜链成长期权+重爬成本的独立定价,消费时与 W_flow 不双计」;目前 P51 推论4 是单向注记,P43 侧无回应,未来消费方(策略层若同时引两件)存在重复计账的解释空间。
4. **(实现侧挂账维持)WIN_STREAK_BREAK_INTEREST=2 处死**:重构批联动时以 P43 判据(ΔV_streak≥L 逐案计算)为替换目标;处死名单条目建议补注替换目标指针。
5. **(挂账维持)待标定④ 攻略~100 金 vs 本文 36-40**:sim 对拍 6 执行时若实测 ≈100,按证明预留路径排查息复利/收入联动差额。

*本报告未修改任何证明/代码/索引文件。*
