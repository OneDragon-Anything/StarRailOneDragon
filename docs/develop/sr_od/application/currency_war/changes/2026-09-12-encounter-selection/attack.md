# 遭遇选档简化 对抗审查报告（无前提攻击）

循环第 10 轮（预期收敛确认轮）。攻击者上下文干净：未采信任何前轮结论，全部发现来自本轮对四文档当前态的逐字直读、判据源原文、生产代码直调与数字脚本实跑（1.052 幂组 / 门槛倒数 / W1 断言 / 低估倍界 / 1.052^18 共 8 组实跑，另有锚点逐行直调）。
本文只攻击不修复；未改任何设计文件与生产代码。范围澄清：裁定 6（结算屏现成读数授权进遭遇决策 + GameState 持久化，README 裁定汇编 6，2026-09-12）与 ADR-0536 退役删除（用户命令 2026-09-12）均为用户既定命令，本轮不攻击其正当性，只攻击四文档融合一致性与落码可行性。
术语约定：**暗装** = 判据未标定期间维持与现行生产行为逐位一致的保守缺省（最低档 + 零刷新）；**窗口** = 判据读取的最近两场已结算普通战斗行；**删失** = 该读数经全部兜底后仍为 None（不可判定）；**carry** = 失读时沿用上次好值的写入方式（`GameState.carry`，代码既有名）；**环** = E-2 拟新建的 GameState 结算观测环；**telemetry-only 行** = 战败结算页补录的 RoundOutcome 行（`_record_round_outcome(telemetry_only=True)`，代码既有名）；**1f / 3b 分支** = cw_screen_battle_wait 分支链里「失败结算页」「前往结算链」两个代码内分支编号；**净值残差** = 遭遇节点自身旗牌真值减去决策时读值的差（E-3 拟测标定量）。

## 结论概览（发现统计）

**高 1 / 中 3 / 低 3。零实质发现未达成，未到定稿门槛。第 9 轮 4 项修复中 3 项已修、1 项（L9-2）details 侧修好但留下两处新残留（归 M10-1/M10-2）。本轮高发现位于九轮均未攻击过的「败局行生产链」面：结算观察半的唯一落盘点在 telemetry-only 门外，败局页补录行从不落账，且页 1 填充率暂存被永不落账的行吞掉——判据的败局分支语义在该现状下系统性失效，方向恰为乐观反转。指定复核点①（M9-1 净值链）裁定互洽无矛盾；②（L9-2 环深论证）裁定不完全自洽。**

- 高 1 项：败局结算行在写入层结构性缺失/填充率恒删失（H10-1，详见发现清单）——κ_S 状态表的边缘/不足两态全部依赖败局行带活填充率入环，现状不成立；缺失后果不是退化为保守，而是窗口被更早的胜局填满 → 恒判足量 → 乐观选档，且 W4 暗装锚对此不设防。
- 中 3 项：README 环深下限「5」与 details §8「已知下限 = 2」未同步（M10-1）；L9-2 修正后公式缺「较晚窗行之后到决策点」行数项且新括注为假命题（M10-2）；E-3 净值残差定谳缺「遭遇节点自身旗牌」读点通道，配对协议给不出该值（M10-3）。
- 低 3 项：「定谳前按原始加成 = 保守向」的成分枚举前提未申报 + 两处方向措辞过头（L10-1）；「触发全集单一源 = §9」三处互指但集合不一致（L10-2）；盲选遭遇行经验表语义未申报（L10-3）。

## 指定复核点专项裁定

### ① M9-1 净值语义全链一致性：**互洽，无矛盾——但保守向缺前提申报、残差定谳缺采集面（归 L10-1/M10-3）**

逐处核对（本轮全部直调命中）：契约 2 两层申报（details :21，旗牌值语义按节点自身 / 决策读点 = 前一普通战斗节点备战帧写入值；快照链锚 `cw_screen_encounter.py:284`/:406 两处逐字命中，:280/:402 代码注释自注「overlay 时 board 不可读 → 用上次备战快照」与本语义一致）；§3 同源申报拆两层 + Δ_t 净值定义 + economy.md:107 原始加成仅作量级锚 + 禁把 −4 归并进 f_min（:69）；契约 3 可加性前提改「已由读点事实定谳」（:22）；§8 D_enc 行改「决策时读 GameState 现存值」（:112）；§9 Δ_t bullet 同步（:126）；E-3 阶段行「净值语义配对定谳」（:155）；design §2.1（:42）/§2.2（:50）同步。八处语义同构，无旧口径残留（四文档 grep「−4 修改器已含/无需显式建模」零命中）。

「定谳前按原始加成取值 = 保守向」与「E-3 净值残差定谳」**无矛盾**：二者是先后两阶段——前者是开闸前的缺省取值（需求多计 → 可及性低估 → fail-safe），后者是标定批用净值语义识别残差并取代缺省；「禁归并 f_min」恰好保证残差可识别，两句话方向一致。判别子闭合性本轮独立复验：生产写端仅 `cw_observation.py:2558`（observe，门 = live ∧ 非空）/:2561（carry）两处，全仓无 enemy_difficulty 的 write_logic/write_prior 调用方；carry 对从未写过字段早退（cw_game_state.py:2265-2266）；Field 默认 (None,'observation')（:401-402）→ 判别子 `source=='observation' ∧ value 非空` 对首读前态/简报恒值态/陈旧态全部拒判，闭合成立。

两个不归入「矛盾」但必须修的缺口：保守向主张是无条件句，其成立依赖「两帧间无其他变动成分」（L10-1）；残差定谳需要的遭遇节点自身旗牌值没有读点（M10-3）。

### ② L9-2 修正后环深论证自洽性：**不完全自洽——已知表下限 2 本身成立，但公式缺一个行数项、括注为假命题、README 未同步（M10-1/M10-2）**

- 已知表部分成立：P1 1-7 窗 = 1-4/1-6（间仅 1-5 补给零行）、P2 2-5 窗 = 2-2/2-4（间仅 2-3 补给零行），R_between = 0、下限 2，与 plane_schedule_observed.md 地面真值（用户 2026-09-02 裁定）逐槽核对一致 ✓。
- 不自洽处：§8 修正后公式 = 「2 + 两场之间的非普通战斗行数」，并括注「决策时点在较晚一场之后恒 0 行」。该括注只在「遭遇紧跟普通战斗」的已知表形态成立；P3 早期遭遇的跨位面窗口（details §4 自己的规则「P3 遭遇 = 同法（最近两场普通战斗）」，无位面辖域）下，较晚窗行（如 2-4）之后到决策点之间有 2-5 遭遇/2-6 奖励/2-7 boss 三行入环——「恒 0 行」为假，真实深度需求 = **2 + R_between + R_after**（跨位面已知形态 = 2+0+3 = 5，恰为第 9 轮旧口径「下限 5」的数值来源）。行为仍安全：深度 10 覆盖 5，超限退化为窗口 <2 → 不足态 fail-closed；但「离散模拟两场之间非普行 ≤6、窗口需求 ≤8」与公式自己的 R_between 定义对不上（已知变异格局下两窗行之间的非普行 ≤2，6 只能是含 R_after 的全跨度读数）——同一节内三种口径并存。
- README 裁定 6 行（:11）仍写「环行模型下限 5 + 余量」，并指「论证 details §8」——details §8 现在写「已知下限 = 2」。指针目标与指针文本数值直接矛盾。

### ③ 全量终扫（九轮修复后仍与代码/宪法/知识源不符的承重断言）：**高 1 + 中 3 + 低 3（见发现清单），其余承重断言直调核实成立**

终扫覆盖面与未发现声明的完整清单见文末「已攻击且未发现问题的面对账」。本轮新增攻击面（九轮未触及）：结算观察半落盘门的排除面（telemetry-only）、败局行填充率暂存生命周期、性能账本缺行的生产佐证（cw_reconcile 2026-09-02 分诊事故注）。

## 发现清单

### 高（1 项）

**H10-1｜败局结算行写入层结构性缺失、败局行填充率恒删失：κ_S 败局分支的输入前提不成立，缺失方向 = 乐观反转且暗装锚不设防**

- 位置（设计侧）：details §2 状态表（边缘 = 恰一胜 ∧ 败局 fill ≥ 1/2 ∨ 删失；不足 = 败局 fill < 1/2 等，:53-54）+ W2b/W3 锁行（:136-137）+ E-3 量测点清单（:155）；details 契约 1「判据消费字段（生产写端 = cw_settlement_obs.py:485-498 结算观察半）」(:16)；landing 3.2「写点 = 结算观察半直写位 `_write_settlement_observation`」(:33)。
- 代码事实（全部本轮直调）：①性能账本唯一落盘调用 = `_write_settlement_observation`（cw_screen_battle_wait.py:133 `performance.record(obs)`），全仓唯一调用点 = :421，位于 `if not telemetry_only:` 门内（:418）——函数 docstring 自注「telemetry-only 面（败局页补录）不写任何一侧」（:297）。②败局页主通道是 1f 分支（:710 注释「1f 是失败页主通道」），1f 只发 telemetry-only 行（:747 → :546）；败局行要落账只有 3b 分支「前往结算」帧全写一条路（:812-818），触发依赖 1f 模板门失配，流程依赖、非常态保证。③页 1 挑战进度填充率暂存（:722-744/:770-795）在 telemetry-only 记录路径被合并（:406-417）并**当场清空**（:416），随后该行不落账——后到的全写行拿到空暂存，填充率恒 None：**败局行即使落账，fill 也是结构性删失而非读失删失**（胜局行不受影响：胜局链 ：647 全写时暂存合并后落账）。④生产佐证：cw_reconcile 的 HP 下行守卫专设「窗内无任何 outcome 行 = 结算漏采/telemetry 断」臂并注「频发 → 查结算观测回路」（:473-509，含 2026-09-02 分诊事故「抽样 5/6 帧合法下行被拒」留证）——性能账本缺败局行是实机高频形态，非理论边角。
- 影响：κ_S 三态里依赖败局行证据的分支（边缘的 fill 判别、不足的 fill < 1/2 与全败判定）在现状下不可达或退化：败局行缺失时窗口被更早的两场胜局填满 → 判足量（天花板全开）——恰与设计「败局是能力不足信号」的保守方向相反；败局行落账但 fill=None 时,「恰一胜 + 败局」恒落边缘-删失分支，「fill < 1/2 → 不足」永不可达（不足态欠触发 → 边缘放行至其二，同样偏乐观）。两条路径都不触发暗装（数据「看起来够」），W4 对此无防线。E-3 量测点清单（fill 读取率/节点类型解析率/档位读出率/混淆矩阵）全部以「败局行在环内」为前提，无一项量测败局行本身是否入环。
- 修正方向（只指方向不代修）：①E-2 环写端显式处置 telemetry-only 面——或双落点（1f 败局页行同样入环）、或把页 1 暂存合并改为「仅全写行消费才清」，并同步修订契约 1 写端锚（:485-498 是构造点非落盘点）；②设计 §2 申报败局行可得性前提，E-3 量测点补「败局行入环率、败局行 fill 可得率」两项，不达标 → 败局分支按 fail-closed 收窄或判据维持暗装；③W2b/W3 注明「以败局行入环前提通过」的测试语境边界。

### 中（3 项）

**M10-1｜README 环深「下限 5」与 details §8「已知下限 = 2」未同步**
- 位置：README :11（裁定 6 行「结算观测环深度 = 10（环行模型下限 5 + 余量……论证 details §8）」）vs details §8（:117「已知下限 = 2」）。
- 事实：L9-2 修复只改了 details §8（模型换轴：跨位面余量并入深度 10 的工程余量，已知表下限改 2），README 裁定汇编未跟着改，且 README 明文指向 details §8 作为论证出处——读者按指针过去看到的是另一个数字。第 9 轮修复验证表曾把「README :11 下限 5」记为已修项的组成部分，本轮模型换轴后该行成为残留。
- 修正方向：README :11 改「已知表下限 2 + 跨位面/变异余量，深度 10（离散模拟需求申报 ≤8）」，或删数值只留指针。

**M10-2｜L9-2 修正后环深公式缺 R_after 项，新括注「恒 0 行」为假命题**
- 位置：details §8 :117（公式与括注原文见指定复核点②）。
- 事实：公式只计「两场之间」的非普战行；「较晚窗行 → 决策点」之间的入环行（跨位面窗口 = 前位面尾段遭遇+奖励+boss，已知 3 行）被括注「恒 0 行」排除——该断言与同节自认的「P3 无在册节点表 + 变异位不设解析上界」自相矛盾（P3 早期遭遇正是 R_after > 0 的形态）。离散模拟申报的「两场之间非普行 ≤6」在公式自己的定义下无来源（已知变异格局 ≤2），应为含 R_after 的全跨度口径。深度 10 与 fail-closed 兜底使行为面无险，但这是第 10 轮指定复核点，论证本体必须闭合。
- 修正方向：公式改「环深 D ≥ 2 + R_between + R_after（R_after = 较晚窗行之后到决策点之间的入环非普战行数；已知表 R_after = 0，跨位面窗口已知 3）」，括注与模拟申报按同一口径重写。

**M10-3｜E-3 净值残差定谳缺「遭遇节点自身旗牌」读点通道，配对协议给不出残差被减数**
- 位置：details 契约 3/§9（「E-3 配对以净值残差定谳」:22/:126）、landing 3.3（配对 = 「遭遇决策帧与其后结算帧（D 值/填充率/档位/胜负）」:49）。
- 事实：净值残差 = 遭遇节点自身旗牌真值 − 决策时读值。遭遇节点无备战帧（契约 2 自注），遭遇屏只显档位词「遭遇其X」不显数值（cw_node_obs.py:36-75 读法即证）；数值旗牌的读取相位 = 备战帧（cw_observation.py:2158-2178），敌方信息覆盖层未建档（cw_screen_encounter.py:17-19 自注「词缀在未建档的『敌方信息覆盖层』里」；provisional.py:96-102 自注 EV 核点亮前置 = 「浮层难度条读数通道」待建）。决策帧只含 GameState 现存值（被减数），结算帧不含任何旗牌——landing 3.3 的两帧配对**结构上不含被减数**，残差无采集面。这违反 iteration-design 写作硬规则 2（实现者需自行设计读点 = 未定稿）。
- 修正方向：E-3 申报遭遇节点自身旗牌的读点方案（位面详情画面敌人浮层的敌人难度值区已建档、plane_schedule_observed.md:92 在册——但遭遇决策流不打开该屏，需申报采集批的取帧路径），或改用等价定谳量并写明换算；读点未定前 Δ_t 的定谳路径不闭合。

### 低（3 项）

**L10-1｜「定谳前按原始加成 = 保守向/fail-safe」缺成分枚举前提；两处方向措辞过头**
- 位置：契约 3（:22「定谳前按原始加成取值 = 保守向（需求多计 ≈4，fail-safe）」）、§3 同源申报（:69「可及性低估 ≤1.22 倍，fail-safe」+ 数量级核对括注「两节点间品质/连胜只增不减，实际比值比例证更保守」）。
- 事实：①净值公式只枚举「档位加成 + 遭遇 −4 + 连胜增量」；economy.md:107 的构成还有「特殊加成（……圣杯羁绊试炼+30）」与「策略加成（金+3/彩+6，每拥有 1 个）」——若存在遭遇节点域特殊加成、或两帧之间新获策略使品质项变动，真净值大于原始加成，缺省取值方向**翻乐观**，无条件 fail-safe 句失真（对拍定谳可兜住，但前提须申报）。②「只增不减」对连胜不成立：败局重置即减；该场景（边缘态前一战斗落败）下实际比值比「零增长例证」**更不**保守，括注方向断言反向（判据本身仍保守，因 −4 低估方向占主导，见①的边界）。③「≤1.22 倍」界只在连胜增量 s ≥ 0 成立：s=+1（前一战斗胜）实为 1.052³ ≈ 1.16，s<0 时 ≥ 1.052⁵ ≈ 1.29（更保守但超界）。
- 修正方向：保守向句加限定（「在已列成分下保守；未列成分的漏计方向候 E-3 残差核」）；「只增不减」改「品质只增、连胜随前场胜负可增可减（减向更保守/增向幅度 ≤+1）」；「≤1.22 倍」改「s=0 时 1.22、s=+1 时 1.16、s<0 更保守」或直接删界留方向。

**L10-2｜「触发全集单一源 = §9」三处互指但集合不一致**
- 位置：总式（design :39 与 details :42「触发全集单一源 = details §9」）、§9（:126「本节开闸门 = 暗装/开闸判定的单一源全集……不另行列举触发集」）、W4（:138「触发全集单一源 = §9」）。
- 事实：§9 清单只含标定门（f_min ∧ g ∧ Δ_t ∧ 条语义定谳）；结构门（D_enc 无现场真读、窗口无可用 D_win 行）列在总式与 §3，不在 §9 清单内。三处宣称 §9 是「全集」单一源，但 §9 本身不是全集——字面执行（W4 只锚 §9 分支）会漏结构门注入形态（W4 行内实际列了三个注入形态，与「单一源 = §9」自相矛盾）。
- 修正方向：「单一源 = §9」限定为「标定门单一源」；结构门单一源写明 = 总式触发词 + §3 真值缺失分支。

**L10-3｜盲选遭遇行（候选未读到 → 默认选卡）的经验表语义未申报**
- 位置：details §3 经验档层（:73「每场遭遇战无论成败产出 (档位 t, D_enc 真值, fill, killed) 数据点」）、W13（:147「`difficulty_node`/`encounter_tier` 非 None 路径」）。
- 事实：候选未读到时生产链盲选 idx0 且**不写** chosen_encounter（cw_screen_encounter.py:217-218 守卫：候选空直接 return）——该遭遇行 encounter_tier 无源。W13 只锁非 None 路径，None 行入不入经验表、入了怎么消费，两文档均未定义；盲选帧恰是识别质量最差帧，其经验样本系统性缺失属未申报的选样偏差。
- 修正方向：申报「盲选行不入经验表」（None 排他）或定义档位回填源，二者取一并写入 §3/W13。

## 数字复算对账表

| # | 文档断言 | 文档出处 | 实跑/直调结果 | 判定 |
|---|---|---|---|---|
| 1 | Δ=10 → g≈1.660，f_min ≤ 0.602 | details §3 :71 | 1.6602 / 0.6023 | ✓ |
| 2 | Δ=20 → ≈2.756，≤ 0.363 | details §3 :71 | 2.7562 / 0.3628 | ✓ |
| 3 | Δ=30 → ≈4.576，≤ 0.219 | details §3 :71 | 4.5759 / 0.2185 | ✓ |
| 4 | 1.052^18 = 2.4905（≈血量×2.5 对账） | details §3 :71；economy.md:109 | 2.4905 | ✓ |
| 5 | f_min=0.5 下最大可及 Δ ≈ 13.7 | details §3 :71 | ln2/ln1.052 = 13.67 | ✓ |
| 6 | W1：1.052⁻¹⁰ = 0.602 ≥ 0.5 | details W1 :134 | 0.6023 ≥ 0.5 | ✓ |
| 7 | 1.052^4 ≈ 1.22（可及性低估界，s=0） | details §3 :69 | 1.2248（s=+1 → 1.164；s=−1 → 1.289） | ✓ 数字 / ⚠️ 界的前提归 L10-1 |
| 8 | 其一~其四门槛 0.602/0.363/0.219 覆盖句 | details §3 :71 | 与 #1-#3 倒数逐位一致 | ✓ |
| 9 | 掉血量 P15v2 遭遇败局 ~12.2 [10.32,14.24] | README :20 | P15v2_REPROOF.md:45 = 12.18 [10.32,14.24]；P15 索引行 = v2 重证成立、只进 λ 概率侧（本轮只作动机引用，合法） | ✓ |
| 10 | ADR-0536 代码注释引用 bridge 3 / encounter 10 / provisional 4 处 | landing 3.2 :29 | grep 实测 3 / 10 / 4（bridge.py:145/:148/:149；encounter.py 10 处；provisional.py:102/:105/:108/:116） | ✓ |
| 11 | 环深：已知表下限 2（README 侧写 5） | details §8 :117；README :11 | 已知表 2+R_between(0)+R_after(0) = 2 ✓；跨位面窗 2+0+3 = 5；三口径并存判定见 M10-1/M10-2 | ⚠️ |

## 第 9 轮 4 项修复验证表

| 项 | 修复要求 | 直调证据（本轮亲核） | 判定 |
|---|---|---|---|
| M9-1 | 契约 2 两层申报；§3 同源申报拆两层 + Δ_t 钉死净值 + :107 仅量级锚 + 保守向 fail-safe + E-3 净值残差定谳 + 禁归并 f_min；§8 改「读 GameState 现存值」；契约 3 可加性改「读点事实定谳」；§9 与 E-3 同步；design §2.1/§2.2 同步 | 八处全在且语义同构（details :21/:22/:69/:112/:126/:155；design :42/:50）；锚点 cw_screen_encounter.py:284/:406、economy.md:107/:111、cw_observation.py:2158-2178/:2555-2562、cw_game_state.py:401-402/:1949/:2265-2266 全部逐字命中；旧口径句（含全成分/无需显式建模）grep 清零 | **已修**（前提申报与采集面缺口另立 L10-1/M10-3，非修复失败） |
| L9-1 | W2b 改「删失分支用例：恰一胜变体 + 零胜变体」 | :136 标签与断言逐条对上（恰一胜变体 → 边缘不升足量；两场全删失 → 不足；（败局，删失）→ 不足；胜局帧 fill=None 不影响归态），与 §2 状态表互洽 | **已修** |
| L9-2 | §8 改「两场之间的非普通战斗行数」+ R_between = 0（下限 2）+ P3/变异位取 10（离散模拟 ≤8 需求申报） | details :117 四要素在册 ✓；已知表 R_between=0 逐槽核对 ✓；但新括注「恒 0 行」为假命题、公式缺 R_after 项、README :11 未同步 | **已修（details 侧）**；残留另立 **M10-1/M10-2** |
| L9-3 | E-3 阶段行显式列「fill 读取率、节点类型解析率、档位读出率」；landing 3.3 设计依据补 §4 | details :155 三项 + 混淆矩阵全列 ✓；landing :51 设计依据 = §3 + §4 + §9 ✓；§4 读链申报与契约 3 档位读出率登记在册 ✓ | **已修**（量测点全集仍缺败局行两项，归 H10-1） |

## 已攻击且未发现问题的面对账

1. **数字族**：上表 11 组全对账；W5/W6/W9/W12/W13 锁行断言与判据式同向，无数字型自相矛盾。
2. **判据数学本体**：κ_S 状态表形态闭合（胜胜/胜败×fill 三态/胜删失/败删失/全败/全删失/窗口<2 唯一归态）；其五/其六无源域与经验正证据域界（W9/W11）一致；经验层「空证据弃权不否决」与冷启动解除自洽；1/2 对称中点零常数论证以条语义定谳为前置，未定谳不激活开闸——与 §9 闸门集合一致；刷新逃生阀两分支同口径、单卡/无选项边角帧与现役代码逐位一致（decide_encounter_ev :345-349）。
3. **宪法与裁定融合**：裁定 2 字面「敌方血量消耗结果」→ 敌方通道选型自洽；裁定 3 = user_playstyle [39]⑤（:274 原话在册，本轮直调）；契约 5 引 [27]③（user_playstyle :273-274）原文一致；λ_death 先例语义记录（00 §3「进决策的是概率非血量数值」:32）与 01 §2 公理 3 原文（:26）一致；f_min/g/Δ_t/条语义四量全【拟】+ 01 §6 三要素义务 + 0.602 翻转线按要素②处置 + g 查表失败维持暗装——与宪法逐条吻合；裁定 6 授权形态叙述（README :11 与契约 5）一致；strategy-work §3 开关生命周期（references :34-42）与暗装 owner/期限义务吻合；「暗装行为等价锚」与 EV 核现态行为（双槽 None 期恒最低 + 零刷新，encounter.py:54-57 与 _fail_low :248-253）逐位对得上。
4. **ADR-0536 退役面**：decisions/ 无 0536 文件（glob 核实）、INDEX.md:424 墓碑行（编号不复活 + 语义承接 = 13_pick_family E3 行）、13_pick_family.md:20 E3 行语义单一源已改指代码本体；17 处现存代码注释引用与 E-2 清理义务一一对账。
5. **落码可行性（除 H10-1/M10-3 缺口外）**：判别子三态闭合本轮独立复验（生产写端仅 observe/carry 两处，无 logic/prior 写者）；D_win 快照可行性成立（战斗帧无旗牌读、结算时字段仍为本节点备战帧写入，live 门 = source 判别可行）；RoundOutcome 双扩字段确为新增（现字段清单无 difficulty_node/encounter_tier，cw_performance.py:38-84）；f_min/g/Δ_t 注册落点（新模块常量区）与 EV 核双槽（provisional.py:105/:116 + calibration.apply 于 bridge.py:101）物理隔离可行；tiebreak 三注册表词表锚（cw_node_obs.py:231、cw_equipment_data.py:233、金币字面）全实存；读法现状与申报一致（read_encounter_options :36-75 每卡单条 :70、档位漏读默认 1 :55、affixes 恒空、剩余次数读数 :82-105 支撑刷新链）；flow.py 基线委托（:539-543）与「基线核本无现役」表述一致；cw_screen_encounter 决策行 journal 锚（:320-321/:438-439）在册，sim 不可观测申报（:37-38）与代码一致。
6. **机制事实**：结算词表含「奖励」（cw_settlement_obs.py:37/:49）；plane_schedule_observed 地面真值与 §4 案例走查逐槽一致；economy §10.2 与地面真值的 P2 差异已被文档正确降级为 1 帧统计参考；「奖励节点亦产结算行」与 148 局 438/438 对账（plane_schedule_observed.md:30-34）一致；战斗中百分比条不采集申报与读数器页 1 帧态门（:484-485）一致；低保线无载 + 结算屏三项定谳点（combat.md:47-48）在册；g=1.052 出处（combat.md:64「UP 自拟合」）与「V3.7 第三方拟合、版本过期已注」表述一致。
7. **写作规范（iteration-design 硬规则）**：三设计文档零过程叙事；依据就地标注抽扫通过；停手令（details :4）在册；引用寿命（搁置注释禁引 changes/、正本指针）申报一致。除 M10-3 所指读点未定处外，实现者试读未见第二处「需自行设计」的语义空位。
8. **锚点漂移**：本轮引用的全部行号锚逐一直调命中（cw_settlement_obs.py:37/:49/:197-206/:253-258/:390-392/:441-443/:450-453/:484-499；cw_node_obs.py:29-30/:36-75/:55/:70/:82-105/:231；cw_game_state.py:214/:401-402/:528-543/:1949/:2260-2271；cw_observation.py:2158-2178/:2555-2562；cw_screen_battle_wait.py:25-27/:119-146/:286-297/:372-375/:384-417/:418-421/:532-548/:647/:722-744/:812-822；cw_performance.py:38-84/:109-110；cw_screen_encounter.py:17-19/:37-39/:277-289/:406/:320-321/:438-439；cw_events.py:594-613/:644-700；bridge.py:137/:145-149；encounter.py 全文；provisional.py:96-118；flow.py:12-16/:539-543；cw_reconcile.py:303-348/:465-509；cw_screen_briefing.py:182-187；cw_equipment_data.py:233；economy.md:107/:109/:111/:137；combat.md:32-48/:62-64；plane_schedule_observed.md:12-38/:92；advantage_layouts.md:17；user_playstyle.md:273-274；P15v2_REPROOF.md:45；INDEX.md:424）——含锚漂移的仅 cw_settlement_obs.py「:485-498 结算观察半」（实际构造在 :484-499，±1 命中，且该锚语义问题归 H10-1，非漂移）。
