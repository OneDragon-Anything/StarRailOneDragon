# gs-opening-seed 阶段 3.2 + 3.2b 合并交付验收 review(r1)

- 验收对象:主仓 `e9d04cad1`(kernel/cw_gain_chain.py 收口 + kind 退役)、`e53dcea1f`(anchor_aware_write 归位 + apply_board_rewrite 清空写经择道);测试仓 `f187cf80`(test_cw_gain_chain.py 改写 + test_cw_yinlang_phase32.py 三处通道期望)、`f7433980`(新增 test_cw_effect_inventory.py)。
- 依据:design.md §2.5(census 全表)/§2.6-1/2/3/9/§2.7(定稿);landing.md §3.2 与 §3.2b;reviews/3.1-r1.md(census 附项 = 3.2 复核前提的闭环对象)。
- **最终判定:验收通过**(无阻断;2 条应修均为申报面/census 口径出入,不改代码,报设计方裁定;3 条建议)。

## 1. 逐项核查结论

### 1.1 四 commit 全 diff 实读 + 入库面

- 主仓 `e9d04cad1`:仅 `kernel/cw_gain_chain.py`(+42/-26)= landing 3.2 主仓文件面,零卷入。
- 主仓 `e53dcea1f`:`kernel/cw_game_state.py`(+21/-1)+ `kernel/cw_gain_chain.py`(+24/-20 委托改写)+ `kernel/cw_effect_inventory.py`(+18/-6)= landing 3.2b 主仓文件面,零卷入。
- 测试仓 `f187cf80`:test_cw_gain_chain.py(+132/-42)+ **test_cw_yinlang_phase32.py(+8/-3,landing §3.2 文件面外,正当性见 1.5 与发现 F1)**;测试仓 `f7433980`:新增 test_cw_effect_inventory.py(125 行)= landing 3.2b「相关测试」面,零卷入。
- 两仓验收 commit 均在位:测试仓 HEAD = f7433980;主仓其上仅一个 docs commit(`13ee1127b`,design.md §2.6-9 gold 退款裁量裁定记录,4 行,与本批代码无涉)。主仓工作区的在飞改动(tool-gain-report README / shop-refresh-terminal)不属本批、未被任何验收 commit 卷入。

### 1.2 收口完备性(5 选择点全收口;委托链行为零变化)

- grep `write_logic_rand if rand`(src,排除 sim/)实码残留仅 3 处:cw_effect_inventory.py:1429/1502(= census 豁免的内部选择点 merge_cascade_write/grant_bench_unit_cascade,§2.5 表「登记不收口」行逐一命中)+ cw_game_state.py:2630(`_resync_board_delta`,清单外,见发现 F2);另 2 处为 `_select_write`/`anchor_aware_write` docstring 中的禁令原文。**无反序/变体形态漏网**(补扫 `write_logic if rand`/`if rand else` 全仓零命中)。
- `_select_write` 调用点 5 处 = 收口计数(cw_gain_chain.py:332 `_write_merge_step`、:388 `_gain_character_core`、:464 `gain_invest_env`、:513 `gain_equipment`、:566 `gain_invest_strategy`),与 design §2.5「HEAD 四处 + invest-landing-chain 增至五处」一致。链内零 `gs.write_logic(` 直调残留;唯一 rand 直写 = :645 欢愉契约临时翻面(无条件 rand,非选择点,处置判定见建议 S1)。
- 委托链行为零变化:`cw_gain_chain._select_write` 改为 `return anchor_aware_write(gs, rand)` 单行薄壳;`anchor_aware_write` 函数体(cw_game_state.py:1803-1805)= 原 `_select_write` 函数体逐字迁移(`if rand or not prep_anchored_of(gs): return gs.write_logic_rand; return gs.write_logic`),语义表达式零改。择道口落位 cw_game_state.py:1790,模块头(:46-50)同步登记读口/择道口双指针;`prep_anchored_of` 旧 import 已从 cw_gain_chain 摘除,`Callable` 两文件均自 collections.abc 导入。

### 1.3 kind 退役

- 两常量 `DEFECT_BENCH_UNOBSERVED`/`DEFECT_EQUIPS_UNOBSERVED` 及 kind 字面量 `gain_chain_bench_unobserved`/`gain_chain_equips_unobserved` 全仓(src)**零引用**;测试仓同样零残留(消费点仅剩 detail 出参字符串与 action-report 族独立 reason,不涉退役 kind)。
- None 拒落档保留:cw_gain_chain.py:377-385(bench)/ :506-512(equips)两守卫分支零写 + `detail='bench_unobserved'/'equips_unobserved'` 出参原样,发射调用删除;词表注释声明退役与去处(gain-chain.md §6 词表同步归末阶段正本)。
- 模块头失败安全自述与实码一致:「bench/equips 未观察(None)= 开局种子底座下生产不可达(局容器缓存单例冷建即种):直构/sim 域保留拒落档零写,无缺陷发射;席满且溢出位被占 = 零写 + 留证禁猜」——对照两守卫分支与 DEFECT_OVERFLOW_TAKEN 留存逐句吻合。
- landing 判据「两缺陷 kind 全仓零发射(grep 断言)」的落码形态 = 台账面断言(调链原语后台账零新增行,测试注释显式声明「禁源码扫描」)——与测试纪律「源码扫描测试一律禁止」一致,grep 核验由本 review 执行(即上条零引用结论),判定合规。

### 1.4 apply_board_rewrite 收口面

- SELL_ALL 三清空写(front_row/back_row/bench,cw_effect_inventory.py:819-836)经 `anchor_aware_write(gs, rand=False)` 择道,与 census 效果账本桥行裁定及 §2.6-9 改道申报一致;清空写段注释(§3 行内)写明双臂语义与择道单一源。
- **gold 退款写保持 logic**(:847-849 `gs.write_logic`,三入门条件未动)= §2.6-9 裁量裁定照办(退款额 = 算术推导面,失配真信号;影响面 = 未锚定 + 先获卡 + 选人力重组组合窗);桥 docstring 人力重组行同步改道说明,判据链完整。
- UPGRADE_ALL 零写分支(:774-777)与其余桥写端(装备改写桥/burst 桥)零触碰(diff 面即证);函数内惰性 import 块新增 `anchor_aware_write` 一项,「模块头零包内 import」契约未破。

### 1.5 test_cw_yinlang_phase32.py 三处改期望正当性(重点攻击面)

逐 hunk 判定,三处均为 design §2.5/§2.7 申报行为的必然结果,**非掩盖回归**:

1. `test_invest_strategy_landing_hacker_wolf`(:409):`active_strategies.source` logic→logic_rand。写端 = gain_invest_strategy 注册腿(rand=False 确定入口);容器 = `_make_gs` 直构 GameState(design §2.4 直构视同未锚定)→ 通道规则必改道 logic_rand。生产对应:投资策略屏在链序(简报→投资环境→投资策略→备战)位于备战 heavy 锚定前(design §1 症状 1 + cw_entry_start.py:436),测试场景即生产未锚定窗。值断言全部保留:`active_strategies.value == ['骇客专家:银狼']`、狼腿合成 2★、改件入栏、`gold.source == 'observation'`(「翻来源域集不含 gold」锁原样)、商店池申报行、去重臂——仅通道维度变化。
2. `test_invest_portal_landing_no_card_pollution`(:469):`active_env.source` logic→logic_rand,同上(gain_invest_env 确定入口 + 直构未闩)。邻居断言零动:双屏防污染锁、欢愉契约值、bench/equips 条件腿 rand=True 透传仍 logic_rand、joy_contract_provisional 留证行——条件腿语义不受影响。
3. `test_invest_portal_env_gifts_full_access`(:493):`bench.source` logic→logic_rand。写端 = ENV_GIFTS chars_immediate 经 gain_character(rand=False 透传,cw_gain_chain.py:632-635),未锚定期必改道。units 值断言与零留证行断言原样。

三处 hunk 只动 source 期望行 + 补成因注释(写明「先判定测试过时再更新」依据:投资两屏在生产链序位于锚定前),无任何值/时序/留证断言被顺手改写;旧期望在收口后必红,更新 = 工程门全绿的必要适配,判定 = 测试过时对齐申报行为,合规。文件面外问题见发现 F1。附注:同文件其余 `source == 'logic'` 断言(:334/:379-380,上阵变换窗/action-report 族)不在收口辖域,期望正确保持未动,反证改动面收敛。

### 1.6 行为抽测(自跑)

- `uv run pytest sr-od-test/test/sr_od/application/currency_war/test_cw_effect_inventory.py sr-od-test/test/sr_od/application/currency_war/test_cw_gain_chain.py -v`:**24 passed**(3.2b 双臂/静默覆盖/回归臂 3 锁 + gain-chain 对拍/时序/通道双臂/溢出三态/未观察退役/种子同构/失配网双臂/分道全绿)。
- `uv run pytest sr-od-test/test/sr_od/application/currency_war -m "not slow"`:**914 passed**(3.1 基线 909 + 本批净增 5:gain-chain 通道双臂改写 +2、种子同构 +1、失配网 +1、test_cw_effect_inventory 新文件 +3 中与改名对冲后净 5,数目自洽)。
- `uv run ruff check` 三主仓文件:All checks passed。

### 1.7 注释与规范

- 新注释全中文、写语义与 why(改道成因/退役去处/择道单一源/仪表保留理由);无 w 轮/rN/批号类会话局部标识符;出处均为持久索引(符号名/gain-chain.md §6/design 章节号)。
- src 新注释零 changes/ 引用;测试 docstring 新增 gs-opening-seed design/landing 引用一条,与既有测试头模式一致,见建议 S2。
- 类型注解齐全(`_select_write`/`anchor_aware_write` 均为 `(gs: GameState, rand: bool) -> Callable[..., None]`);择道口落位(cw_game_state.py:1790)与两处 docstring 指针一致;禁相对导入合规(cw_effect_inventory 维持函数内惰性 import 契约)。

## 2. 发现清单

**阻断:无。**

**应修(2;均 = 申报面/census 口径出入,不改代码,报设计方裁定)**:

- **F1 yinlang 测试改期望在 landing §3.2 申报文件面外**:`f187cf80` 卷入 test_cw_yinlang_phase32.py(landing §3.2 文件面仅列 test_cw_gain_chain.py;§3.2b 的「相关测试」宽面盖不住 3.2 commit)。改动本身正当性已核(§1.5,测试过时对齐申报行为),缺的是申报面补记——处置方向:设计方在 landing §3.2 文件面/进度账本补列该文件(注明「通道期望对齐适配」),不改代码。
- **F2 census 清单外写选择形态一处**:`cw_game_state._resync_board_delta`(:2630)保留 `self.write_logic_rand if rand else self.write_logic` 散写形态,未入 design §2.5 census 表任何行(3.1 census 附项 grep 模式带调用括号,未捕获该选择形态,本次复查发现)。行为无缺口:board 无观察基座早退(种子面 None),生产链序首观察与置闩同点 → 锚定前不可达;派生行通道随触发行 rand 入参,锚定后语义不变。按 census 自身铁律「清单外 = 回修本表,禁静默」:处置方向 = 设计方补登记一行(派生链基座行,建议处置「锚定后可达,天然合规,不收口」),不改代码。

**建议(3)**:

- **S1 欢愉契约临时翻面(cw_gain_chain.py:645)与 3.1 census 附项口径差定谳**:3.1 附项记「同属收口辖域 = 3.2 范围」,实码处置 = 不迁。复核判定不迁正确:无条件 rand 直写非「写选择点」(design §2.5 收口对象为 if-else 选择形态);若经择道口,置闩后 rand=False 会把翻面改落 logic,破坏临时仪表「值不变翻 rand、观察差异静默自愈留证」的锚定后语义。建议设计方在 census 备一句口径,消歧即可。
- **S2 测试 docstring 的 changes/ 引用收敛**:test_cw_gain_chain.py 模块头新增 gs-opening-seed design.md/landing.md 引用,与既有测试头模式一致,但按 changes/ 铁律属长期引用——与 3.1 S2 同性质,末阶段正本(gain-chain.md 通道规则节)落地后收敛为正本指针。
- **S3 收口计数清单随批位置**:landing §3.2 判据「grep 写选择计数 = 收口计数(清单随批)」,本批清单以 commit message(「5 处写选择点」)+ 本 review §1.2 行号清单为凭;后续批次建议把行号清单直接落 commit message 或账本凭据栏,免翻 review。

## 3. 最终判定

**验收通过。** 3.2(5 选择点全经 `_select_write` 收口、择道语义 = design §2.5 表达式逐字一致、两未观察 kind 全仓零引用且 None 拒落档/detail 出参保留、模块头自述与实码一致)与 3.2b(anchor_aware_write 归位 cw_game_state 公开口、委托链行为零变化、SELL_ALL 三清空写经择道、gold 退款写按 §2.6-9 裁量保持 logic、UPGRADE_ALL 与其余桥写端零触碰)均达成 landing 完成判据;新增 3 锁 + 改写用例对齐申报行为,直接受影响 24 passed + 全量 914 passed + ruff 通过。F1/F2 两条应修为申报面/census 口径 reconciliation,由设计方裁定后随文档批落,不阻断后续阶段开工;yinlang 三处改期望判定为「测试过时对齐申报行为」,合规。
