# IMPL_ADV_R87 —— 货币战争决策实现设计件·对抗式设计审查(第 87 轮)

> 审查对象:IMPL_DESIGN.md + IMPL_FIX_LEMMAS.md 的 **R86 落位后态**(两文件 2026-09-02 15:03/15:05 版)+ `docs/develop/currency_war/redesign/01_strategy_layer.md` §4.2/§4.3/§4.4。
> 对照单一源:NEW_MATH_FRAMEWORK / DESIGN_MANDATE_LAYER(按需抽查);数值锚点全部直调代码注册表复核(各条目内附亲读文件:行号)。
> 冻结边界前置申报:本轮六项发现全部落主链(R86 修复批自身 + cap 消费位 canonical 枚举完备性 + 观测通道账目衔接),不涉 D_ε 门/有效性判据/标定通道/哨兵/激活率门/cap 消费参数化度量面/验证装置的度量面本体;冻结残余清单 9 项**零新增零重复**(文末节)。
> 结论合格线自检:以下每条=论断+证据(亲读行号/直调结果)+推理链;结论强度取最弱一环(已逐条标注)。

---

## 一、新问题清单(严重度降序)

### N1(中高)R86-2「消费面完备断言」按字面落码对目标缺陷类**检出力≈0**(与被修缺陷同源循环),且断言方向缺反向半边

- **论断**:R86-2② 的断言形态「凡 aggregate_economy 消费链消费的数值进账字段 ⊆ 分型登记 ∧ 各有定义聚合语义,新增进账字段未登记即红」存在两读,两读都封不住它要封的病(aggregate 静默丢字段):①按「消费集=aggregate 现行枚举所实现字段」读——消费集由被修对象自身定义,新增 EconomyEffect 数值字段未被 aggregate 枚举 ⇒ 不入「消费集」⇒ 断言恒绿,恰是 R83-1/R85-3 缺陷本体(漏 flat 五族)的复现通道零检出;②按「消费集=EconomyEffect 数值型字段全集(独立语义分类)」读——断言只约束「字段 ⊆ 分型登记」(登记完备性),对 aggregate **实现侧**是否覆盖分型登记中的聚合型字段零约束,防漏半边缺失。能真正封病的形式=**反向断言「分型登记中『进 aggregate』型字段 ⊆ aggregate 实现枚举」**,R86-2 全文(头注段+登记表 R86 对照表 R86-2 行)均未声明此方向。
- **证据**:①R86-2 原文(IMPL_DESIGN.md L75 头注 R86-2 段;IMPL_FIX_LEMMAS.md L3263):「②消费面完备断言(静态断言测试位)=凡 aggregate_economy 消费链消费的**数值进账字段**(消费实证=cw_economy.py L118-120 `_strategy_economy` 直调)⊆ 分型登记 ∧ 各有定义聚合语义,新增进账字段未登记即红」——「新增进账字段未登记即红」的被触发条件是「消费链消费」,而消费链的唯一数值入口就是 aggregate(cw_economy.py L118-120 亲读:`aggregate_economy(state.active_strategies)`);②aggregate 实现枚举=cw_investments.py L595-622 亲读,确认不含 `interest_flat_per_node`(L110 定义在册)等五族——若落码批只照 R86-2② 字面写断言,把 flat 补进分型登记后 aggregate 仍漏 flat,断言不红(读①);③同型先例=登记表 R77-1(IMPL_FIX_LEMMAS.md/IMPL_DESIGN L66):「完备性核对=override>0 枚举对拍已知 T0 集」**同义反复句作废**(集合定义自被对拍对象,恒过、检出力恒零)——R86-2② 读①是同一病的新实例,而 R86 批对 R77-1 先例零引用;④R85-3 原形态「枚举 ⊇ 全字段集」(IMPL_FIX_LEMMAS.md L3244)虽然过宽,但其**方向**(对拍 aggregate 实现侧)恰是被 R86-2 作废时丢掉的有效半边——作废理由(bool/tuple 不可聚合)只支撑「收窄字段集」,不支撑「掉转方向」。
- **推理链**:缺陷类=「EconomyEffect 新增/既有数值字段未进 aggregate 枚举 ⇒ 消费链静默丢值」(R83-1 定谳本体)→ 该类缺陷的回归锁必须**独立于 aggregate 现状**地定义「应有字段集」→ 独立集只能是分型登记(人工语义分类)→ 故锁=「分型登记中聚合型字段 ⊆ aggregate 实现」→ R86-2② 只写了「消费 ⊆ 登记」方向 → 两读均无此方向 → 检出力缺陷成立。最弱一环=「读①是否为编者本意」不可完全排除(若编者意图读②,则缺陷降为「断言只覆盖登记完备性、漏实现完备性半边」,仍成立但降档为『断言不完整』);但无论哪读,现文本都无法让落码批写出有检出力的断言,规格缺陷成立。
- **附带**:分型登记的**载体未点名**(哪个文件/模块承载逐字段分型表;对照 R86-5 自家「禁无载体义务」纪律与 R75-5「落点文件点名挂落码批+候选载体盘点」先例,R86-2 连候选载体盘点都没有)。

### N2(中)R86-1「触发后补全批核查承接」通道**三件无载体** + R79-3 canonical 行后果语义未随批打标=双源冲突

- **论断**:R86-1 把滤噪职能改为「区间外 ⇒ 一律进注册表补全批核查(非升帽即关闭并入核查关闭计数)」,但该通道的①**触发帧计数**②**核查批执行者/节律/聚合口径**③**核查关闭计数**三者均无载体点名;且 R79-3 canonical 行(触发器定义单一源处)的后果「⇒ 强制注册表补全批」是无条件式,与 R86-1 的「核查可关闭」语义冲突,R86 批落位清单未在 R79-3 行打 supersede 标——按其自家「登记单一源/打标不删史/落位清单完备」协议,这是漏扫位。
- **证据**:①R86-1 原文(IMPL_DESIGN.md L75):「超区间频度由 R79-3 触发帧计数承载,不另设账」——但 R79-3 行(IMPL_DESIGN.md L67)全文只定义了触发器与后果,**未命名任何 §5.2 观察键**;本轮对 §5.2 观察键行全列核查(L565-569:`m6_caplatch_window`/`funding_support_shortfall_open`/`p2_sink_*` 三键/`p2_idle_switch_unevaluable`/`interest_flat_overdeduct`),无利息触发器计数键——「由 R79-3 触发帧计数承载」指向不存在的载体,恰违反 R73-5/R83-4 两轮封死的「残余申报无观测键」病;②「核查关闭计数」一词在 IMPL_DESIGN.md L75 与登记表 R86-1 行(L3262)各出现一次,无 §5.2 行、无载体;③核查批的判定者/节律未钉——对照 R86-5 同批刚立的「禁无载体义务」纪律(登记表 L3266(b)「凡带『每 N 批』节律的义务无执行载体必 decay」),核查通道是**事件触发**义务,连「事件→核查动作」的承接载体(谁在批判读时消费触发计数、以什么聚合口径决定开批:逐帧?逐批去重?阈值?)两读开放;④R79-3 行后果原文(IMPL_DESIGN.md L67):「第二触发器=实机遥测『单轮实际利息值 > cap_resolved 组装值 ∧ 注册表无 override 命中帧』⇒ **强制注册表补全批**(非循环同族,只依赖观测量、可测可证伪)」——R86-1 定谳「区间外={真超息,OCR 误读大值}两类均送核查、非升帽即关闭」,即后果从「强制补全」改为「核查(可关闭)」,但 R79-3 行无 [R86-1 标];R86 批落位清单(IMPL_FIX_LEMMAS.md L3262 落位列)=「IMPL_DESIGN 头部 R86 批段 + R85 批段 R85-1②(b) 处标 + 登记表 R85-1 行 + 本行」,不含 R79-3 行。
- **推理链**:R86-1 的修法本体(区间收窄二值判定)经不等式级复核成立(未在册升帽无先验上界 ⇒ 入解释集则区间无界 ⇒ 滤噪区分度 0,该论证成立)→ 但「区间外一律触发」把 OCR 误读大值与「升帽策略在场但观测漏」两类噪声帧也变成触发证据 → 触发频度=OCR 误读率×结算帧数+漏读通道频度,核查成本随之上抬,而频度/成本口径零登记(R75-3 对其通道给了「频度=误读率×命中率」,本通道无对应物)→ 承接通道若无载体,则「强制核查」要么悬置(R86-5 立法目的反转复发)、要么按 R79-3 字面逐次强制补全批(对已注册策略的误触发也开批=流程成本无界)→ 两态都不可接受,规格必须补齐。最弱一环=「核查批实际成本」无实测定量(核查是流程动作非运行时开销),故本条强度取『中』而非中高。

### N3(中)cap 消费位 canonical 枚举表(R70-1 第二族/行 9-11)**漏登 s_line.S 动态目标线的 +50 分量**——自家第二族谓词按字面即可命中

- **论断**:01_strategy_layer §4.4 的 [41] 整买纪律 S 公式「**S=批成本+50**+Σ(窗口预留卡价)+E[刷费]×2(恒>50——超息囤钱是常态路径)」中的 +50 系息饱和线字面(金位阈值类、[41] 一族授权前提、判据语义系于「饱和线之上不生息」),逐字满足 R70-1 第二族谓词定义,但 §2.10 canonical 枚举表行 9(「金位阈值三消费位」穷举:r2 溢余段/M6+stockpile 分界/§3.1·§3.4 M6 溢余义务范围)、行 10(L 视界族五位)、行 11(arm2 floor_eff+M3 解锁包)均未登 s_line.S——「三消费位」自封穷举与第二族谓词输出不符。
- **证据**:①S 公式原文=01_strategy_layer.md L116(亲读);②IMPL_DESIGN.md L111:s_line.py 模块行「S 动态目标线 / B / n̄ / ρ」(模块存在);L232:batch_form 行消费 `s_line.S`(判据 `g ≥ S`,出处列「S 公式全段」)——S 的 +50 分量在 `g ≥ S` 比较式右侧,是「金存量与息饱和线字面 50 的比较」的封装形态;③canonical 第二族谓词(IMPL_DESIGN.md L356):「比较式任一侧为金存量与息饱和线 g*=10×cap 的**字面 50**/静态常量比较,判据语义系于『饱和线之上不生息』(**[41] 一族授权前提**)」——S 公式出处即 [41],谓词字面命中;④行 9 穷举清单(L368)三消费位无 S;行 10 五位(L369)无 S;行 11(L370)无 S;⑤fail 方向与行 9 r2 位同构:息律局(cap 5→10)真饱和线 100,S 仍按 +50 ⇒ 升帽可达局在 g∈(50,100) 带整买提前放行=消耗生息金=行 9 r2 位「放行面 fail-open」同向;买断制局(cap=0)反向(S 含 50 使目标线虚高=过度囤金)。且 R72-2(行 11)已确立「已参数化形态同辖,禁以字面不匹配逃逸扫描」——S 的 50 连参数化形态都不是,更逃不掉。
- **推理链**:R70-1 立第二族的目的是「族定义由上轮攻击报告措辞反推」病根封死+「全仓机械扫描(设计件+登记表)」→ S 公式在设计件链内(01 §4.4+IMPL_DESIGN L111/L232 两处引用)→ 谓词字面命中却不在表内 → 要么扫描域声明(「设计件」是否含 01 文)两读,要么首轮扫描漏一面 → 无论哪种,行 9 的「三消费位」穷举句与谓词输出矛盾,R71-1/R73-2 五次「漏一面封口」叙事的第七实例风险在案。修复成本低(补行+口径定谳:升帽可达局 S 的 50 分量是否随 cap_sup 条件式,可沿用 R71-1 行 10 判据同源同向先例),但**必须先补登**——落码后 s_line.py 代码面含字面 50,届时扫描即红。最弱一环=无;本条证据链完整(公式原文+谓词原文+穷举清单三处亲读)。

### N4(中低)R86-3 过扣键的**建模对象未对照机制原文核实**:「取款 ⇒ flat 停息」在效果原文与注册表建模中均无支撑,键谓词把「取款已触发」当「过扣已开始」

- **论断**:`interest_flat_overdeduct` 键与「过扣残余」的承重前提是「取款翻转后 flat 利息停止 ⇒ 扣减式仍减 Σflat ⇒ 差值偏小」。但①效果原文(cw_invest_data.py id='304001' L283 亲读:「每回合获得利息+2金币。当你的金币少于20时,从狸狸那里取出10金币存款。进入第三位面首领节点时,取出所有存款」)**没有**「取款后 +2 停止」的语义——+2 是无条件陈述句,取款条款语法上辖「存款」不辖「利息+2」;②注册表建模注释(cw_investments.py L242-244 亲读):「金<20 取 10(**流动性保险,行为条件流**);P3 首领取全部存款(存款累计值消费端推)。**数值侧先建固定息**」——现行建模把取款族整体划出数值侧、固定息无条件入账,若真机制即无条件 +2,则过扣残余构造性为空,键监控的是不存在的机制;③即便真机制是「+2 系存款利息、存款耗尽即停」,过扣起点=**存款耗尽**(初始资金 30,金<20 两次取款后才耗尽,P3 首领一次清空),而键谓词=「金<20 谓词真(**该分支已触发取款**且金未回补 ≥20)∨ P3 首领取出后」——首触发帧即计数,系统多计存款未耗尽的前置帧;键声称观察「过扣残余暴露面」,实际观察「取款已发生面」,两集不同。
- **证据**:上述三处行号均为本轮直调亲读;R85-2① 已为「狸是否并入结算屏利息行」登记了同型**对账前置项**(IMPL_DESIGN.md L73 R85-2 段:落码批前置核对,一次实机/sim 对账封口),而「取款是否停息」这一更上游的机制二义**无对账前置项**——同批内同类风险一个立前置一个不立,标准不一。
- **推理链**:攻击纪律②(建模对象对照玩法文档/机制原文)→ 本键的机制假设在原文与注册表两侧都查无支撑 → 键的计数口径、量级界(Σflat×翻转后剩余帧数)、方向申报(假阴性向)全部悬在未证假设上 → 修法低成本:补一条对账前置项(落码批实机/sim 观察「取款后结算屏利息行是否仍含 +2」一次封口,结论回写键的辖域或撤销),并把键谓词的过扣起点改挂「存款耗尽」代理(或显式申报「存款余额不可观测,键系取款发生面代理计数,两向误差申报」)。最弱一环=游戏内机制真值本轮无法实机验证(只能证「设计未核实」,不能证「假设为假」),故取中低。

### N5(低)R72-1「全程零观测」残余账与 R86-1 二值判定的**账目衔接缺失**:零观测升帽局在触发器侧的暴露无账

- **论断**:R72-1 残余账(「全程零观测的晚发牌构造」≤135 金自有界+频度申报)辖的是**经济判据组装面**(cap_resolved vs cap_sup 组装差)。R86-1 后,同一构造在**触发器侧**产生新暴露:升帽策略在店/注入但观测漏(OCR 漏读名/漏读候选)⇒ 锁存不置位 ∧ 注册表 override 命中为假 ⇒ 该局真实利息按 cap_true 结算 > cap_resolved 组装值 ⇒ 扣减式差值超区间 ⇒ 触发证据成立。该通道:①不在 R72-1 账(其频度申报口径=「首次属性命中帧前的备战期帧」×组装差金量,不辖触发帧计数);②不在 R75-3/R76-5 账(彼=误读**撞实名**假置位向,此=漏读**不置位**向);③R85-2③ 的延伸申报辖 Σflat 名 miss(扣减漏源),不辖 override 名 miss。三本账均不收,悬空。
- **证据**:R72-1 残余账原文(IMPL_DESIGN.md L60);R75-3 申报原文(L62:误读撞实名⇒假置位,漏读向「并入 R72-1 残余账频度申报」——该「并入」的对象是置位面频度,非触发器面);R85-2③ 原文(L73:Σflat 经 OCR 读名的误读名 miss=扣减漏源)。三段亲读,均无触发器侧口径。
- **推理链**:R86-1 把区间外帧全部送往核查通道 → 零观测升帽局的触发帧是「核查必关闭」类(注册表已有该策略,补全批查无缺)→ 它是 N2「核查关闭计数」的主要预期来源之一 → 若不登记,首批实机一旦出现该构造型,触发计数与核查关闭计数都无归因分键可落(违反 R24-2 分键纪律的精神)。修复=在 R72-1 残余账或 R86-1 段补一行账目衔接声明(零观测构造 ⇒ 触发器侧结构性触发通道,频度=漏读率×升帽局占比,核查预期关闭,归因分键建议)。最弱一环=量级小(≤漏读率×升帽局占比×每局帧数,且后果仅流程成本非行为错误),故低。

### N6(低)R86-5 复查节律的两处残余两读

- **论断**:①计数起点钉死依赖「被否批通常无 run 无从产生占比读数」(R86 批段 L75 原文)——「通常」对冲词自认:若被否裁定发生在某 A/B 批判读**之后**(该批有 run、有 B2 占比读数),该批是否仍作第 0 批不计入?字面是(起点=被否裁定落定所在批),但「不计数理由」(无读数)在该态不成立,首查时点差一批,两读开放;②「执行载体=复查义务登记进各批判读报告例行复查节+进度账本例行项(点名到批)」——**登记动作本身**无载体:谁在第 0 批落定时把例行项写入进度账本、后续各批边界由谁接力点名,未指定;复查义务的防 decay 机制自身可 decay(自指)。
- **证据**:R86 批段 L75 与登记表 R86-5 行(IMPL_FIX_LEMMAS.md L3266)亲读;(b) 支理由原文「载体点名到批判读报告例行节+进度账本例行项,两处任一可见即非悬置」——「可见」判据存在,但「谁保证可见」缺失。
- **推理链**:R86-5 立法目的=禁节律义务悬置 → 其自身登记动作是同类义务 → 按同判据须有载体(建议:第 0 批判读报告结论节强制含「R85-5 复查节律已激活,起点=本批」一行+progress 账本迭代级例行项一次写入,后续批由判读模板例行节承接)。最弱一环=触发概率低(被否批带 run 属边缘态;登记漏一次即可由任一批判读者发现补登),故低。

---

## 二、冻结残余

**无新登。** 本轮六项均落主链规格层(N1 断言形态/N2 观测通道载体/N3 canonical 枚举完备/N4-N5 建模对象与账目衔接/N6 节律计数),不涉冻结族度量面;维持被审方 R86 批前置申报口径(观测通道规格=实现规格非度量装置)。既有 9 项零重复零引用立案。

## 三、攻过零新症角度(列证备查)

1. **R86-4 守卫撤除辖域+§6.4 行为锚**:零新症。代码直调核实 `_GOLD_AMT_RANGE` 三键表(cw_settlement_obs.py L251:base(0,99)/streak(1,9)/interest(2,9))、守卫仅施于右列路径(L304 取端、L316 判定)、粘连直读路径确无守卫(L296-300)、`_GOLD_STREAK_PINNED` 交互(L253-257/L318-319)——R86-4 的行号证据全部准确;§6.4 第 6 条行为锚两件已落位(IMPL_DESIGN.md L638),锚二「同一 token 集两路 interest 输出恒等」设计可实现(撤 interest 键守卫后右列 L316 对该键短路,两路对纯净 token 集恒等)。
2. **R83-1 per-strategy 查询路径规格**:零新症。代码核实 aggregate 枚举确漏 flat(cw_investments.py L595-622)、ledger per-strategy 路由确有 flat(cw_effect_ledger.py L224-226)与 gold_at_node 台账路由(L227-231)、「禁经 aggregate」规格与代码事实一致。
3. **R81-1/R78-2 在 01 §4.2/§4.3 与判前锁 v6 的落位链**:零新症。01 §4.2 补条(L105)与 §4.3 第四触发条(L111)的 [R81-1/R81-5 标] 在位;§5.1 v6 清单行 14(L540)与 R81-1 落位判据(时间戳序+W=None 期声明段)一致;§2.12(L384-391)消费指针 grep 可见,R83-5/R85-5/R86-5 的被否分支联动口径三处自洽。
4. **R85-1 观测通道认定**:零新症(本体)。`parse_settlement_gold_detail` 确为纯函数且当前无生产调用方(cw_settlement_obs.py L260-321 亲读,全仓调用面未核出生产接线),「潜伏非活缺陷」定性成立;(2,9) 守卫对 cap=10 局合法值 10 的误杀构造性成立(L316)。
5. **R86-2 对 R85-3 原断言「不可满足形态」的定性**:零新症。EconomyEffect 确含 bool(L111-112)/tuple(L101)/str 期权(L102)/概率(L121)/调度(L108-109)异质字段(亲读 L82-128),「全字段 ⊆ 枚举」按字面不可满足或逼语义错账的论证成立——本条只攻其**替代形态的检出力**(N1),不翻其作废理由。

## 四、结论(含适用域限定)

本轮立案 6 项(N1 中高/N2·N3 中/N4 中低/N5·N6 低),三件齐;其中 N1/N2 直接受益于 R77-1/R86-5 自家先例与纪律,属「修复批自身」攻击面的实锤;N3 系主链 canonical 枚举完备性的机械可验漏登。R86 批五项的修法方向本体(区间收窄二值判定/字段分型登记方向/守卫辖域钉死/计数起点钉死)经攻击**全部成立不翻案**,缺陷集中在**义务载体与断言方向**的落码规格层。

**适用域限定**:①本轮攻击域=设计文档态(落码未发生),N1-N3/N5 均为「落码批义务规格」缺陷,不构成现行代码行为缺陷(`parse_settlement_gold_detail` 无生产调用方、分型登记与断言均未落码);②数值锚点以 2026-09-02 代码态为准(若落码批已并行改码,行号需复核);③N4 的机制真值(取款是否停息)需实机/sim 对账封口,本轮只定谳「设计未核实」非「假设为假」;④审查域=R86 落位后态,R87 修复批落位后 N1-N6 应逐项获得 [R87-Nx 标] 或反驳。
