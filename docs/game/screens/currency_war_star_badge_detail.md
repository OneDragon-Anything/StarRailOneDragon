---
screen_id: currency_war_star_badge_detail
screen_name: 货币战争-星徽详情
source_image: screens/货币战争-星徽详情/燃血星徽.webp
---

# 货币战争-星徽详情

## 何时出现

点星徽图标(owned 装备栏/奖励面板)误开详情弹窗(2026-08-17 M53 停机建档:点球恢复原语点空白时点到星徽图标)。弹窗盖备战但背景是备战。

## 状态流转

- 入口:点星徽图标 → 本弹窗(「XX星徽套组」标题 + 星徽名/类型「流派星徽」/品质「星徽钻石」/属性/效果/适配角色头像行/合成公式;左右箭头切套组成员)。
- 出口:右上 X 关回备战。**不用 ESC**(bug#2:面板已关时 ESC 落备战弹中断挑战)。
- 入口链防御面守卫(ADR-0607):星徽详情已知触发场景全在对局内,入口链无已知触发路径;入口链四挂点守卫(注册表 `cw_entry_start._STAR_BADGE_GUARD`,双锚 AND 同 is_precise 判据)点 X 关闭,具名状态 `星徽详情弹窗关闭中`,作为防御面兜底——变体帧上 AND 漏接 = 有界具名 FAIL(可诊断不可自愈)。

## 处理面清单(多面并存,判据分叉调和)

同弹窗现有四个处置面,是有意分叉不是漂移,禁无据统一(调和声明 = ADR-0607):

| 面 | 判据 | 定位 |
|---|---|---|
| 对局内 1d 分支(`cw_loop.py`) | OR:任一锚命中即派发 | 召回优先(M53 起生产里程;变体帧经套组标题锚天然免疫) |
| 专属 op `CwScreenEmblemDetailPopup` | 被 1d 派发后执行 | 点同款「按钮-关闭」 |
| OverlaySpec(`cw_overlay_registry.py`) | 单锚 `标识-流派星徽` | 分类/清场表成员(变体同样漏,如实申报) |
| 入口链注册表守卫(ADR-0607) | AND 双锚 @0.9 | 防御面,低误关优先(is_precise 同判据) |

另有一条对局内触发路径记录:备战盲点误开(`prep_actions.py`)。

## 识别特征

- id_mark 组合:`标识-流派星徽`(830,190-960,230,lcs 0.9)+ `标识-套组标题`(70,65-260,105,lcs 0.9,「XX星徽套组」)。
- 注:类型行可能是「流派星徽」外的变体(通用星徽?);套组标题兜底。变体锚核验(翻页切成员逐帧离线 analyze)随 S3-badge 采样,**待采样**——结论回填义务含四处锚面同改:yml 锚文本 / 入口注册表 spec / overlay registry `anchor_area` / 1d 内联双锚(只修入口侧则对局内继续漏,ADR-0607)。

## 可交互元素

| 元素 | 位置 | 行为 |
|---|---|---|
| 按钮-关闭 | 右上 (1815,35-1900,95) | 关弹窗回备战 |
| 左右箭头 | 弹窗两侧(≈(530-580,430-480) / (1330-1390,420-490)) | 切换套组成员(自动化不点;S3-badge 采样用) |
| 空白遮罩区 | 详情卡外 | 点空白能否关闭未采样(S3-badge 待采样,结论回填本节) |

## automation 要读信息

- 无需读——误开处理即关。未来若要做星徽效果解析(策略消费),效果文本行 (766,363-1150,440) 可 OCR。

## 识别快照

- fixture:`screens/货币战争-星徽详情/燃血星徽.webp`(2026-08-17 M53 实拍)。
