# 货币战争 · 新数学决策框架总纲

> 定位:**统一决策体系总纲**——把已站住的证明族(P38 起新树)整合成一套可实现的决策框架,是实现方案的前置件。
> 本文件只做整合,不改任何证明/代码;每条判据引用具体证明件编号,无证明支撑的决策面显式标「无数学,待立项」。
> 结论强度纪律:每条论断 = 论断 + 证据链(证明件编号 / 注册表 / 玩家裁定),取最弱一环。
> 输入面:`docs/game/currency_war/research/math_proofs.md`(终态索引)+ 16 件核心已证件全文 + `DESIGN_MANDATE_LAYER.md`(骨架层设计)+ 公理栈五条 + 玩家裁定三条。
> 注:本纲 ADR-NNNN 编号指 2026-09-01 前旧决策树(已整树归档于迭代支撑目录),仅作历史出处指针。
> 直通线注记并入批(2026-09,DIRECT_LINE_MATH_CHECK §4):λ_death 桶定义补 bench 前后台强度维(§2 λ 行/§3.3 #11)【R178 supersede 标(IMPL_ADV_R178 症2,打标不删史):本头注「桶定义补 bench 前后台强度维」系 R26-H1 前口径活文字——现行权威=IMPL_DESIGN §1 lambda_death 行「板面/bench 维**不入键**(PL 键实证…)」(亲读);桶定义 bench 维已随 PL 键时代退役(本纲 §2 λ_death 行裁定一/R26-H1 supersede 标在案);本位系 R63-4「总纲物理打标挂账下批」(IMPL_DESIGN §1 lambda_death 行 R63-4 标)的逃逸位——挂账仅在 §2 行本位兑现,头注/#11 漏扫;登记=design_telemetry.md 文末 R178 节】;直通线入口七条注记、状态机 S0→S3 信号直锁边、M5 开局形态分流的单一收录源=IMPL_DESIGN §2.7.1,本纲不重复(防双源)。

## 0. 公理栈与玩家裁定(框架的地基,一切判据的合法前提)

**公理五条**(全部已固化,框架内不再论证):

1. **零调参**(skill ADR-0026):禁拍死值顶替未标定参数;数值判据必须可溯源(注册表/推导/带 CI 的估计);未标定项 fail-closed 或走支配性结构判据,不塞拟合常数。
2. **超额收益隔离**:一切证明在「连胜 = 0」的底线口径上成立;连胜成长价值是上行注记,只增不改向(P51 §F.4 / P43 §⑥)。
3. **纯金流记账**:血(hp)不入任何账与自变量——hp=0 仅是死亡事件定义;胜率/掉血只经 λ_death 的概率侧进入决策(P51 §R.3 / P15v2 消费限制 / [39] 裁定)。(**辖域注记(R27-3):hp 不入账本与金流/价值复合;经 λ_death 的概率路由键[B1 血带维]间接入场系用户裁定二既裁例外——概率侧非价值侧,重审=IMPL_FIX_LEMMAS L-B3 修订注记**)。
4. **阅读门**:证明件须过无偏对抗审查(五门验证,以 2026-09-01 验证终态为准)后才可消费;**任何证明以该日五门验证终态为准,不分新旧树**——站住者可消费(P16/P35/P1/P5 等旧树站住件与新树同权),作废/不成立者禁引(终态索引已标),待证明者按 §4 钩子处置(结构占位,默认行为退常规门/骨架义务)。
5. **参数溯源**:框架内每个数字标【注】(注册表直读)/【推】(闭式或递推推导)/【拟】(观测拟合,必带 CI,禁点值当闸门)。

**玩家裁定三条**(2026-09-01,最高权威):

- 等待件域 = **阵营相关单位,不限星级**(P39 臂一谓词②;历史「star≥2 ∧ 目标线名单」双收窄判废);
- **hp 不作决策输入**([39];现行代码 `_xp_gold_floor` 的 HP 分支属处死名单);
- **首次 0hp 保底 1hp,再次归零结束**(一次性保底;P9 候选线语义)。

---

## 1. 决策面全集映射(每面一个权威判据)

先给三层权限总图(与骨架层拼合的骨架,详 §5):

```
证明层:选线/终局(证据门 P38 + 换线 P16)——决定骨架为谁服务,无单笔否决权
骨架层:义务 M1-M7 无条件执行,只受四项硬约束(可负担/席位/S 预留/不可逆护栏)
EV 层:只增不减——在骨架之上追加优化动作,本节数学判据的主辖域
支配性结论:零参数、无条件、优先于一切带参 EV 比较(公理 1 的执行形态)
```

**三段管辖**(P48 §③(b) 三段管辖表,全框架统一):按死亡危险率 λ_death(板面战力桶 × 节点类型,P51 §4-B-2 分层表)分三段——

> **【R26-H1/安全带架构裁定 supersede(R32-中① 打标,打标不删史)】下列三段管辖(含 §1.1-§1.7 各表「三段管辖」列)已随安全带架构裁定(2026-09-01,本文件尾部)整体退役**——现行权威=IMPL_DESIGN §2.0 姿态架构:发展优先默认姿态+升档器(血线硬地板+λ 顾问)只升不降+arm2 结构守息门(g*=10×cap_resolved)+血线地板解锁包;实机刻度上 λ≈0 段=空集(全表 CI 下界最低 0.258>0.1),三段退化单段、段路由空转。本段与各表「三段管辖」列保留为历史档,**禁据此落码或指导键结构**;核读指针=IMPL_DESIGN §2.0-4 消费纪律改写清单。

- **λ≈0 段**(健康/早期:分层 λ CI 上界 ≤0.1):守息逻辑生效,息账 L(P47)与再遇账(P1/P41)主导;
- **中间带**:P48 统一式,λ_death 项作为连续项进判据右端;
- **λ>0 段**(濒死/掉血带):转化优先(P51 支配),S 囤钱线降级为目标缓存,息票保护失据(P23.4/P36-a)。

### 1.1 买(卡牌买入)

| 子面 | 权威判据 | 证明 | 三段管辖 |
|---|---|---|---|
| 序 1/序 2(线成员/场况插件) | **骨架义务,不走息律门**(M2;[2]/[11]/[6] 文档依据链) | DESIGN_MANDATE_LAYER §3.2 | 全段无条件 |
| 序 3-5(EV 买:通用插件/压库/凑星) | **支出否决域 D = 真破息 ∧ 无当轮兑现 ∧ 非豁免;豁免 = 条件不等式 E_rev / E_line** | **P46**(P1 谓词 = L(g,c,R_全局,Ī)>0;E_rev ⇔ V_opt ≥ L+fee,P41 定价;E_line ⇔ V_ms·ΔP̂ ≥ L,P38 通道) | λ≈0 全效;λ>0 段 P1 的息账保护失据(息票随死清零),否决域自动松——由 floor_eff=0 投影(P39 (c))与 P51 转化优先承载 |
| 压库买入 | **档匹配(c ∈ T_search)∧ free≥2 即买 / free=1 过 V_slot 净门** | **P49** ③ | 全段(纯金小额,不受 λ 分段) |
| 合成完备购(第 3 份) | **满栏不阻断**(merge §2.5 机制事实);排序批前置卡点 = P30① 待证 | 机制直读;P30 待证 | 全段 |
| P2 锁线核心卡 | **待证明占位**(P25 v2 形态:V_a > ρ·C_buy/Ī + L + ΔW_trunc),默认关 | P25(待证钩子,§4) | — |

**支配性优先序**(实现时先于数值比较执行):燃料件/1★ 可逆资产买入净成本≈0([11]/P41 ① 通道结构)→ 无条件放行;残余补部署(P24)零支出严格支配。

### 1.2 卖(卡牌卖出)

| 子面 | 权威判据 | 证明 |
|---|---|---|
| 燃料件(1★ ∧ 与锁线/种子线零重叠 ∧ 边际贡献≈0) | **支配性卖出条款,零参数**:卖出净成本 = 0(全额退 + 还池),bench 满(V_slot>0)时弱支配持有 | **P41 ②**(修复批消 u) |
| 骨架件/贯穿件 | **不卖,fail-closed**(数值闸门在 u 标定 CI 落地前空转);线内件双通道 max(V_opt, V_ms·ΔP̂) 接管 | **P41 ②** 分类表 |
| 未定型期/危局轮 | 零重叠集卖最弱 → 退「卖最弱 1★ 垫件」;线内禁卖护栏不解除 | **P41 ②** 辖域条款(对齐 M4) |
| 卖回凑息档序 | **V_comp=0 档恒先卖 → 每金压缩损失 Δ金/c 升序贪心(实现走精确小 DP),1费尾差贴息线;2★ 同档最后** | **P49 ⑤** |
| 主不等式(中间类件,标定后) | 卖 ⇔ refund + V_slot > max(V_opt, V_ms·ΔP̂·[x∈K]) + V_power(V_power=0 系待 P51 W 接管的缺省项) | **P41 ②** |

**不可逆护栏**:卖出是 fail-closed 的正确落点(r2 H1 散刷事故同判;DESIGN_MANDATE_LAYER §3.5)。

### 1.3 升级(买经验)

| 子面 | 权威判据 | 证明 | 三段管辖 |
|---|---|---|---|
| 是否升(存在性) | **臂一(阵容驱动):谓词三元「板满 ∧ 等待件(阵营相关单位不限星级,玩家裁定)∧ 边际贡献>0」+ 硬可负担——骨架义务 M3,arm2 无权否决存在性** | **P39 ②** 臂一 + DESIGN_MANDATE_LAYER §1.3③ | 全段(人口位当轮兑现,[33] 明文例外) |
| 何时升(调度门) | **臂二(搜牌驱动):非峰值级 ∧ ΔV_band ≥ U_L + C_int ∧ g−U_L−Σ窗口预留 ≥ floor_eff**(ΔV_band 项系 P39 修复批遗留:窗口前提重锚未做,禁单独作闸门,边界带结论以 sim 对拍项 2 裁决) | **P39 ②** 臂二 | **floor_eff = λ_death 分层现算的 50/0 二值投影,零拟合常数**:λ≈0 段=50(守息),λ>0 段=0(该花就花)【R26-H1 supersede(R32-中① 打标):floor_eff 50/0 二值投影已随三段管辖退役——现行=arm2 结构守息门 g*=10×cap_resolved(零 λ 依赖)+升档器血线地板解锁包,核读=IMPL_DESIGN §2.0-4/§6.2;本格保留为历史档,禁据此落码】 |
| 怎么买(形态) | **整买引理(Leontief 互补):只以补齐批形态发生;触发 iff 双臂门 ∧ g ≥ S** | **P48 ①②** | S = B + floor_resolved + Σ窗口预留 + E[刷费]×2 |
| 囤钱 vs 花钱 | **统一判据:花(a) ⇔ V_a(x) > ρ·(x/Ī) + L + ΔW_trunc(a);三账不同源防双计(时移/息差/存量清零)** | **P48 ③** | λ≈0 段 W_trunc≈0 退化原判据;中间带统一式;λ>0 段让渡 P51(S 降级目标缓存,批可破息走 V ≥ L + W_trunc 门) |
| 多目标排队 | 比较器 = 每金价值率 ρ_i/S_i(域=全部目标视界内可完成;截断域走小枚举/DP) | **P48 ④** | — |
| lv9→10 特档 | 摊还账闭式;全部标定带内停手窗恒负 → lv9 停留账面正确 | **P35** | — |

**实现护栏**:禁 scoring 小数等级线性计价通道(P48 头注);cap 感知硬约束(消灭 LevelUpRejected)。

### 1.4 刷新

**权威判据 = P40 三层门**(单一结构,R0→R1→R2):

- **R0 停止**(n=0):R0-1/R2 维持结构门(合格集、预算/息账递推,**不依赖 V̄**);R0-2 改**结构判据**——合格集空 ∨ 预算门不过,即 fail-closed;**V̄ 整带(含其下沿保守闸形态)降为 EV 比较项,禁作闸门**(P40_VALIDATION 已登记的降级路径:价值门 V̄ < c_eff/P 未标定,不得拦截刷新存在性)——无目标刷新 fail-closed(r2 H1 事故的正确门);
- **R1 完成门**(启动):c_eff·E[refreshes|j] + Σ卡费 + L ≤ V_gap(不可分投资的承诺账;启动后不受单步门否决,三类合法退出)。**V_gap 🔴 未标定前 R1 fail-closed = 无付费刷新**(骨架义务与免费刷新不受影响);标定通道 = sim 对拍批,与 V_ms 同源单标定(禁双源,见 §3.3 #2);
- **R2 预算门**:spend = c_eff·n + Σ命中卡价,息损项恒走 P47 L 递推;溢余段(>50)自由,g≤50 跨档需 V_gap 覆盖。

三段管辖:R2 的息账在 λ>0 段失去保护对象(息随死清零),预算约束退化为硬可负担 + P51 转化判据;变体①-⑩(淘金客/概率事件/轮岗/市场干预/节点语境等)全部 = 参数替换,骨架不分叉(P40 §④)。

**支配性例外**:危机帧刷新通道不变式(P36-a)——「B>0 ∧ B≥刷价 ⟹ n≥1」是结构约束,已无条件落地(ADR-0506),先于 R0 存在性。

### 1.5 压库

**权威判据 = P49**:V_comp = A×Δ金(线性律,二维表脚本生成);档匹配 = 结构必要条件(跨档零压缩);「5费伪命题」限深缺口(3星差3张,搜索不启动),浅 5费窗口档匹配照常买(用户勘误)。辖域随三段无变化(小额可逆资产,与 [11] 动态无损通道耦合——有卖回计划不进 p46 域)。

### 1.6 装备(含冶金炉)

| 子面 | 权威判据 | 证明 |
|---|---|---|
| 组件保留 | V_comp(逐配方望远镜边际);分层 = 最近兑现距离 × partner 到位率——**近兑现张最贵,绝不喂** | **P42 ①③** |
| 合成配对/时机 | 禁忌 = 删域(∞ 成本项);锁线后凑齐即合 = 弱最优(尺度假设);令牌可定向重取例外;字典序贪心 | **P42 ②⑤** |
| 冶金炉 | 逐件准入门(P42 ③)+ 批量捆绑约束(P50 ③ 退化定理)+ 吞吐账(⌈d/3⌉,角色模式 d≥2 严格省炉)+ 时机 = 纯期权(非锅炉局) | **P42 / P50** |
| 玩家裁定消费口径 | [42]:默认保留不喂(λ 难预知,软例外不立规);唯一确凿例外 = P3 boss 掉落流终止时冗余件开放;近兑现张任何时刻绝不喂 | P42 ③ / P50 ③ 层1 |
| 工具时机 | 投影仪/令牌/特权卡按 P42 ④(令牌目标 = 流失败积 S(A) 二项尾 argmax;交集空持有等待) | **P42 ④** |
| 变宝为废语境 | **牺牲合成先行,零参数严格优**(EV 差 = 0.5(V_h−V_s) > 0,对读法鲁棒);推迟分支预算 1 帧保守 | **P34-a / P34-b** |

装备面与卡池 disjoint(P42 ⑥):全部动作零金币,P46 恒不辖;装备域无付费补救通道(结构差),错过成本更陡。

### 1.7 换线

**权威判据 = P16**:E_rounds(c) = distance/per_round 的超几何精确口径 + θ 滞回 + D_min 驻留(无抖动序列,振荡 ≤1/(2·D_min));判定 = E(alt)·(1+δ)+θ < E(cur)·(1+δ) ∧ 驻留 ≥ D_min。
**实证注(结构性休眠)**:`should_switch_e` 生产零调用点 + 三轮 AB 换线事件 0 起——新设计必须接线或显式放弃(P16 状态注)。换线瞬间 P41 卖出不等式自动翻转(贯穿件 V_opt+V_power 塌缩)。

### 1.8 横切修正项与支配性族

- **连胜取舍(P43)**:不是独立决策面,是支出侧的**加性外差项**——支出会改变战斗胜负时,花侧加 ΔV_streak(引擎口径 DP);默认姿态 = 保息,深破(L≥9)连胜账单独从不证成;`WIN_STREAK_BREAK_INTEREST=2` 旧常量在处死名单(P43 §⑤)。
- **残余补部署(P24)**:零支出域(C=I=0)补空槽严格支配整轮禁运——无条件,先于一切 EV(backs M1)。
- **危机火力(P36-a/a′)**:通道不阻断不变式 + 应急带储备线降档(R*_crisis≡0)——零参数结构,已落地(ADR-0506/0508)。
- **等待件部署 d=0(P51 B6/P24)**:买牌/部署当轮落板;XP 升级兑现滞后 d≥1 系保守折价假设(P37v2;关联级实证含 boss 混杂)——下游抵扣按通道折后计。

### 1.9 无数学,待立项(显式标注)

| 面 | 现状 | 依据 |
|---|---|---|
| 开局板面部署编排(M5) | 有文档依据([20] 过渡配方)与 r1 归因,**无 EV 证明**——按 comp 知识(TRANSITION_SYSTEMS)执行,不立数学判据 | DESIGN_MANDATE_LAYER §3.2 |
| 投资策略选牌 | 变体只做参数替换进各面(P39/P40 变体表),**投资牌本身的购买 EV 无专件**——归投资域,待立项 | P40 §④ 出域声明 |
| 事件决策 | 骨架层命题 B 列「事件面」为 EV 面之一,证明集无专件——待立项 | DESIGN_MANDATE_LAYER §3.6 |
| 阵容/羁绊具体选择 | comp 知识(`research/final_comps/`)+ 证据门(P38 供概率因子),**选线价值权重 V_ms 未标定**——结构在、数值空转 | P38 / P46 待标定 |

---

## 2. 状态函数清单(决策所需状态量 · 单一源 · 计算式)

| 状态量 | 语义与计算式 | 单一源 |
|---|---|---|
| g(存量金) | 当前金位,状态直读 | GameState |
| level / xp_cur | 等级与当前级已攒 XP(含买牌送 XP 结转) | `cw_state`(XP_PER_BUY=4 / XP_TO_NEXT_LEVEL 确定表) |
| R_全局 | 当前节点 + 后续位面按**实际长度**求和(金跨位面继承);⚠️ 现行 `cross_plane_remaining_nodes` 写死 9 是待修缺陷(dd-003) | `schedule_of` 实际长度 |
| Ī(净收入率) | **按收入日程逐节点现算** net_income = base(r)+streak(决策前相)+败轮底金(P39 修复批消参,常量 7 废) | `cw_economy` 收入三表(全【注】) |
| L(g,d,R,Ī) | 息档损失 = 双轨迹递推(p47 规范解;cap 按 resolved 参数化:默认 5/息律投资 10/开源节流 9/买断制 0) | **P47**(`tools/cw/proofs/p47_check.py`,可 import loss_exact) |
| λ_death(桶×节点) | 死亡危险率,直测分层表带 CI(P51 §4-B-2);**决策时可观测量 = 分层胜率 × 槽序表剩余战斗数(P51 Λ.1 生成式);禁用事后 R、禁用桶级混合值、hp 不入自变量**(三处口径 supersede 裁决 R63-4:桶含血带维+bench 维、以设计件 §2.10 分键为单一源;本行「桶×节点」为历史形态);**桶定义注记(直通线并入批,DIRECT_LINE_MATH_CHECK §2-P51/§4 注记 6)**:「板面战力桶」= 板面战力 **+ 备战席常驻件前后台强度贡献**(黑塔线强度主体=备战席小黑塔星级合计;圣杯/万敌同有非标准板面)——桶不含 bench 维 ⇒ 直通线 λ 系统性高估(实际战力更强 ⇒ 死亡率更低)⇒ 三段管辖 50/0 投影错段(健康段误判 λ>0 ⇒ 过早弃守息);修法=桶定义声明含 bench 维(sim 结算键层面确认)+ λ 表在直通线局补标定(挂采集 #3 辖域扩展,直通线语料薄)【裁定一/R26-H1 supersede(R32-中① 打标):本修法句作废——λ 表键=难度带×血带×位面×节点(PL 键,裁定一采 B1;板面/bench 维**不入**键),直通线强度异质性经难度带维+位面维承载,不再要求桶定义扩 bench 维;「桶不含 bench 维 ⇒ 三段管辖 50/0 投影错段」的担忧随三段管辖退役消失;直通线局补标定诉求降格为机会性精修项(安全带裁定瘦身清单);核读=IMPL_DESIGN §1 lambda_death 行/§2.7.1 注记 6(R27-4)】 | **P51**(采集 #3 = λ 连续模型,到位后替换分层表;桶含 bench 维) |
| W_floor / W_flow | 等待代价两通道:W_flow∈[0,0.5] 小项;W_floor = λ_death×(g+Φ)——**消费形态 = 区间敞口比较,禁边际引用** | **P51** |
| Φ(未来纯金流) | E[局终金 − g](实现轨迹净额估计) | P51 §4-B(反事实拆分挂 A/B) |
| ρ(整买价值率) | (ΔV_band+ΔV_pop)/R,仅双臂门真时 >0 | P48(A7 摊派口径,sim 对拍 2 裁决) |
| S(动态目标线) | B + floor_resolved + Σ_w 窗口预留卡价 + E[刷费]×2;B = 4⌈max(0, need−xp_cur−4n̄)/4⌉;λ>0 段降级为目标缓存 | P48 ② |
| n̄(resolved 自然 XP 流) | 买牌 +4/张 ∪ 投资每节点/每刷 XP 流,按局内 resolved 投资逐项求和 | P48(A3;估计器🔴) |
| q / P_shop / E[refreshes] | 槽级命中概率 / 单店至少一张概率 / 缺口期望刷次(超几何 DP;**第 4 参 c = 同费非目标已拿走数,禁传 cost**——P41 A2 防错注) | `cw_shop_odds` |
| taken_c / j | 同费已购总数 / 特定卡持有数(池衰减量) | GameState 持有计数 |
| V_opt(囤件期权) | u·P_miss·C_rescue(二维表按当前等级取格;消费处金可行性截断现检) | **P41 ①** |
| V_slot(bench 槽价) | 有空槽 = 0;free≤1 = max 被阻断动作净 EV;合成完备购不入阻断集 | **P41 ③** |
| V_comp(压缩价值) | A×Δ金(二维表,脚本生成) | **P49 ①②** |
| ΔV_streak(连胜账) | 引擎口径 DP 两次求值相减 | **P43 ①** |
| ΔP̂(完成概率增量) | P̂(集齐\|H∪{x}, B−c) − P̂(集齐\|H, B),Π Binomial 尾(阈值边际 ≥8-10pp 或走 DP) | **P38** |
| 板满 / 等待件谓词 | 板上人口位满 ∧ bench 存在阵营相关单位(不限星级)∧ 上场边际贡献>0——P39 臂一三元 | `cw_chars` 阵营标签 + 板面状态 |
| bench free | bench 空槽数(BENCH_CAPACITY 基准 9) | `cw_state` |
| 目标线 K | comp 的 core/shared/flex 名单(COMP_LIBRARY),运行时可判定——线内/线外分类的锚 | `cw_comps` |
| T_search | 当前/即将进入付费 D 的目标档集合(压库档匹配谓词) | P49 ③(组成规则🔴) |
| p̄(换线命中率) | 多项×超几何精确式(旧 union bound 高估 +8.1% 已勘误) | **P16**(`tools/cw/proofs/p16/pbar_exact.py`) |
| resolved 投资状态 | 息 cap / 刷价 / XP 流 / 池参数的语境修正开关(买断制/息律上调/淘金客等) | `cw_investments.STRATEGY_ECONOMY`(overlay 登记,单一源)+ `cw_effect_ledger.MechanismMutation`(运行时突变视图)。**指针纠错(修复项11,2026-08-31)**:原行指向 `cw_invest_data`(INVEST_MUTATIONS 登记)——该符号不存在且 cw_invest_data 为生成文件勿手编,机制修改器审计 F0 已定谳改指此两侧 |

---

## 3. 零调参审计(框架内还剩几个数字?)

**目标 = 拟合常数归零**(公理 1)。审计结论:**框架内没有裸拟合常数充当闸门**;全部数字落入三类,未标定项一律 fail-closed 或退支配性结构判据。

### 3.1 【注】注册表(零调参豁免——机制真值,非拟合)

REFRESH_PROB 概率表(实机 OCR,ADR-0091)、XP 门槛表(2026-08-31 用户定谒)、XP_PER_BUY=4/单击 4 金(flat,lv6/8/9 覆盖面🟡)、SHOP_REFRESH_COST=2(粗估,实机校准)、息函数 min(g//10,cap) 与 cap_resolved、STREAK_GOLD_TABLE、LOSS_GOLD_BY_NODE、POOL_COPIES/DISTINCT_CARDS/SHOP_SLOTS、sell_refund(1★全额退/手续费规则)、BENCH_CAPACITY、装备池 8/36 与 K8 图谱、炉/锅炉效果文本、轮岗 ROTATION_CHANCE=0.2(读屏即真值可现场替换)。**共计 ~14 组,全部单一源在代码注册表。**

### 3.2 【推】推导量(闭式/递推/DP,数值随注册表重算,无自由参数)

L 递推(P47)、E[refreshes]/p̄(超几何 DP,P16)、V_comp 压缩表(P49)、ΔV_streak(P43 DP)、floor_eff 50/0 投影(P39:λ_death 现算的二值投影,零拟合;**已退役,R26-H1/R32-中① 打标——现行=arm2 结构守息门,核读=IMPL_DESIGN §2.0**)、P38 闭式带(+8pp 乐观偏差已带内声明)、P50 吞吐精确分数、P34-a 期望差、S/B/ρ 形状(P48 结构)。**零自由参数。**

### 3.3 【拟】观测估计(带 CI,禁点值当闸门)——剩余未收口项清单

| # | 参数 | 分级 | 消费面 | 框架内的落点(不塞拟合值) |
|---|---|---|---|---|
| 1 | u_x / H_x(使用概率/需要时距) | 🔴 | P41 V_opt | **数值不授权**(P41 修复批撤销);燃料类已消参,中间类 fail-closed 持有 |
| 2 | V_ms / V_gap(成型边际价值) | 🔴 | P46 E_line / P40 R1 | 形式判据空转,禁双源(P46/P40 必须同一标定);标定前 E_line 不放行 |
| 3 | w(人口位战力当量) | 🔴 | P39 臂一 | 臂一只用 w>0(板面谓词保证),不需精确值 |
| 4 | ρ 摊派口径(A7) | 🔴 sim | P48 | 均匀摊保守首版;阈值表随 sim 对拍 2 重标 |
| 5 | n̄ 估计器 | 🟡 | P48 B | 实估,误差二阶(A3 声明) |
| 6 | A / T_search 组成 | 🔴 | P49 | 活跃窗口按付费份额确定性查表;跨窗口项按下界略 |
| 7 | λ_s 装备来源流 / partner 到位率 | 🔴 | P42/P50 | 一切绝对 EV 空转,只有序/支配结论可消费;禁拍 λ 死值 |
| 8 | v_F / v_B(炉/死库存价值) | 🔴 | P50 ④ | sim 是唯一标定通道;平衡式只给阈值形态 |
| 9 | c_frame / q(P34-b) | 🟡/已半收 | 变宝为废推迟分支 | q plaza 先验 0.15[0.08,0.30] 只撑量级;保守预算 1 帧维持 |
| 10 | Δp(d→胜率换算) | 🔴 | P43/P23 消费方 | P43 只供 ΔV_streak 外差项,换算归消费方带 CI |
| 11 | λ_death 连续模型 | 🔴 采集 #3 | 全框架三段管辖 | 分层直测表带 CI 先行;P51 A/B(§8)因果化挂账。**λ 标定语境声明(修复项15,2026-08-31)**:分层表的职级难度/词缀语境=**采集局口径**(120 局账本语料),跨难度域/跨词缀域复用属外推(同 ADR-0424 禁外推精神),设计内禁——理想态 λ 表加难度维,待遥测积累。**直通线补标定(直通线并入批)**:桶定义含 bench 前后台强度维 + 直通线局补标定,详 §2 λ 行注记【R178 supersede 标(IMPL_ADV_R178 症2,打标不删史):本位「桶定义含 bench 前后台强度维」系 R26-H1 前口径活文字——现行权威=IMPL_DESIGN §1 lambda_death 行「板面/bench 维**不入键**(PL 键实证…)」(亲读),桶定义 bench 维已随 PL 键时代退役;本位系 R63-4 物理打标挂账的逃逸位(挂账仅在 §2 行本位兑现,头注 L8 与本行漏扫),「详 §2 λ 行注记」指针指向已打标行(裁定一/R26-H1 标)但本位文字独立可读故仍需本位标记;登记=design_telemetry.md 文末 R178 节】。**路由权威降格待新钥匙重建(v3)**(2026-09-01,出处 = IMPL_LAMBDA_FOUNDATION_ATTACK 总判决:λ 表方向注记可用、路由权威不可用;新钥匙形态待用户最终批,详尾部「对抗批结论已交付」块) |
| 12 | p_hit/q_conv(P36-b/c) | 🟡/🔴 | 危机预算阶梯 | 不变式(P36-a)零参数已落地;数值阶梯不授权 |
| 13 | V̄(刷新价值门量) | 🔴 | P40 R0-2(原价值门) | **比较项不授权,禁作闸门**(P40_VALIDATION 已登记降级路径);R0-2 退结构判据(合格集空 ∨ 预算门)为其 fail-closed 退路 |

**审计结论:剩余 13 项观测估计,全部带分级与 fail-closed/支配性退路;框架内可直接落码的判据(支配族 + 结构门 + 注册表推导量)不含任何一个拟合常数——零调参目标在判据层达成,数值层待标定清单独立挂账。**

### 3.4 语境与出域声明(机制修改器审计已裁;修复项14/15,2026-08-31 落地)

| # | 声明 | 内容与消费规范 |
|---|---|---|
| 1 | **池构成族接线(修复项14)** | 专家顾问族(十余张「使其加入商店」)/黑塔纪元(216 张特殊池)/人才下沉/二·三·四费援军(含市场干预 `refresh_shop_rewrite_every_3cost` 旗标)= **v_c/a_c 池构成硬突变**:resolved 固定牌库变化时,`cw_shop_odds` 概率表/压缩价值表(V_comp)/p̄/E[refreshes] **须整体重生成**,不得沿用突变前表(economy §1 已有指令「固定牌库是 resolved 的」,本行 = 设计侧消费规范接线注记)。 |
| 2 | **标准局型声明(裁定 B4,修复项15)** | 本设计全部判据标定与消费口径 = **标准博弈局型**;超频博弈(时长/奖励结构不同)**出域**——若需支持,位面日程须按局类型分表(plane_node_table 局型分表)+ λ/Ī 重标定,骨架不分叉但参数域不通用。 |
| 3 | **λ 语境出域(修复项15)** | 见 §3.3 #11 行内声明:λ_death 分层表只在职级难度/词缀的**采集语境**内成立,跨域复用禁(同 ADR-0424);词缀对 λ 的影响经直测表隐式吸收,不逐词缀建模。 |
| 4 | **库藏生锈装备面消费规范(修复项15;2026-09-01 终裁收窄)** | 词缀「库藏生锈」在场时(备战席每 1 件未穿装备 → 敌伤+3%/受伤−4%,至多 10 件),处置分两层:**①支配条款(立即生效,零参数)**——词缀激活 ⇒ 无用件(非保留组件,recycle_qualified 判据)聚拢穿到**指定工具人**,**配对安全谓词**:该角色身上简易件须全为无用件(防穿着自动合成把有用组件拖进非预期产物,机制见 equipment_mechanics §1.1);穿戴零金币成本、可逆([24] 转移常态)⇒ 纯降未穿计数=支配(与 P24 补部署同族)。**立足点仅此一条**(词缀罚归零);炉料恰好穿着等只是顺带形态,非理由(P50 辖域=每炉吞吐优化,非目标获取主通道——主通道=好运令牌+组件合成)。**②数值层(待标定)**——保留件([29] 组件期权)与词缀罚的精确取舍需 λ(k) 曲线(sim 注入词缀扫件数,与扑满过热注入同通道),标定前保守兜底=保留集收紧至活跃配方缺口件。数值直读 `affix_effects_data`(词条识别名=库藏生锈,`cw_comps.RUST_AFFIX_NAME` 单一源),禁在设计内复制数值。 |

---

## 4. 待证钩子的接口(5 件占位与默认行为)

原则:**结构背书形态,数据到位前不授权数值**——占位判据给出式子与辖域,默认行为 = 该面退回常规门/骨架义务,不用占位式子做闸门。

| 钩子 | 命题(一句话) | 框架占位 | 默认行为(数据到位前) | 卡点 |
|---|---|---|---|---|
| **P26** | P1 硬节点备战支出 EV 门(v2:EV=Δp_prep·G_loss+Δstreak·S_streak−C−I;纯金流闭合,L_node 只辖触发面水位) | 「硬节点(boss 前)备战加码支出」面:统一门与 P23.1 同构,G_loss=λ_death 销毁敞口(P51 §F.3,**区间敞口比较禁边际引用**) | 备战支出走常规门(P40/P46/P39),不加码不特批 | Δp_prep 条件表 + L_node 分位口径未采;门值带 (C+I)/G_loss≈7-32% 仅结构背书 |
| **P25** | P2 中段锁定线核心卡出现即买(V_a > ρ·C_buy/Ī + L + ΔW_trunc,P48 统一式) | P2 锁线核心卡买入面(§1.1 末行) | **设计占位默认关**;线内核心卡走 M2 义务通道照买(骨架层),不走本判据 | Δλ_death 标定;v2 形态证成(死线 v3.3 落码起算) |
| **P29** | 锁线态囤牌羁绊感知(买入段+兑现段双段 EV,兑现折价 δ) | 锁线后线内成员买入的精细化定价(与 M2 义务、E_rev 通道的关系:本件若证成 = 给 E_rev 的线内侧加兑现段项) | 不立数值闸门;消费 M2 + P46 E_rev 现行形态 | Δp_tier 按档分解标定 + δ 折价建模(挂 w795) |
| **P8** | 扑满应对(轻投入凑羁绊刷伤害,禁深花;门式 c < Δq·R,零参数结构形态) | P40 变体⑥ 的扑满节点 V̄ 分量(V̄ 同降为 EV 比较项,不作闸门;轻投入支出上限并入 R2) | **数值闸门全部撤回**;只保留零参数结构(禁深花保血授权、扑满不适用深花);sim 过热环境注入解锁 R/Δq/s 分布实采 | piggy_reward 零观测;Δq=伤害阈值达标口径禁胜负阶梯代理 |
| **P12** | P2 找件收益 λ_death 纯金流重述(benefit=Δλ_death×(g+Φ)) | P2 找件/伤害带支出的收益侧定价(λ 概率侧骨架) | **方向级消费**:Δλ_death>0 方向注记可引(P15v2 下界/registry 背书);幅度授权禁(j≥1 翻正依赖的 B/P 分列 OCR 未进遥测);battles 禁 9(P2=7 槽,ADR-0366) | 结算屏三项 OCR 进遥测(硬前置);w153 脚本 import 断裂挂账 |

---

## 5. 与骨架层的拼合(权限分界)

骨架层(DESIGN_MANDATE_LAYER,义务 M1-M7)与本数学框架的**权限分界一句话**:义务无条件 + EV 只增不减 + 证明选线。数学框架不重新划分权限,只填充三层各自的判据内容。

### 5.1 逐条映射(M1-M7 ← 数学背书)

| 义务 | 数学背书/接口 | 关系说明 |
|---|---|---|
| M1 线成员同帧部署 | **P24**(零支出补部署严格支配) | 数学把「部署禁运」证成严格劣;义务的存在性由文档依据链([13]/[8])承载 |
| M2 线成员买入 | [11]/[6]/[2] 文档依据;P41 支配性(1★ 可逆净成本 0)、P1 再遇账(旧树,语义由 P41 ① 新树接管) | 义务不走息律门;P46 否决域**只对序 3-5 EV 买**生效 |
| M3 升级整批日程 | **P39 臂一**(谓词三元)+ **P48 整买引理/S 线** | p39 臂二 = **调度门**(何时),无权否决 M3 存在性(r3 教训,§1.3③);cap 感知硬约束 |
| M4 腾席卖出 | **P41 ② 支配性条款**(零参数)+ 未定型期/危局辖域条款 | 燃料件卖出判据 = P41 分类表第一行,与 M4 判据逐字对齐(P41 修复批③) |
| M5 开局板面部署 | **无数学**(comp 知识 + r1 归因) | §1.9 显式立项面;deploy(M1)落地后自动覆盖 |
| M6 溢余转压库 | **P49**(档匹配即买)+ P48 命题 3([41] 资产形态:囤的规范形态 = 可逆资产,g+Σ活期退金 ≥ S 触发) | 硬约束 = 金>50 之上 + S 预留保护 |
| M7 装备转移常态 | **P42 ⑤**(字典序分配,穿戴可逆 = 无资源承诺)+ [42] 裁定 | 分配/合成规划留 P42 EV;M7 是骨架地板 |

### 5.2 三层各自的数学内容

- **证明层**:证据门(P38 完成概率因子)+ 换线(P16)+ 停手线([13])——只产出线选择与 L(r) 场景,给 P48 供 S 目标线,无单笔否决权;
- **骨架层**:义务动作只受四项硬约束(可负担/席位/S 预留/不可逆护栏);**HP 不在硬约束里**(骨架常开使危机成因不可达,优于危机特设出口——DESIGN_MANDATE_LAYER §3.3,对 r2/r3「危局接管门」提案的架构级否决);
- **EV 层**:本总纲 §1 全部判据;只增不减,可在骨架之上追加,无权否决或改写骨架动作。

### 5.3 输入缺失的定向 fail(骨架层 §3.4 与数学层衔接)

可逆动作(买/部署)缺输入 → 默认做;不可逆动作(卖/刷)缺输入 → 默认不做。数学侧对应:未标定参数(§3.3 清单)在 EV 层一律 fail-closed(不产生追加动作),**绝不向骨架层渗漏为一个否决**——这是三轮失败「义务静默蒸发」的防线在数学框架侧的镜像。

### 5.4 与既有决策记录的衔接

- ADR-0482(策略命题权威 = 证明与实证):本框架是消费侧整合,不立新策略命题;
- 处死名单沿用:WIN_STREAK_BREAK_INTEREST=2(P43)、floor=50 原值(P39 修复批)、`_xp_gold_floor` HP 分支([39])、`cross_plane_remaining_nodes` 写死 9(dd-003)、scoring 小数等级通道(P48);
- 实现落地时的验证场 = 骨架层命题 A/B(骨架-only)与命题 B(骨架+EV 三臂)——本框架 §1 判据即「EV 层增量合法性」的检验对象。

---

## 6. 总纲一页(呈报用)

1. **一个权限骨架**:义务无条件(M1-M7,四项硬约束)+ EV 只增不减(§1 七面判据)+ 证明选线(P38/P16)——数学填 EV 层与证明层,不重划权限。
2. **一把三段尺**:一切金位决策按 λ_death(桶×节点分层)分三段——健康段守息(P47 L + P48 判据)、中间带统一式(V_a > ρ·x/Ī + L + ΔW_trunc)、濒死段转化优先(P51,S 降级缓存);floor_eff 50/0 投影零拟合。【R26-H1/安全带裁定 supersede(R32-中① 打标):本条「一把三段尺」路由已整体退役——现行=发展优先默认姿态+升档器只升不降(安全带架构裁定),核读=IMPL_DESIGN §2.0;本条保留为历史档】
3. **七面各有唯一权威**:买=P46(豁免条件式)、卖=P41(燃料件支配零参数)、升级=P39 双臂+P48 时序、刷新=P40 三层门(R0/R2 结构门,V̄ 降比较项;R1 标定前 fail-closed 无付费刷新)、压库=P49、装备=P42+P50+P34、换线=P16;P43 是横切加项;开局部署/投资选牌/事件决策 = 无数学待立项(显式)。
4. **状态函数 22 项各带单一源**(§2),L 递推与 λ_death 分层表是全框架共用的两个税基——单一源防双计(P47/P51 分工:息账与存量清零账互不包含)。
5. **零调参达标**:判据层零拟合常数(支配族 + 结构门 + 注册表推导量直接可落码);剩余 13 项观测估计全部 fail-closed/支配退路挂账(§3.3)。刷新面 R0/R2 走结构门,V̄ 仅作 EV 比较项不作闸门。
6. **五个待证钩子只占位不授权**(P26/P25/P29/P8/P12,§4),默认行为全部退常规门/骨架义务。
7. **上代死因的数学侧镜像**:义务不被 EV 否决(臂二降调度门)、fail 按可逆性定向、HP 全面出账——三轮「没做占绝对主导」的失败形态在本框架中无发生通路。

## ⚠️ 数据基座质疑登记(用户,2026-09-01,持续有效)

> **用户原话立场**:「我对依赖这个的保持很大的疑问」——指 λ_death 的**整条数据依赖链**:无战力建模→查表替代→表依赖实机语料(151 局,单账号/窄难度带/词缀混入)→sim 注入量产但尾部为插值(模型风险,sim 表本身从同一语料拟合=循环)→样本薄影响效果。

**依赖链一览**(本框架半数判据的承重墙):
```
实机语料(151局) ─标定→ λ_death 分层表(桶×节点,带CI)
                        │
sim战斗表(同一语料拟合) ─注入扩样→ 尾部桶(插值,带模型风险)
                        │
                        ▼
三段管辖路由 / P51等待代价 / V_power卖面惩罚 / floor_eff投影 / P48统一式 / R2预算
```

**已成立的防御**(消费侧):CI 保守端 / fail-closed(不确定=不可逆不做)/ 粗桶二值路由 / held-out 验证 / 漂移检查。

**用户保留的疑问**(对抗批 IMPL_LAMBDA_FOUNDATION_ATTACK 的攻击对象,结论未出前本登记持续有效):
1. 保守消费声称「错的代价=少赚不是送死」——过度保守本身会不会就是饿死螺旋(已知的 r3 形态)?
2. 桶=可观测量代理,可交换性假设(桶内状态同分布)是否真的成立?
3. sim 循环性:尾部插值的模型风险是否被 CI 正确计价,还是被当成了采样噪声?
4. 样本量:主对角线够、尾部薄——尾部恰是濒死带(λ 消费最敏感的区域)是否可靠?
5. 有没有不依赖 λ 标定质量的替代姿态(robust/minimax/纯结构规则),代价多大?

**约束**:对抗批结论未收账前,任何「λ 基座已稳」的表述禁止出现在设计件/呈报中;本登记的解除=对抗批交付+逐问闭环+用户确认。

### 对抗批结论已交付(2026-09-01;出处 = IMPL_LAMBDA_FOUNDATION_ATTACK.md,射界含全景 10 项)

**六问+扩射界逐问一行化结论**(证据链与强度标注详攻击报告,此处只收口):

1. 保守性反例实证——「λ 错=少赚不送死」闭环不成立(sim 保守臂 35/40 死 vs 消费臂 30/40、死时金中位 194;实机 52 死局死前 5 轮零动作、35/49 死时金≥50);
2. 可交换性证伪——同桶亚组危险率差 3-25 倍(hp 分层 λ3 0.000→0.904),桶可交换性作「近似」都不够格;
3. 虚防御实锤——防御清单中「held-out 验证/漂移检查」两项查无对应物;CI 只计 seed 噪声,模型风险(β 未点估计/掉血带删失拟合/胜率 clip)完全账外,实机实测 +17pp 漂移无哨兵报警;
4. 尾部翻转——最弱结论 CI 裕度仅 9%,同格亚组/模型扰动量级为其 3-10 倍,0.1 阈值带内翻转是常态非边缘风险;
5. 替代姿态——生产已有的 hp/进程结构规则事实上是兜底主干(捕获 λ 表丢掉的 25 倍梯度),minimax 退化不可作主姿态、序贯宜作漂移哨兵;推荐 posture=结构规则为主干 + λ 表降格敞口注记 + 局内序贯示警补充;
6. 总判决——**λ_death 基座 =「需兜底」:方向/量级注记可用,路由/翻转敏感判据的权威不可用**(兜底清单 R1-R5,详攻击报告 §6)。

**扩射界连带标注(2026-09-01,出处同上;全景 10 项逐项详报告第三部分)**:

- **P37v2 击穿→v2.1 重证落档(2026-09-01)**:实机 q_full=0.269(CI[0.212,0.328])击穿 sim 带;v2.1 已按实机口径重闭(证明件+脚本断言全绿):主体保留(默认域 ρ≥0.269 闭合),两处如实翻转(ρ∈[0.25,0.269) 段/cap10 域),「2.0×裕度」声明废除(2×需 ρ≥0.538 出消费带),最劣格 0.93×/最优 3.77×。待独立对抗复验。
- **R09 亏损表降【拟】**:LOSS_GOLD_BY_NODE 系小样本差分众数(boss 格 n=8),非注册表——P51 溯源表标「【注】注册表」是标注错误,应降「【拟】语料差分,n 披露」(值不改;streak 表弹窗直读 49/49 不降);
- **p_win_p2_by_rung 生产循环(R6 待办)**:该表(0.016/0.413/0.657)为 sim 双跳循环产物且已接生产 C4 存活轮数门——全景表里比 λ 更紧迫的循环暴露;R6 = 出生产 C4 门或换实机可估代理 + 漂移哨兵。

**解除条件保持不变**:本登记的解除 = 逐问闭环 + 用户确认;未解除前,一切「λ 基座已稳」的稳态表述继续禁止。

### 附:语料依赖面全景(2026-09-01 扩,用户指令「都可能有这个问题」)

> 判据:**依赖实机对局积累**(而非一次性注册表采集)的输入,全部继承本登记的质疑。一次性采集(牌池/刷新概率/退金/羁绊档位/装备效果/comp 知识=游戏内直读或 API)不在质疑面内——它们的正确性由采集时点核验,不随局数变化。

| # | 语料依赖项 | 语料来源 | 下游消费 | 同病暴露 |
|---|---|---|---|---|
| 1 | λ_death 分层表(P51) | 151 局实机 | 三段管辖/等待代价/卖面惩罚/floor_eff/P48 | 已列(主对象) |
| 2 | **掉血公式 B+P(P15)** | 结算屏三项(新采,2-3 局起步) | **P12 幅度授权**/λ 画质/危机归因 | **用户点名**:同病——参数=小样本直估 |
| 3 | **P12 升级幅度授权** | 经 P15 单价+λ 通道 | M3 幅度/升级预算 | **用户点名**:全链语料 |
| 4 | 收入日程表 R09(连胜金表) | ADR-0439 实机 108 局/767 轮金差分 | Ī/等待代价/P48 右端 | 同病:单账号差分 |
| 5 | sim 战斗表/胜率模型(cw_coarse_battle/cw_battle_calib/win_model) | 同一 151 局拟合 | ΔP̂(P16)/V_ms/全部 sim 注入的底座 | 同病+循环(与 λ 表同源) |
| 6 | P51 转化优先下界(5.8 金/轮) | 由 λ 表推导 | 濒死带转化判据 | 随 #1 |
| 7 | P37v2.1(实机重证:q_full=0.269 CI[0.212,0.328],E[I_gain]=1.34 点 CI[1.06,1.64];两下端格翻转已如实入证明)/P35(0.78-1.48 来源存疑待核,攻击报告标注) | 实机 replay 统计(154 局) | XP 转化/长时域升级授权 | 已治(实机口径);P35 来源待核 |
| 8 | 13 项【拟】标定(V_ms/V_gap/u/N_crisis 等) | sim 对拍(底座=#5) | 各判据开闸 | 循环病(经 sim 间接依赖语料) |
| 9 | P15v2 扩样清单(rung 败局格/boss 胜率/P2 段) | 实机局数积累 | P15 收紧 | 显性挂账 |
| 10 | P31 中层支配/P17 级4 重证 | 实机 A/B 实验 | 中层判据 | 挂账 |

**攻击批射界同步扩展**:IMPL_LAMBDA_FOUNDATION_ATTACK 覆盖上表全部(每项:语料量实数/单账号偏差形态/循环性/替代姿态),报告一册收口。

### 用户三裁定(2026-09-01,亲录)

**裁定一:λ 钥匙选型=按数学优劣定**——v3.1 对比 B1(位面维)全口径占优(AIC 1176 vs 1213、LOO 0.6663 vs 0.6828、空格 35 vs 53),**采 B1**;预登记 A 回归触发器(新策略产生足够「同期板面差异」语料时重评 A,板面=因果量优势届时兑现)。选型依据与数字=P51_V3_REBUILD.md v3.1 节。

**裁定二:hp 不作策略入参的根因声明(公理解释的权威口径)**——原规矩的根本原因=**我们无法对战力和胜负做准确建模,hp 只是这个根因的后果**(不是 hp 本身有问题);用户对粗略模型导致策略效果差保持警惕。**当前仅两处数学件被授权开此缺口**:①λ_death 表(血带入键)②P15 掉血公式(结算屏三项中两项为 hp 分量)。**此二件之外,任何其他战力相关内容(战力建模/胜负建模及其衍生量,hp 仅为其后果之一)进入策略,须用户逐项确认后才能实施**——本条为硬闸门,辖域=战力建模面而非仅 hp 字面,后续批次禁擅自扩缺口。

> **【待玩家确认(R32-中② 挂账,2026-09)】血线硬地板 hp 直接触发支出解锁的授权确认**:IMPL_DESIGN §2.0-3② 的血线硬地板(hp<阈值 ⇒ 解锁 M3 破息批至饱和线下+F7 禁令+转化优先)系 hp **直接**进决策(非经 λ 间接、非支付通道类同构)——按裁定二硬闸门辖域读属「授权两件之外」的候选缺口;现存授权依据=安全带裁定的下游设计扩展(`lambda-risk-philosophy.md`「升档器=血线硬地板+λ 顾问」,设计产物),安全带裁定用户原话只点名「薄 λ 顾问」、未点名血线地板的 hp 触发形态。**确认前按现状运行**(行为已在设计批文落地并有移除等价性测试位与 `neardeath_unlock` 遥测覆盖,本挂账只补确认账、不改行为)。请用户逐项确认:①血线硬地板 hp 触发形态——授权成立 ∨ 撤回(撤回=行为回滚+ADR);②血线阈值【拟】标定域(≤15 或双触发可并存声明);③解锁包清单(M3 破息批至饱和线下/F7/转化优先)。同批核:「已裁三件」表述中③(0.1 死阈值)的出处系攻击报告实证而非用户裁定文本——该阈值已随 R26-H1 退役,授权归因偏弱随退役消解。确认结果回写本节。

**裁定三:用户对实机局数少的怀疑,如实常驻记录**——用户立场:「我不可能一直跑实机采集」「怕样本太少影响效果」。已落的对应物:本登记(持续有效)/质量门与四层兜底(P51_V3_REBUILD §消费端声明)/扩样依赖=结算屏遥测(每局=25 轮样本)+sim 仅压力测试。该怀疑的解除条件=质量门实测过线+用户确认,不以"分析说没事"自动解除。

### 安全带架构裁定(2026-09-01,用户)

**方向批准:发展优先默认 + 危险只升不降的薄 λ 顾问**(替代原「0.1 阈值重推+三段管辖重设计」路线)。用户原话条件:「只要不难实现,而且在我们决策正确的前提下基本不会作用,那加入是更好的」。

**两条件固化为验收性质(缺一不批)**:
1. **实现不难**=顾问是判据外的独立薄层(消费既有 λ 表+血线,不改七面判据本体)——架构批落位时验证;
2. **正确决策下基本不作用**=激活率门:骨架+EV 臂 sim 批中顾问触发率低于阈值(具体数值随架构批定,预期个位数百分比内);触发率超标=顾问过敏感=回炉。

**归因修正同步入档**:「52 死局攥钱死=反应缺失实证」**降格为相容旁证**(因果不可识别,攻击报告 §I-1 自认);用户假设「旧死因主因=决策质量」复盘批验证中(DEATH_POSTMORTEM.md)。数学论证(吸收态结构要求切换信号)不依赖该语料,维持成立。

### 死因复盘结论回写(2026-09-01,DEATH_POSTMORTEM.md,57 死局逐局分类)

- **用户假设「主因=决策质量」成立为最大单一主因:A 类 31/57(54%)**——但内部结构关键:**A1「漏买/不建队」21 局**(金够、店里有、就是不买,极端局全局买 0)≫ A2/A3 买而烂/转线失败 10 局。
- **B 类「反应缺失」18 局(32%)非零**——「不是没有危险反应」的否定**不成立**;且 **A1+B 同根(消费被 interest/level 姿态压制,无血线→紧急消费通路)=39/57(68%)**——最大死因族不是"决策多样烂",是**单一错误模式:该花不花**。
- C 不可挽 8 局(14%)=预期地板。保守口径至少 6 局(11%)"买了就显著改变",最多 15 局(26%)可能翻盘。
- **设计对照(重要)**:骨架层 M1-M4 义务动作常开**直接治 A1+B(68% 形态)**——「义务常开使危机态不可达」与数据吻合;A2 归 EV 层待证;C 为地板。**A/B 验证判据基线**:用本报告同口径复算,A1+B 占比塌向 C 地板=骨架生效信号。
- **安全带裁定维持**(B 类 32% 真实存在,安全带非死重);激活率门不变(那些 B 死局发生在烂策略下,M1-M4 生效后正确决策下触发应稀少——由 sim 批实测)。
- **瘦身清单(假设部分成立的兑现,呈用户批)**:λ 表冻结 B1 薄层不再加厚(尾部标定/bench 维扩展/候选升级降级为可选,按需再启);战力硬闸门不开新口子;现有语料长期够用。

### 瘦身清单改判(2026-09-01,用户)

「λ 冻结/战力封账」改判为**机会性精修道**:战力建模工作不封账——**有并行闲槽即可继续做(精益求精)**,条件两条:①不影响主体流程(主线的判据/落码/sim 一律不等精修结果,B1 薄层按既定设计消费);②新产出进策略仍过硬闸门(逐项用户确认)。精修项池:λ 尾部标定/bench 维扩展/候选形态升级/词缀-λ 交互等——按闲槽派批,主流程优先级恒高。

### 血线硬地板三件挂账·确认回写(2026-09-01,用户)

**用户裁定:「只要不影响我们的核心——以发展为主线——就没问题」**。三件按此确认:
1. **形态授权**:血线地板作为升档器触发源 ✓(升档器系=只升不降的独立薄层,默认姿态=发展优先不变);
2. **阈值标定域 ≤15** ✓(标定批验收前置);
3. **解锁包清单**(M3 解锁至饱和线下+F7 禁令+转化优先)✓。

**绑定约束(确认的条件,写入验收)**:血线地板的一切行为不得在安全域内推迟或抑制发展动作——P52 定理的安全域 D_ε 上顾问/地板不动作(激活率门),D_ε 外仅做危险响应增强(解锁=花更多保命,非压制发展);若任何实现或标定结果违反此约束(如安全域内频繁触发),按 R36-3 (d) 全架构回炉。


### 血线阈值标定域扩域·新增待确认缺口(R42-2,2026-09-02 编排者代登记)

R42 修复将设计侧标定域**收回已确认的 ≤15**(总纲本节为权威);(15,40] 扩域作为新增缺口分立待用户确认——设计件 §2.0-3② 已按 ≤15 收回执行(gap 帧键恒 0 空窗申报),扩域若获确认再按 (15,θ] 分支启用。确认前现状=≤15。
### λ 基座质疑登记·带条件解除(2026-09-02,用户)

**用户裁定:「只要是以发展为主线,这种求精的,可以,但必须标注后续待观察」**——质疑登记按下述条件解除:

1. **前提条件(与发展主线绑定)**:λ 基座一切消费(顾问/表/键)不得在安全域内推迟或抑制发展动作——与血线地板确认同款绑定约束,违反=全架构回炉(R36-3 (d));
2. **状态=「后续待观察」,非「稳定」**:λ 基座转入观察名单,禁在任何文档/呈报中使用「已稳/已解决/无需再查」类终态表述;观察载体=既有监控族(漂移哨兵 calib_drift_alarm/键重估触发器 key_board_risk_contrast 与并列度监督/标定批质量门/首批 sim 激活率基线对照);
3. **解除范围**:「禁稳态表述」的登记级禁令撤销,替代为上述观察纪律;登记原文与四轮攻击史留档不删。

技术闭环依据(四轮攻击+轴修复验真+三裁定实施)见 P51_V3_REBUILD v3.3-7 闭环声明。

> ## 度量家族冻结宣言(用户裁定 2026-09-02)
> D_ε 门/有效性判据/标定通道/哨兵等度量家族,**规格冻结于 R65 后态**。裁定内容:
> ①不确定性帧一律默认安全,维持发展主线(检测器两难的政策选择由用户定谳:宁可漏报风险,不打断发育);
> ②R66 七项发现转冻结残余清单,不派修复批,随实机数据积累再校准;
> ③本族后续只允许错账修正,禁止新增验证装置。
> 主链(卖面链/PL 键/三臂/执行序/组-端)经 R51-R66 连续十余轮注册表直调攻击零新症,设计对抗循环就此收口,落码准入由本冻结替代「连续 5 轮零新问题」门。