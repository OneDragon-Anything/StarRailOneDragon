# 货币战争-投资策略(currency_war_invest_strategy)

> source_image: sr-od-test/screens/货币战争-投资策略/default.webp(2026-08-15 live 建档,1-2 后首遇)

## 何时出现

投资策略节点(每局 1-2 轮后触发;对局内**独立屏**,非备战 overlay —— 右上有「返回备战界面」
按钮可回备战(选择保留待选)。2026-08-15 live 实锤)。

## 状态流转

```
进入(3 张策略卡)→ 点卡名选中(白边高亮,可换选)→ [可选] 点卡下刷新(每卡 1 次免费,
只换该卡内容)→ 点确认 → 返回备战(下一节点)
```

## 识别特征

- id_mark「标识-请选择投资策略」[855,78,1065,118](顶部标题;live precise 实证)

## 可交互元素(live 逐个验证 2026-08-15)

| 元素 | 坐标(中心) | 行为 |
|---|---|---|
| 策略卡 x3 | 卡名列 y~474(卡中心 x~460/940/1460) | 点**卡名行**选中(白边+确认亮起);再点他卡换选(live 验证切换) |
| 刷新按钮 x3 | y~855(每卡下方) | 「刷新次数1」:换该卡策略(live 实锤 卡3 借力打力→战术专家:佩拉);每卡 1 次免费 |
| 确认 | (978,983) | 确认选中策略 → 返回备战(不可逆出口) |
| 返回备战界面 | 右上 (~1789,57) | 回备战屏(选择待定;非推进出口) |
| 攻略/图例 | 右上/左上 | 信息查看(非自动化目标) |

## automation 要读信息

- 3 张卡名(y 465-505 过滤带;「区域-卡名行」[200,455,1720,505])→ HandleInvestStrategy._read_options
  (OCR 卡名行,左→右排序)→ decide_event 白名单打分
- 已选策略写 session.active_strategies(append+去重;经济/刷新费判定读)

## 注意

- 点卡名 y~474 才选中(点描述区/卡底不选中 —— 曾致 flat-loop 18min,I16 已推翻)
- 刷新按钮是**每卡独立**的(v1 建档遗漏,2026-08-15 VLM 枚举 + live 验证补);P5 决策面
  (何时值得刷新)待工具域后评估
