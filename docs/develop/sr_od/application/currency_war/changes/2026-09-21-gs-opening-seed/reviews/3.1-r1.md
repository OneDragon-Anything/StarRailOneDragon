# gs-opening-seed 阶段 3.1 交付验收 review(r1)

- 验收对象:主仓 `9c60e0ca9`(kernel/cw_game_state.py + kernel/cw_reconcile.py)、测试仓 `e3a214d4`(新增 test_cw_game_state_opening_seed.py + 修改 4 个存量测试文件)。
- 依据:design.md §2.2/§2.3/§2.4/§2.5/§2.7(定稿)、landing.md §3.1。
- **最终判定:验收通过**(无阻断;2 条应修均为设计文本与实码的口径出入,处置归设计方裁定 + 末阶段正本更新,不阻断 3.2 开工)。

## 1. 逐项核查结论

### 1.1 两 commit 全 diff 实读 + 入库面

- 主仓 `git show 9c60e0ca9`:仅 `kernel/cw_game_state.py`(+138/-26 内含)与 `kernel/cw_reconcile.py`(+5),两文件 = landing 3.1 文件面,零卷入。
- 测试仓 `git show e3a214d4`:5 文件(新增 test_cw_game_state_opening_seed.py 225 行 + 4 个存量文件小改),文件面 = landing 3.1 申报面(landing 写作 `test_cw_game_state*.py`,另 3 个测试文件的修改属申报行为变化的测试适配,正当性见 1.3)。
- 两仓工作区在验收路径上干净;验收 commit 均为 HEAD~1,其上各一个无关批 commit(银狼升费 / LV999 卖价锁),不影响本审。

### 1.2 种子值逐字段对照(design §2.2 A 表)

实读 `seed_opening_state`(cw_game_state.py:1717-1777):23 个 `_seed` 调用,与 A 表逐一吻合——

front_row/back_row 空行(**先写**,L1747-1748,写序合规)、bench = 9 空槽 BenchView(`BENCH_CAPACITY_DEFAULT=9`,cw_game_state.py:146)、equips=[]、occupied_equips={}、spheres=OreSight()(空读面:count=None/colors=()/points=(),L473-481)、gold=3、level=3、xp=(0, XP_TO_NEXT_LEVEL[3])=**表查单一源**(cw_economy.py:51 → (0,4))、hp=OPENING_HP_BASE(=82,cw_opening_hp.py:22)、streak=0、back_layout=6、deploy_cap=3(注释声明 = level + 财富宝钻 0)、overflow_warning=False、overflow_card=''、active_env=''、active_strategies=[]、enemy_affixes=[]、shop_refresh_cost=REFRESH_COST_BASE(=2,cw_economy.py:109)、prev_node_spent=False、encounter_refresh_used=0、supply_refresh_used=0、strategy_refresh_left={}。

- 写通道:`_seed` 统一走 `write_logic_rand(target, value, produced_by='GsOpeningSeed', evidence='seed:opening', sig=_SEED_SIG)`(L1743;_SEED_SIG = family='logic_action'/actor='GsOpeningSeed'/screen=''/mode='compute',L1713)——与 design §2.3 写法逐参一致,常量单一源(_SEED_ACTOR/_SEED_EVIDENCE/_SEED_SIG,L1709-1714)。
- B/C 类零种:实读种子函数体,无任何 B/C 字段;测试 `_BC_UNSEEDED` 21 字段抽检全 None 断言(含 node 族/plane_bosses/selected_difficulty/tracked_account_observed 等)。
- board 不直写:种子函数无 board 写调用(禁双写合规);board 派生行实态见发现 F1(与 design 文本有出入,实现自洽)。

### 1.3 四个存量测试文件逐 hunk 判定(重点攻击面)

逐 hunk 均可追溯到 design 已申报的行为变化,无「改期望值让测试变绿」、无顺手改无关断言:

1. **test_cw_budget_disclosure.py**(3 处):构造金 53→120 与期望式 `64-cap→120-cap`。正当性已实码证实:`reserve_cap`(cw_economy.py:1407)= 息线 + 排程升级费,`upgrade_plan_fee`(L1388)经 `level_of(gs)` 读取**被种子化的 level** → 息帽抬高,原 53 不再保证「溢余>0」的非退化判据;期望公式(gold − cap)未动,判据意图保留。①臂断言值 0 未变,仅注释如实改写成因(种子金 3 低于息帽)。
2. **test_cw_equip_domain_container.py**(2 处):M7 两个 fail 门锁从「session 容器留 None」改「直构容器承载」。正当性 = design §2.6-7 申报的「A 类域 None 守卫分支生产死化(留 sim/测试直构域)」——种子后 session 容器两域恒有值,None 前置仅直构可达;门本体与断言(`fail_reason` 指向)原样保留,`_build_equip_wear_plan(session: Any)` 经本体直通支接 GameState,调用合法。
3. **test_cw_game_state.py**(1 处):恢复局空默认拒写测试。`_MIRROR_FIELDS` 四字段全覆盖无丢失——plane_bosses(非种子)保持 None 断言保留,三 A 类镜像字段断言改为「保持种子态(logic_rand/seed:opening)、不落中继痕迹」,空值闸拒写语义照旧被锁;`_MIRROR_FIELDS` 另两处消费(L1179 直构域 / L1285 后到真值)未动、语义不受影响。
4. **test_cw_takeover_intel_pipeline.py**(1 处):失败零写断言 `enemy_affixes is None` → `== [] ∧ source='logic_rand'`。正当性 = 种子空表语义申报(design §2.2 enemy_affixes 行),「失败不留半真值」的测试意图原样保住。

### 1.4 挂点正确性

实读 `game_state_of`(cw_game_state.py:1663-1702)四支:**直通支**(isinstance GameState,L1680)不种✓;**None 一次性支**(L1682-1683 直接 return 新容器)不种✓;**属性挂载支**(不可弱引用桩,L1688-1695)冷建即种✓;**弱引用表支**(L1697-1701)冷建即种✓——后两支即「两个缓存单例建支」,与 design §2.3 挂点(死写)一致。接管局同构:接管容器走同两支,冷建测试锁已覆盖。

置闩点(cw_reconcile.py:428-445):`prep_anchored = True`(L443)位于 bench 侧 `tracked_account_observed=True` 写回(L433-438)同 try 块内、其后——两闩同一成功条件,写失败走 except 同不置✓。测试双臂覆盖:bench 写回成功置位 + deployed-only(bench=None)不置位(新增测试 4/5)。`game_state_of` 已在 cw_reconcile.py:19 导入。

`prep_anchored` 载体:非 Field 簿记 `prep_anchored: bool = False`(L2140),居 frame_class_* 同区,带定义注释(生产唯一置位写点 = reconcile 同点;测试/直构域直赋专口;粘性无复位)——与 design §2.4 一致。读口 `prep_anchored_of`(L1780)经 game_state_of 解析、缺省 False、直构直通,一次性支容器恒 False✓。

### 1.5 carry 收窄语义

守卫实读(cw_game_state.py:2652):`if target.value is None or target.source == 'logic_rand': return`。`FieldSource` 五分类字面量(L270)= observation/logic/logic_rand/carried/prior;`write_logic_rand` 落帧 source 恒 'logic_rand'(L2746)——守卫精确命中随机态来源。非 rand 来源逐臂结构性不变:observation/logic/carried/prior 均落入原沿用路径(value 换帧 + source='carried' + evidence=f'carried:{frame}'),存量回归臂有测试锁(logic 臂沿用行为断言,新增测试 7)。

call-site 盘点:`.carry(` 生产调用 11 处 = cw_observation.py 8 腿(gold/level/hp/enemy_difficulty/deploy_cap/streak/board/shop_refresh_cost,design §2.5 所列 8 条)+ cw_screen_prep.py 3 腿(bench/front_row/back_row,L301/327/330)。方法层单点收口,11 腿统一受辖;sim/ 零 carry 调用。design 影响面矩阵漏计后 3 腿,见发现 F2。`any_logic_rand`/`logic_rand_fields` 生产消费点为零(排除 sim),种子后冷建全 rand 态无隐藏判定面影响。

### 1.6 census(全量 logic_action 写端核对;worker 移交编排方,本节即核对清单)

grep `write_logic(|write_logic_rand(`(src/sr_od/application/currency_war 排除 sim/)全量分类,对照 design §2.5 census 表:

- **表内全部对上,零缺口**:获得链写选择点 5 处(cw_gain_chain.py:313/373/449/499/552,与 design「invest-landing-chain 后增至五处」吻合)+ L631 值不变翻面(欢愉契约临时标 rand,同属收口辖域)= 3.2 范围;简报三写点(briefing.py:56/59/62)= 不收口有据;位面详情写门(plane_intel.py:61/65)= 不收口有据;`cw_loop._absorb_selected_difficulty`(cw_loop.py:918)= 不收口(C 类种子不涉);always-rand 天然合规 9 直写点(cw_gain_effects.py:138、collect_ore.py:108、pick_equip.py:65、pick_planner.py:98/189、pick_supply.py:80/105、tool_use.py:246/317)——design 点名的六模块全数命中,无清单外 rand 直写点。种子本体(cw_game_state.py:1743)= 本批自身。
- **清单外写端(有;均为 logic 直写、非通道选择点,处置方向建议如下,禁自行拍板,报设计方裁定)**:
  1. `cw_effect_inventory.py` EffectLedgerBridge 写端群(约 12 处:gold:842/1180/1203、hp:996、bench:1048/1273/1243-capacity_project、equips:1064/1091、front_row:815、back_row:819、row:1263)——目标域 = A 类种子域。方向建议:登记备查;其触发窗 = 效果账本事件(在局节点、现役均在锚定后),**「未锚定期无效果事件」前提建议在 3.2 收口清单评审时显式确认一句**(若存在前置授卡路径则需评估纳入通道规则)。
  2. `cw_exec_state.py:66/86`(PrepActionExecutor → gold/equips)——触发窗 = 备战动作执行(备战入口 heavy 锚定之后)。方向建议:登记备查。
  3. `cw_action_report/` 动作上报族中目标为 A 类域的 write_logic 若干(buy_card/sell_*/swap_deploy/deploy_move/level_up/wear_equip/open_bookcard/open_tome/refresh_shop/pick_equip:77/pick_planner:107-213/tool_use:159-498)——纯 logic 直写无通道选择,现役动作窗在锚定后。方向建议:登记备查。
  4. `cw_strategy_manager.py:128`(ensure_match_container → selected_difficulty,C 类二写端,与 census 已列 `_absorb_selected_difficulty` 同域同性质)。方向建议:并入 census selected_difficulty 行同处置(不收口)。
  5. `cw_loop.py:987`(接管失效 → tracked_account_observed=False)——B 类安全网域,种子/通道规则双不涉,非裁决对象;登记备查。
  6. 其余(cw_screen_report 观察摄入 payload/B/C 域、cw_screen chosen_* 域、encounter_refreshed_in_visit、round_fresh_buys、receipts/match_final/node_ord、cw_op_equip_all)不涉 A 类种子域通道裁决,无需入表。

### 1.7 行为抽测(自跑)

- `uv run pytest sr-od-test/test/sr_od/application/currency_war/test_cw_game_state_opening_seed.py -v`:**7 passed**(冷建全 A 字段值/来源/journal 过滤键、None 支与直构不种、观察覆盖落 logic_rand_outcome 零三分流、reconcile 置闩双臂、直赋专口、carry 收窄双臂)。
- `uv run pytest sr-od-test/test/sr_od/application/currency_war -m "not slow"`:**909 passed**(worker 自述 908,差额 1 条来自其后无关批新增,非本批波动)。
- journal 行断言核:种子行过滤键 actor='GsOpeningSeed'、mode='compute'、行字段集 = 种子全集(board 不产行,见 F1)。

### 1.8 注释与规范

- 新增注释全中文、写语义与 why(种子语义/闩语义/收窄理由/写序契约);无 changes/ 引用(引用 = 正本 fields.md 章节与符号名);无 w 轮/rN/批N 类会话局部标识符。
- A 类字段定义注释「开局种子底座」一句逐字段到位(23 字段 + board 禁双写注,实读全数命中)。
- 类型注解齐全(seed_opening_state/prep_anchored_of/_SEED_* 常量/prep_anchored 字段/测试函数);禁相对导入合规(两仓改动均绝对导入;主仓函数内懒 import 带防环理由)。
- `uv run ruff check` 两主仓文件:All checks passed。

## 2. 发现清单

**阻断:无。**

**应修(2;均 = 设计文本与实码口径出入,不改代码,报设计方裁定 + 落末阶段正本更新)**:

- **F1 board 种子派生行口径**:design §2.2 board 行(「值 = 空派生」)与 §2.3/§2.6-4(journal ≈23 种子行 + **1 行 board 派生行**)预期派生随动落行;实码 `_resync_board_delta`(cw_game_state.py:2582-2584)在 board 无观察基座时跳过(派生单一源「观察基座 + 行变更增量」的既有契约)→ 种子面 board 保持 **None**、零派生行。代码注释与测试均按实态自洽锁定,消费面与改前冷建态一致、无行为回归。处置方向:正本更新时按实码修正(journal 增量 = 23 种子行 + 0 board 行;board 保持 None 至首观察),design 措辞相应回修。
- **F2 carry 影响面矩阵漏计 3 腿**:design §2.5 记「carry 腿共 8 条」,实码 carry 调用 11 处(漏 cw_screen_prep.py:301/327/330 的 bench/front_row/back_row 失读腿)。种子落地后此 3 腿同落收窄(方法层统一守卫,行为方向与设计意图一致、无错收);处置方向:正本更新(fields.md §2.2 失读处置①)按实码补记 11 腿清单。

**建议(2)**:

- **S1** census 清单外写端清单(§1.6 第 1-5 项)交设计方裁定登记;其中 EffectLedgerBridge 的「未锚定期前置可达性」建议在 3.2 收口清单评审时显式确认。
- **S2** 新增测试 docstring 引 fields.md「开局种子底座」语义域——该小节随末阶段才落,迭代内引用合法;末阶段落地后自然闭环,勿遗漏。

## 3. 最终判定

**验收通过。** 代码与测试交付完整达成 landing 3.1 完成判据(种子 23 字段值/通道/写序、B/C 零种、闩同点置位与双臂、carry 收窄与非 rand 存量不变、观察覆盖台账语义、挂点三支语义、注释与工程门),新增 7 锁 + 全量 909 passed + ruff 通过。F1/F2 两条应修为设计文本 reconciliation,由设计方裁定后落末阶段正本更新,不阻断 3.2 开工;census 核对结论(§1.6,含清单外写端清单)随本 review 移交编排方。
