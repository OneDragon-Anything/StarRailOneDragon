# 板面格子结构(部署空间机制)

> **性质**:对局内上场名额与前后排格子数的机制事实。来源=**用户口述(最高权威)**,2026-08-26 run 26 崩坏局后澄清。
> 版本基准:V4.4(4.5 更新无相关修复条目)。

## 规则(口述原文,2026-08-26)

> 「lv8 不是后台有 8 个,这个是错误的,lv8 是说总共可以上 8 个角色。正常情况下,就是固定的前台 4 个,后台 6 个,如果有钻石,或者召唤物,后台才会变多。」

拆解为三条机制事实:

1. **等级决定的是「总共可上场人数」(deploy_cap),不决定格子数**——lv8 = cap 8 人(前台+后台合计最多 8 人上场)。
2. **格子数正常恒定:前台 4 格 + 后台 6 格**;等级升高(人口扩展)是「往同样的格子里填更多人直到 cap」,不是「长出新格子」。
3. **后台格数只在两种情况下变多:钻石 / 召唤物(系统单位)**——即 8 格后台(393-1529 带)的触发条件是「钻石或召唤物存在」,与 level 无关。

## 量化公式(口述追加,2026-08-26 同日)

> 「你可以用 6 + cap - level 来判断后台有多少。」

**后台格数 = 6 + (cap − level)**:

- 无钻石无召唤物:cap=level → 后台 6 格(基线,与第 2 条一致);
- 钻石 +1:cap=level+1 → 后台 7 格;钻石/召唤物叠加 +2:cap=level+2 → 后台 8 格;
- 即 **cap 与 level 的差值(现成 OCR 读数:画面 X/Y 里的 Y 与等级)直接编码了后台扩展量**——不需要单独识别钻石图标,两读数相减即格数。

## 布局几何:整排居中重排(交互实锤,2026-08-26 追加)

格数变化时后排**整排居中重排**(排中心恒 960),三档不共享列位:

| 格数 | 槽中心序列 | 带 |
|---|---|---|
| 6 | 604 / 746 / 888 / 1030 / 1172 / 1314 | 533-1387 |
| 7 | **534 / 676 / 818 / 960 / 1102 / 1244 / 1386** | 463-1457 |
| 8 | 464 / 606 / 748 / 889 / 1031 / 1174 / 1316 / 1458 | 393-1529 |

证据 `[交互实锤]`:点真槽中心开角色详情(534→万敌/1386→佩佩),点列间空隙
(606/746/1458 等)无响应;空槽台座模板扫峰与占用位一一对应;万敌 534→676→
960→1244 覆盖拖测逐位读对。⚠️ 曾误记「7 格=6 格右扩一格(604..1458)」,
错位 +71px,已勘误(ADR-0390)——格心错位时窗口只盖半片台座,读数/拖拽/
点击全族伪象。

「钻石局检测」问题因此消解:布局选档信号 = `read_deploy_cap`(paddle 直读权威)与 `session.level` 之差,单一公式覆盖全部档位。

## 对旧模型的勘误(推翻 ADR-0281 的 level 驱动模型)

`cw_back_layout.py` 现行模型「level≥7 → 后排 8 格」**归因错误**:其唯一实证(「lv7-8 → 8 格,狸猫局交互实拍」)的局**本身带召唤物(狸猫)**——8 格来自召唤物,不是来自 lv7+。按本篇口述,无召唤物/钻石的 lv7/8 局后排仍是 6 格。

**影响面**(run 26 = lv8 无召唤物局实证崩坏):
- 按错误的 8 格坐标拖拽 → 拖向不存在的 7/8 号格被游戏拒绝(白拖/弹回);
- `read_deployed_chars` 按 8 槽读人 → 把背景误读为空槽或错位身份;
- 板满门/围栏的空位计算(len(back)=8)全部失真 → 散牌填位与 cap 判定错乱。

**正确驱动因子判定**(供修法):后排格数 = 6(基线) | 8(当且仅当局内有钻石或召唤物/系统单位)。level 只进 cap,不进布局选档。

## 证据等级

- 本篇三条规则:`[口述]` 最高权威(用户 2026-08-26,亲见 run 26 崩坏后澄清)。
- 量化公式 6+(cap−level):`[口述]` 最高权威(同日追加)。
- 8 格与召唤物关联:口述 + 狸猫局实拍(交互定名)双重印证。
- 公式的交叉验证通道(推荐运行时对账用):CV 空槽检测(后排 y 带槽位签名/空槽模板匹配)与公式值不一致时留 obs_conflict 证据。

## 消费方

- `cw_back_layout.py`(布局选档驱动因子修正——**已落地,ADR-0385 双通道**,
  2026-08-26:公式通道 `6+(cap−level)` + CV 通道槽位 std 签名实测
  (`cv_back_slots`),对账一致用公式值/不一致采 CV 实测值+留证两值
  /CV 不可判退公式;7 格档未建档保守 8 格超集 + 留证采集);
- `deploy_bench.py` 布局选档 / `cw_observation.read_deployed_chars` 槽位读取
  (均已改经 `select_back_layout` 双通道入口,ADR-0385);
- 判读一局时的板面口径(几人上场/几格空位的真值基准)。
