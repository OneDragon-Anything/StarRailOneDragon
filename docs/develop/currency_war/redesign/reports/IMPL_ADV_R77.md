# IMPL_ADV_R77 —— 货币战争决策实现设计件·对抗式设计审查(第 77 轮)

> 审查对象:IMPL_DESIGN.md + IMPL_FIX_LEMMAS.md 的 **R76 落位后态**(重点=R76 七项修复批自身 + 任务书五特别新面:①StrategySession 锁存载体交错生命周期;②N3 实例账本键语义;③sim 置位 A/B 可比性残余;④K=3 扫描带;⑤R76-6 注册表完备性核对可执行性)+ R68-R76 九修复批残留交互。
> 对照单一源:NEW_MATH_FRAMEWORK / DESIGN_MANDATE_LAYER / math_proofs(按需抽查);**全部数值锚点与代码生命周期断言本轮亲自直调代码/注册表复核**(逐条文件:行号见各发现),未采信被审方自述值。
> 冻结边界前置申报:本轮全部发现落主链(观察键判读本体/观测载体/治理通道/时序判据);K=3 是观察键判读阈值参数(判据参数化本体=主链,按 R70-R76 先例不入冻结族);实例账本/遥测披露=既有协议载体面非度量装置。文末冻结残余节=零新增零重复。

---

## 一、新问题清单(严重度降序)

### N1【中高】R76-6 治理通道检视项关键词被注册表内正例证伪;「完备性核对」按文本执行零检出力

**论断**:R76-6(c) 登记的采集检视项关键词集「未注册名效果原文含**『利息上限/利息档』字样** ⇒ 触发注册表补全批」被已知正例直接证伪——注册表内升帽主力「利息上调」的效果原文**不含这两个词**(原文=「你的最大利息提升至10金币」);同条登记的「完备性核对=落码批前枚举 override>0 条目对拍已知 T0 集」是同义反复,对它自称要防的风险(游戏侧存在注册表外的第 4 升帽策略)构造性零检出力。R76-6 的风险登记 (b) 因此**有名无实**:风险真、检视通道检不出。

**证据(注册表直调,本轮亲跑)**:
- `kernel/cw_investments.py`:`INVESTMENT_STRATEGIES` 全量 335 名(亲跑 len=335 ✓);带 `interest_cap_override>0` 条目恰两条=开源节流(9)/利息上调(10)(亲跑枚举 ✓,与 R76-6(a)「现状恰两条」一致)。
- **效果原文逐字亲读**:`INVESTMENT_STRATEGIES['利息上调'].effect` = 「获得25金币。**你的最大利息提升至10金币。**」——含「提升至」,**不含「利息上限」、不含「利息档」、不含「上限」**;对照开源节流 effect=「利息上限提升至9」(含「利息上限」)。即:同一效果(升帽)在注册表内已知正例中存在**两种措辞族**,R76-6 检视项关键词只覆盖其一。
- 全注册表 effect 含「上限」共 13 名,逐条亲读全部为生命值/能量/层数类「上限」,利息相关仅开源节流一条——证明关键词扫描可执行且需正例派生词表。

**推理链**:
1. 未来第 4 升帽策略若按「利息上调」措辞族(「最大利息提升至N」)书写效果原文(同一游戏内同一效果两种措辞已是实锤先例),它出现在三选一 → OCR 读名 → 按名查表 miss → 不置位,且采集判读按 R76-6 检视项扫「利息上限/利息档」两词 → **不触发补全批** = 漏检;
2. 「完备性核对=枚举 override>0 条目对拍已知 T0 集」:注册表的 override>0 集合**本来就定义自**已知 T0 集,枚举对拍恒过,无论游戏侧缺什么——该「核对」对 R76-6(b) 登记的风险(知识缺口)检出力恒零,属把「风险登记」误当成「风险治理」;
3. 真正有检出力且**当下即可执行**的核对被漏登:对全量 335 名的 `effect` 原文跑正则扫描(词表从已知正例派生,如「利息上限|最大利息|利息.{0,6}提升至」),本轮亲跑该扫描可行、13 命中可 10 分钟内人工分诊——覆盖「注册表已知名中效果建模遗漏」半边;注册表外长尾半边仍只能靠采集检视项,但检视项词表必须按上扩。

**修复方向**:①检视项词表改由注册表已知正例派生(至少补「最大利息」「提升至N金币」族;或直接改判据=「效果原文命中利息数值变动正则」);②「完备性核对」改述为两段:注册表内=effect 原文正则全扫(可执行、有检出力);注册表外=采集检视项(词表同①);删「对拍已知 T0 集」的伪核对表述。严重度定中高而非高:该通道是观察级治理通道,不阻塞落码行为判据,但**其存在价值当前为负**(给了一种已治理的错觉)。

### N2【中】锁存对判据的帧可见性时序未钉死:sim 侧已隐含钉死「置位同帧生效」,实机侧两序行为不同,且未被 R76-2(b) 的分叉申报覆盖

**论断**:R72-1 原钉死的观测时序条款(「升档器求值后、EV 判据求值前,消费同一快照」)被 R73-1 标作废(载体缺位)后,**历经 R73-1/R76-1 两次载体迁移始终未重钉**「overlay handler 置位」与「备战判据求值」在同一备战期内的先后序;而 sim 侧的等价时序已被代码隐含钉死=置位同帧对判据可见。两侧时序若错开一帧,升帽可达局在「首见帧~次帧」窗口内 sim 按判据已组装、实机按现值未组装,属**结构性分叉**,不在 R76-2(b)「offered-vs-injected 分布分叉」申报辖域内。

**证据(代码直调)**:
- sim 侧时序已钉死:`sim/engine_p1.py` L758-760 注释明文「本轮策略选卡注入(overlay 在备战期出现 → **收入结算后、决策前**…)」;L762-766 注入点在收入结算之后、后续决策消费之前 ⇒ sim 锁存(按 R76-2 规格=注入处过谓词置位)**同帧**对判据可见。
- 实机侧时序未钉死:设计件 R73-1 标/R76-1 全文只有「判据经 derived 读锁存」「derived 读 session」,**无任何一句**钉 handler 置位与判据求值的先后;实机投资 overlay 的弹出时机(handler L110-113 注释「备战 → 金币过场动画 → overlay 自动弹出」)与 PrepDirector 判据求值在 op 链中的相对次序,设计件未声明,handler 代码现状也不含锁存逻辑(落码前无从读出)。
- R76-2(b) 申报的分叉=「实机 offered 集合置位(含未购)⇄ sim 注入即置位」——申报维度是**置位事件集合的构成**,不是**置位生效帧与判据帧的相对时序**;后者是独立维度,R76 批未申报。

**推理链**:
1. 若实机判据求值先于 overlay handler(PrepDirector 先决策、overlay 后处理),锁存对**本帧**判据不可见、次帧才生效 ⇒ 同一日程场景下 sim 在帧 t 已按 cap_sup 组装、实机在帧 t 仍按 cap_resolved;
2. 这恰落在锁存设计要修的「曾可达-现不在店」窗口内 ⇒ `m6_caplatch_window`(§5.2 R72-3 键)的 sim/实机计数差含**时序结构分量**,R76-2 标只申报了分布不可比,判读方按该申报归因会把时序差误读入「offered 构成差」;
3. A/B 命题判读:cap_sup 组装早一帧=付费刷新/买入放行面早一帧收窄,行为差非纯观测差。

**修复方向**:一句时序钉死即可消解——建议「实机锁存置位点(overlay handler)在备战判据求值**之前**完成;若 op 链现状无法保证,则显式申报『实机锁存次帧生效』并与 sim 注入点对齐(sim 侧改为注入后次帧生效)」。二选一,但必须选一个并写入 R76-1/R76-2 落位面。

### N3【中】R76-3 实例账本三处键语义缺口:键含费用维与「重现=同名」口径互不咬合;同键多实例未定义;「费用 OCR 可观且稳定」被本仓既有代码反证

**论断**:R76-3 补齐的三要素仍有三处实现级缺口:(a) 键=`normalize(名)+费用`,但重现匹配=「normalize 后**同名**即重现」(费用维不参与)——费用维进了键却不进任何判读语义,且造成「同名异费重现关闭 (名,费) 实例 streak」的无申报口径;(b) 店面同帧出现两张同名同费牌(合法形态)时实例身份/数量语义未定义;(c) 费用维的承重前提「费用 OCR 可观且稳定」与本仓既有代码对该维度的可靠性定性矛盾。

**证据(代码直调)**:
- (a) R76-3 标原文:「①键=按件键控(normalize 后归一名+费用)」「③重现匹配口径=normalize 后同名即重现」——两条款并列,费用维只进键不进匹配;
- (b) sim 店面抽取 `engine_p1.py` L803 `st.shop = cards_pool.draw_shop(...)` 按费用档概率独立抽卡,同名同费同帧多张无去重;实机店面 5 槽同理(注册表 `cw_shop_odds` 按档抽);R76-3 全文无 multiplicity 条款;
- (c) `obs/cw_shop_obs.py` L54:「`invalid_cost`:费用不在 1-5(**OCR 误读/缺读**),两查都无法做」——本仓既有代码把店面费用读数明确定性为**可能 OCR 误读/缺读**并设了专门的 invalid 分支;R76-3「费用 OCR 可观且稳定」无出处、与同一对象上的既有定性冲突。

**推理链**:
1. (a) 的两读后果:若判读实现按「(名,费) 在店」判离店、按「同名在店」判重现,则一张同名异费牌的重现会关闭原 (名,费) 实例的 streak——宽口径保守向(R76-3(b) 自辩)尚可接受,但**未申报**;若实现按「(名,费) 在店」判重现,则与登记口径相反,同键不同值——两实现两结果;
2. (b):两张同牌卖掉一张,按计数「离店一半」还是「未离店」?streak 开/闭事件账本在重复键上无定义 ⇒ 判读不可落码(与 R76-3 自认罪的「结构不匹配」同型,只是降了一级);
3. (c):费用缺读帧(实机上确会发生,既有 invalid_cost 通道即证)键退化为 (名, None) 还是跳帧?未定义;若跳帧,「连续 ≥K 未闭合」的连续性在缺读帧断链=系统低估顺延时长。

**修复方向**:①费用维二选一——要么进匹配口径(重现=同名同费)并申报窄口径方向分析,要么从键中删除(键=归一名,与重现口径对齐;费用维降为遥测披露字段);②补 multiplicity 条款(建议:同键计数制,在店计数 0 才算离店);③补费用缺读帧的连续性条款(建议:缺读帧不计入 K 计数、按顺延保守向=冻结 streak 而非断链)。

### N4【中低】锁存载体改判 StrategySession 与在码「归属他批禁触」注记未对账:落码批会撞上一条设计件未勘误的在码约束

**论断**:`telemetry/state.py` L58-59 存有一条在码注记——「为什么是模块槽而不是 session 字段:**StrategySession(cw_strategy.py,归属他批禁触)无法加正式字段**」——即历史上曾有一条对 StrategySession 加字段的批归属禁触约束,且当年正因它放弃了 session 字段方案;R76-1 的落位恰是「给 StrategySession 加正式字段」,设计件零冲突自查对该在码注记零提及、零勘误,文件面预登记也未包含该注记的清除/改写。

**证据(代码直调)**:
- `telemetry/state.py` L56-61 亲读:注记原文如上;所指类路径「cw_strategy.py」已陈旧(类现行=`kernel/cw_strategy_session.py`,L27-28 亲读);
- 反向证据(冻结已可能松弛):`cw_strategy_session.py` L137-140 亲读:「动态 setattr 升正式字段(asdict/repr 完整;getattr 兜底随之可删)」——表明存在字段晋升机制,禁触可能已历史失效;但**设计件不能替实现者赌它失效**。

**推理链**:实现者按 R76-1 落码、打开 cw_strategy_session.py 加字段,随后任何读到 telemetry/state.py L58 的人(或 code review)都会面对两条矛盾的在码/设计约束;w306 时代的禁触若仍被某批主张,落码批的文件面预登记(只登记了 kernel/cw_strategy_session.py)不覆盖争议。属「设计件与在码约束对账缺口」,修复成本一句话,不修则把裁决冲突下沉到落码批。

**修复方向**:R76-1 落位面补一句:「telemetry/state.py L58 『归属他批禁触』注记系 w306 时代历史约束,随本批载体裁决作废,注记随落码批改写(指向 kernel/cw_strategy_session.py 现行路径+作废声明)」。

### N5【低】session 新字段的遥测披露与序列化语义未申报:实例账本的自然表示在既有 asdict→JSON 通道不可序列化;离线复盘通道断

**论断**:R76-1/R76-3 给 StrategySession 新增 `cap_sup_latched`/候选名/实例账本三类字段,但未申报任何遥测披露键与序列化形态;实例账本的自然键=(名,费用) 二元组,作 dict 键经既有 asdict→JSON 披露通道(tuple 键被强转/丢结构)不可无损序列化;离线复盘(match review 按遥测档案判读)在无披露键时无法重审 `m6_caplatch_window`/实例账本判读,只能信运行时计数。

**证据(代码直调)**:
- 遥测对 session 是**按字段点选披露**非整包:`telemetry/recorder.py` L233 `trace.xp_expect_ledger = _to_jsonable(asdict(_led))`(单账本点选);`telemetry/schema.py` L306 自述曾存在「三个 session 披露键有写点、无遥测落盘」缺口——session 字段进遥测必须逐键接线是既有事实;
- asdict→JSON 通道先例:`kernel/cw_intention.py` L1296 `_to_jsonable(asdict(obj))`;tuple 键 dict 经 `json.dumps` 键被字符串化、结构信息丢失(语言级事实);
- R74-2(c)「遥测新字段跨臂同记录」是载体层申报,不含字段→遥测的接线与序列化形态;R75-3 的「置位时落候选名快照」只覆盖误读审计一隅。

**推理链**:观察键的两个消费通道——超标判读(运行时,可读 session)与跨局判读/复盘(离线,只读遥测档案)——后者在当前申报下无输入;`m6_caplatch_window` 与 shortfall 键的超标判读声明「计数>0 的批触发复评」,复评方(复盘协议按档案工作)拿不到账本明细 ⇒ 复评不可执行。聚合投影与实例账本的两层一致性同理:投影的推导源(自账本派生 or 独立计数)未声明,独立计数则漂移无界。

**修复方向**:补一条申报:①实例账本表示=JSON 安全形态(键=「名|费」复合字符串或 list[entry]);②披露键两张(m6 帧级事件+账本快照随局终落 run summary);③聚合投影显式声明=自实例账本派生(单一源,防两层漂移)。

### N6【低】K=3 provisional 槽缺「标定前缺省行为」声明:与既有【拟】槽位纪律(None ⇒ 不触发)的两种缺省语义未择一

**论断**:既有 provisional 槽纪律(DESIGN_MANDATE_LAYER M4 行:`N_crisis 归宿=audit/provisional.py【拟】槽位,标定前 None ⇒ 不触发(禁硬编码点值)`)是**缺省关**;R76-3 的 K=3【拟】扫描带 [2,4] 未声明标定前 K 取 3(判读激活)还是 None(判读不触发、键降为纯观察信号)。两缺省行为不同且各有后果:取 3=未标定阈值已进判读(违槽位纪律精神);取 None=「永久顺延」判读通道接通前回炉触发器恒哑(需申报挂账)。

**证据**:R76-3 标/对照表 R76-3 行全文(亲读,引文见 §544 行)无缺省行为句;MANDATE M4 行先例引文如上(亲读);R76-3(b) 自引先例为「N_crisis=2/R23-5【拟】槽位」却未随引其「None ⇒ 不触发」半边。

**推理链**:这不是悬置参数(R76-3 已按公理落槽),是**槽位缺省语义**未声明——实现者二选一都会偏离某一条既有纪律,且两读的观察结论可判读性不同(激活 K=3 则回炉触发器可fire,None 则键纯观察)。

**修复方向**:一句钉死——建议对齐先例:「标定前 K=None ⇒ 顺延判读不触发、键保持观察信号;扫描带 [2,4] 挂实机数据期(通道=N1 修复后的实机-only 估计量,样本量口径随 N6-scan 补)」;若有意 K=3 即激活,须显式申报偏离先例的理由。

---

## 二、冻结残余

**零新增零重复声明**:本轮 N1-N6 全部落主链(观察键判读本体/治理通道/时序判据/观测载体对账/序列化披露载体),不涉冻结族(D_ε 门/有效性判据/标定通道/哨兵/激活率门/cap 消费参数化度量面/相关验证装置);K=3 属判据参数化本体=主链(前置申报边界),实例账本/遥测披露键=既有协议载体非度量装置(按 R74/R75 先例)。冻结残余清单 9 项维持不变。

## 三、攻过零新症角度(列证备查)

1. **R76-1 全部生命周期断言亲读复核全过**:obs/cw_observation.py L1691 每备战帧 `state = GameState()` ✓;L1925-1928 session→state 回填(含注释自述)✓;kernel/cw_state.py L150「一回合决策时的局面快照」✓;cw_strategy_session.py L28-29「每局新建,局终销毁」+L121 `active_strategies` 注释 ✓;handler L182-184 append 先例、L137-140 `match is None` 局外分支 ✓——R76-1 对 R75-4 的改判成立,载体生命周期论证无懈可击(N4 是其对账缺口,非论证错误)。
2. **R76-2 注入点实证复核全过**:cw_sim_invest.py L34-59 `SimInvestProfile.picks` 单名日程、L14-16 plaza 频次表+注册表外丢弃披露 ✓;engine_p1.py L762-766 单名 append+state 同步 ✓;「三选一/候选集」sim 零建模复核(grep)✓。A/B 残余除 N2 时序维外,offered-vs-injected、频次表 vs offering 分布、OCR 过滤集三维均已申报穷尽,零新增。
3. **R76-4 勘误成立**:R76 报告 N4「sim 零店面建模」确系误报——engine_p1.py L803 `draw_shop` 亲读 ✓;真因( cw4 义务通道全仓零实现)复核:`grep funding_support` 全包**零命中** ✓,「funding_support 系设计级概念」申报为真。
4. **R76-5 复核全过**:str_utils.py L147/L188/L211 模糊族实有 ✓;`get_strategy`(cw_investments.py L758-761)=normalize 后 dict.get 精确查询 ✓;钉死精确查询与 miss 不置位规格自洽,零新症。
5. **derived 读 session 的依赖方向**:decision 层消费 session 有现行先例(handler L138 `match.strategy.decide_invest(..., match.session, ...)`),cw4 derived 读 session 不新增依赖方向,零新症(N2 攻的是时序未钉死,非方向)。
6. **R76-7**:sim 纯逻辑引擎无渲染面复核(目录结构+早前轮 grep 一致)✓;≥200 张+Wilson 口径对齐 574 样本先例(handler L117-118)✓,零新症。
7. **R76-3 费用维可观性半边**:实机确有店面费用读数消费链(shop.py L729/L841-869 记账用 `card.cost`、cw_shop_obs `check_shop_pool` 两查入口)——费用**存在**读数通道成立;缺口只在可靠性定性(N3(c))与缺读帧语义,不翻「可观」本身。

## 四、结论(含适用域限定)

R76 批七项对 R76 审查 N1-N7 的修复**主体成立**:载体改判的生命周期论证经亲读全数核实,sim 注入点改写、精确查询钉死、sim 侧豁免申报均落位合格。但五特别新面攻出六项新症:N1(R76-6 治理通道被注册表内正例证伪+伪核对,中高)使「升帽策略⊆注册表」风险名为已治理实为零防护;N2(帧可见性时序未钉死,中)是 R72-1 时序条款死后两次迁移未重钉的欠账,sim 侧已隐含钉死而实机侧两序行为不同;N3(实例账本键语义三缺口,中)使顺延判读仍不可落码;N4-N6(在码禁触注记对账、序列化披露、K 缺省语义,低)为对账/申报缺口。建议:N1/N2 视为落码批前置,N3 为判读落码前置,N4-N6 随批一句话收口。

**适用域限定**:本报告基于 R76 落位后态的**设计文本**与当前代码注册表/实现亲读直调;cw4 包尚未落码,全部发现为设计级缺陷(实现者按文本落码即触发),非现网行为缺陷。N1 的注册表正则扫描与 N2 的时序条款均为一句级修复,不阻塞其余落码面;若后续批补齐 N1 词表+N2 时序钉死,两项随之消解。数值锚点(335 名/override>0 恰两条/利息上调 effect 原文/13 个「上限」命中)全部为本轮亲跑输出,非转引自被审方。
