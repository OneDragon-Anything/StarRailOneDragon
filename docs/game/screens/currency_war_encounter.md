---
screen_name: 货币战争-遭遇节点
appears_in: [currency_war]
last_updated: 2026-08-04
source_image: 待归档(基线 `.debug/sr_od_mcp/screenshot/screenshot_20260804_111031_308056.png`)
---

# 货币战争-遭遇节点(二选一遭遇选择)

## 何时出现 + 状态流转

对局内**位面推进中**随机出现的「遭遇节点」选择屏(非每轮必有;出现时机由肉鸽地图节点决定)。玩家从两张遭遇卡中**二选一**,确认后进入该遭遇的战斗(或效果),打完回备战。

- **入口**:备战 → (地图节点为遭遇时) → 本屏。
- **出口**:点一张卡选中 → 点「选择」→ 进入对应遭遇(战斗)→ 结算 → 回备战。
- **放弃**:「返回备战界面」(右上)可退回备战(放弃本次遭遇选择,具体是否扣次数待确认)。

## 识别特征(稳定锚点)

- **标题「遭遇节点」**(画面正上方,x≈900 y80)—— 画面独有,可作 id_mark。
- 「遭遇其一」「遭遇其四」(两张卡标题)—— 出现哪几个「其X」视当前遭遇池而定(不一定固定是一/四;检测用「遭遇其」前缀更稳)。
- 底部「选择」按钮 + 「剩余次数：N」。
- ⚠️ 勿用「难度」「奖励预览」等作特征(多屏共有 / 易变)。

## 可交互元素

| 元素 | 位置(中心,1080p) | 说明 |
|---|---|---|
| 遭遇卡·左(如遭遇其一) | (665, 500) 卡身 | 点**卡身**选中(非文字角)。左卡通常难度低 |
| 遭遇卡·右(如遭遇其四) | (1288, 550) 卡身 | 点卡身选中。右卡通常难度高、奖励更好 |
| 「选择」按钮 | (1082, 898) | **选中一张卡后才可点**(未选中时灰置) |
| 「返回备战界面」 | (1717, 67) | 右上,放弃本次选择退回备战 |

**两张卡内容(实测一例)**:
- 左「遭遇其一」:难度 1(1 个蓝 V,易)、奖励「金币 ×2」。
- 右「遭遇其四」:难度 4(4 个红 V,难)、奖励「随机 4 费角色 ×3」。

(具体卡牌内容随遭遇池变化;难度越高奖励越好,「难度越高敌人越强大,请谨慎选择」。)

## 交互模型(实测 2026-08-04,**踩坑固化**)

正确序列 = **① 点卡身(选中)→ ② 点「选择」(确认)**。

⚠️ **两步之间不要插任何点击**:旧实现插了一个「空白点击 (960,540)」想关详情,实际会**取消选中** → 「选择」时无选中 → 不推进 → 死循环(plane1 round6 后卡死 7 分钟,OCR 反复见同一屏,根因)。点卡身是「选中」不是「开详情」,无需关。

bug#1 缓解:关键 click 前 `mouse_move`(零移动 click 不被框架 before_screenshot 判成拖拽)。

## 识别快照

`analyze_screen`(基线图 `screenshot_20260804_111031_308056.png`):
- 匹配画面:**无**(本屏未进 screen_info,`screens=[]`)→ 纯 `round_by_ocr('遭遇其一')` 检测。
- 全量 OCR:`返回备战界面` / `遭遇节点` / `遭遇其一` / `遭遇其四` / `-难度-` / `M` / `奖励预览`×2 / `金币` / `随机4费角色` / `2` / `3` / `难度越高敌人越强大，请谨慎选择` / `剩余次数：1` / `选择`。
- 视觉大模型(智谱 GLM-4.5V):两张六边形卡左右并列;左卡难度 1 蓝 V + 金币×2;右卡难度 4 红 V + 随机4费角色×3;初始无卡选中;「选择」未选中时灰边。

## 备注 / 待查

- **screen_info 现状**:本屏**未建模**(`screens=[]`),坐标暂硬编码在 `battle_loop.py` handler。待按规范把卡身/选择/返回坐标进 screen_info(task#20 + 拆 op task#22)。
- **策略化选卡(待做)**:当前默认选左卡(难度低、稳)。应按 `target_comp` 评估两卡价值(右卡「随机4费角色×3」对缺核心卡的 comp 极值)—— 属节点决策(见 `docs/game/currency_war/strategy/08_node_decisions.md`)。
- 「遭遇其X」的 X 不固定(其一/其四实测,可能有其二其三)→ 检测用前缀「遭遇其」+「选择」+「遭遇节点」组合更稳。
- 「返回备战界面」放弃是否扣「剩余次数」待确认。
- fixture 待归档到 `sr-od-test/screens/货币战争-遭遇节点/`(需确认测试仓存在)。
