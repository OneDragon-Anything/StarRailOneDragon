# 实机效率会话交接物(2026-09-01,编排者→效率优化会话)

> 游戏跑道已让出(本会话不再碰实机)。以下为效率工作需要的全部现状与事实。

## 已落地的效率改动(server 已加载)
- battle_loop.py:分支2 wait 1.5→0.8;分支3 两路 1.5→1.0;分支6 1.5→1.0(纯等待参数,零决策改动)。

## 关键事实(实测,别再踩)
1. **OCR 不是瓶颈**:全图一次 0.14-0.67 秒(det 恒 ~0.05s+每框 8-10ms),已在 DirectML GPU(GPU/CPU 切换无收益)。证据=`OCR_BENCH.md`+`ocr_bench_result.json`(redesign 目录)。
2. **周期大头是 sleep**:生产 ~5 秒轮询周期里 80-90% 是 round_wait/time.sleep(常见 1.5-2s,战斗中 2s),OCR 只占 3-13%。**砍 round_wait 是主杠杆**(安全分支可 1.5-2→0.5-1,周期近减半);战斗轮询画面状态单一可砍狠,点击后转换期保持保守。
3. **稳定门解耦已降级**:原以为要重构 gate 体系省 29s/局——实测成本是睡眠量化非 OCR 算力,砍 sleep 即得大部,不必动 gate。

## ⚠️ 与遥测的冲突(已发生的事故)
效率优化(点空白加速+wait 0.8)把结算页1驻留压到 ~1-2s,而结算面板要 **2-4s 才渲染**——读数点早于面板出现,三项遥测 damage 捕获 0/15(读数器本身没坏:同帧离线复读全对,-5/-3/+2 与 hp 精确对账)。
**约束**:任何进一步压结算驻留的改动,必须保「面板已渲染帧」可达(读点对齐方案五条入口=`REAL_MACHINE_COLLECTION_1.md` 遥测验证批节⑧);否则遥测字段(结算三项/难度)全瞎。

## 文件地盘(防撞车)
- **你的**:battle_loop 各分支 round_wait/等待参数、录屏通道修复(连续两批不可用,gdigrab 报 I/O 错)。
- **我的(勿动)**:settle_collect_hooks.py(临时采集钩子族,采完删)、cw_settlement_obs.py 读数器、telemetry 四字段、结算分支的读点接线。
- 共享文件=battle_loop.py:改等待参数请避开结算读点接线行(分支2 暂存/1f/3b/分支3 直读)。

## 遗留问题清单(效率侧)
- 录屏通道修复(start 假成功/fixed 失败);
- 稳定门若真要解耦(锚定/稳定分离),先砍 sleep 看剩余量再定;
- 遥测侧我挂账的(与你无关):fill_ratio 过渡帧污染、P2r1 双行 hp=0 疑误判。

## ⚠️ 跑道交接时的现场状态(2026-09-01 收摊时)
1. **游戏停在位面简报屏**(开局流中间态残屏)——**起局前必须先跑 `ExitCurrencyWarMatch` 清回大厅**,简报屏残留直接起新局会死循环;
2. 哨兵/断流监控全停(rewatch 核岗 0),旧水位已清——你起局前按 runtime-ops 重新武装;
3. **tooltip 读点时序修复已落地但未实机验证**(battle_loop 结算页1 面板渲染延迟门:面板 visible 或 4s 超时才点空白;ruff 过,CW 快速集 3111 过,5 红全在并行批改动面)——重验判据在 REAL_MACHINE_COLLECTION_1.md 遥测验证批节⑧;你效率改完后顺手跑 1-2 局即可验;
4. 停止记录:最后一局开局 46 秒被叫停(`已停止[mcp:stop_run]`),非故障。