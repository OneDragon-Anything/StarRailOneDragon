# P26 · P1 硬节点备战支出的 EV 门(v2 重推 2026-09-01)

状态:**待证明(参数化)**。骨架用已证命题;卡点=Δp_prep 未标定 + L_node 分位口径未采(双挂账,采集可离线补)。
本版为 P26_VALIDATION 判决后的重推件:旧版数值面(分位 42/39、P15 已废回归 11.32−0.37·rung、门值 ≈5-10%)作废禁引。

## 命题

P1 硬节点(遭遇/boss,`_hard_node` 口径)备战帧,一笔备战支出 C(围栏内体系件买/carry 装备/弃息 D)的期望净值:

```
EV(C) = Δp_prep · G_loss + Δstreak·S_streak − C − I(息损)
```

触发判据形式:EV(C)>0 ⇔ Δp_prep > (C+I−Δstreak·S_streak)/G_loss,与 P23 停手窗门式同构(P23.1)。

### 量纲闭合声明(纯金流公理,P51)

**血量不入任何账与自变量**。L_node(条件败面伤害,血量单位)不直接进金账——本式无任何血→金换算因子,hp_to_gold×L 旧形态(P12 判死)违 P15v2/P51 纯金流公理,禁用。金账承载方式:

- **G_loss = λ_death 通道的销毁敞口**(P51 §F.3 底线口径,P48 统一囤钱判据同源形态):败面概率变化 Δp_prep 承载的是「败局 → 局终销毁」事件下的存量金 + 未来基础/息流敞口,即 G_loss = λ_death(板面战力桶×节点类型,剩余轮数近似视界)×(存量金 g + 未来流)。λ_death 生成式待标定(P51 采集 #3 挂账)。
- **L_node 的合法位置 = 触发面**(水位比较 hp−L_node<emergency_hp,ADR-0448 族),只判「备战帧是否被该硬节点威胁」,不作数值授权;消费形态=**区间敞口比较,禁边际引用**(P51 消费纪律)。

### L_node 单一源(触发面参数)

- 注册表 `streak_floor_loss_damage`(**均值口径**,w324 冻结语料 417 条,ADR-0424):encounter = 24.32 − 4.53·rung(斜率 SE 2.41 显著)/ boss = 26.71(斜率 CI 含 0 退常数;battle 复用 `vd_p1_loss_*` 单一源)。
- P15v2 实测带(冻结核 `tools/cw/proofs/p15/fit_results.json`,sha 848dc1aa,2026-09-04 按现行冻结语料回填):P1 battle 10.45 CI[9.59,11.24] / encounter 12.18 / boss 26.79;rung 梯度方向已立(rung0 11.52 vs rung1 6.94 CI 分离)不立系数。
- **分位口径(尾部)未标定**——挂采集账(见下),标定前禁写具体分位值。

## 已证骨架

- P23.1(死亡窗支出分配 EV 门式):同结构门式在死亡域已证;本命题是其在前窗硬节点的平移(单节点 m=1 退化),结构不需新数学。
- P15v2:P1 收益侧 L 的条件口径已重证成立(原回归作废禁引;禁无条件均值双计的教训直接沿用)。
- P20(**方向命题**):备战支出中「体系激活」腿的 Δp 有通道、散件升星在此模型无通道;胜率差锚 +11.7pp CI[+1.4,+22.0] 可用(倍数 2.6-2.9× 已被 P20_VALIDATION 撤回,禁引)。

## 卡点与采集挂账(双挂账)

1. **Δp_prep(备战支出→硬节点胜率)未标定**——P20 给的是「激活 vs 散件升星」的相对方向命题,不是「该帧花 C 金 vs 不花」的绝对条件胜率差。
2. **L_node 分位口径(遭遇失血分布尾部)未采**——W774 REPORT §2③ 挂账项;均值口径(注册表)与 P15v2 带只辖触发面,分位口径辖门值分母的稳健带。

采集挂账(可离线补,不需新实机):现 replay 语料(decisions.jsonl+outcomes.jsonl)按备战帧(围栏内支出额,carry 装备数,engines_formed rung)分桶 × 硬节点败率出条件表;样本量足格(≥10/桶)后回填本命题数字表,同时出 λ_death 生成式(P51 采集 #3 同批)。标定完成前,设计判据(p1_iface_hardnode_prep 的水位比较 hp−L_node<emergency_hp)只作触发面使用,金额侧交既有息账门——**触发不等于数值授权**,零魔数。

## 门值带(按真实 L_node 量级的三典型场景)

门值 =(C+I−Δstreak·S_streak)/G_loss;Δstreak 项未标定前取 0 作保守下界,C 组合口径 = P20 成本层 1-2 金/件 × 围栏 2 件 = **2-4 金带**,息损 I 并入同一带(取 C+I ∈ [2,4] 金,与 P26_VALIDATION 复算带一致):

| 典型场景 | 敞口/G_loss 量级锚 | 门值带 (C+I)/G_loss |
|---|---|---|
| ① P15v2 encounter 实测带(12.5 级) | G_loss 取 L 量级下界锚 | (2-4)/12.5 ≈ **16-32%** |
| ② 注册表 encounter 均值(rung0=24.32) | 24.32 级 | (2-4)/24.32 ≈ **8-16%** |
| ③ 注册表/P15v2 boss(26.7-27.2) | 27 级 | (2-4)/27 ≈ **7-15%** |

汇总:**门值带 ≈ 7-32%**(分位口径标定后收窄);P26_VALIDATION 同带复算对账。注意 G_loss 真值是 λ_death 敞口而非上表 L 量级本身——上表用 L 量级作占位带,L 与销毁敞口的换算随 λ_death 标定回填。

## 检验点

- 条件表出来后,Δp_prep 若 < 门值带下界(≈7-15%,场景②③)→ 备战授权应降级为只辖装备/上场型买(零刷新),设计回炉;落在带内(至 ≈32%)→ 按场景分档授权,禁跨场景引用单一门值。
- L_node 用条件败面而非无条件均值(P15 口径命题直接约束本命题)。
- λ_death 标定回填前,本式仅作结构背书,不作数值授权(纯金流量纲闭合的代价=门值分母待真值)。

## 重推对照表(2026-09-01,依 P26_VALIDATION 六条)

| # | 判决修正建议 | 本版落点 |
|---|---|---|
| ① | 删「42/39 分位已标定」无源数值,锚改注册表均值口径 + 分位回挂采集账 | 「L_node 单一源」节:注册表 24.32−4.53·rung / 26.71;分位显式「未标定挂账」 |
| ② | P15 已废回归换 P15v2 实测带(battle 10.45/encounter 12.18/boss 26.79 带 CI,73 局语料 848dc1aa) | 同节引 P15v2 + `tools/cw/proofs/p15/fit_results.json` 冻内核;rung 只立方向不立系数 |
| ③ | EV 式血量直加金账改道 λ_death 概率侧(敞口比较) | 「量纲闭合声明」节:G_loss=λ_death 销毁敞口,L_node 只辖触发面,禁 hp_to_gold 形态,禁边际引用 |
| ④ | 门值带按真实 L_node 重算(7-32%) | 「门值带」节三典型场景表,汇总 7-32%,含占位带声明 |
| ⑤ | P20 引用降级(方向级) | 「已证骨架」:倍数撤回禁引,+11.7pp CI 锚保留 |
| ⑥ | 索引行同步 | math_proofs.md 第 38 行已同步(门值=(C+I)/G_loss,λ_death 敞口口径,双挂账) |

出处:W774 设计批 REPORT.md §2③;消费设计 = `p1_iface_hardnode_prep`/`p1_iface_carry_equip`;验证=P26_VALIDATION(2026-09-01)。
