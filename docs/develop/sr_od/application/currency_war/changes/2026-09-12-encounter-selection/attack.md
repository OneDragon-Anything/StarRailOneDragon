# 遭遇选档简化 无前提对抗审查报告

**循环第 15 轮**（2026-09-12；无前提攻击：干净上下文，只信本轮直调核实的代码体、规范原文与脚本实跑；不继承前轮结论）

## 一、结论概览（发现统计）

- **发现：高 1 / 中 5 / 低 7。零实质发现不成立，未达定稿门槛。**
- 本轮定性：第 14 轮 11 项修复**主体全部落地**（验证表见第五节，9 项已修、2 项带残留）；承重面全部站住——判据数学本体、15 组数字脚本复算零错、宪法程序对账（裁定 6 例外扩大程序完成、ADR-0536 退役链闭环）、锁表主体归属、落码接口可行性（现役 `EncounterPick.refresh` / `refresh_used` / chosen_encounter 写端 / 刷新执行链全部兼容新判据）。**本轮发现均为文档措辞收敛、残留清理与登记补项，无判据本体返工。**
- 术语定义（首次出现）：**环** = GameState 结算观测环（深度 10 的 RoundOutcome 结算行循环缓冲，E-2 新建）；**暗装态** = 判据未过开闸门时的保守缺省行为（恒最低档 + 零刷新）；**开闸门** = 判据从暗装转激活需满足的全部条件；**缺环率** = 归档对局中「应产结算行的节点」里环内无对应行的比例（E-3 离线量测）；**净值口径** = Δ_t 按档位加成 − 遭遇 −4 + 窗间增量取值（相对前一普通战斗节点旗牌读值口）；**翻转线族** = f_min 标定置信区间跨越即判据方向翻转的临界值集合（净值口径 = {0.738, 0.444, 0.268}）。
- 唯一高发现是**登记缺项**而非设计缺陷：裁定 6（用户 2026-09-12 既定命令，本轮不攻击正当性）的 hp 例外按 04 §7 头注「唯一登记处」义务应落正本授权对账表，四文档与 E-4 清单均无此项——融合一致性缺口，一行修复。

| 级别 | 数量 | 一句话 |
|---|---|---|
| 高 | 1 | 裁定 6 的 hp 例外未登记正本授权对账表（04 §7），E-4 清单漏项 |
| 中 | 5 | 环写点落位矛盾（含「两构造点」残留）；landing 3.2 范围段 W14 归属残留；遭遇行 difficulty_node 装填语义两读；E-3 配对 D_enc 采集通道未指明；残留结算屏行不入环缺执行判据 |
| 低 | 7 | 锚点漂移 3 处（含 L14-5 修复后 :1964 再漂移）、词表引用指向被覆盖定义、design 摘要两处口径差、:71 双口径无限定词、「零现役接线」表述歧义、E-2 文件面缺 cw_settlement_obs.py |

## 二、指定复核点专项裁定

### ① 翻转线族重锚后全链自洽：**通过（带 1 低发现）**

- **四处承重数字一致**：details :126（§9）/ :158（E-3 阶段）/ landing :49 / details :71 括注段，净值口径线族 **{0.738, 0.444, 0.268}** 逐处一致；脚本复算 1.052⁻⁶=0.7377、1.052⁻¹⁶=0.44437、1.052⁻²⁶=0.26777，与三位小数逐位吻合。
- **与净值语义互洽**：线族 = 档位原始加成 {10,20,30} 减遭遇 −4 修改器的净值 {6,16,26} 在零增长前提下的门槛，与契约 3 净值定义一致；「f_min ∈ [0.4,0.6] → 可及其一/其二、[0.4,0.444] 段并及其三」复算成立（0.7377≥0.6 全段可及；0.44437≥0.444 含端可及；0.26777<0.4 全段不可及）。
- **W1 注入态区隔清晰**：details :136 明写「候选档注入净增量 +10……注入值非真实其二净值 6——测试态标签」，断言比值 1.052⁻¹⁰=0.6023 ≥ f_min=0.5 与判据式同向，复算成立。L14-6 修复到位。
- **低发现（L-6）**：details :71 同段前半仍保留 Δ=10/20/30 **原始加成口径**门槛（0.602/0.363/0.219，复算成立）作数量级例证，但该半段无「原始加成口径·仅量级锚·非开闸线」限定词，与后半段承重净值线族同段并存——M-3 指定的 4 处已重锚，此为同段内误读风险残留。

### ② W14 归属 E-3 后阶段闭环：**主体通过（1 处残留升中）**

- details E-2 :157「锁 W1-W6、W7、W8-W13（W14 = E-3 离线对账锁）」✓；E-3 :158 完成义务含缺环率量测且开闸门判据句含「缺环率量测可忽略（W14 离线对账）」✓；landing 3.3 完成判据 :62「W14 环完整性对账锁绿」✓；design :76 与 README :25 的「锁表 W1-W14」指 details 锁表全表覆盖范围，语义正确 ✓。
- **E-2 无不可满足判据**：W13 产出端锁属 E-2 新建结构可测；details :152 表注声明 W1-W3/W5 在 E-2 期以「三常量 + 条语义测试注入态」激活，与 landing 3.3 :63「激活全绿」两阶段口径自洽 ✓。
- **残留（中-2）**：landing 3.2 **范围段 :29** 仍写「锁 W1-W6、W7、W8-W14」——与本阶段完成判据 :40「W8-W13 全绿（W14 = E-3 离线对账锁，见 3.3）」同文件自相矛盾。M-2 修复漏改这一处；账本按范围段立任务会把 W14 混进 3.2 交付面。

### ③ 全量扫尾：**13 项发现，见第三节**

除发现清单所列外，四文档全部承重 as-built 断言（代码拓扑/接口签名/宪法引用/研究文档出处）经直调逐一成立，见第六节对账。

## 三、发现清单

### 高

**H-1｜裁定 6 的 hp 例外未登记正本授权对账表（04 §7），E-4 清单漏项**
- 位置：landing「正本更新清单」（现 4 行）+ details 契约 5；判据源 = `04_survival_budget.md:70-72`「**hp 进决策授权对账表（单一源；00 §5 指定位）**……任何新形态进入前须过 00 §3 硬闸门（逐项用户确认）**并在本表登记**」+ `00_framework.md` §5 约束单一源表「hp 进决策的全部例外（**含挂账中**）｜本篇 §3 硬闸门 + 04 §7」。
- 内容：裁定 6（P/B 掉血分量授权进遭遇决策 + GameState 持久化，用户 2026-09-12 逐项确认）是 00 §3 两件硬闸门之外的新 hp 消费形态，确认为已完成程序（四文档申报一致，正当性不在攻击范围），但它是「已授权未消费」的挂账态例外——按 04 §7 头注属**必须登记**形态。四文档（grep 验证零匹配）与 E-4 正本更新清单均无 00 §3 / 04 §7 的登记项。裁定汇编与契约 5 全部住在 `changes/` 目录——双层文档流铁律明示 changes 会不定期删减，登记不落正本 = 例外溯源随目录删减断链，未来任何「hp 进决策」审查失去对账锚。
- 修正方向：E-4 清单补一项「`04_survival_budget.md` §7 授权对账表登记裁定 6 例外行（授权日期 2026-09-12、形态 = 结算屏现成读数 P/B 可作遭遇决策输入 + 观测环持久化、辖域 = v1 不消费仅挂账、退路 = 敌方通道）」← 3.3 或末阶段；00 §3 是否同步加行由编排者裁（04 §7 为 00 §5 指定位，单点登记已满足纪律字面）。

### 中

**中-1｜环写点落位规格自相矛盾（含第 14 轮 L14-4 漏改残留）**
- 位置：details :16（契约 1「环写点 = 该构造唯一点（三路均入环）」）、details :119①（「环写点 = RoundOutcome 创建点——1f 行与 3b 全写行均入环」）vs details :119④（「1f 行 W239 防毒守卫（:372-375）保留现状——**其抑制形态不入环**」）+ details :119③（「任一构造点命中即入环，**两构造点**均未命中 = 结算处理未运行」）。
- 代码事实：RoundOutcome 构造唯一点 = `read_round_outcome`（cw_settlement_obs.py:487-499）；W239 守卫在调用方 `_record_round_outcome` 内 :372-375（`if telemetry_only and _obs.killed is not False: return`），**位于构造点之后**。若环写入按「创建点」字面落进 read_round_outcome 函数体内，守卫拦不住——boss 胜局页 1 幻影行（killed=True 的 telemetry-only 行，1f 模板门误命中形态）会入环，直接违反④「抑制形态不入环」。
- 矛盾二：③ 的「两构造点」与契约 1「构造唯一点 + 三路调用」直接冲突——L14-4 的同文档漏改点。
- 修正方向：①环写点位改写为「`_record_round_outcome` 内 W239 守卫之后、观察半直写之前（1f 显式败局行与 3b 全写行均达该位，胜局行同位入环）」；②③「两构造点」改「构造唯一点未命中」。

**中-2｜landing 3.2 范围段 W14 归属残留**——见复核点②；修正 = :29「W8-W14」改「W8-W13（W14 = E-3 离线对账锁，见 3.3）」。

**中-3｜遭遇行 `difficulty_node` 装填语义两读（牵连 W13 锁面与 E-3 配对）**
- 位置：details :111（§8 表「difficulty_node：结算快照取**该节点最近备战帧**现场真读旗牌值——live 位为门，carry 恒值不入，无现场读 → None」）vs details :73（§3「每场遭遇战无论成败产出 (档位 t, **D_enc 真值**, fill, killed) 数据点——载体 = 遭遇战 RoundOutcome 行（扩字段 encounter_tier + difficulty_node）」）+ W13 :149（「difficulty_node/encounter_tier 非 None 路径」）。
- 代码事实：遭遇节点**无备战帧**（details 契约 2 自录；cw_screen_encounter 决策链无 prep 段）。「该节点最近备战帧」按读法 A（节点自身备战帧）→ 遭遇行 difficulty_node **恒 None** → W13「非 None 路径」对遭遇行结构性必红、§3 经验数据点的 D_enc 成分恒缺；按读法 B（结算时刻时间上最近的备战帧 = 前一普通战斗节点备战帧写入值 = D_enc 口径，同一 live 门）→ 全链自洽——但文档没有这句，两读均通字面。
- 修正方向：:111 补一句「遭遇行取值 = 决策时 D_enc 口径（前一普通战斗节点备战帧现场真读，同一 live 门）；普通战斗行 = 自身备战帧快照」，W13 锁面按行类型分别注明非 None 路径。

**中-4｜E-3 配对输入「决策帧 D_enc 口读值」的采集通道未指明**
- 位置：landing :49（3.3「遭遇决策帧与其后结算帧配对（**决策帧 D_enc 口读值**/选档档位/填充率/胜负）」）；关联 details :104（「journal 锚行（cw_screen_encounter 决策行 log 已有）」）。
- 代码事实：现役决策行 log（cw_screen_encounter.py:320/:438）只含 `options/pick/refreshed/reason`——**不含 D_enc 读值**；E-2 的「归因串 e3-simple 走 journal 决策行」也未申报携带 D_enc 字段。E-3 自备采集脚本可从对局档案的备战帧 difficulty 遥测序列重构 D_enc（取遭遇决策前一普通战斗节点 live=True 帧；economy.md:111 在册「difficulty 字段+live 标记」），但「live=True 过滤」纪律未写入 E-3 范围——live=False 帧是简报恒值（≈108 毒化形态，details §3 live 门同判），裸值取数会引入毒化样本。
- 修正方向（二选一，E-2 或 E-3 落一处）：①E-2 义务补「journal 归因行附 D_enc 判别子通过值（或判别子失败标记）」；②E-3 范围补「D_enc 口读值 = 档案中前一普通战斗节点备战帧 live=True 值的重构判读纪律」。

**中-5｜残留结算屏行「不入新局环」缺执行判据，⑥ 的清空机制防不住它**
- 位置：details :119⑥（「环与经验表生命周期 = 局：开局清空——relaunch 残留结算屏行（旧局 plane/round）不入新局环（跨局污染消除）」）。
- 代码事实：残留行构造时序在「开局清空」**之后**（run 启动首帧即结算屏为在册场景，cw_settlement_obs.py:68-80 relaunch 兜底注释；`_mark_relaunch_residual` cw_screen_battle_wait.py:571-577 只标记不改行为）。新局环建立时为空，残留行随后在构造点正常构造——开局清空对它无作用。兑现「不入环」需把 residual 标记传入环写入位作排除判据，该接线四文档未申报。
- 修正方向：⑥ 补「环写入位判 `_mark_relaunch_residual` 返回值，residual=True 行不入环不占位（缺环由缺环率量测覆盖）」。

### 低

**L-1｜cw_reconcile 缺行臂锚点漂移**：details :119「cw_reconcile:473-509 缺行臂」——缺行臂（观察缺口→采新+留证）实际在 :512-518；:473-509 是跨节点下行守卫主干（loss 背书/胜战拒信臂）。佐证断言本身成立，仅行号漂移。

**L-2｜Field 默认值锚点漂移**：details :70「Field 默认 (None, source='observation')（cw_game_state.py:401-402）」——语义成立，现态行号 :407-408（:401-402 在 Field docstring 的 prior 行）。

**L-3｜enemy_difficulty 锚点再漂移**：details 契约 2「kernel/cw_game_state.py:1964」——现态 enemy_difficulty 字段在 :1971（:1964 是 deploy_cap 注释行）。L14-5 修复后并行批迁移造成的再次漂移；设计头注「并行批迁移后以符号锚为准」已免责，照实登记。

**L-4｜词表引用指向被覆盖定义**：details :119 对账类型集引「parse_settlement_node_type 词表 `cw_settlement_obs.py:37-38`」——该函数内 `_TYPES` 定义两次（:37-38 与 :49-50，内容逐字相同，实际生效 = 第二个）；引用行号指向被覆盖的第一个定义。M-4 修复语义（词表 ∖ 补给 + 精英来自 `_normalize_node_type`，单一源 = 两函数现码）成立无碍。

**L-5｜design 摘要与 details 单一源两处口径差**：①design :64 开闸门摘要句「三常量 ∧ 条语义定谳」枚举不完备（漏缺环率量测门），虽挂「单一源 = details §9」，只读总纲者会漏一门；②design :48「写端 = RoundOutcome 创建点两路入环」与 details :16「三路均入环」计数口径差（胜局路由 landing :29「既有写点单点双落」覆盖，design 摘要漏该半句）。details 为单一源，两处均不改变承重语义。

**L-6｜details :71 双口径段无限定词**——见复核点①；原始加成口径三个门槛数字本身复算成立。

**L-7｜「零现役接线」表述歧义**：details 契约 2 / :70「cw_difficulty_account……零现役接线」括号限定「（from_strategies/total()）」——合成机制确实零调用方（grep 验证）；但同文件 `marginal_value` 有现役消费链（cw_survey19_hooks.encounter_tier_score ← cw_events.decide_encounter 基线核 :690，基线核本身非现役）。按现措辞（括号限定）精确，按「该文件零接线」整体读不成立——不动亦可，留意判读口径。

**L-8｜E-2 文件面缺 cw_settlement_obs.py（装配位两形态未指明）**：landing :33 文件面列了 cw_screen_battle_wait.py 的「difficulty_node 快照装配」但未列 cw_settlement_obs.py。双扩字段走构造参数形态（read_round_outcome 签名扩参）需改该文件；走调用方 `_obs` 后填形态（dataclass 非 frozen，setattr 可行）则不需——落法未指明；后者与中-1 的环写点位天然兼容。建议 landing 3.2 注明装配位取后填形态（或文件面补该文件）。

## 四、数字复算对账表（脚本实跑，禁心算；g = 1.052）

| 文档数字 | 位置 | 复算值 | 判定 |
|---|---|---|---|
| Δ=10 → g≈1.660 / 门槛 ≤0.602 | details :71 | 1.6602 / 0.6023 | ✓ |
| Δ=20 → ≈2.756 / ≤0.363 | details :71 | 2.7562 / 0.3628 | ✓ |
| Δ=30 → ≈4.576 / ≤0.219 | details :71 | 4.5759 / 0.2185 | ✓ |
| 1.052¹⁸ = 2.4905（≈血量×2.5） | details :71 ↔ economy.md:109 | 2.4905 | ✓ |
| W1 比值 1.052⁻¹⁰ = 0.602 ≥ 0.5 | details :136 | 0.6023 | ✓ |
| 其二净值门槛 1.052⁻⁶ = 0.738 | details :71/:126 | 0.7377 | ✓ |
| 其三门槛 ≤0.444 | details :71/:126/:158 | 0.44437 | ✓ |
| 其四门槛 ≤0.268 | details :71/:126/:158 | 0.26777 | ✓ |
| f_min=0.5 最大可及净增量 ≈13.7 | details :71 | 13.67（净增 13→0.5174 可及；14→0.4918 不可及） | ✓ |
| f_min∈[0.4,0.6] 档位结论 | details :71 | 其二全段可及 / 其三 [0.4,0.444] 段含端可及 / 其四全段不可及 | ✓ |
| 环深度：同位面 2 / 跨位面 5 / 位面内极端 9 / 跨位面叠加 12 | details :117、README :11 | 2 / 5 / 9 / 12（R_after=3 = 2-5遭遇+2-6奖励+2-7boss，与 plane_schedule_observed P2 地面真值吻合） | ✓ |
| P15v2 遭遇败局 ~12.2 [10.32,14.24] | README :20 | P15v2_REPROOF:45 = 12.18 [10.32,14.24]，四舍五入 ✓ | ✓ |
| 遭遇等级加成 {0,10,20,30} / −4 修改器 | README :14、契约 2/3 ↔ economy.md:107/:111 | 原文逐字吻合 | ✓ |
| M41 −22 / P2r1 +2 符号相反 | design :87、details :56 | cw_settlement_obs:169（−22 实锤）/ economy.md:137（P2r1 挑战进度 +2、净掉血 23 仍败局） | ✓ |
| ADR-0536 注释引用 bridge 3 / encounter 10 / provisional 4 | landing :29 | 逐一数出 = 3 / 10 / 4 | ✓ |

## 五、第 14 轮 11 项修复验证表

| 项 | 判定 | 依据（本轮直调） |
|---|---|---|
| M-1 开闸门判据句补缺环率 | **已修** | details :128、:158 与 landing :49 三处均含「∧ 缺环率量测可忽略（W14 离线对账）」；details :42 总式触发全集含缺环率。design :64 摘要句枚举不完备 → 降级低发现 L-5 |
| M-2 W14 归 E-3 | **残留** | details :157、landing :40、design :76、README :25 四处已改对；**landing :29 范围段「W8-W14」漏改** → 升中-2 |
| M-3 翻转线族重锚 | **已修** | design :73 / details :126 / :158 / landing :49 四处均为净值线族 {0.738, 0.444, 0.268}；同段原始加成口径残留 → 低发现 L-6 |
| M-4 对账类型集定义 | **已修** | details :119 =「parse_settlement_node_type 词表 ∖ 补给 + 精英来自 _normalize_node_type，单一源 = 两函数现码」；「精英」确只在 _normalize_node_type（:586）✓。词表引用指向被覆盖定义 → 低发现 L-4 |
| M-5 生命周期 = 局 | **已修** | landing :29「环/经验表开局清空（生命周期 = 局）」+ details :119⑥；residual 行排除判据缺口 → 新中-5 |
| L14-1 [27]③→[39] 收口③ | **已修** | README :11「01 §2 公理 3/[39] 收口③」；details 契约 5 引 user_playstyle.md:274——行号核实，收口③原文确在该行 |
| L14-2 README 进度行 | **已修** | README :30「第 14 轮发现已修，第 15 轮待跑」 |
| L14-3 「非暗装全同」措辞 | **已修** | details :58「两场全删失 → 不足……刷新逃生阀仍可用，非暗装全同」；与总式 :41-42（∅ → 可刷新 / 暗装零刷新）区分一致，W3/W2b 锁面同口径 |
| L14-4 构造唯一点 | **已修（带残留）** | details :16「构造唯一点 = read_round_outcome（:487-499），三路调用」✓；details :119③「两构造点」漏改 → 并入中-1 |
| L14-5 锚点更新 | **已修（部分再漂移）** | :2168-2188 ✓、:2565-2572 ✓ 现态吻合；:1964 → 现态 :1971 再漂移 → 低发现 L-3 |
| L14-6 W1 标签 | **已修** | details :136「候选档注入净增量 +10……注入值非真实其二净值 6——测试态标签」 |

## 六、已攻击且未发现问题的面对账

1. **RoundOutcome 构造与三路拓扑**：构造唯一点 :487-499；胜局 :647 / 1f telemetry-only :546 / 3b :818 三路进入 `_record_round_outcome`；1f 不落账（:372-375 守卫 + :418 `if not telemetry_only`）与「落账依赖 3b『前往结算』单路径 :812-818」断言逐位成立。
2. **killed 三重兜底**：`parse_settlement_won` :197-206（挑战成功/挑战失败/进度符号）+ 调用方进度符号判定 :384-387 + hp 对比 :395-405（置信门 :395 + 轮次邻接门 :400）。
3. **fill 读数器**：页 1 帧态门（「点击空白加速」:484-485）、页 2 心形假值防线在册、暂存采集 :722-744/:770-795、合并消费 :406-417（暂存值优先）；「±N progress 暂存跨场滞留路径 :377-382、经 :384-387 可翻转下一 boss 胜局行 killed」的病灶断言成立（:377 合并分支仅在 delta=None 时清暂存，胜局行带值不清 → 滞留属实）。
4. **live 门五环语义链**（details :70）：Field 默认 (None,'observation')（:407-408）、carry 对未写字段早退（:2287-2288）、observe 拒 None（:2269-2271）、carry 写 source='carried'（:2291）、写入半 miss 走 carry（cw_observation.py:2570-2572）——「首枚真读前恒 (None,'observation')、value 非空合取封死假阳性、简报恒值被拒」全链成立；FieldSource 词表含 carried/prior（:220）。
5. **读链/写链锚**：cw_observation.py:2168-2188（两级管线、spec 无字段阶段跳过）、:2565-2572（live ∧ 非空才 observe）；简报回退 cw_screen_briefing.py:183-186。
6. **EV 核现状与处置对象**：双 provisional 槽名 ENCOUNTER_G_GOLD / ENCOUNTER_DSTAT_MAP + calibration.apply 注入单点（bridge :98-101）；None 期决策树肢 3 结构性 fail 向 → 恒最低难度、零刷新，README/design 现状断言成立；`MandateV1Strategy.decide_encounter` = bridge:137；基线核 cw_events.decide_encounter :644 且 strategy_id 值域收缩 mandate_v1（currency_war_config.py:74-83）→「基线核本无现役」成立；flow.py 基类 :539-543 委托基线核。
7. **cw_difficulty_account**：from_strategies/total 在册、品质「明文不建」（:60）、缺 Δ_t 与 −4 修改器成分（文件全文核）——「合成值不可用作 D_enc」判据成立。
8. **ADR-0536 退役链**：decisions/0536 文件不存在（glob 零命中）、INDEX.md :424 墓碑行（编号不复活）、13_pick_family E3 行（:20）已改指代码本体、四文档「已退役删除（用户命令）」表述一致。
9. **刷新逃生阀接口可行性**：现役执行链 `pick.refresh` 触发（cw_screen_encounter.py:296）、`refresh_used` 参数现役（bridge:140）、每局单次标志 `_encounter_refresh_used`、刷新整排重置（advantage_layouts.md:17）——「∅ → 刷新一次 / 已用不刷 / 刷后仍 ∅ 照选最低」全部有现役接口承接。
10. **经验层与 chosen_encounter**：盲选不写（cw_screen_encounter.py:217-218）与 encounter_tier=None 申报一致；chosen_encounter 写值 (difficulty, rewards)（:226-227）可作 encounter_tier 拼装源；board_state_of 决策读点 :284/:406；journal 决策行 :320/:438 在册。
11. **sim 不可观测申报**：cw_screen_encounter.py:37-38「T5 前引擎无遭遇决策段」；E-3「算法质量验证通道 = 实机本批」一致。
12. **Settlement 准入注释现状**：cw_game_state.py:547「伤害不入本结构……无决策消费」在册——E-2「注释修订 ≠ 扩字段」义务的事实基础成立；cw_performance.py :109「enemy_hp/damage 仍未灌值」与 damage_dealt 生产写端（cw_settlement_obs.py:450/:494）不符——landing 如实化义务成立；telemetry/schema.py OutcomeRecord :561-583 现无双字段——E-2 同步义务成立。
13. **宪法与规范对账**：裁定 6 例外扩大程序 = 用户逐项确认（00 §3 程序完成），details 契约 5「合法性唯一来源 = 用户确认、非 λ 先例延伸」表述诚实；g=1.052 的 V3.7 口径与 CI 缺口申报符合 01 §6 要素②（现状不可执行 → 不可开闸）；f_min/g/Δ_t 三要素登记义务（owner/期限）齐；暗装臂 = strategy-work §3 合法开关形态（行为输入未就绪 + 裁决期限「下一实机对局窗口，超一个对局周期回炉」）；数字三形态标注逐数在册。
14. **研究文档出处**：economy.md:107/:109/:111、combat.md:32/:62-64、plane_schedule_observed（P1/P2 地面真值、P2 尾三行、P3 无在册表）、advantage_layouts.md:17——四文档全部引用逐一对上。
15. **条件语义与保守回退自洽**：§2 条语义两假设判别式（普通战斗/遭遇分桶各判）、判为位面累计语义的回退链（败局 fill 证据废 → 恰一胜+败局归不足）与回写面（§2 状态表 + W2 锁 + W11 fill 分支）闭环；删失归态保守单调序（全胜 > 一胜一删 > 一胜一败(fill≥1/2) > 一败一删/全删/全败 → 不足）内部无矛盾；「恰一胜+删失→边缘」与「（败局，删失）→不足」区分自洽。
16. **Δ_t 方向条件论证**：Δq+Δs ≤ 4 → 保守向 / >4 → 乐观向推导成立（原始加成取值 vs 真实净值 = 6+Δs+Δq 的比较），E-3 按选档结果标定净值消除条件依赖的路径可行。
17. **环行模型**：奖励节点产结算屏（plane_schedule_observed 436/438 实证）、补给零行、深度 10 覆盖单一位面形态、跨位面叠加 fail-closed 安全降级声明——与 §8 论证一致；「环内保有最近 1~2 遭遇行」在 P2 2-5 / P3 开局两决策点抽验成立（经验层不受环深限制的分工正确）。
