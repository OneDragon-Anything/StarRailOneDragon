# 0299 - 决策框架 v2 买入面差异解剖批(engine_seed 生成通道+锁线目标集补 fixed)

- **Status**: accepted(2026-08-25;**平局线 ±3.3 首次达成**:n=100
  配对差 +1.74(SE 2.72),自合流总验 -8.24 收进平局线内且首超 v1)
- **Context**:ADR-0295/0296/0297 三批合流总验 -8.24(SE 2.68,
  insights 2026-08-24),残差主体=买入面(v1 15 买/局 vs v2 9.3)。
  本批=同局对拍解剖(方法资产:`.debug/temp/currency_war/
  d2_buy_gap_diagnosis.py`——v1 每 buy 在 v2 同轮归因到
  供给分叉/层1 未生成/层2 过滤/层3 评分≤0/层4 仲裁拦/matched
  六桶),按主导层修复。
- **解剖表**(seed 900000-900009,改前):缺口 6.2/局 =
  **层1 生成缺 21.6%**(集中在 r1-r2 低价种子件:v1 reason=
  bridge_seed 27/engine_seed 14/line 9/copy 3)+ 供给分叉
  58.1%(v2 早期少买→金/池/刷新轨迹分叉→v2 店无同卡,**下游
  回声非独立根因**)+ 层3 评分≤0 仅 0.7%、层4 仲裁拦 0。
  → **主导层=层1 候选生成,不是评分/约束**。
- **Decision**(生成层两件):
  1. **engine_seed 买候选通道**:`_buy_tag` 补 engine_seed 标签
     (v1 `LineStrategy._engine_seed_wants` 直通单一源,ADR-0260
     门:P1/过渡体系阵营(仙舟/列车同行/持续伤害)/未持有同名/
     bench 满不种/同轮已卖不回买;金够由层4 gold_floor 辖)。
     注册:buy_tag_priority(bridge_core 之后)+ 四覆盖态放行
     标签集。评分走既有板面查表(_PIPELINE_TAGS 增 engine_seed,
     买后经部署管线:早期 cap 宽种子即上场,配方档/板深维显影
     ——首块砖不需要专门评分项,主阵营种子(仙舟/列车)天然
     正分)。
  2. **锁线目标集补 fixed**:`_target_names` 锁线分支从「只并
     各桥 core」改为「并全部桥 fixed∪core」(与未锁分支同口径;
     v1 `_bridge_seed` 不分锁线态)。旧版锁线后桥 fixed 件
     (飞霄)候选不生成 + 无卖保护;顺带 fixed 件不再被当
     off_target 卖(v1「买进来的每一张都是方向件」)。
  3. 回归锁:test_cw_adr0299_buy_gap.py(9 锁:engine_seed
     生成/四门反例/目标件不降级/标签全注册/锁线目标集含
     fixed∪core/飞霄候选)+ 0293 hash 锁更新
     (57450124→91de0e92,数值字段全部不变)。
- **Considered Options**(全部 sim 配对,snapshot 池,单进程;
  v1 臂同窗重跑):
  1. **评分侧种子持有进度项**(targets 计数集扩到过渡体系件):
     **否决**——动机=flow 系引擎种子(持续伤害是 flow 非主
     阵营,桑博/卡芙卡/艾丝妲)在 recipe_tier(主阵营口径)
     恒 0 显影、首块砖被「非正分」拒(解剖表 layer3 20→13 实
     证机制成立);但双窗 30+30 配对差 -1.27/+1.27劣于生成-only
     +2.07/+2.07(种子持有项抬高 base 分,压缩其他买的相对
     分值且 targets 项提前饱和,buys 11.2→10.2),**机制真而
     净效果负,不采纳**;DOT flow 首砖 0 分显影登记为已知
     评分层盲区,量级 0.4 买/局,待后继批有净增益证据再议。
  2. **copy/pair 通道迁移**(v1 r383b 副本门/凑对门):本批
     不做——解剖表残余 layer1 7.7% 多为副本(希儿/娜塔莎
     二份)与散买(阿格莱雅/貊泽),单卡量级小且 v2 有
     copies_cap/目标类标签覆盖大头,收益不抵语义引入面。
  3. **bench_capacity 拦截**(layer4 1 例/100 局)与层2 过滤
     (0 例):非残差源,不动。
- **验证**:
  - 30 局配对(900000-900029):buys 8.6→11.2/局(缺口 6.2→
    3.9);final_hp 配对差 **+2.07(SE 4.65)**;
  - 验证窗(900030-900059):**+2.07(SE 5.02)**(双窗一致);
  - **n=100 终验(900000-900099)**:v1 32.37 vs v2 30.63,
    配对差 **+1.74(SE 2.72)**——从合流总验 -8.24 收进 ±3.3
    平局线内;buys 15.0 vs 11.3(未到 14-15:残余=供给分叉
    回声 61.6%+copy/pair 通道未迁移,非本批目标);解剖表
    终态:layer1 6.7%/layer3 4.0%/layer4 0.1%。
  - 测试:0299 新锁 9/9;0291/0293/0295/0296/0297/0298 全绿
    (0293 hash 锁按流程更新);ruff 通过。
  - ⚠️ 已知无关红:`test_ci_smoke_snapshot_batch` 的
    `equip_value_strategy_key_coverage`(stash 本批改动后
    依旧红)=工作树在飞 worker(ADR-0298 域 cw_sim_checks.py)
    的既有失败,非本批引入,不辖。
- **Consequences**:
  - 正:买入面残差主导层(生成层)清偿,v2 首次配对差转正;
    engine_seed 语义单源直通 v1(不复制判据);种子件无阵营
    级保护争议消除(件值交评分层,ADR-0296 同则);解剖对拍
    方法脚本留档可复用(下一残差批次直接跑)。
  - 负/风险:①供给分叉回声(61.6%)是「v2 行为不同」的固有
    属性,不是可修对象——判读买入面时须区分回声与真缺;
    ②DOT flow 首砖 0 分显影(评分层已知盲区,0.4 买/局)未
    清偿;③copy/pair 通道未迁移,残余 layer1 ~0.7/局;
    ④v1 臂本身在演进(在飞 worker),后续配对须同进程重跑
    v1 臂(ADR-0295 教训,本批已执行)。
