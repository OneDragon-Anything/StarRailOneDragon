# 0305 - H3 档内交叉表(离线)+成型转化洞+金充裕不买诊断批

- **Status**: accepted(2026-08-25;诊断批——件1 实证表落地,件2/件3
  诊断如实报+杠杆证伪回退,唯一行为改动=口径钉死与新锁)
- **Context**:0304 收账后三件:①「等实机」被拆除——outcomes
  语料(191 行,board_before 板面快照)已够做 H3 档内交叉表
  (0304 悬置档内细分的原始理由=「需实机 core trio×败率交叉表」);
  ② 0304 附带观察「v1 到达 rung2 战斗轮次 30 vs v2 10 但 hp 没跟
  上」需诊断(早成型→更高难度节点?成型后没乘胜追击?rung 计数
  口径差?);③ 批㉞④ 登记的「评分域金充裕不买 20+ 段」杠杆追击。
- **Decision**(三件):
  1. **件1 交叉表落地**(game 侧研究文档,非评分实装):
     `docs/game/currency_war/research/h3_tier_core_crosstab.md`
     ——档位(recipe_tier 分桶)×核心在场数(关键阵营凑档 ≥2
     代理;board_before 键=阵营名,角色不可见=数据边界)×败率。
     结论:**低/中档(tier<5、5-6)档内平坦**(core 差 10.6pp/
     8.0pp<15pp 判据,0304 悬置对低/中档正确);**梯度集中在
     tier≥7**:core1 100% vs core2 50%(50pp,方向与「空壳满线」
     假设一致)但 n=4/12/1 小样本=方向性旁证非定论。消费建议:
     档内细分项若实装只挂 tier≥7;真 trio 语义需遥测补 deployed
     角色名(改进方向登记)。
  2. **件2 成型转化洞=口径伪影+保护弱真值**(不实装策略改动):
     - **口径证伪**:20 局配对(seed 500-519,同节点序)重测
       「到达 e2 前战斗结算数」:v2 53 vs v1 50(几乎相等)——
       0304「30 vs 10」系未定义口径的一次性数字,**不可再引用**;
       口径已钉死进代码(`cw_sim._battles_before_engines` + 批报告
       `avg_battles_before_e2`,锁测试锁 None/计数语义);
     - **hp 没跟上的真机制**:双臂成型后均大出血(e2 时 hp 77-84
       →终局 36-49,post-e2 败场 2.7-3.6)——池 battle rung2 桶
       胜率仅 **44.4%**(n=9,Δ口径)且 **rung≥3 无桶**(下探
       rung2)——sim 域 e2 保护弱、e3 无回报,成型速度→hp 的
       转化被校准层天花板截断。旁证:H3 矩阵 e2 77.8%(killed
       口径,n=9)与池 44.4%(Δ口径)同 9 行不同判——**胜判定
       口径差(killed vs 相邻 hp 差分)在 rung2 桶显形,3/9 行
       异号**;v2 相对 v1 在诊断窗 hp 领先(41.05 vs 32.50),
       「v2 hp 没跟上」前提不成立;
     - 本窗 v2 avg_final_hp 41.05/hp_ge_60 0.20 vs v1 32.50/0.05。
  3. **件3 金充裕不买=诊断坐实,修法三窗否决默认关**(通道保留):
     - 诊断(20 局 probe,seed 520-539,包装四层抓 reject 流水):
       金 28-41 段 110 轮,店有引擎 54 轮中 9 轮零采纳;主导拒因
       =「非正分」(0 分板面差分 27 次:零分买候选 bond_fallback
       32/bridge_core 15/pair 11/engine_seed 5)+次因
       boss_levelup_ban 9 次(LevelUp 分高达 13.4 被 [12] 息引擎
       门拒——ADR-0266 校准域不动,旁证登记);
     - 修法=crisis_buy_bias 的常态域对偶(`goldrich_buy_bias/
       goldrich_min_gold/goldrich_buy_tags` registry 三参,scoring
       只顶 0 分差分,负息崖不翻越);**三窗 A/B(30+30+30 配对,
       gap base−fix = −1.80/+0.03/+2.47,SD 带 12-16 内)无一致
       方向 → 默认 0=关闭**(同 form_refresh_ev/copy_swap 豁免的
       证伪回退模式);成型加速确认(battles_before_e2 3.17→2.38)
       但 hp 不跟——与件2「e2 保护弱」同根,**评分域该杠杆已被
       校准层天花板封死**,登记到 Δ 池 rung 桶扩容(rung2 n=9 →
       攒样扩 rung3 桶+胜判定口径统一)之后再议。
- **Considered Options**:
  1. **件3 偏置默认开(0.5)**:否决——三窗无一致方向,anchor +
     1.8 但 fam_b −2.47;无一致增益的行为改动不进默认(0303
     豁免同型裁决)。
  2. **件2 按乘胜问题打(成型后升星/追级权重)**:否决——诊断
     显示 post-e2 星级推进存在(star_gain 0.7-1.0)且 sim 无
     rung3 桶,任何追级权重改动在 sim 不可验证(0304 件2 同边
     界),硬做=不可标定参数进 registry。
  3. **件2 修 Δ 池胜判定(killed 口径替换 hp 差分)**:未采——
     池语义变更动指纹、全部基线作废,且 rung2 n=9 太小不足以
     定标;登记为 Δ 池扩容批的组成部分一起做(单点改=白付费)。
- **验证**(snapshot 池 066c41856dd5d4f5,单进程串行):
  - 件1:离线语料分析(无 sim);脚本+报告 JSON 存
    `.debug/temp/currency_war/cw_dev/h3_crosstab*`;
  - 件2:20 局双臂配对(seed 500-519)+池桶真值直查
    (battle rung0/1/2 = 0%/41.9%/44.4%,n=40/31/9);
  - 件3:20 局 probe 诊断(seed 520-539)+三窗 A/B
    (0-29/900000 族/900030 族,30×3×2=180 局);
  - 定向锁:`test_cw_adr0305_goldrich_rungstat.py` 7 条(默认关
    回退锁+开臂加性/金门/关通道/息崖+rung 口径 2 条);定向
    0302/0291/0303/0305 33 passed + batch 报告消费 10 文件
    74 passed;ruff 通过;全量 pytest 未跑(CPU 配额纪律,欠账
    延续)。
- **Consequences**:
  - 正:0304 悬置的实机证据落地(档内细分=只挂 tier≥7 的方向性
    依据);0304「30 vs 10」伪影纠正+口径钉死进代码(防再犯);
    金充裕不买的拒因流水坐实(后续任何评分域再攻此洞有基线数据);
    件3 通道保留(Δ 池扩容后可直接 A/B 复验);
  - 负/风险:①tier7+ 梯度小样本(n=17),定论需语料攒厚+deployed
    角色名入 outcomes;②Δ 池胜判定口径差(killed vs Δ)在
    rung2 桶 3/9 行异号——池可信度受损登记,扩容批一并修;
    ③金充裕段的主导拒绝(LevelUp 息引擎门)未动(ADR-0266
    校准域),该段金滞留现状维持;④全量 pytest 欠账延续。
