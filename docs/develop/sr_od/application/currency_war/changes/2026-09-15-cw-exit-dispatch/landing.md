# 2026-09-15-cw-exit-dispatch 落地

## 3.1 对局中名单修正（移出 5 张大厅态画面）

**范围**：`cw_screen_state.LOBBY_STATE_SCREENS` 增加难度确认、数据银行、装备图鉴、竞争对手图鉴列表、竞争对手图鉴详情 5 张。消费点核查结论（已实测）：5 屏名的 src/ 引用共 4 处——overlay 注册表条目（UPPER_SCREENS 帧态门，走单屏名精准匹配）、入口链 area 锚定导航、观察 rect 查表、注释——均不经过 `in_match_screen_names` 判定链，移出不受损；该判定链的消费方仅 app 入口门、退出路由、返回大世界三处（即本迭代受益方，design §2.2）。边界：不动其它名单项、不改任何建档 yml。
**设计依据**：design.md §2.2「移出对局中名单」
**文件面**：`src/sr_od/application/currency_war/cw_screen_state.py`
**依赖**：无
**优先级建议**：5
**完成判据**：
- 5 屏名在 `in_match_screen_names` 输出中消失（对照 design §2.2 移出清单）
- 既有消费点核查结论记录（零消费 = 直接过；有消费 = 停手上报，禁擅自迁移）
- §12 通用工程门（引用，不复述）
**验收凭据形式**：单测或脚本输出对照名单 + 消费点 grep 记录

## 3.2 退出链三分发结构与停机位判据统一

**范围**：`cw_entry_exit.py` 路由判定按 design §2.1 重排（识别 → 退出链映射 → A 类屏名 → 大厅 → 默认委托 → 认不出兜底）；新增委托对局循环节点、敌人信息关闭节点；A 类名单常量落 `cw_screen_state` 并被路由与 `cw_loop.py` stop_at_prep 停机位共用；删除路由内备战双锚判定、miss 链「委托一次 CwLoop」步骤与战斗暂停路由死项。**不含**：`_prep_anchors_hit` 本体及其余三处穿透调用点、战斗兜底行为、任何建档 yml。
**设计依据**：design.md §2.1（分发结构、默认委托、同源常量、互踢推演）、§2.2（清单）、§2.3（改动面）
**文件面**：`src/sr_od/application/currency_war/operations/cw_entry/cw_entry_exit.py`、`src/sr_od/application/currency_war/operations/cw_loop.py`（仅停机位段）、`src/sr_od/application/currency_war/cw_screen_state.py`（A 类常量）、`sr-od-test/` 对应测试
**依赖**：3.1（同文件面顺序执行，避免交织）
**优先级建议**：8
**完成判据**：
- 行为锁（fixture 流程测试，全部通过）：①列车同行事故帧 → 路由走委托，全程零「备战退局发起」触发；②免战归档帧 → 直接退局发起（不委托）；③干净备战帧 → 直接退局发起；④敌人信息浮层帧 → 点关闭回路由；⑤认不出帧（未建档）→ 数拍重试后按计数判战斗中，**全程无 CwLoop 委托发生**（对照 design §2.1 ⑥ 删除委托步骤）；⑥委托后对局循环在 A 类画面立即交还（停机位与路由同源，无往返）
- 角色信息提示/装备详情浮窗：维持委托现状（design §2.2 B-1 注裁决——补清场登记越建档边界且场景不可达，降级后续卫生批）
- `ROUTE_BY_SCREEN` 无「货币战争-战斗暂停」项，且有注释说明真实入口（直边）
- 穿透调用点零改动（对照 design §2.1「穿透双锚本体保留」三处行号）
- §12 通用工程门（引用，不复述）
**验收凭据形式**：测试名清单 + `ruff check` + 涉及测试全绿

## 3.3 实机验证

**范围**：重启项目 MCP server（加载新代码）；真实对局走完整退出链并核对日志；登记的未验证假设「免战画面点门形图标弹中断挑战弹窗」如实验证或如实登记未构造。边界：不调参、不扩清单。
**设计依据**：design.md §2.1（未验证假设登记）、§2.4
**文件面**：无代码改动（运行与日志取证）
**依赖**：3.2
**优先级建议**：9
**完成判据**：
- 真实对局退出链走通：日志出现 委托对局循环 → 处理 → 备战退局发起 → 中断挑战弹窗 → … → 大厅，且「按钮-退出对局」无连续返回（≤2 次）
- 免战假设：验证通过则记录；未构造则在本迭代 README 进度区如实登记遗留
- 开商店等备战子态帧上全集识别返回的屏名区分度：如实记录观察结果（attack 存疑项；异常 = 回设计修订，禁静默）
- §12 通用工程门（引用，不复述）
**验收凭据形式**：日志片段对照（委托→停机→退局链节点序）

## 末阶段：正本更新

**范围**：按「正本更新清单」逐条更新正本
**设计依据**：本文件「正本更新清单」节
**文件面**：清单所列正本文档
**依赖**：3.1–3.3 全部完成
**优先级建议**：0
**完成判据**：清单清零；正本与实现一致
**验收凭据形式**：文档对照 review

## 正本更新清单

- `docs/develop/sr_od/application/currency_war/flow/`：新增退出链章节（分发结构、A 类名单与停机位同源契约、逐屏归档清单、穿透双锚与干净判据的语义分工）← 3.2 / 3.3
- `docs/game/screens/currency_war_prep_skip.md`（免战画面 doc）：补「退出链直接退出类成员」语义 ← 3.2
