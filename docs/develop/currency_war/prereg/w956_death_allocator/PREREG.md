# W956 · 危机帧购买兜底集 A/B 预注册判据(判前落盘,批中不改)

> 对象 = 方案 B「危机帧买侧兜底集」(crisis_fallback_enabled 注入臂;DESIGN §2/§3.3:经验件,A/B 先行)。
> E1/A′ 分配器件(拒供退役+估计器保底)为**无条件生效**件,双臂同载,不进本 A/B 判据(其验证=单帧锁+守卫移除红检+sim 回归扫描)。

## 1. 口径

- 臂:off = registry 缺省(``crisis_fallback_enabled=False``);on = ``dataclasses.replace(reg, crisis_fallback_enabled=True)``。同 seed 配对。
- 窗:seeds 956100..,n=150/臂,pool=snapshot,planes=1,expect-fingerprint ``6400d5d8edeaf68d+eqg1``(与 W907~W955 各批同指纹)。
- 判前锚:W955 基线(哑火 0/61、危机帧实花 98.7%/37 金/4646 金、death 残留 15/300、终局 hp 中位 31.0)。

## 2. 指标与判线(跑前写死)

- **M0(开火面)**:on 臂 decisions 中出现 tag=`crisis_fallback` 买动作的帧数 > 0。M0=0 → 输入死,不判正负,先补观测。
- **M1(主判据,方向)**:终局残金(局终持金)中位 on ≤ off(兜底集把 death 帧攥金兑换成战力)。
- **M2(残留面)**:death 域 alloc 残留 ≥2 轮局数 on ≤ off(W951/W955 口径;W955 基线 15/300)。
- **M3(守卫,全净才判正)**:①危机帧实花>0 占比与帧中位实花对 W955 基线不回归(占比 -3pp 内、中位 -30% 内);②终局 hp 中位不降超 ±2 噪声带;③hp≥60 率 ±2.6pp 内;④检查器无新增病灶族。
- **裁决**:M0>0 ∧ M1 ∧ M2 ∧ M3 全过 = 判正 → 翻默认开(除开关);M0>0 但 M1/M2 未过或 M3 任一破 = 判负 → 兜底集不落地(翻回默认关留码待清或删码),只留 ADR;E1/A′ 件独立生效不受影响。

## 3. 声明

- 双臂均含无条件 E1/A′ 变更,off 臂即「E1/A′ 后 HEAD」,与 W951/W955 的差异归 E1/A′(回归扫描另行对账,不混入本裁决)。
- 兜底排序(merge 优先→费用升序)为启发式,本 A/B 只判集语义有效性,不精调排序。

---

## v2(ADR-0510 重定价后的方案 B 复活验证;编排者裁决批)

前提变化:必死子带 I 退役(opp=c)落地后,death 帧分配器获得出清通道;
重跑本 A/B(种子窗 **959100..**,n=150/臂,口径同 §1)验证:
- **R1(重定价行为面)**:off 臂 death 帧 chosen 空率显著下降(前批 22/22=100%);
- **R2(方案 B 复活)**:on 臂 fallback 提案采纳数 >0 且 M1 残金/M2 残留较 off 改善 → 复活=翻默认开;采纳仍 0 → 维持判负留码默认关;
- R3 守卫:危机面/终局 hp 对 off 不回归(同 §2 M3 口径)。
