# P6 前两轮压库的期望账([1][15][16] 精确化)——支柱改写

> 状态:**已证**(边界精确化;「不用特殊处理」结论成立,理由改写为期权账)
> 数据源:真实表(开局金 5、`BASE_REWARD_GOLD`=5、`STREAK_GOLD_TABLE`=(1,1,2,2,2,3,4)、息=min(5,gold//10)、`sell_refund` cost=1 无手续费)
> 计算脚本:`tools/cw/proofs/w120_p5_refresh_levelup.py`(同脚本 P6 节)
> 提出:2026-08-25 用户口述;证明=W120 批

## 命题

「前两轮很难凑 10 金拿息,通常都是压牌库,统一经济策略不用特殊处理」。

## 证明

### ① 三臂推演(r1/r2 奖励节点;r3 战斗买 0;开局金 5)

**口径已裁决(2026-08-26 run13 实机)**:r2 奖励节点结算屏读数 `连胜×0`——r1/r2 两奖励节点全过后连胜计数仍为 0 ⇒ **奖励节点不计入连胜数**(V3.7 必修一口径正确;本证明原按「计入」推演已修正)。但**连胜金是否照发未裁**(run13 停于 r3 前;经济账按「计数 0 ⇒ 连胜金 0」修正,若实机后续观测到奖励轮有连胜金再回改):

零买 r1/r2/r3 末金 = 5+0+5 +0+5 +5+5+5 = **11/16/26**(基础 5/轮,奖励轮连胜金 0);若实测奖励轮连胜金照发(计档=2),则为 11/18/26。买 b1+b2 张 1 费后 r3 末金 = 26−(b1+b2)。**两种口径全部轨迹金 ≤26,离 50 息基远(结论不变)。**(不计入但照发金口径:11/18/26。)

### ② 息损量化(买 1 张 1费,5 轮累计息差)

金 5/9/11/19/21/25/29 开局买 → 息差 1/2/0/0/0/0/0;**恰在档线(10/20/30)买 → 3金(5 轮)**;档线内买 → 0。即:

- 金 <10 时利息恒 0(无息可损);
- 买后不跨 10 的倍数 → 零息损([11] 同档口径的数值确认);
- 跨档 → ≤1金/轮,收入 6-7/轮下 1-2 轮回档(回档账=真实息损下界 1-3 金)。

### ③ 收益侧诚实账(支柱改写)

- 压库命中率增益 ≈0(P4 已证:全买光 15 张只抬命中率 ≤1.0pp);
- e0→e1 边际 +1.4金/轮(P3)只在**战斗轮起(r3+)**兑现——r1/r2 买件的战力收益为零;
- **真支柱 = 零/微息损(≤3金)+ 1费卖出全额退的免费期权价值(P1:1费再遇 7-15 轮,弃购代价>1-3金息差)+ 3合1 素材积累。**

### ④ 临界 X

买牌开始付息 ⇔ 购买使金**跨过 10 的倍数向下**;1费的 X = 金恰为 10/20/30…(gold%10=0)时买付息,其余金位零息损。统一 EV(C_interest 口径)自动覆盖。

## 策略含义

[1][15] 口述保留(权威),执行理由改写:r1/r2 买 1-2 费不是「压库提升命中」(已证伪),是**零成本的囤牌期权+压缩噪声**——结论「前两轮不设特殊规则,统一经济策略」成立。

## 对实现的检验点

1. r1/r2 帧金 ≤9 时 1费购买候选应零息损放行(C_interest=0);
2. 金=10/20/30 整档时买 1费应计 ≥1金息损(非零);
3. r1/r2 无「强制压库」分支(统一路径,无 per-round 特判)。
