# T-188 交付报告:阶段一·设计件同步(landing 3.4)

> 交付范围:landing.md 3.4 文件面中的**正本文档两件**;`kernel/cw_game_state.py` 容器 docstring「同步通道」声明更新不在本批(任务书明令纯文档批、禁碰 src/,见「边界申报」)。
> commit:`docs/` 两文件逐文件点名,只 commit 不 push(commit hash 见 git log)。

## 更新了哪些正本 + 各更新了什么

### 1. `docs/develop/sr_od/application/currency_war/sim/sim-design.md`

- **§0 术语**:「投影」行(指向已不存在的 `kernel/cw_state.simulate`)替换为两行正名——**容器**(GameState 唯一状态类型/单例宿主 `board_state_of`/渠道签名写入口/frozen 帧替换)与**单一转移函数**(`apply_shop_action_logic`,LogicOutcome 出参,「投影/投影写」别名声明)。
- **§1.3 与 live 的对应关系**:架构图投影线改单一转移函数;图后新增「唯一状态类型 = 容器 GameState」段(引擎无本地工作帧、渠道细分指 §2.4)。
- **§2.1 单一源纪律**:整节重写为 as-built——全动作族(BuyCard/SellBench/LevelUpShop/RefreshShop/CloseShop + v2 三动作)收敛单一转移函数;LogicOutcome 拒绝出参语义(applied=False+reason+零容器写,禁引擎自判);`executed` Optional 双来源(live 回执/sim None 自算 k);引擎白名单面(池登记/XP 账本/金转录/装备分配/披露键/免费刷注入/抽牌重采样/break-redecide/LEVEL_CAP 守卫);锁面更新为现存两件(`test_cw_engine_transfer_spy.py` 结构锁 + `test_cw_transfer_golden.py` 金样锁,替换已删的 `test_cw_sim_shop_single_source.py` 陈旧引用)。
- **§2.2 逐动作契约要点**:权威语义源列改「转移函数腿」;BuyCard 槽置换/LevelUpShop xp_apply_clicks+level_cap/RefreshShop 只扣金+刷后牌面重写方/SellBench 装备回收/v2 族槽表中间形态整表写;DeployMove 改「不入转移函数(围栏部署 = obs 申报对齐)」。
- **§2.3 申报清单**:#2 免费刷注入更新(executed.refresh_paid 参数通道);#4 改「XP 记账载体与时序」(引擎本地账本+容器回声写+轮末延迟结转 vs live 即时+`sim:engine` 前缀失配抑制)。
- **§2.4(新增)sim 引擎的容器读写渠道**:渠道封闭集三通道表(logic_action/obs/白名单)、驱动器 decide 期投影写维持现状、`sim:engine` 抑制登记面、读口族 12 口、阵营不入容器申报差异(fields.md §3.2.3,细节归代码注释)。
- **§2.5(新增)商店域三态定长契约**:ShopSlot 词表/定长 5/买光=[empty×5] 合法/None 仅离屏;全 unknown 窗行为(花钱禁发射+仅 CloseShop+停机留证);sim 抽牌恒全真值不产 unknown;投影口槽置换;锁 = `test_cw_shop_slot_model.py`。
- **§6**:C6 守恒行的代码形态引用更新(容器库存并入,非旧 `extend` 写法);对账门完备性段改「走单一转移函数路径」。
- **§7.2**:白名单数据结构行的锁面引用更新(现存两件锁)。
- 附带:P35 回放脚本引用改语义描述(原路径为 `.debug` 易失过程件,不入库,正本不留悬挂指针)。

### 2. `docs/develop/sr_od/application/currency_war/sim/sim-wiring.md`

- **整表重对账为容器直写形态**(原「35 字段三档接线」前提失效):
  - §一 写入渠道(封闭集三通道:logic_action 域集/obs 事件面全列/引擎白名单);
  - §二 逐域接线表(19 行:每域的产生面渠道+事件、消费面读口);
  - §三 读口族 12 口 + 阵营不入容器申报;
  - §四 保真位面(旧 readable/trusted 位族由容器 None 语义吸收,引擎五位不写不读);
  - §五 sim 未接域(level_up_cost/selected_difficulty/简报域/商店刷新计数组/节点屏刷新计数组);
  - 羁绊口径分层/P2 段/已知缺口节保留并修正陈旧引用:`simulate_p2_ab`(已不存在)改 `vd_p2_enabled` registry 注入配对 + `simulate_p2_sensitivity` 裁决口径;economy.md 引用补全路径。

## 正本纪律自检

- as-built 无状态:新增文本无进度标记/变更史/会失效快照(§5 复盘通道既有「设计件(载体在途)」标注为该节自身性质声明,未动)。
- 不引用 changes/:两正本自足,未引用迭代目录内容(唯一性指针 = fields.md/sim-power-model/skill references 等正本区与 skill)。
- 直白表述:新增术语(容器/单一转移函数/槽置换/读口族)均给定义或随文定义。

## 边界申报

- landing 3.4 文件面含 `kernel/cw_game_state.py`(仅注释,容器 docstring「同步通道 = feed_sim_truth」声明已过时,现役 = 引擎直写+渠道签名);任务书明令禁碰 src/,**该注释更新未做**,需编排者另派注释微批或并入后续批。
- 文档引用符号/路径已逐一 grep 实证存在(含测试仓三锁文件、kernel 符号、读口 12 口、`simulate_p2_sensitivity` 等修正项)。
- L1 测试集(`-m "not slow and not legacy_baseline"`)照跑:49 passed / 0 failed(基线 46 因并行批新增测试面已自然上移,本批纯文档零代码影响)。

## 验收记录(编排侧,2026-09-14)
- sim-design §0-§7.2 + sim-wiring 整表 as-built 同步审阅通过:渠道封闭集/单一转移函数+LogicOutcome/executed Optional/裁定 A 渠道归属/商店三态契约/锁面引用(现存两件)全部与代码现状对齐;陈旧符号(simulate_p2_ab)修正。
- 符号/路径 grep 实证;49 passed 零代码影响(基线 46→49 为并行批新增锁,合理)。
- 边界申报:kernel cw_game_state 容器 docstring 陈旧通道声明(feed_sim_truth)→ 并入 T-194 末批注释微修。
- 结论:**accept,置 done**(3.4 完成)。
