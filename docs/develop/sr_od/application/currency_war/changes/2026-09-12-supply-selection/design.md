# 补给选卡升级 迭代设计（总纲）

> **状态：定稿（深化批已收敛）**——总纲留概览与裁定汇编，细节单一源 = details/supply-value-spec.md。角色层阶段（P-4）依赖 env §2.1 机器落地；P-1..P-3 不依赖、可先行。

## 0. 元信息

- 迭代目标：补给节点选卡（事件面 E4）决策升级——角色维度入价、刷新判据重做、装备价值升格为通用机器、契合锚定两态化。
- 状态：定稿
- 文档清单：details/supply-value-spec.md —— 三层价值模型/刷新判据数学/装备机器升格/锁表/阶段划分
- 依赖：仅角色层阶段（P-4）依赖 `2026-09-12-invest-env` §2.1 机器落地（角色机器第二消费位，禁二次建模）；P-1..P-3 不依赖、可先行

## 1. 问题与动机

### 1.1 现状（接线全通，算法层四 gap；依据 = 2026-09-12 本会话全链直调盘点）

观察/op/入账链已通：`read_supply_options`（每列角色+装备 OCR、钻双通道 SIFT+文本、动态列数）→ `CwScreenSupplyNode` 真策略消费 → 刷新 session 级一次 → 出口验真写 `chosen_supply`。算法 `decide_supply` 三分支：带钻选钻 / 无钻无条件刷新找钻 / key_equips 契合+10 与 `_EQUIP_VALUE` 通用表。

gap：

1. **角色维度零定价**（最大）：`SupplyOption.char` 已读但打分只用装备——选项是「角色+携带装备」bundle，候选阵容 carry 在列 = 0 分（补给选项含角色的机制依据 = 打捞人才库类效果「随机获得一个未被选择的角色及其携带的装备」）。
2. **刷新无条件找钻**：无钻即刷，不看 offer 质量；用户裁定已否定（README 裁定 2/4）→ 定稿 = 无条件**先刷**（裁定 4）+ 唯一跳刷例外 = 带钻（严格支配可证），见 details §2。
3. **契合锚定**：+10 契合消费 `state_of(session).target_comp`（P1 期过渡配方对物化的伪 comp，T-155 对投资选卡已裁辖域错位）→ 定稿 = 两态锚定（未锁 = 无绑定项；已锁 = locked_comp.key_equips），details §1.3。
4. **组件缺口维度缺失**：as-designed 规格「选项价值 vs 目标线组件/成品需求」（08 E4，P42 hoard_gaps 参数化待 derive）→ 定稿 = 结构版入（已锁态 key 材料命中 +3）、数值化挂账（双前置：λ 标定批 + owned 装备读数接线），details §4。

新增执行面缺陷（深化批盘点发现）：`_supply_refresh_used` session 级布尔无复位点——多补给节点局（「人身意外险」类加补给阶段）后续节点免费刷新被吞；定稿 = 旗标升格节点戳 `(plane, round)`（details §2 执行模型）。

卫生项（扩）：`strategies/impl/flow.py:537` 与 `strategy-docs/13_pick_family.md` 仍标「OCR 未就绪」，实际 T#99 已全链接通；`cw_strategy.py:173` 同款滞后；`flow.py:495-497`「supply/encounter 等 pick 族消费面不变」括注随本迭代过期——三者随落地阶段修正。

### 1.2 根因归层

语义层：选项价值模型缺「角色维」（装备单维承载 bundle 价值）+「统计先验 vs 锁定契合」未分态。

## 2. 方案（初稿框架——深化批补数学细节）

### 2.1 价值模型三层

1. **带钻层（最高，保留）**：带钻选项优先选（裁定 1）。财富宝钻/红钻/蓝钻细分价值待标定（宝钻 = 人口上限，量级或高于星徽材料——深化批按效果定义分档，禁一概「碾压」）。
2. **角色层（新增，相位感知——用户裁定 2026-09-12「除了终局的，在 P1 还要考虑过渡阵容的」）**：
   - **P1（意向未定型）**：角色价值 = 终局候选命中 **+ 过渡阵容成员**。成员身份单一源 = `cw_card_identity.line_identity_tier`（registry_core / transition_component / unrelated 三分档，转线前瞻已消费的同一机器，禁二次建模）——registry_core > transition_component > unrelated 0；过渡价值的权重随 P1 剩余时长衰减（深化批定量）；
   - **锁线后**：收敛到锁定方向（core 命中提权、shared 次之、全集外 0）；
   - 价值机器与 env 送卡型详设 §2.1 同源（第二消费位），相位维为本迭代新增语义。
3. **装备层（两态，裁定 5/6）**：
   - **未锁线**：通用「输出最需要」统计先验——现有 `_EQUIP_VALUE`（对位锚法）升格为共享机器（落点升出 cw_events 私有字典，消费位 = 补给/武装箱/穿戴分配等），统计面可接 plaza_meta 装备频次校准；
   - **已锁线**：锁定阵容 `key_equips` 契合优先（现行 +10 语义保留，锚从伪 comp 换锁定阵容）。

   三层的常数/公式/锚点推导定稿 = details/supply-value-spec.md §1（带钻细分：宝钻 > 红=蓝钻，配方可证；角色档序 40/20/12/9×w_P1；装备机器三件套落新模块 `kernel/cw_equip_value.py`，六消费位迁移表同节）。

### 2.2 刷新判据重做

- 废除「无钻即刷」（裁定 2）；
- 裁定 4 = 免费刷新先用：机制为整排重掷（advantage_layouts.md:18），重掷连高优先级一起丢 → 判据形态 = 「当前 offer 最优 vs 重掷一次后的期望最优」比较，**候选核心角色在场为保护性跳刷的边界情形**（深化批定：无条件先刷 vs 顶级保护跳刷，sim 可标定）；
- 刷新 = 整排重掷后终局重选（现行 op「刷新=终结本轮、下轮重进重读」的执行形态保留）。

定稿（details §2）：结构式 = 刷 ⇔ `E[max 新 offer] > max 当前 offer`；**唯一数学可证的跳刷例外 = 带钻**（严格支配，零分布参数）；一般式因 offer 分布无注册表无统计 → fail-closed，「候选核心角色在场跳刷」不采纳、挂实采钩子；无条件先刷 = 裁定 4 用户权威（如实声明非数学定理）+ 零成本侧证。执行面修正 = 刷新旗标升格节点戳（details §2 执行模型）。

### 2.3 深化项收敛指针（全部已深化）

| 初稿待深化项 | 定稿 |
|---|---|
| 角色价值机器接口对接 | details §1.2（`char_candidate_tier` 底座复用 + `line_identity_tier` 过渡维 + w_P1 线性衰减） |
| 刷新判据数学 | details §2（结构式 + 带钻跳刷支配证明 + fail-closed 面 + 节点戳修正） |
| 组件缺口维度 | details §4（结构版入 +3 / 数值化挂账双前置） |
| `_EQUIP_VALUE` 升格 | details §1.3（新模块 `cw_equip_value.py` + 六消费位迁移表） |
| 锁表与落地阶段 | details §5（V1-V3 + S1-S12）/ §6（P-1..P-5，依赖切分） |

## 关键取舍（初稿已定）

| 备选 | 为何放弃 |
|---|---|
| 带钻「基本赢/碾压一切」定性 | 用户定谳有误（2026-09-12）：钻强但出现率低，注释已删、行为修正入本迭代 |
| 无钻无条件刷新找钻 | 同上裁定；且刷掉 key 装备/角色无下行保护 |
| 装备选择各消费位各写一套 | 用户裁定做成通用机器（裁定 6）；`_EQUIP_VALUE` 升格共享，禁第二套 |
| 候选核心角色在场跳刷 | 非可证支配（需 offer 分布，采纳 = 拍分布），挂实采钩子（details §2） |
