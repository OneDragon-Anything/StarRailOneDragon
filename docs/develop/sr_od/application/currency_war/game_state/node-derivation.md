# 流程 hook 场景一:节点推进(node_enter)派生规则判定方案(R5;派生规则单一源)
> **持久家声明**(2026-09-11 晋升入库):本文自临时档 `.debug/temp/currency_war/流程hook场景一-节点推进-判定方案.md` 内容原样迁入;temp 原件降为工作副本,后续修订以本文件为准(单一源引用指针 = node-domain.md §5 与本目录 README.md §5)。

> 任务 = 节点推进判定方案批(流程 hook 场景一:节点推进触发 hook 判定方案;只读设计,零代码)。
> 规范正本 = 统一观察架构设计文档 v10 §12(流程转点观测锚,`docs/develop/sr_od/application/currency_war/screens/op-layer.md §5`)
> + 用户裁定(同节点重入不算推进/触发时点=分派前/三条必覆盖流转/全面梳理)。
> **R4(2026-09-10,文档拆分结构裁定升格)**:**本文升格 = 派生规则单一源**——
> 原遥测设计件(已删,git 可溯)(现行 v3.8,文件名 v3.6 字样防断链)§3.4.1 四腿正文整节删除改一行
> 引用,判定规则本体语义以本文为唯一正本(用户拆分裁定 2026-09-10:守卫族漂移
> 根因 = 双处书写;升格记档 = 关联行)。**v3.8 同步小修(R8)**:失配显影辖域说死(§3.2-7/
> 规则四③,与迁移批修订三方统一)/规则六现态申报+目标态限定/规则②③
> 镜像窗口前提申报×2/走查集指针收口/行号基准刷新(见下行基准注)/§⑤-0.5 收账/
> cw_anchor 行号快照注(原「易失注」,已随 cw_anchor 入库改写,§3.2-2)。折入面:①**四规则组终版全文**(用户终裁 2026-09-10:①备战腿
> ②位面过渡腿③BOSS 简报腿④弹窗腿+类型派生;守卫族终版{结算窗,开局链}=
> 逐名{结算窗 token,简报,投资环境,等待1-1},0p/0q 出族 = 迁移批修订·守卫族终版);
> ②**0p 幂等锚正文化**(R7 高-2:树上已落的简报屏多 pass 重入防双推进补丁,
> 原遥测设计件(已删,git 可溯)零记载——按实码 cw_game_state.py:1871-1908 写进规则③细则);
> ③**单字段双层形态落文**(字段层次终极版,迁移批修订:top_bar_raw 观察层+
> node_ord 逻辑层,生效序读口 effective_node_ord=max 派生计算非存储,失配→
> observe_vs_logic_mismatch 缺陷行);④级联走查两段(0p→商店/0q→商店恰一次
> 推进)+ 既有 10 场景走查与 18 边全景图守卫族口径同步(R3.1 已勘的 E12 边序
> 保持)。**载体规范单一源分工**:版本 id/单版本事务/渠道签名/行型 = [journal.md](journal.md) §2-§3
> v3.7,本文不复写;本文管判定规则本体(触发面/候选值/守卫/推进语义)。
> **R3(2026-09-10,用户关键纠正)**:**R2 退役段首推断是错的**——「先渲染备战再弹窗」
> 是游戏**渲染顺序**,不等于 bot **采样可得**:弹窗自动弹出快于 bot 轮询时,干净备战帧可能一帧都采不到,纯直读触发会漏(补给类弹窗「确认即完成节点」形态是真漏;其余形态触发时点推迟到弹窗消化后,选卡动作先于触发,违反裁定 2「分派前」)。勘误申报全文见 §5-①。R3 触发**恢复双腿**:①备战帧腿(干净备战帧被采到→直读顶栏,R2 腿);②弹窗腿(看到弹窗画面而备战帧未采到→段首推断 last+1 回归,R1 机制+守卫);两腿落**同一行锚**(弹窗腿触发后本节点内不再重复触发,反之亦然)。
> 仍有效裁决面(R2 折入保留):重入不算推进/分派前触发/同批原子切换(battle_done)/漂移信息项。
> **行号基准注(v3.8-低-1 随批刷新)**:本文行号锚基准 = 2026-09-11 工作树
> (cw_loop.py 3276 行版;R8 实测)——**cw_loop 锚全量刷新**:0p 分支 :2212-2241
> (分支写点 :2230)、0q 分支 :2250-2270(写点 :2255)、备战双锚 :2339-2340、
> 效果 tick :2351-2384、0c :1897-1901、0e :1925-1930、补给 :1934-1939、
> 0n :2115-2138、dispatch :2236;kernel 锚同步:守卫族常量 :140-146、规则②
> 三源 :1836-1868、规则③幂等锚 :1871-1908、派生接线 :1590-1599/:1774-1809、
> 失配触发 :1420-1426、_emit_defect :538-556、effective_node_ord :1721-1729、
> journal 双门 :238-248。下文历史行号与本注冲突时以本注为准(旧锚保留为历史
> 凭据不加改,判读按符号名定位)。
> 版本链:R1(三类窗口+段首推断)→R2(单窗口纯直读,**退役推断=勘误**)→R3(双腿)
> →R3.1(2026-09-10,用户玩法实证勘误:E12 边序修正——BOSS 简报实序 =
> 奖励关后 BOSS 战**前**(screen_flow_timing.md #26「奖励关后 BOSS 关前」),
> 先于 boss 战斗与商店;商店自动开触发源含「BOSS 简报关闭后」(#14/#27);
> R1.1 批落锁 test_popup_leg_boss_briefing_predecessor_advances)
> →**R4(2026-09-10,派生规则单一源升格,见上)**→**R5(2026-09-10,节点边界定义
显式化小修,零规则改动,见 §3.3 定义基准节)**→**R7(2026-09-20,触发模型换代:
节点推进动作上报化)**——节点边界定义重立(推进 = 上一节点终结动作落地:结算
确认/补给确认);`node_ord` 改两态字段(锚定态 observation / 推进态 logic)+
观察态门双条件;备战画面改判对账锚定面(锚定写端两处 = 备战顶栏/补给屏节点条);
旧四规则组(备战/位面过渡/BOSS 简报/弹窗)+ 守卫族 + 幂等锚 + 循环分支写点
整体退役;倒退免疫保留;「禁 observe 直写序键」裁决推翻(改判记录 = §3.2-7)。
实现锚 = kernel `advance_node_effective`/`report_node_advance`/
`observe_node_anchor`(R6 版次序位见修订总览,居 R4 后)。
> **R8(2026-09-21,用户裁定:去证据门)**:动作 op 不做任何确认——点击即
> 上报,观察态门为唯一门;转移证据机制(探锚/等待/bail/补报)整体退役,
> §3.3-2 触发点的证据集同源条款/上报时点门表述随改(死点击幻影自愈链
> 见 §3.3-4 申报);完成判据白名单回归 CwScreenBattleWait ③段出口判定
> 独用。kernel 三原语零改动。
> 本报告只做判定方案设计,不动任何代码;行号凭据以 2026-09-10 工作树为准。

---

## ⓪ 修订总览(R1 → R2 → R3 → R3.1 → R4 → R5)

| 版本 | 触发 | 内容 |
|---|---|---|
| R1 | 首版 | 三类触发窗口(备战帧/弹窗族/战斗窗段)+ 段首推断(last+1) |
| R2 | 用户裁决「单窗口纯直读」 | 退役段首推断(**后经 R3 勘误 = 退役是错的**) |
| R3 | 用户关键纠正(渲染顺序 ≠ 采样顺序) | 触发恢复双腿(备战帧腿+弹窗腿),两腿同一行锚;勘误申报见 §5-① |
| R3.1 | 用户玩法实证勘误 | E12 边序修正:BOSS 简报实序 = 奖励关后 BOSS 战前(screen_flow_timing.md #26/#14/#27) |
| **R4** | 文档拆分结构裁定(用户 2026-09-10)+ 同日用户终裁三连折入(迁移批修订) | **升格 = 派生规则单一源**(原遥测设计件(已删,git 可溯) v3.7 §3.4.1 整节删除改一行引用,本文为判定规则本体唯一正本):①四规则组终版全文(§3.3 重写);②0p 幂等锚正文化(R7 高-2,规则③细则,按实码);③单字段双层形态落文(字段层次终极版,§3.2/§3.3);④级联走查两段(0p→商店/0q→商店)+10 场景走查与 18 边全景图守卫族口径同步(§3.1/§3.6)。上位裁定:触发模型终裁(派生 = 统一 state 内部逻辑,`_swap` 内管线,R1.2 已落地)+ 单版本模型(载体规范归 [journal.md](journal.md) §2-§3,不复写) |
| **R6** | 用户裁定(2026-09-15):遭遇节点入口归类落定 | **定义显式化(零规则改动)**:遭遇节点入口 = **选完事件后的备战画面**(读法二落定);遭遇屏(规则④)先触发 = 过渡检测语义,备战帧(规则①)同序补录恰一次推进不变。§3.3 映射核对表同步 |
| **R5** | 用户节点边界正式定义(2026-09-10 裁定③「判定方案候同步」) | **节点边界定义显式化(零规则改动)**:§3.3 规则组前置「定义基准」节落定义本体(节点推进=进入该节点备战画面(商店开)/补给节点特例=补给选择画面即节点入口/投资策略卡进备战前拿的)+「各腿触发点与定义基准的映射核对」段(备战腿=定义本尊/弹窗腿·0q·0p=过渡标记/补给推进=补给屏);遭遇节点入口归类(遭遇屏 vs 备战帧)两读法占位(定义未裁) |
| **R7** | 用户裁定(2026-09-20 会话五点)+ 三轮对抗收敛 | **触发模型换代:节点推进动作上报化**——定义重立(§3.3-1:推进 = 上一节点终结动作落地,结算确认/补给确认);node_ord 两态生命周期(锚定态 observation/推进态 logic)+ 观察态门(value 在场 ∧ source=observation 双条件);推进生效原语 `advance_node_effective`(序号前进唯一入口,效果尾段同临界区);观察锚定 helper `observe_node_anchor`(四分支处置含首锚定,倒退免疫保留);锚定写端两处(CwScreenPrep/CwScreenSupplyNode);旧四规则组+守卫族+幂等锚+`_note_branch_screen` 五调用点原子退役;BOSS 类型直定迁移 CwScreenBossBriefing;「禁 observe 直写序键」改判(§3.2-7);§3.1/§3.6 按新模型重标注 |
| **R8** | 用户裁定(2026-09-21) | **去证据门修正**:点击即上报,观察态门为唯一门;转移证据机制(探锚/等待/bail/补报)整体退役,原证据集同源条款/上报时点门作废;死点击幻影自愈链申报(§3.3-4);kernel 三原语零改动 |

---

## ① 结论(判定方案一句话版;R7 触发模型换代版)

节点推进 = **上一节点终结动作落地**的上报(用户裁定 2026-09-20;去证据门修正
2026-09-21:点击即上报,观察态门为唯一门):战斗节点终结 =
结算胜利确认(点击「继续挑战」);补给节点终结 =
补给选择确认(确认点击)。实现 = kernel 三原语
(cw_game_state.py):①**`report_node_advance`**(动作路径唯一入口;观察态门 =
`node_ord` value 在场 ∧ source=observation **双条件**,candidate = 生效序 + 1,
trigger 封闭集 = {settle_confirm, supply_confirm});②**`advance_node_effective`**
(推进生效原语 = 序号前进唯一落账入口:candidate 去重守卫 → 写 node_ord——来源由
调用方定,动作上报 = logic 推进未确认态/锚定补推 = observation——→ hist 占位 →
效果推进尾段 `tick_effect_boundary` 同临界区);③**`observe_node_anchor`**(观察
锚定 helper,两写端共用:CwScreenPrep 备战顶栏 / CwScreenSupplyNode 补给屏节点条;
四分支处置 first_anchor / advance 补推 / reanchor / stale_dropped,**倒退免疫保留**
——R<v 不写留证)。观察的角色 = 对账锚定 + 兜底(漏上报自愈),不再是推进触发面。
节点身份 = 节点序 `(plane-1)*9+round_num`(与效果账本 `advance_node` 同坐标系),
位面末节点推进后由序号公式自然跨位面。R1-R6 四规则组(备战/位面过渡/BOSS 简报/
弹窗)、守卫族、缓存守卫、幂等锚、循环分支写点(`_note_branch_screen`)已整体退役
(§3.3-6 退役申报);类型派生权威链保留,BOSS 类型直定迁移至 CwScreenBossBriefing
观察 node(经 `_write_derived_node_type`,简报屏仍是 boss 类型权威)。生效序读口 =
`effective_node_ord` = max(node_ord 现值, node_hist_ord)(派生计算非存储)。载体
规范(单版本事务/渠道签名/行型)单一源 = [journal.md](journal.md) §2-§3,本文不复写。

---

## ② 完成判据对照表

| 任务书要求 | 结果 | 落点 |
|---|---|---|
| 画面流转全景图(全部画面与合法流转,每条边标注判定来源) | 齐:18 条边,逐边标注识别锚、判定值与**所属腿(A/B/零触发)** | §3.1 |
| 节点身份定义(唯一标识;1-1 vs 重入;战斗窗段内相位 vs 独立节点) | 齐:身份=节点序二元键,类型为载荷属性;战斗窗/结算窗/弹窗=同一节点的段内相位 | §3.2 |
| 跃迁判定规则(比较域;重入排除;弹窗插播不误触发;恢复接管首节点语义) | 齐:双腿判定规则本体(腿守卫/弹窗族清单/prev_branch 寄存器)+重入拒绝+恢复局语义 | §3.3 |
| 触发时点与载荷(分派前实现位;载荷字段) | 齐:双腿触发点位(行号锚定);载荷六字段表 | §3.4/§3.5 |
| 边界场景走查(点名三条+战斗失败+同节点多场+boss 节点+无选卡直进) | 齐:10 场景逐个走查,**每屏标注走哪条腿** | §3.6 |
| 第二交付:动作执行 hook 统一形态建议 | 齐:一套登记表+两族触发口+单一落载体+单一消费管道,四条论证 | §3.7 |
| **R4:四规则组终版全文(派生规则单一源)** | 齐:四腿(备战/位面过渡/BOSS 简报/弹窗)+类型派生+共同语义+推进去重键+倒退留证+权威降位重锚;守卫族逐名枚举+0p/0q 出族 | §3.3 |
| **R4:0p 幂等锚正文化(R7 高-2)** | 齐:锚判别/必要性推演(移动中的 effective)/残余边界,按实码落规则③细则 | §3.3 规则③ |
| **R4:单字段双层形态(字段层次终极版)** | 齐:top_bar_raw 观察层+node_ord 逻辑层+effective_node_ord 派生读口(max 非存储)+observe_vs_logic_mismatch 缺陷行 | §3.2/§3.3 |
| **R4:级联走查两段+口径同步** | 齐:「0p→商店」「0q→商店」恰一次推进;S1-S10 与 E1-E18 守卫族口径同步(R3.1 已勘的 E12 边序保持) | §3.1/§3.6 |

---

## ③ 变更面(零代码:流转全景图+判定规则本体)

### 3.1 画面流转全景图(每条边标注判定来源与所属规则;R4 守卫族口径同步)

判定来源速记(R7 触发模型):锚族 = `round_by_find_area`(screen_info id_mark);OCR = `round_by_ocr`;双锚 = 备战「备战标识-购买经验」+「按钮-出战」;**settle** = 结算确认动作上报(`report_node_advance(trigger='settle_confirm')`,宿主 CwOpSettleConfirm)/ **supply** = 补给确认动作上报(supply_confirm,宿主 CwActionPickSupplyOp)/ **锚定** = `observe_node_anchor`(prep = CwScreenPrep 备战顶栏 / supply-bar = CwScreenSupplyNode 补给屏节点条),零 = 不推进不锚定。历史规则①-④注记(R4 版次)随版本链可溯(git 历史);行号 = 历史工作树快照,判读按符号名定位。

| # | 边(从 → 到) | 判定来源(识别锚/判定值) | node_enter 语义(规则) |
|---|---|---|---|
| E1 | 入口链 → 难度确认 → 位面简报(0r) | 「标识-本场对局首领」(:2231) | 零(开局链段;入口链不接上报,0r 自身不触发) |
| E2 | 简报「下一步」→ 位面过渡(0q) | OCR「点击空白处继续」(:2192) | 零(位面过渡腿已退役,过渡屏不推进;跨位面由序号公式随结算确认自然承载,开局同样零) |
| E3 | 位面过渡 → 投资环境(0s) | 「标识-投资环境」(:2247) | 零(开局事件) |
| E4 | 投资环境 → 等 1-1(WaitOneOne)→ 1-1 备战帧 | 「标识-备战阶段」轮询 → 备战双锚(:2277) | **锚定 first_anchor 建 1**(`observe_node_anchor` v=None 首锚定行,写端 = CwScreenPrep 备战顶栏;开局链期间拾取的效果由首拍尾段发放);开局链各屏零推进 |
| E5 | 备战帧 → 备战环(CwScreenPrep)→ 出战 | 双锚 → dispatch(:2932) | 零(节点段内,重入;锚定走 reanchor) |
| E6 | 出战 → 战斗窗 → 结算窗 | 闩 ∨ 帧锚(:2982/:3114) | 零(段内相位) |
| E7 | 结算「继续挑战」→ 下一节点画面 | 下一画面二态:干净备战帧(慢弹窗)∨ 弹窗(快弹窗) | **settle 上报**(CwOpSettleConfirm:点击即上报,零证据探查;上报先于下一节点任何画面分派恒成立) |
| E8 | 结算后自动弹 overlay 族(按节点类型:商店面板 0n/奖励面板/投资策略 0e/遭遇 0c/补给/巨星/祈愿;可链式连弹,currency_war_prep.md:32) | 各分支锚(:1840/:1868/:1877/:2058 等) | 零推进零锚定(弹窗 = 下一节点段内子阶段;弹窗腿已退役,弹窗族画面不再推断推进) |
| E9 | 弹窗选完 → 回备战帧 | 备战双锚 | reanchor(等值锚定:CwScreenPrep 观察 node;若动作推进未被锚定过则走首锚定/补推分支) |
| E10 | 补给节点:备战帧(nodeseq current=supply)→ 点「返回补给阶段」→ 补给屏;或补给屏先被采到(自动弹形态) | 双锚+`read_node_sequence`(:2821)→「标识-补给阶段」(:1877) | 补给屏节点条锚定(CwScreenSupplyNode 读「备战阶段 X-Y」节点条 → `observe_node_anchor`;自动弹形态唯一锚定点);divert 形态备战帧已锚,本读数走 reanchor/stale_dropped 分支 |
| E11 | 补给确认 → 下一节点干净备战帧(无战斗无结算) | 备战双锚 | **supply 上报**(CwActionPickSupplyOp:确认点击即上报,零证据探查;漏报形态由观察锚定补推自愈) |
| E12 | boss 段(边序:奖励关结算 → BOSS 简报(0p)→ 商店自动开(0n)→ boss 备战帧 → 出战 → boss 结算;实证 = screen_flow_timing.md #26「奖励关后 BOSS 关前」) | 「标识-阵营徽记」(模板锚)∨ 共享判别 | **settle 上报推进 (p,9)**(奖励关结算确认;点击即上报 ⇒ 上报先于简报 op 分派 ⇒ hist 已在 boss 节点);BOSS 简报屏 = **类型直定屏**(CwScreenBossBriefing 观察 node 经 `_write_derived_node_type` 直定 boss 类型,目标 = 已推进 hist——无证据等待 ⇒ 上报先于任何画面渲染 ⇒ hist 已在位,错位形态结构性不可达),零序号推进;boss 备战帧 reanchor;boss 结算零触发(E6 同段内相位) |
| E13 | boss 结算 → 位面过渡(0q)→ 下位面 2-1 画面 | 0q(:2192)→ 备战双锚或弹窗 | **settle 上报推进 (p+1,1)**(boss 结算确认;序号公式 9+1=10 自然跨位面,位面过渡屏零推进零锚定);下位面备战帧 reanchor(漏报/幻影形态由锚定补推/等值 reanchor 自愈) |
| E14 | 恢复接管 → 首画面(备战帧/弹窗/战斗窗/锁定态) | 恢复检测(:1711) | 备战帧 → **锚定 first_anchor**(恢复重建);弹窗 → 零(弹窗腿已退役,无推断);锁定臂(直出战)→ 首次结算确认被观察态门 value 守卫挡,战后备战帧锚定补齐 |
| E15 | 终局:团灭/HP 归零 → 挑战失败屏 → 大厅(3c) | 「标识-挑战结束」/「标识-创业指南」(:3127/:3008) | 零(对局结束) |
| E16 | 节点内插播 overlay 族(0a0/0a/0a2/0a3/0a4/0b 巨星/0d/0e2/0e3/0f 武装箱·消耗品/0g/0h 祈愿/0i/0k/0t/1b/1d/1g) | 各自 id_mark 锚(0 系序位全在备战双锚前) | 零(非节点边界标记,无推进无锚定) |
| E17 | 暗色锁定返回(0m)→ 回 overlay | 「按钮-返回XX选择」(:2020-2024) | 零(重入;无推进面) |
| E18 | 弹窗中途介入备战环 | 环内 event_overlay 检测 bail → 0x 接管 | 零(本节点已推进) |

### 3.2 节点身份定义(身份键沿 R2 无修订;R4 补状态面落点)

**身份键 = 节点序 `node_ordinal = (plane-1)*9 + round_num`(基 1)**,二元可逆。裁决依据(要点重述):

1. 类型不入身份键(属性入载荷;类型读法多源不入键防第二失真源)。
2. 坐标系与效果账本同源:`advance_node` 去重键同一公式(cw_effect_inventory.py:226;cw_loop :2299-2300);`AnchorEvent.node_seq` 即该键(cw_anchor.py:108)。**cw_anchor 行号快照注(适用全文三处引 :108/:270/:405 段)**:该文件已入库(commit 4985b6b88,2026-09-11;原「未入库易失」警示随入库退役)——引用值为入库前工作树快照,行号判读按符号名定位,终裁对照以本文行内名单为准。
3. 跨位面连续(1-9→9,2-1→10),推断腿(last+1)跨位面自洽。
4. 开局 1-1 vs 重入 1-1:寄存器 None→首节点必触发一次;重入零触发。
5. 战斗窗/结算窗/弹窗 = 同一节点的段内相位(玩法:一节点一战;弹窗在下一节点渲染后弹出,归属其段内,currency_war_prep.md:32)。
6. 坐标系粗糙面(invest-env 增节点)为既有面信息项,见 §5-③.4。
7. **序键状态面落点(R7 两态版;R4「字段层次终极版」改判)**:身份序在统一 state
   的载体 = 单字段双值——`top_bar_raw`(Field[str],观察层,顶栏原文,observe()
   写点 = 观察汇聚,原文缺读不写禁猜)+ `node_ord`(Field[int],**两态字段**:
   **锚定态 observation** = `observe_node_anchor` 解析读数直写——节点域专用口,
   不经通用 observe(),先例 = `settle_truth` 的结算金币专用口;**推进态 logic**
   = 终结动作上报经 `advance_node_effective` 写入,未确认态);`node_hist_ord`
   = 单调水位 + 去重第二道防线(工程结构位,run 段随容器重基线);**生效序读口
   = `effective_node_ord` = max(node_ord 现值, node_hist_ord)(派生计算,非存储
   字段;kernel cw_game_state.py)**——本方案的判定与比较域统一在该读口。观察
   覆盖 logic 失配的通用三分流(`observe_vs_logic_mismatch` 缺陷行)不辖节点域
   ——helper 四分支显式处置(§3.3-4)。
   **「禁 observe 直写序键」改判记录**:R4 字段层次终极版曾裁决四腿全部
   write_logic、禁 observe 直写序键;R7 推翻——解析顶栏/节点条序号与解析 OCR
   金价同类,归读数不归推算,observe 直写取得合法位(锚定态,含 v=None 首锚定行);
   旧裁决全文随 R4 版次入 git 历史。

### 3.3 跃迁判定规则(规则本体;R7 = 动作上报 + 观察锚定模型,派生规则单一源)

> 本节 = 派生规则唯一正本。R7(2026-09-20)触发模型换代:节点推进 = 终结动作
> 上报;观察 = 对账锚定 + 兜底。R1-R6 四规则组(备战/位面过渡/BOSS 简报/弹窗)、
> 守卫族、缓存守卫、幂等锚、恢复局禁用分支整体退役(本节 6 号退役申报),历史
> 版次全文按本文版本链惯例可溯(git 历史)。实现锚 = kernel `report_node_advance`/
> `advance_node_effective`/`observe_node_anchor`(cw_game_state.py)。

**1. 节点边界定义(R7 重立,取代 R5「定义基准」)**:

> **节点推进 = 上一节点终结动作落地**:战斗节点终结 = 结算胜利确认(点击
> 「继续挑战」);补给节点终结 = 补给选择确认。位面末节点推进后由序号公式
> `(plane-1)*9+round` 自然跨位面(9+1=10=(2-1)*9+1)。备战画面从「推进触发面」
> 改判为**对账锚定面**(锚定写端 = CwScreenPrep 备战顶栏);补给屏节点条同为
> 锚定面(补给「自动弹」形态——无本节点备战帧——的唯一锚定点)。开局首节点
> (E4,无前置终结动作)由观察锚定建 1(首锚定行);入口链(cw_entry/)不接上报。

**2. 动作推进路径(`report_node_advance`,动作路径唯一入口)**:

- **观察态门(双条件)**:`node_ord.value is not None ∧ node_ord.source ==
  'observation'` 才放行——只有已被观察确认的节点值才允许被动作推进;门挡 →
  obs_event arbitrate 留证零推进,重复上报/重试/任何第二条动作推进路径在结构上
  不可能。**value 守卫不可省**:Field 缺省 source='observation',新容器 =
  (None, 'observation'),只查 source 会在开局/恢复局首锚定前误放行,且
  candidate = effective+1 在生效序 None 时无定义。恢复局锁定臂(直出战)首次
  结算确认被 value 守卫挡,由战后备战帧锚定补齐(绝对值坐标,无 skew)。
- candidate = 生效序 + 1 → 经推进生效原语落账(source=logic,推进未确认态;
  观察锚定同值翻回确认态后,下一节点终结动作由此解锁)。trigger 封闭集 =
  `{settle_confirm, supply_confirm}`(集外显式炸错;journal 行 actor 归因映射 =
  kernel `_NODE_ADVANCE_ACTORS`:settle_confirm→CwOpSettleConfirm /
  supply_confirm→CwActionPickSupplyOp)。
- **触发点 1(结算确认;去证据门,用户裁定 2026-09-21)**:`CwOpSettleConfirm`
  (operations/cw_op/)——点击「继续挑战」(含长按兜底重试)→ **点击即上报**
  settle_confirm → 交回宿主。无证据等待 ⇒「上报先于下一节点任何画面渲染」
  恒成立 ⇒ boss 流「上报落后于流转、简报直定写错节点」结构性不可达——
  简报 op 被分派时 hist 已在 boss 节点,类型直定目标恒正确。重复/死点击
  重试上报被观察态门挡(零推进零危害);战败分支不进本 op、不推进(终局
  流转 E15,节点序止于终局)。
- **触发点 2(补给确认)**:`CwActionPickSupplyOp` 点「确认」→ 补给参数
  上报 + supply_confirm 推进上报直接双发(点击即上报,零证据探查);漏报
  形态兜底归观察锚定。
- **报告时点语义**:两触发点均**点击即上报**(用户裁定 2026-09-21)——
  无证据等待 ⇒ 上报先于下一节点任何画面渲染;**「先推进后选卡」结构性
  成立**(确认点击先于下一节点一切弹窗与选卡,由因果序保证,不依赖任何
  弹窗推断)。

**3. 推进生效原语(`advance_node_effective`,序号前进唯一落账入口)**:

candidate 去重守卫(candidate ≤ 生效序 → 零推进返回,同序幂等;生效序 None =
本 run 首建,任何正 candidate 放行)→ 写 `node_ord`(**来源由调用方定**:动作
上报路径 = logic(推进未确认态,观察态门语义的前提);锚定补推路径 = observation
(观察采新即确认态——否则补推后源态落 logic,下一次终结动作被门误挡一拍))→
`node_hist_ord` 占位(单调水位)→ **效果推进尾段 `tick_effect_boundary`**
(账本 `advance_node` + 在场效果盘点发放 `grant_effect_node_refresh_balance`
+ 容量重锚 `project_effect_capacity`)同临界区执行。锚定补推(R>v/首锚定)同样
是真实节点进入(恢复局/锁定臂/漏报的自愈路径),序号前进不经尾段即该节点
每节点发放永久丢失。锚定直写不经通用 observe()(R>v 与 logic 现值失配会误入
观察覆盖失配安灯三分流;节点域专用写口,先例 = `settle_truth`)。

**4. 观察锚定(`observe_node_anchor`,两写端共用 helper;处置规则 R = 本帧读数,
v = `node_ord` 现值)**:

| 分支 | 处置 | 依据 |
|---|---|---|
| v = None(首锚定) | 任何 R 经原语落账(observation 确认态)+ 尾段发放 | E4 开局建 1 / 恢复局重建;开局链期间拾取的效果由此首拍发放 |
| R > v | 观察采新经原语(observation;**补推 = 漏上报自愈**,序号前进同帧拿到尾段发放) | 绝对值坐标,采新即对齐 |
| R == v | 直写同值(observation;**logic 态此分支 = 动作推进被观察确认翻锚定态**,下一节点终结动作由此解锁) | 重复上报解锁即锚定语义 |
| R < v | 不写 + obs_event arbitrate 留证(**倒退免疫**) | 节点序局内单调(游戏无回退流转,E15 终局除外);R<v 只能是读数误读或幻影推进残余 |

锚定写端两处:①**`CwScreenPrep` 观察 node**——备战 heavy 观察回执携带本帧
plane/round → `observe_node_anchor(node_ordinal_of(plane, round))`;读数链
(`read_phase_round` 直读/缓存/守卫)语义不变;外循环仲裁路径的 prep_clean 漏斗
读不挂锚定(纯域读数,无缺口);读数缺位/局外 = 跳过禁猜。②**`CwScreenSupplyNode`
观察链**——补给屏顶部居中节点条「备战阶段 X-Y」读数 → 同一 helper(该读数位
不在 A_PHASE 识别区,screen_info 单独建档)。

**幻影推进残余的自愈路径(申报;去证据门形态 2026-09-21)**:点击即上报下
死点击会产生幻影(上报了但游戏没走)——残余形态 = logic 值领先一档,后续
锚定被 R<v 分支挡住、下一次终结动作被观察态门挡、再下一节点观察 R>v 补齐
——绝对值坐标保证无累积误差,偏差有界一个观察周期;重复上报被观察态门挡
(零推进零危害)。效果账本不回滚(`grant_free_refreshes`
计数器与 `advance_node` 递减无逆操作;接受该残余边界并留证——最坏损失 =
幻影那次多 tick 一档发放,金额正确、时机略早、期间无消费方,用户已知悉)。

**5. 类型派生(权威链保留,触发点随 R7 迁移)**:节点类型 `node.kind` 权威链 =
①专属画面直定——**BOSS 简报屏 `CwScreenBossBriefing` 观察 node**(经 kernel
`_write_derived_node_type` 直定 boss 类型;目标 = 已被奖励关结算确认推进的 hist
节点。**直定前置守卫**:直定仅在简报 op 被分派(锚命中)时发生,而点击即上报
(2 号)⇒ hist 已在 boss 节点——「hist 落后、
类型写错节点」的形态结构性不可达;简报屏重入走冲突纪律幂等,零序号写)+ 
②商店查现行链(接口 = `chain_node_type`,链写端在役:过渡屏 + 备战帧整帧覆盖;
店开上下文查链直定 node.kind,链缺位/位越界/未辨 = 零写诚实缺位,零内建回落;
查链目标 = 生效序与观察镜像的最新者,`_shop_panel_type_target`)+ ③冲突留证
(类型两源不一致 → obs_event arbitrate,最新直定赢)。直定写入带**倒退免疫**
(镜像序 > 目标序 = 镜像更鲜活,丢弃留证不写,类型由镜像源承接)。遭遇/补给/
策略的休眠类型直定路径(`SCREEN_NODE_TYPE_DIRECT` 三成员)随弹窗腿退役(生产
从未有写点,零行为变化)。

**6. 退役申报(R7 原子切换,一次提交完成、无并存窗口)**:

| 退役物 | 原位置(符号锚,kernel/cw_game_state.py 除注明外) |
|---|---|
| 弹窗腿本体 + 守卫族/弹窗族常量 + 缓存守卫 + 恢复局禁用分支 | `_derive_node_inferred`、`SCREEN_CONTEXT_POPUP_FAMILY`、`SCREEN_CONTEXT_GUARD_PREV` |
| 位面过渡腿 | `_derive_node_plane_transition`(跨位面由序号公式承载,1 号) |
| BOSS 简报腿 + 幂等锚(序号半部) | `_derive_node_boss_brief`(类型直定半迁移至 CwScreenBossBriefing,5 号) |
| 循环分支写点 ×5 | `cw_loop._note_branch_screen` 及五处调用点(BOSS简报/位面过渡/简报/投资环境/等待1-1);开局链五屏上下文对写入随之消失——零逻辑消费,journal 画像面变化 |
| 休眠类型直定三成员 | `SCREEN_NODE_TYPE_DIRECT` 遭遇/补给/策略行 |
| 备战腿派生写法 | `_derive_node_observed` 的 write_logic 写法 → 锚定 helper(4 号) |

观察漏斗(`obs/cw_observation.py::read_game_state`)退出**推进域**写入
(`node_ord`/`node_hist_ord`);上下文对、`top_bar_raw` 观察层、`gs.node` 观察
镜像写端**保留在漏斗**(观察域,供决策层坐标消费与 journal 审计——字面拆掉
gs.node 镜像即决策坐标全断)。由此录帧(`cw_match_recorder` 经漏斗)与一切旁路
读数零推进副作用。

**7. 与效果账本的关系**:节点推进触发的效果推进段 = 推进生效原语尾段同临界区
(3 号),推进键 = `effective_node_ord` 派生读口;发放面 = 在场效果盘点(每节点
余额),拿卡登记期发放语义归选卡登记点(burst 桥 `apply_effect_burst_grant`;
逐效果「拿卡当节点是否即享」核查结论单一源 = docs/game/currency_war/research/
per_node_pick_node_grant.md,效果域细则 = [effect-domain.md](effect-domain.md)
§7.3)。

**单一源边界申报**:本文管**判定规则本体**(触发面/候选值/门与守卫/推进语义/
类型派生);**载体规范(版本 id 分配/单版本事务与原子性/渠道签名/行型 schema/
落盘)单一源 = [journal.md](journal.md) §2-§3**(载体规范),本文不复写、不设副本。

### 3.4 触发时点与实现位

> **R4 状态申报(置顶)**:本节分派面 fire 接线(v1 §3.4 形态:分支体内直呼
> emit_anchor)**已被触发模型终裁取代**——派生规则 = 统一 state 内部逻辑,实现 =
> kernel `observe_screen_context` 派生管线(喂入 = read_game_state 漏斗
> `read_game_state` + cw_loop 分支写点五调点,R1.2 已落地);裁定 2「分派前:
> 先完成计数推进,再做本节点动作」由**派生在同一观察写入临界区内完成、先于本
> 节点任何动作 op**继续兑现(§3.3 引言)。本节以下保留作触发点位的历史判定依据
> (哪些画面/哪些时点);锚行/emit_anchor/battle_done 同批原子切换等形态归
> exogenous 退役表处置候裁(原遥测设计件(已删,git 可溯) v3.7 §3.6.1/§5-7),本方案不再扩写。
> **R7 注**:派生管线形态亦已随触发模型换代退役(§3.3-6);现行 = 动作上报 +
> 观察锚定,本节以下保留作历史判定依据。

- **实现位 = 两处,均在 `_dispatch_screen_op` 调用前**(裁定 2「分派前」):①W1 备战分支体内(tick 后、达标臂前,≈:2322 后);②弹窗族清单四分支体内(0c :1840 前/0e :1868 前/补给 :1877 前/0n :2058 派发前)。
- **形态 = 分支体内直呼 `kernel/cw_anchor.emit_anchor`**(模块级函数;H6 选型建议=候选②轻量变体,沿 R2 论证:emit_anchor API 不收宿主对象、候选①需未构造实例、候选③依赖未落地收编批)。**防散点封闭面(R3)**:emit_anchor('node_enter') 全仓合法调用点白名单 = W1 ∪ 弹窗族四分支,单点清单测试锁(集外 = 红,同 §12.4-B①)。
- **实施纪律(沿 R2,裁决③)**:现役 battle_done 行(cw_screen_battle_wait.py:365-368,出节点语义)与新锚**同批原子切换**,禁并存窗口(同节点两行破坏指标 3);归 H2/schema 修订批排程,未排程前新锚不得先接线。
- **正本收敛申报(沿 R2 更新)**:建议实施批落文时在 §12.2 node_enter 行补玩法时序句(「先渲染备战再自动弹;弹窗可快于 bot 采样,触发双腿=备战帧直读∨弹窗段首推断」,出处 currency_war_prep.md:32),归 H2/锚实现批①落文面;本批不动正本。
- **与在飞迁移衔接(沿 R2)**:批 4 任务书锚定 W1 位次不得位移;本方案不依赖 progressed/applied 回执,与 M1 终裁零冲突。腿 B 分支体在清查批 1(C 族 confirm 验关拆除)改动面内(0c/0e/补给确认链),腿 B 落位=分支命中后 dispatch 前,批 1 改的是 op 内验关段——不重叠,申报批 1 任务书知会即可。

### 3.5 载荷字段(node_enter 锚行;R3 恢复两字段)

> **R4 状态申报**:本节 = 锚行形态(§3.4 状态申报所指)的载荷设计;派生管线形态
> 下,推进事实的落账映射由原遥测设计件(已删,git 可溯) v3.7 §3.4.2 承载(推进值/来源腿/前值/触发
> 画面/节点类型各归行字段),本节不再扩写,保留作锚行形态候裁时的参考。

时点键(plane/round/node_seq)按 §12.3 七字段封装;payload 逐槽:

| payload 字段 | 值 | 来源(零新读屏) |
|---|---|---|
| `from_ord` | 上一节点序;首节点/恢复局 = None | loop 寄存器 last_entered |
| `trigger_screen` | prep_frame / encounter_screen / invest_overlay / supply_screen / shop_panel(=触发腿与分支;R3 恢复该字段——两腿形态下有区分度) | 触发分支 |
| `node_type` | battle/encounter/supply/reward/boss | 腿 A:主 = nodeseq current 槽(`read_node_sequence`+`read_node_type`),读缺 = 位面节点台账兜底,再缺 = None;腿 B:主 = 弹窗类型映射(遭遇屏→encounter/补给屏→supply;商店面板→台账,battle/boss 不可分;投资策略屏→台账),同一台账兜底,再缺 None(「按节点类型弹对应 overlay」currency_war_prep.md:32 为映射机制依据) |
| `tick_status` | ticked(腿 A:tick 挂点已先行)/ pending(腿 B:候本节点备战帧 tick;消费侧按 status 分) | 触发腿(R3 恢复该字段) |
| `tick_expired[]` | 到期移除效果 spec 名清单(腿 A 恒可得;腿 B 恒空=待 tick) | `advance_node` 返回 expired(:2301;「递减未到期」清单现役不产出,候 H2) |
| `from_resume` | 恢复局首节点 = true | 恢复检测确认点(cw_loop;容器旗标 resumed_match 已退役) |

`effect_ref` 槽位恒空(§12.0 结构锁不变)。

### 3.6 边界场景逐个走查(R7 按动作上报 + 观察锚定模型重写;每屏标注推进/锚定通道)

| 场景 | 走查(R7 模型) | 结论 |
|---|---|---|
| S1 开局进 1-1(点名 a) | 等待1-1 → 1-1 备战帧锚定 **first_anchor 建 1**(CwScreenPrep 顶栏,开局链各屏含过渡屏零推进;开局链期间拾取的效果由首拍尾段发放) | ✓ 恰一次 |
| S2 结算后进选遭遇/选投资策略(点名 b) | 快弹窗(备战帧零采样):结算确认**点击即上报** → **settle 上报** → 弹窗分派在推进后(「先推进后选卡」由因果序保证);慢弹窗(备战帧先采到):锚定 reanchor → 弹窗 = 子阶段零参与 | ✓ 恰一次,先推进后选卡 |
| S3 选补给后直进下一节点备战(点名 c) | 形态一(divert,备战帧先):补给节点备战帧锚定 → 补给屏节点条 reanchor → 确认 **supply 上报** → 下一节点备战帧 reanchor;形态二(自动弹,无本节点备战帧):结算确认直接弹补给屏 → 补给屏节点条锚定(唯一锚定点)→ 确认 supply 上报 | ✓ 两形态各节点恰一次 |
| S4 战斗失败 | 无节点级失败态(gameplay/currency_war.md)→ 无退回重打流转;HP 再归零 = E15 零触发 | ✓(零触发) |
| S5 同一节点内战斗多场 | 机制不存在(一节点一战);重复上报被观察态门结构性挡(推进后 source=logic,第二次上报门挡留证) | ✓ 零多触发 |
| S6 位面 boss 段 | 奖励关备战帧锚定;奖励关结算确认(**点击即上报**)→ **settle 上报 (p,9)** → 简报屏类型直定(目标 = 已推进 hist,直定前置守卫)→ 商店/备战帧零序号推进(boss 备战帧 reanchor)→ boss 结算确认 → **settle 上报 (p+1,1)**(序号公式跨位面)→ 2-1 备战帧 reanchor | ✓ 同构无特判 |
| S7 结算后无选卡直接下一节点 | settle 上报(点击即上报);商店面板 = 子阶段零参与 | ✓ |
| S8 弹窗插播 | E16 插播族/E17 暗色锁定/E18 环内 bail 全部零推进零锚定(弹窗腿退役,无推断面) | ✓ 零误触发 |
| S9 恢复接管 | 备战帧先 → 锚定 **first_anchor**(恢复重建);弹窗先 → 零(无推断面)→ 消化后备战帧锚定;锁定臂(直出战,无备战帧)→ 首次结算确认被 value 守卫挡 → 战后备战帧锚定补齐,绝对值坐标无 skew | ✓ 首节点恰一次 |
| S10 补给 divert vs 自动弹竞态 | 自动弹形态:结算确认点击即 settle 上报;补给屏节点条锚定兜底;divert 形态:补给节点备战帧已锚定 → 补给确认 supply 上报过门 | ✓ 两形态收敛 |

**级联走查两段(R7 重写;旧「级联双推进」破口随弹窗腿退役结构性消失——推进唯一
动作入口 + 观察态门,「专用腿推进后弹窗腿再 +1」形态不复存在)**:

| 走查 | 序列 | 恰一次推进论证 |
|---|---|---|
| 级联 A:奖励关→简报→商店 | 奖励关结算确认(**settle 上报 (p,9)**)→ 简报屏类型直定(零序号写)→ 简报关闭商店面板被采到 | 商店面板零推进(弹窗腿退役);推进后 source=logic,任何重复上报被观察态门挡;简报屏多 loop pass 重入 = 类型直定幂等(冲突纪律) |
| 级联 B:boss→下位面商店 | boss 结算确认(**settle 上报 (p+1,1)**)→ 位面过渡屏 → 下位面商店面板块被采到 | 过渡屏零推进(位面过渡腿退役,跨位面由序号公式承载);商店面板零推进;下位面备战帧锚定 reanchor 同序 |

### 3.7 第二交付:动作执行 hook 统一形态建议(沿 R2,窗口面随 R3 更新)

> **R4 状态申报**:本节 = hook 时代的锚行架构建议;hook 线现状 = 销案口径
> (机制不建,用户销案裁定 2026-09-10「hook 销案同意并记录」;订阅两层设计+销案
> 依据归 hook 线结论文档,候写、本批不写)——本节保留作历史论证存档,不再
> 扩写;R7 现行触发模型见 §3.3(动作上报 + 观察锚定,无独立 hook 框架)。

**建议:一套登记表(ANCHOR_REGISTRY)+ 两族触发口(动作族=op 基类发射口;边界族=分派面触发窗口白名单)+ 单一落载体(emit_anchor→ExogenousEvent 流)+ 单一消费管道(遥测)。不建第二注册表,也不建统一订阅 dispatcher。**

论证四条(R2 定版,要点重述):

1. **动作锚触发时点在 op 执行路径内,边界锚不在**:动作锚触发时点天然在画面 op 决策动作 node 的执行路径内(动作发射点即触发点);boundary 触发点实证不在任何 op 执行路径内(H6/对抗审发现 5)——两层触发口是机制事实的投影,硬塞实例注册表需「分派点持未构造实例」时序倒置。
2. **防双源判据=登记/落盘/消费三面统一**:登记一套(ANCHOR_REGISTRY+触发时点型列,H1 已裁决)、落载体一套(emit_anchor 单一出口)、消费一套(遥测单管道 §12.5-7)——触发位置分族不产生第二记录路径。
3. **消费者订阅便利=零分叉**:消费只按 anchor_id,族别已在型列申报;dispatcher 抽象当前无第二消费者(账本挂点语义独立,§12.5-6 接线非订阅),收益零成本一,不取;A9 效果施加批出现多消费者时再演进,升级面收敛在 emit_anchor 出口。
4. **封闭集纪律两族统一**:边界族白名单 = R3 双腿窗口(W1 ∪ 弹窗族四分支);动作族=注册表内声明。同题同构。

与 M1 终裁衔接:动作族按单一发射口形态申报;boundary 族不依赖动作回执,零波及。

---

## ④ 验证记录(逐边核对凭据;2026-09-10 工作树实测)

**A0. R4 增补实测(kernel = `src/sr_od/application/currency_war/kernel/cw_game_state.py`;2026-09-11 工作树)**

| 凭据项 | 实证 | 结果 |
|---|---|---|
| 派生管线固定次序(规则落地形态) | observe_screen_context :1599-1685:上下文对→top_bar_raw 观察层(:1653-1655)→节点域判定(① :1658-1661;④ :1662-1666,elif 互斥;② :1667-1671;③ :1672-1675)→类型派生(:1676-1685,0p 跳过防双写) | PASS(§3.3 引言) |
| 规则①备战腿(逻辑层+倒退静默现状) | _derive_node_observed :1732-1754:candidate<hist 静默跳过(G10 留证未落码,差异在原遥测设计件(已删,git 可溯) §3.7.2-M3);同序照写+hist 占位 | PASS |
| 规则②三源序+禁猜 | _derive_node_plane_transition :1836-1868:phase_round→gs.node 镜像 plane→hist 反解((hist-1)//9+1);plane 缺→return 禁猜(:1860-1861);候选≤hist 拒绝(:1863-1864);零类型写 | PASS(C-3 终裁) |
| 规则③幂等锚 | _derive_node_boss_brief :1871-1908:effective 缺禁猜(:1892-1893);镜像已锚 hist 且类型 boss→零写返回 hist(:1894-1897);候选=effective+1(:1898);双写 node_ord+kind 同 actor 同批(:1901-1907);docstring 幂等锚必要性+残余边界自证 :1878-1888 | PASS(R7 高-2 正文化依据) |
| 规则④弹窗腿守卫族+缓存守卫 | _derive_node_inferred :1757-1793:守卫族=SCREEN_CONTEXT_GUARD_PREV;恢复局禁用 :1777-1779;开局候选=1 :1780;c 缺位/≠hist 拒绝 :1782-1785;候选≤hist 拒绝 :1787-1788 | PASS |
| 守卫族逐名终版 | SCREEN_CONTEXT_GUARD_PREV :140-146 = {货币战争-战斗等待, 货币战争-简报, 货币战争-投资环境, 货币战争-等待1-1};0p/0q 出族申报注释 :135-139 | PASS |
| 生效序读口 | effective_node_ord :1721-1729 = max(node_ord.value, node_hist_ord),派生计算非存储,None 安全 | PASS(§3.2-7) |
| 单字段双值载体 | top_bar_raw Field[str] :1195-1200(观察层,唯一写点=观察汇聚,缺读不写禁猜);node_ord Field[int] :1201-1208(逻辑层,四腿全部 write_logic,无 observe 直写序键例外) | PASS(修订节·字段层次终极版) |
| 失配缺陷行 | _emit_defect :538-556(默认 kind='observe_vs_logic_mismatch');observe 覆盖 logic 失配触发 :1423-1426 | PASS(§3.2-7/规则四③) |
| 商店类型查链缺位 | chain_node_type:链写端已在役(链观察落地批),token 按现行链实值,缺位/位越界/未辨 = None 诚实缺位,零内建回落 | PASS(规则四②已接线) |
| R1.2 纠偏三连在账 | 纠偏三连回执 2026-09-10(迁移批修订四条终裁):守卫族收缩终版+0p/0q 出族+级联破口锁两条 → 单字段双层(双专名作废)→ 字段层次终极版(top_bar_raw 新增,observe 直写序键路径拆除,四腿全 write_logic 零残留,锁 21 条) | PASS |

**A. 实码核对(文件:行号)**

| 凭据项 | 实证 | 结果 |
|---|---|---|
| 备战双锚判定(腿 A 判定面) | cw_loop.py:2277-2278 | PASS |
| tick 挂点位置与键 | cw_loop.py:2289-2322;键式 :2299-2300;守卫注释 :2291-2292 | PASS |
| 账本守卫语义 | cw_effect_inventory.py:217-254(==去重 :240-241;登记当节点不推进 :247-249;tick_node :256-259) | PASS |
| 腿 A 落位后的动作序位 | 达标臂 :2323 起;锁定臂 :2783-2806;守卫 :2475 起;清场 :2854-2882;dispatch :2932-2934——均在 W1 后 | PASS |
| **腿 B 弹窗族四分支** | 0c :1840-1844;0e :1868-1873;补给 :1877-1889;0n `_shop_open_anchors_hit` :2058-2081——命中即 return,序位全在备战双锚前 | PASS |
| 战斗窗双入口 | :2982-2983;`_frame_in_battle_window` :3114-3135(腿 B prev_branch=战斗等待的来源分支) | PASS |
| 0q/0s/0p/大厅/兜底 | :2192-2224;:2247-2270(链 WaitOneOne :2267-2270);:2169-2183;:3008;:3086 | PASS |
| 恢复检测 | :1702-1719;锁定探针 :2748-2782(腿 B 禁用判据=恢复检测态复用) | PASS |
| 现役 node_enter 行(出节点) | cw_screen_battle_wait.py:365-368(原子切换实施纪律对象) | PASS |
| read_phase_round 直读/缓存/守卫 | cw_observation.py:904-989(兜底 :986-989;单调 :957;值域 :952;reset :917-921)——腿 A 直读通道;腿 B「c==last」守卫的读数面 | PASS |
| emit_anchor/幂等/登记表 | cw_anchor.py:294-375(幂等 :357-363;effect_ref :331-335);node_enter 行 :192-203(host=「画面分派点」——双腿语义兼容) | PASS |
| 台账 node_type 兜底先例 | flow.py:300-344(_alarm_node_type_fallback→ledger_node_type) | PASS |
| dispatch 包装分工 | cw_loop.py:1418-1481(触发点落分派面与 分工一致) | PASS |

**B. 正本与玩法文档核对**

| 凭据项 | 实证 | 结果 |
|---|---|---|
| 自动弹窗机制原文(腿 B 的机制出处) | currency_war_prep.md:32:「节点结束后游戏先渲染备战画面,再按下一节点类型弹出对应 overlay(可链式连弹);备战锚点在 overlay 下仍可透出命中」——R3 勘误后该原文的**正确用法**=弹窗类型全集与链式形态的机制依据(「按节点类型弹」=腿 B node_type 映射依据);其渲染顺序**不再**被当作 bot 采样顺序使用(R2 勘误根因,见 §5-①) | PASS(用法勘误申报) |
| §12.2 node_enter 行 | :1164(boundary/tick 回执/同节点重入难点原文);正本收敛申报见 §3.4 | PASS |
| §12.6-H6 三候选 | :1355-1362;本方案选型建议(候选②轻量变体)白名单=R3 双腿窗口 | PASS |
| §12.5-6 账本接线 | :1311-1315 | PASS |
| §3.4 过渡相位类 | :359-388(E1/E2/E12/E16 零触发语义一致) | PASS |
| §3.2 奖励节点无独立画面 | :330(奖励面板随备战帧→腿 A,无独立分发分支的佐证) | PASS |
| §2.2 十屏事件族 | :230 | PASS |
| 玩法机制(无节点级失败态) | gameplay/currency_war.md:36 | PASS(S4) |
| 流转时序 | screen_flow_timing.md:76/79/90/93(#14「选完投资策略→回备战自动开店」=弹窗消化回备战形态);R3.1 补:#26(#90 行)「奖励关后 BOSS 关前」钉死 0p 边序 + #14/#27(自动开店触发源含「BOSS 简报关闭后」)+ #29(boss 结算→位面过渡→下位面照常开店) | PASS(R3.1 勘误后) |

**C. 视觉实证(测试仓画面档案)**

| 项 | 实证 | 结果 |
|---|---|---|
| V1 遭遇 overlay 帧无进度屏显 | 货币战争-遭遇节点/default.webp:左上无 X-Y,无节点条 | PASS(**腿 B 必须推断的直接依据**——弹窗帧直读必走缓存) |
| V2 投资策略 overlay 帧同 | 货币战争-投资策略/default.webp:顶栏无 X-Y 无节点条 | PASS(同上) |
| V3 补给屏节点条屏显(位置不同) | 货币战争-补给/default.webp:顶部居中节点条「备战阶段 1-5」+9 槽+当前槽「补给」标签;不在 A_PHASE 识别区(解释 cw_loop :1613 缓存注释);腿 B 不依赖该读数(推断制),记录为归因真值源候实现批扩 area | PASS |
| V4 备战帧顶栏可读 | read_game_state 每轮消费(正本 §2.3) | PASS(腿 A 直读依据) |

---

## ⑤ 偏差与未尽事项

### ⓪R7 申报(触发模型换代批)

0. **历史小节读法**:⑤-①/②/③ 与 §3.4/§3.5/§3.7 为四规则组时代的历史论证,
   状态申报已置顶各节;判定语义以 §3.3(R7)为准。
0.5 **挂账清账**:四规则组关联挂账项随整体退役销账——弹窗族清单保守化残留
   (⑤-③.1)与镜像窗口前提申报 ×2(§3.3 旧文)随守卫族消亡失去载体;倒退留证
   现由锚定 helper R<v 分支在产(§3.3-4);权威降位重锚不再需要(观察锚定模型
   下绝对值坐标采新即对齐,无「推断误 +1 钉死权威源」形态);坐标系粗糙面
   (invest-env 增节点漂移,§⑤-③.4)为既有信息项维持挂账。

### ⓪ R4 申报(升格批新增;序在最前)

0. **术语映射(读历史小节前)**:本文 R3 及之前所称「**腿 A/备战帧腿**」在 R4 =
   **规则①**,「**腿 B/弹窗腿**」= **规则④**(§3.3);⑤-①/②/③ 等历史小节
   保留原措辞不改写,读法按本映射。
0.5. **收账(v3.8-低-2)**:测试锁 `test_popup_leg_boss_briefing_predecessor_advances`
   已按本文 R4 语义重写,守卫族含 BOSS简报的旧断言不再成立(凭据 =
   sr-od-test/test_cw_state_journal.py:531-547)——§⑤-③.2 挂账项销账。
1. **倒退留证/权威降位重锚/N=2 载体未落码**:§3.3 规则三所载 G10 倒退 obs_event
   留证、权威降位重锚、N=2 跨帧记忆载体——现码静默跳过(cw_game_state.py:
   1748-1749),实现批位 = 遥测 M3(差异清单在原遥测设计件(已删,git 可溯) §3.7.2,该正本为本
   申报的镜像指针);非本批范围(纯文档)。
2. **top_bar_raw 生产读链透传候漏斗接线批**:观察汇聚已带 top_raw 形参并在位,
   生产漏斗暂不透传(GameState 本体禁碰,kernel 侧就绪)——规则①的观察层输入
   在生产链路候接线,判定语义不受影响(缺读禁猜路径已有)。
3. **商店类型查链**:件B 链写端已落地(链观察落地批 2026-09-16,过渡屏 +
   备战帧写端在役)——`chain_node_type` 按现行链实值返回,M3 走查商店类型
   按实值判;链缺位(新局首店前)仍 = None 诚实缺位,按「预期空」对待
   (§3.3 规则四②)。
4. **锚行形态挂起**:§3.4/§3.5/§3.7 为触发模型终裁前的历史设计(emit_anchor/
   载荷六槽/battle_done 同批原子切换/动作执行 hook 统一形态),状态申报已置顶
   各节;其处置(exogenous 退役表/hook 线结论文档)候编排者,本批不写结论文档。
5. **R1.1 扩员锁的口径时代**:test_popup_leg_boss_briefing_predecessor_advances
   落锁于 R3.1 口径(prev=BOSS简报 ∈ 守卫族触发);R4 终版下该形态 = 规则③
   处置 + 规则④拒绝(BOSS简报出族)——锁语义是否随 R4 重推,归锁的存在性纪律
   判定(锁红 ≠ 改动错),候 M3 验收轮,本批不动代码与测试。

### ① R2 退役错误勘误申报(用户令③;R3 修订根因)

R2 依据 currency_war_prep.md:32「先渲染备战画面,再弹 overlay」退役段首推断,犯了两类错误:

- **错误一(机制混淆):渲染顺序 ≠ 采样顺序。**「先渲染备战再弹窗」是游戏侧渲染时序;bot 侧采样由轮询节奏决定——弹窗自动弹出快于 bot 轮询时,干净备战帧可能一帧都采不到(loop 首帧即弹窗帧,0 系分支接管)。R2 §3.3 规则五「不漏论证」(「消化后备战帧必在后续某轮被分派」)在**事件层面**成立,但 R2 同时把弹窗分支排除出触发窗口,导致触发时点推迟到弹窗消化后——选卡(本节点动作)发生在触发前,违反裁定 2「分派前:先完成计数推进,再做本节点动作」。
- **错误二(真漏形态):补给类弹窗「确认即完成节点」。**补给节点无战斗无结算,补给屏先被采到时(自动弹形态),选完即完成节点、直接进下一节点——本节点再无备战帧,纯腿 A 对该节点**真漏触发**(R2 §3.6 S3 走查只走了 divert 形态,漏了自动弹形态,走查不完备)。
- **修复(R3)**:触发恢复双腿(§3.3 规则一/二)——腿 B 在弹窗首帧分派前触发,修复时点违反与真漏两缺陷;两腿同一行锚(规则三)。R2 的「单窗口/零推断」结论整体作废,R1 的段首推断机制经守卫精化后回归。

### ② 裁决保留面(用户令②)

四条 R2 裁决中仍有效并继续执行:同节点重入不算推进(规则三 ==last)/触发时点=分派前(规则一双腿落位)/battle_done 同批原子切换(§3.4 实施纪律)/坐标系漂移信息项挂实采(§5-③.4)。裁决②的「先备战后弹窗」时序**作为渲染机制仍然成立**(弹窗类型全集与链式形态的依据),仅其「bot 采样必得备战帧」推论作废。

### ③ 悬空与信息项

1. **腿 B 弹窗族清单保守化残留(如实申报)**:清单只收四类(遭遇/投资策略/补给/商店面板——prep.md:32 自动弹族中有独立分发分支者)。链式连弹第二弹(投资环境)不入清单:其前必有策略弹窗先触发(链式定义),若某局环境单独先弹且快于备战帧采样→该形态漏触发,由 §12.4 指标 1 对账(锚行数 vs 档案段数)显影后按实证扩清单(宁漏勿错,漏可查错难收)。巨星(非固定节点,megastar.md:12)/祈愿(特定节点前自动弹,wish_trial.md:15)不入清单——与节点边界的对应关系未定谳,挂实采观察。
2. **实施纪律(沿 R2)**:battle_done 行退役与新锚接线同批原子切换,归 H2 排程,未排程前新锚不得先接线。
3. **W1 位次锚定**:批 4 任务书须锚定 W1 位次(tick 后、达标臂前)不得位移;腿 B 落位与清查批 1 改动面不重叠(批 1 改 op 内验关段),任务书知会即可。
4. **信息项(维持)**:invest-env 增节点坐标系漂移(cw_node_reader.py:4-5;值域守卫 round≤9 cw_observation.py:952)——既有坐标系三方同键的漂移面,非本方案引入;腿 B 推断(last+1)在漂移形态下仍自洽(序线性),直读走缓存。挂实采观察。
5. **正本落文面(归实施批,本批不动正本)**:§12.2 node_enter 行补双腿时序句;§12.6-H6 裁决记录并入本方案选型建议;H2 申报 tick_status/trigger_screen 载荷字段。
6. **`advance_node` 回执面**(沿 R2):「递减未到期」清单现役不产出,tick_expired 只含到期移除,扩清单候 H2。
7. **sim 侧**(沿 R2):node_enter 锚「实机先行」;sim 域触发点=引擎节点段边界申报(§3.3-A1),候落盘面接线批,本设计不覆盖。

> 记账:R4/R3 完工登记在进度账本(dag.py note,2026-09-10)。

