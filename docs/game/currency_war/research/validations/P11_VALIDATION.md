# P11 验证报告:零息损购买(逐件证明验证,一件一批)

> 验证对象(以索引定位,**实际文件报告**):`docs/game/currency_war/research/math_proofs.md` L23 索引行 →
> `docs/game/currency_war/research/proofs/p11-overflow-gold-opportunity-cost.md`(**P11 = 溢余金机会成本命题**,全文 69 行)。
> **重要定位澄清(先行)**:任务标题「P11 零息损购买」中的「零息损判据:购买后仍在同一息档 / 跨档损 1 金上界 / 1★ 全额退=活期资产 / 动态卖回恢复通道」**不在 P11 证明文件内**——它们是口述 `[11]`(user_playstyle 经济簇)的内容,其数学承载分布在 **P47 命题三(+动态修正)/P13③/P46 [11] 行**。P11 本体只含「溢余段(g−s≥50)Δinterest=0 ⇒ C_dec 下界=0」这一条零息损命题(它是同档判据在息帽段的特例)。本报告五门以 P11 文件为主对象,同时对「[11] 零息损口径族」做一致性核(任务已知背景③④点名要求),不越件下结论。
> 阅读门覆盖:math_proofs 索引行 + P11 全文 + P47 全文 + user_playstyle §经济与利息([11]/[17]/[28]/[41])+ economy.md §2/§10.1(卖回还池/息律投资)+ 06_input_model §3(注册表地图)+ DESIGN_MANDATE_LAYER(M2/修正三)。`data/characters/*` 等 60+ 角色图鉴文件与本件无交集,声明跳过(理由:P11 不消费角色数值,仅消费息律/退金/收入锚点)。
> 公理栈:零调参(判据查表不拟合)/参数溯源(常量归注册表单一源)/超额收益隔离(息账与战力账分门)/纯金流(息损=金位轨迹差,不含卡牌效用)。全部按此口径执行。

---

## 门一:内部推导(逐步核)

### P11 命题 1(纯溢余段 g−s≥50 ⇒ C_dec 下界=0)——**成立**

- interest(g)=min(⌊g/10⌋,5)(下文门二直调核实)。g−s≥50 时:g≥50 ⇒ interest(g)=5(帽满);g−s≥50 ⇒ interest(g−s)=5。故 Δinterest=0,**精确成立,非近似**。
- 「持有边际利息=0」:g>50 时 ∂interest/∂g=0(帽截断),结构性成立。
- **支配性成色:该条零参数可证**——只需机制常量 cap=5(注册表 `GOLD_CAP_INTEREST=50`),无标定参数、无分布假设。这是 P11 最硬的一条。

### P11 命题 2(穿 50 段 C_dec ≤ Δ档×回档轮 ≤ 7 金)——**方向成立,式为松上界,量级核对有一处口径含糊**

- 表值逐格复算(手算对拍):g=85,s=42 → 息前 5/息后 43→4,Δ=1 ✓;g=100,s=60 → 5/40→4,Δ=1 ✓;g=120,s=90 → 5/30→3,Δ=2 ✓;g=160,s=135 → 5/25→2,Δ=3 ✓。回档轮@13 = Δ×10/13(0.77/0.77/1.54/2.31)✓;C_int=Δ×回档轮(0.77→表中写 1.0,系进位;3.08≈3.1 ✓;6.92≈6.9 ✓)。
- 松上界性:回档轮按「每档 10 金/收入 13」计,忽略了贴线距离不足 10 金的事实(85−42=43 到 50 仅 7 金,真实回档 0.54 轮)与「一轮可跨多档」(Ī=13-19 ≫ 10)——真实 L(P47 递推)在 (25,·,Ī=13) 量级 ≈1-3,P11 的 6.9 高约 3-7×。作为**上界声明**方向正确(「≤7 金量级」),但注意它与 P47 证伪的旧口径 t×min(R,3) **同构**(Δ×回档轮数),只是参数域(P2 高收入)使其退化为可容忍的松界。
- **数值瑕疵(小)**:L43「全部帧的面值成本都比决策成本高 ≥20×」——按本表 C 列,最差帧 135/6.9=19.57,**严格 <20**;表内「≥22.5×」用的是分母 Δ×2(=6,P6 回档账下界口径)而非 C_dec 列(6.9)。分母口径未在表头声明,两列混用。结论量级不受影响(20× 附近),但表述不严谨。
- **跨档损 1 金上界([11] 口径,邻件核对)**:[11]「跨档最多损 1 金」在 Δ=1 且 Ī≥1、花后距下一档线 ≤Ī 时成立(如 11→9,1 轮爬回,L=1 精确);但 P47 命题三自表内就有反例:(45,6) 贴档线跨档 L=2>1。**「最多损 1 金」不是可推广的上界**,P47 已按「一律走递推」收口——[11] 原文的「最多」应读作「该例损 1 金」而非全局上界(修正建议见文末)。

### P11 命题 3(非息价值上界 ρ(g)·s,ρ≈0)——**结构成立,实证支撑为回放账级**

ρ 是显式登记的校准层参数(P11 自己声明「初值可取 0」,registry L409 有 `溢余金流动性影子价 ρ` 条目),W151 四局「金 85-160 出局/死亡」是 ρ 实现值≈0 的行为证据。非零参数、非证明件,如实分级 ✓。

### 一致性核(任务已知背景③:P11 零息损口径 vs P47 息档损失)

- **不矛盾,且是包含关系**:P11 的零息损=「同档判据在帽段的特例」(g−s≥50 ⊂ interest(g)=interest(g−s)=5)。P47 命题三把一般同档判据收窄为三读分带(近 50 区 max=0;低金带 4-10),**只收窄了「任意金位同档零损」(P13③ 的推广),没有触碰 P11 的帽段命题**——帽段 L=0 是精确的,P47 溢余段行(55,5)→0 与 P11 一致。
- P13 文件内仍留「同档零损在任意金位成立」的推广表述,靠 P46 [11] 行的勘误注(「已被 p47 三读分带收窄,不再沿用」)消费侧拦截——**P13 本体未回改**(属 P13 件辖域,此处仅登记链路)。
- P47 A1 已把 cap 参数化(resolved);**P11 全文未参数化,仍以 cap=5 为隐含前提**(见门四)。

**门一判定:通过(附两处小瑕疵:高估倍数分母口径混用、≥20× 在最差帧严格不成立)。**

---

## 门二:锚点直调(注册表逐项)

| P11 引用锚点 | 直调结果 | 判定 |
|---|---|---|
| `cw_horizon.interest` = min(g//10,5) | **模块已不存在**(全仓 grep `cw_horizon` 在 src 下零命中;现单一源 = `kernel/cw_plane_table.interest`,L144 `min(gold//10, GOLD_CAP_INTEREST//10)`,数学同式);P11 复算脚本 `tools/cw/proofs/w153_p11_p12_p2_refresh.py` L19 import 该已删模块,**实跑 ModuleNotFoundError,「可重跑复核」承诺失效**(亲测复现) | ❌ 引用漂移 |
| `decision_v2.registry`(hp_to_gold=0.5) | 值对(`kernel/cw_registry.py` L322 `hp_to_gold: float = 0.5`),模块路径漂移(registry 在 kernel 不在 decision_v2) | ⚠ 路径漂移,值对 |
| 息律常量 | `cw_factions.INTEREST_THRESHOLD=50`/`INTEREST_RATE=0.1`;`cw_plane_table.GOLD_CAP_INTEREST=50`;`cw_economy.INTEREST_WEIGHT=4.0` | ✓ |
| `sell_refund` 手续费 | `cw_state.sell_refund`(L877-890):1★=cost 全额退、无手续费;star≥2 且 cost≥2 再 −1;cost=1 exempt(live 实测 2★1费=+3)——与 [11]「1★ 全额退=活期资产」的前提逐字吻合 | ✓ |
| 溢余金影子价 ρ | `cw_registry` L409 显式条目(P11 出处注明) | ✓ |
| 计算脚本 | 存在但 import 断(见上) | ❌ |
| W151/W152 实测锚(收入 13-19/轮、金 85-160、spend 42-135) | 回放账声明,非注册表;本验证未重放四局原始数据(超一件辖域),按「数据边界=4 局 87 条中 14 帧」的声明采信 | ⚠ 未复核(声明透明) |

**门二判定:值锚全部核实,但文件锚(cw_horizon/decision_v2.registry 两处模块路径)已漂移,复算脚本断裂——证明的数值可信(同式手算对拍通过),可复现性不可信。**

---

## 门三:建模对象 vs 玩法文档

- **[11] 原文(含 2026-08-31 动态精确化)对拍**:P11 的数学化对象是 **[17]**(溢余即花,簇主条),不是 [11];P11 L6 明写「口述依据=[17]」——**对象正确,无冒名**。[11] 的动态精确化(「节点 1 压库买、节点 2 可卖回备战凑息=无损的原因」)在 P47 命题三动态修正(A 段截断+fee′+时点敞口)被形式化,P11 不辖此域、也未误 claim——分工干净。
- **「卖回还池」口径(任务点名)**:economy.md §2 已按 2026-08-31 建模指令收口——「r=3 不是独立机制参数,是不变量的自动推论:剩余牌库=固定牌库−持有副本总数,买/卖/合成只改持有计数,池自动正确」;**往返池中性、退金按 §3 表(1★/1费 净 0)**。这与 P11/[11] 的「零损」口径兼容:卖回解除压缩 ⇒ 压库收益只留在未卖回的牌上 ⇒ 「凑息时卖回量最小化」(user_playstyle [11] 残余项③如实登记)。P11 文件本身未提卖回还池(它早于该精确化),但其 ρ≈0 结论不被还池机制扰动(还池影响的是压缩价值 p49 域,非息账)。**口径一致**。
- **economy 息档机制**:§10.1「利息=gold//10,上限 5(样本 13→1/2X→2/3X→3 全吻合)」+ 收入公式行(基础+息+连胜;败轮=基础+息)——P11 的 interest 模型与实测口径逐字一致。息结算时点按 P47 A2 用户裁定(节点末/轮初等价),[11] 卖回凑息的形式前提已确认。
- **M2/P46 消费面(任务已知背景②)**:DESIGN_MANDATE_LAYER 修正三与 M2 行以「[11] 零息损 ⇒ 买线成员/可逆义务动作有活期资产属性、不会因执行而受损」为豁免依据。对照数学:**近 50 区成立(动态残损 ≈0,P47 升级为精确)**;低金带动态残损 = A 段前 s 轮息差(例 (9,8):静态 5,s=3 → 2),是「小额正成本可逆」而非零成本——P46 已用 L 基判据承接(低金带同档进域),但设计层「不会因执行而受损」的表述强于数学(另有 P47 待标定①时点敞口未消)。

**门三判定:通过(P11 建模对象=[17] 无错位;[11] 动态口径的承载与消费链完整;M2 表述略强,列修正建议)。**

---

## 门四:实证 vs 账本(零息损购买行为预测可达性)

- **静态同档判据:三层可达** ✓——①决策放行:`arbiter.interest_rule` L319-320(同息档 `after//10 >= gold//10` 放行)、L317-318(1 费净 0 放行)、L617+ 三相位共用前置臂(reason 串「[11] 同档/1费零息损放行」);②预算截断:`economy_cycle.tier_truncated_spend`(min(want, gold%10),零标定参数);③sim 账本检查器:`sim/checks/segments.seg_check_lossless_buy_missed`(判据逐字对齐口述精确口径 `(g//10)==((g−cost)//10)`,副本形态四分类豁免诚实)。遥测有卖回格(recorder sell_income)+ 金轨迹视图。**预测→行为→账本闭环成立**。
- **动态卖回通道:数学层成立、实现层部分可达** ⚠——「凑息向卖候选」(candidates for_gold/_release_sell_gate)存在,但 P46 登记的「买入意图定期/活期标记」**未落码**(P46 明言「实现面登记项,不改代码」)⇒ 「有卖回计划 ⇒ 不进否决域」的分支在实现层靠 posture/候选结构间接近似,无显式标记可审计;P47 待标定①(卖回时点 vs 息结算先后,±5 金深花格)未消。**动态零损的可证性是「数学已备、实证缺口两处」**。
- **息律投资对「同一息档」判据的扰动(任务点名)**:注册表直调证实四类中第三类修改利息通道——`cw_investments`:开源节流 cap=9 / 利息上调 cap=10 / 买断制 cap=0(L≡0)+ 狸财经 +2;`cw_effect_ledger.interest_with`/`cw_economy` 聚合取 max。扰动核对:
  - **P11 的「g−s≥50 ⇒ Δ=0」在 cap≠5 局整体失效**(cap=10 时档线延至 100,g=160,s=90 → Δ=3≠0)——P11 全文 cap=5 隐含前提,**未做 P47 A1 式的 resolved 参数化声明**;
  - 同档谓词实现(`after//10` 比较、`gold%10` 截断)按 10 金步长计数,对 cap=9/10 **仍方向正确**(能捕到 50 上方新增档线的真实息损,偏保守侧);对买断制(cap=0)**过度保守**(无息局仍拦跨 10 线支出、`ev.interest_cost` 仍按裸 ⌊g/10⌋ 差计档=收不存在的息成本);
  - `ev.interest_cost` 本体**未夹 cap、未接 ledger override**:基线局 g>50 段也按裸 floor 差高估(例 interest_cost(75,30)=3 档,真实 Δ=1)——arbiter 侧靠「只在穿 50 时辖权+上界口径声明」兜住语义,但息律投资局(cap 9/10/0)下该式与真实息机制的偏差**无任何声明**。

**门四判定:静态可达(强);动态可达(中,两处缺口已挂账);息律投资扰动在 P47 已参数化、在 P11 与 ev.interest_cost 实现层是未声明的盲区。**

---

## 门五:参数溯源与支配性成色

- **零参数可证性分级**:①溢余段零息损(Δinterest=0)——**零参数,纯结构定理,支配性成色最高**;②穿 50 段 ≤7 金——依赖 recovery_rounds_P2(收入 13-19,回放账标定)+Δ 档,P11 已声明「归 registry 校准」✓;③ρ(g)——显式校准参数,初值 0,P10 ①上界背书 ✓;④[11]「跨档最多损 1 金」——**不可零参数证明**(需 Ī 与档内位置条件,P47 贴档反例 L=2),该上界应作废为「例值」。P11 的推论公式把 recovery_rounds_P2 与 ρ 明确归 registry,无暗参数 ✓。
- **超额收益隔离**:P11 只管「每金值多少」(C_dec),预算/授权显式让位 DP refresh_budget/gold_floor(L57)——与 FLIP/经济循环层的分工声明清晰,无双重计罚 ✓。纯金流口径:C_dec 不含卡牌效用(V 归收益侧 P12)✓。
- 消费面引用健康度:P13/P17/P18/P25/P33 等引用 P11 均限于「溢余段 C 下界=0/零息损口径」,无一超范围引用(此前 P29 曾有「κ 由 P11 推出」的超引,已被该文件自行勘误撤回)——**消费纪律良好**。

**门五判定:通过(支配性成色:核心命题零参数;辅助界有参但溯源完整;[11] 的「最多损 1 金」是唯一不可零参证明的口径,已被 P47 收口)。**

---

## 最终判决

**P11 站得住(有条件通过)。** 命题本体(溢余段 Δinterest=0 ⇒ C_dec 下界=0;穿 50 段 ≤7 金量级上界;ρ≈0)数学正确、锚点值全部直调核实、建模对象([17])无错位、消费面无超范围引用。四项需处置的发现(均不翻案):

1. 🔴 **可复现性断裂**:`cw_horizon` 模块已被重构删除,P11 复算脚本 `w153_p11_p12_p2_refresh.py` import 失败(亲测)——「零随机纯表值可重跑」的承诺当前不成立。修法:脚本 import 改 `cw_plane_table.interest`(一行),P11 数据源行同步勘误。
2. 🟡 **cap 参数化缺位**:P11 未像 P47 A1 那样声明「cap=5 基线、息律投资局按 resolved 重算」——cap=10 局「g−s≥50 ⇒ Δ=0」整体失效。修法:P11 加一行 resolved-cap 前置声明(数值不重算,判据条件改为 `g−s ≥ 10×cap_resolved`)。
3. 🟡 **实现层盲区**:`ev.interest_cost` 未夹 cap/未接 ledger override(基线 g>50 高估、买断制局收幻影息成本);P46 的「有卖回计划」分支缺买入意图标记落码。修法归对应实现批(P11 只需在检验点补一条「息律投资局 interest 通道须走 resolved cap」)。
4. 🟢 **表述小瑕疵**:高估倍数表分母口径(Δ×2 vs C_dec)未声明且「≥20×」在最差帧严格不成立(19.57×);M2「不会因执行而受损」在低金带应读作「小额正成本可逆(动态残损 ~2 金)」;[11]「跨档最多损 1 金」应加注「例值非上界,贴档线可达 2(P47 命题三)」——属 user_playstyle/P13 辖域,本报告仅登记。

**跨件一致性结论(已知背景③④)**:P11「零息损」与 P47 息档损失**一致且为包含关系**(帽段特例精确成立);息律投资扰动已由 P47 A1 参数化承接,P11 与实现层未跟上——按上列 2/3 处置后全链闭合。

> 本验证只读未改任何文件(本报告为唯一产出);P13/user_playstyle 的表述修正属对应件辖域,未越件执行。
