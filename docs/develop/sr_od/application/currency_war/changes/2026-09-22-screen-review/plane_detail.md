# T-27 位面详情 修法设计(plane_detail)

## 0. 元信息

- 迭代目标:货币战争全画面规范符合性审查(2026-09-22-screen-review)·只设计不落码,待用户裁决
- 状态:对抗收敛待用户裁决(对抗轨迹:r1 未收敛 2 条中→修订→r2 收敛 0 条实质;已攻不立 9 项留档)
- 审查输入:`.debug/progress/2026-09-22-cw-screen-review/reports/T-27-r1.md`(发现 F-1..F-3;总判定 = 有问题,高 0 中 0 低 3)
- 真值基线:本稿全部「现状」陈述以审查时点工作树代码为准,落地时若代码已再变,以落地时点代码重新对账后再动笔。符号锚 = `文件::符号名`,行号仅作本稿定位辅助。文档路径根 = `docs/develop/sr_od/application/currency_war/`,代码路径根 = `src/sr_od/application/currency_war/`(下文反引号短路径均相对此两根)。
- 修法性质:F-1 = `screens/README.md` §5.1 登记面补行;F-2 = `flow/outer_loop.md` §2.3 已退役号表补录(正本单面);F-3 = 注释次级指针单句重锚。**零行为变化**(无任何逻辑/签名/常量改动;代码侧仅 F-3 一句 docstring,`sr-od-test/` 零触碰)。

## 1. 问题与动机

### 1.1 F-1 README §5 能力矩阵无位面详情行(低)

- **现状症状**(审查报告 F-1):`screens/README.md` §5.1 标题聚类为「简报与过场(货币战争-简报 / 货币战争-BOSS简报 / 货币战争-位面过渡)」,矩阵无位面详情行(全文 grep「位面详情」于 §5 零命中);而同 README §8 文档索引把 plane_detail.md 归「简报与过场」族,§6 终结总表兜底行「推进型画面(简报/过渡/详情/弹窗)」点名「详情」。读者按 §8 索引进族后,在本该承载能力面的 §5.1 找不到本屏行。
- **根因归层**(根源两问):①根在哪层 = **约定层(登记面)**——§5.1 的聚类枚举与矩阵行未把本屏成员纳入;登记事实本身无恙(单篇动作清单 = [plane_detail.md](../../screens/plane_detail.md) §4、终结语义 = §6 兜底行,均在册且与代码一致,审查 §3 项 3/4/11 同证),缺的只是 §5.1 这一行。②修的是根还是症状 = 本屏行是 as-built 事实登记,内容完全由代码与在册正本决定,不依赖任何待裁决政策(§2.1 取舍「政策不变量」论证),补行即治本屏登记缺失;同族其余缺行屏(位面情报/等待1-1/下一步,同无 §5 行)的覆盖政策属家族面,升级登记 T-37 一次裁决(§2.1 登记行),不逐屏零散补。
- **解决到哪**:§5.1 标题聚类补名 + 新增位面详情矩阵行(逐字目标 = §2.1);家族覆盖政策登记 T-37。
- **明确不解决**:`screens/plane_detail.md` 本身(审查 §3 判其九节合规,零 diff);姊妹屏的 §5 行(各自审查辖域,是否补行随 T-37 覆盖政策裁决);README §5 头部边界申报(§2.1 取舍 C 拒绝);§5.1 简报行的注册表指针(同节不同行,归 briefing.md 稿 F-2,协调见 §2.4)。

### 1.2 F-2 注释引用已退役号制「0a4」,正本不可解析(低)

- **现状症状**(审查报告 F-2):`operations/cw_screen/cw_screen_plane_detail.py::CwScreenPlaneDetail.__init__` 注释「画面档名/入口锚(与外循环 0a4 分发判定同源同参;…)」——「0a4」为已退役分支号,`flow/outer_loop.md` §2.3 现行号表与已退役号段(0e/0s/0m/0p/0q/0t/0j/0a2/1b/1d/1g/1f/2/3/3b/6)均无此号,新读者只看代码与正本无法解析。家族残留:`cw_screen_role_detail_overlay.py` 两处「同 0a4/0t 家族口径」、`cw_screen_shop_card_detail.py` 两处「同 0a4 位面详情口径」——共 3 文件 5 处,互以对方为对号条目。
- **根因归层**:①根在哪层 = **表示层**——号制已退役(outer_loop.md §2.3 记法节自述 0x 号为「历史演化物」,现行分发 = 两阶段身份分发)后,在码残留号引用的对号表(§2.3 已退役号段,自述用途 =「读旧档案/旧文档时按此对号」)漏收 0a4,引用面整体不可解析。先例须两面读:plane_transition 稿对 0q 的处置是双面结构——其 F-2 修掉的是**代码注释以现役口径引用 0q**(「外循环 0q 的误分发计数只认 fail」→ 换现役符号锚 `cw_loop.py::CwLoop.OP_FAIL_REDISPATCH_LIMIT`;其取舍原文 =「画面 op 注释指认现役机制必须用现役符号锚;『现役口径引用 + 考古加注』双层并存,读者仍会先按旧号找机制,病灶未除」),其保留面仅辖**屏文档的历史退役指针**(「原 0q 误读兜底已退役(退役记录 = outer_loop §2.3)」,经审查核「退役号表对号可解析」保留);姊妹稿对代码侧退役号引用的处置同为清/换路由(expert_invite 稿清 0k、bookcard 稿清 0k/0i)——**无先例裁决过「代码注释 + 补录对号 = 合规保留」**。本稿 5 处 0a4 引用恰属代码注释面(以退役号指认现役机制/现役口径),保留路线不能靠先例背书,须立「机制连续 vs 机制替换」分界判据自行论证(判据与逐号裁决尺全文 = §2.2):0q 型 = 号所指机制已被替换(语义漂移)→ 必须换现役符号锚;0a4 型 = 号所指机制连续存在(纯记法退役:分发臂/入口锚/1.5s 校准口径现役原样在,git `434cbac36~1` 旧分发表逐字实证 `('0a4 位面详情', '货币战争-位面详情', '标识-位面详情标题')`)+ 3 文件 5 处互为对号条目 → 补录对号即治愈。②修的是根还是症状 = 补录对号表一行使全家族 5 处引用可解析 = 修根(该表的存在目的即此);清/换引用是同效他选路由,但按 §2.2 逐号裁决尺 0a4 落「机制连续 + 家族互指 → 补录保留」支,治愈代价最小且保住家族紧凑互指标识——单点清还会让姊妹 4 处引用失去家族语义锚(互指悬空)(§2.2 取舍 A/B)。
- **解决到哪**:`flow/outer_loop.md` §2.3 已退役号段补录 0a4 一条(语义证据链 = §2.2);本屏与姊妹文件的注释零改动。
- **明确不解决**:姊妹两文件注释自身的去留(T-33 角色详情浮层 / T-34 商店牌详情各自审查辖域——补录后其引用已可解析,是否进一步改纯语义随各自稿);号制引用可解析性的类级清点(登记 T-37 一行,§2.2);`plane_detail.md` §1「号制已退役,不引 0x」申报(辖该篇自身行文,与代码注释经对号表解析不冲突,零改动);`cw_loop.py` 分发段注释块(无 0a4 残留,零触碰)。

### 1.3 F-3 模块 docstring「见外循环分支注释」指针失效(低)

- **现状症状**(审查报告 F-3):`cw_screen_plane_detail.py` 模块 docstring 卷首段末句「此前不在分发名单的教训(2026-08-30 残局恢复局,ADR-0269『新增画面忘进名单』结构性缺口)见外循环分支注释」——被指的 `cw_loop.py` 位面详情分支注释现文 =「验消失迁入 op(单尝试);关不掉 → on_fail_retry 映射 round_retry」(:1279,迁移批重写所剩),不含该教训(全文件 grep 无 ADR-0269/「忘进名单」);教训的现役落点 = `kernel/cw_overlay_registry.py` 模块头(「ADR-0269 病灶」)与 `kernel/cw_obs_core.py`。ADR-0269 与「2026-08-30 残局恢复局」引用本身合规(审查 §3 项 16),唯次级指针扑空。
- **根因归层**:①根在哪层 = **表示层**——「验消失迁入 op」迁移批重写外循环分支注释时,上游 docstring 的次级指针未随迁;不是语义层(教训语义在现役落点完好),不是流程层(指针写法本身合规,错在目标漂移)。②修的是根还是症状 = 把指针重锚到现役落点 = 修复表示;该症状类(迁移/删除批的上游引用面清查缺失)的流程级根已由 battle_wait 稿 §2.1「跨批登记面」登记(归宿 = harness landing 模板固定判据,与注释卫生批并案裁决),本稿不重复立项。
- **解决到哪**:docstring 该句次级指针改为现役落点(`kernel/cw_overlay_registry.py` 模块头 + `flow/outer_loop.md` §2.2「三处缺一不可」纪律;落点选择论证 = §2.3)。
- **明确不解决**:该句其余成分(「处理迁移」措辞、「2026-08-30 残局恢复局」语义出处、ADR-0269 引用——均经审查核合规,零借清理之名扩面);模块头「ADR-0584,A1」引用(T-26 已登记的 ADR-0584 家族在册成员,归宿随 T-37 出路裁决,本稿不触);`kernel/cw_obs_core.py` 与 `kernel/cw_overlay_registry.py` 本体(零改动)。

- **在册核对一致项边界**:审查报告 §3 全部 16 项一致面(两 node 形态/观察门/重入裁决/无 report 完备锁/零策略器问询/单尝试重试归外循环/外循环交接/零 until/坐标真相源/as-built 无状态/数值逐位/指针有效性/类型导入/合法持久索引)为落地禁触碰边界——本稿三修法均不进这些面,落地批禁借清理之名改动其语义。审查 §4 静态不可判面(success_wait 实机时序/残留兜底覆盖面/merged 新鲜度/锁面执行结果)不在本稿修法域(需实机或跑测试,本迭代边界外)。

## 2. 方案

### 2.1 F-1 修法:README §5.1 补位面详情行 + 家族覆盖政策登记 T-37

逐点位目标(行文逐字给足,实现者照抄;表列结构与 §5.1 既有行一致):

| # | 位置 | 现状 | 目标(逐字) |
|---|---|---|---|
| 1 | `screens/README.md` §5.1 标题 | `### 5.1 简报与过场(货币战争-简报 / 货币战争-BOSS简报 / 货币战争-位面过渡)` | `### 5.1 简报与过场(货币战争-简报 / 货币战争-BOSS简报 / 货币战争-位面过渡 / 货币战争-位面详情)` |
| 2 | 同节表「位面过渡点空白」行后新增一行 | (无) | `| 位面详情点 X 关闭 | game 侧画面档 = docs/game/screens/货币战争-位面详情.md(检视浮层;用途见 research/plane_schedule_observed.md「位面详情画面」条) | operations/cw_screen/cw_screen_plane_detail.py::CwScreenPlaneDetail(阶段一身份行「货币战争-位面详情」,分发判定单一源 = flow/outer_loop.md §2.2;单篇动作清单 = [plane_detail.md](plane_detail.md) §4) | 点 X 即终结(空决策形态) |` |

依据(就地):①行内容真值 = 代码与在册正本——单动作 = 点「货币战争-位面详情.按钮-关闭位面详情」、阶段一身份行「货币战争-位面详情」、空决策形态无 report(`screens/plane_detail.md` §1/§2/§4 + `op-layer.md` §3 推进型行;按钮 area 在册 = `assets/game_data/screen_info/currency_war_plane_detail.yml`,审查 §3 项 11);②终结语义与 §6 兜底行「推进型画面(简报/过渡/详情/弹窗)|点推进/关闭即终结(空决策形态)」同文;③任务书核对面明列「§5.1(位面详情行)」(审查 F-1 规范条款出处)——本迭代的核对预期即该行存在;④game 侧两依据文件实存(docs/game/screens/货币战争-位面详情.md;docs/game/currency_war/research/plane_schedule_observed.md,其「位面详情画面」条 = 该屏用途与运行时读取价值的 game 侧记载)。

行辖域声明:本行登记的是**推进型处置臂的动作面**(点 X);同屏的浏览/采集能力面(点位面卡切换、节点/词缀/boss 识别)宿主 = `CwScreenPlaneIntel`(plane_intel.md §3/§4),其 §5 登记位缺行,归 T-37 覆盖政策裁决,本行不代登记。

**登记行(T-37 原样收编)**:

> **§5 能力矩阵覆盖政策(T-27 登记)**:`screens/README.md` §5.1 标题聚类枚举(简报/BOSS简报/位面过渡/位面详情——后者随 T-27 补入)与 §8 文档索引「简报与过场」族(7 篇:briefing/boss_briefing/plane_transition/plane_detail/plane_intel/wait_one_one/next_button)口径不一致;位面情报(含其采集能力面)、等待1-1、下一步同无 §5 行。已核 T-23/T-26 两报告发现清单不含缺行项;T-25 报告 5 低未见 §5 缺行(以其报告原文为准)。裁决面 = §5 矩阵覆盖政策二选一:①全量登记(§5.1 补齐族内缺行屏 + 其余节同类清点);②显式边界申报(写明 §5 登记哪些成员、缺行 = 范围外非缺口——申报措辞须与 §5.3「查看详情」行/§5.4 牌详情句/§5.5 标题清单的既存 overlay 登记事实相容)。plane_detail 行随 T-27 落地,两裁决下均保持正确(行 = as-built 事实登记,不依赖裁决结果)。

**取舍**:

- 备选 A:登记不补(只登记 T-37,本稿零落地)——放弃:行内容是零争议的 as-built 事实登记(单动作/单 op/兜底语义三方在册),在任何覆盖政策裁决下都保持正确(**政策不变量**:行 = 事实,政策裁的是「还有谁该有行」,不裁本行真伪);悬到汇总批只让本发现失去直接落点,且任务书核对面本身预期该行存在。
- 备选 B:补行同时把 §5.1 标题扩到 §8 全家族 7 屏——放弃:姊妹屏的行内容与登记必要性属各自屏辖域,代补会与已完成审查(T-23/T-26 报告逐条不含缺行发现)的结论面相抵;同族缺行 = 同一根,家族面归 T-37 一次裁决(跨件半问:禁逐屏零散补)。
- 备选 C:不补行,在 §5 头部加「矩阵不主张全量登记」边界申报——放弃:「全量不主张」式申报与既存事实冲突(§5.3/§5.4/§5.5 已在册多个 overlay 成员,边界线划不出来);「部分登记」申报对读者无判别力(仍分不清缺口与范围外),等于把本发现的病灶(覆盖口径不可判)固化成条文。

### 2.2 F-2 修法:outer_loop §2.3 已退役号段补录 0a4

逐点位目标(逐字):

| # | 位置 | 现状 | 目标(逐字) |
|---|---|---|---|
| 1 | `flow/outer_loop.md` §2.3 已退役号段,「0a2(选择装备重试腿)/」之后插入一项 | 「…/ 0a2(选择装备重试腿)/ 1b、1d、1g(详情弹窗族,现役载体 = 阶段一身份先行)/ …」(全段无 0a4) | 「…/ 0a2(选择装备重试腿)/ 0a4(位面详情分发臂,现役载体 = 阶段一身份行「货币战争-位面详情」)/ 1b、1d、1g(详情弹窗族,现役载体 = 阶段一身份先行)/ …」 |

补录语义的证据链(五角互证,角 5 = git 考古定谳):

1. `cw_screen_plane_detail.py::__init__` 注释「与外循环 0a4 分发判定同源同参」辖本屏画面档名/入口锚——「0a4 分发判定」即本屏分发分支;
2. `cw_screen_shop_card_detail.py` 两处「同 0a4 位面详情口径」——「0a4 位面详情」连用即等号;
3. `cw_screen_role_detail_overlay.py` 两处「同 0a4/0t 家族口径」——0t 已在册(商店卡牌详情弹窗),家族对偶锁定 0a4 = 位面详情;
4. 号系旁证 = `flow/outer_loop.md` §2.2「0a0/0a 碰撞由列车同行整行副题锚消解」(0a 系 = overlay 分发分支的历史序号,0a0 现行在册/0a2 已退役在册,0a4 同系);
5. git 考古定谳:`git show 434cbac36~1` 旧分发表逐字在档——`('0a4 位面详情', '货币战争-位面详情', '标识-位面详情标题')` 与 `('0a4 位面详情关闭', '货币战争-位面详情', '按钮-关闭位面详情')`(该提交 `cw_loop.py:871-872`)+ 分发分支注「0a4. 位面详情 overlay(主循环兜底):情报采集 op 失败退出残留/开局自动…」——0a4 = 位面详情分发臂由仓库历史直接证实,非仅旁证推断;现行入口锚「标识-位面详情标题」与旧 0a4 判定锚同铭 = 机制连续的锚级实证。

依据(就地):§2.3 自述 =「已退役号(读旧档案/旧文档时按此对号)」+「权威源 = cw_loop.py 分发段注释块,本表与代码注释冲突时以代码为准」——现行代码已无 0a4 分支(两阶段身份分发,无冲突),3 文件 5 处在码引用即该表要服务的「旧档案」,补录即其存在目的;先例引用 = plane_transition 稿 0q 处置的双面结构(正读见 §1.2 ①),本稿 0a4 与其分界的裁决尺如下;entry 行文风格对齐同段在册条目(辖域 + 现役载体,零出处引用——changes/ 内容禁入正本,本节证据链只服务对抗审,不随落地进表)。

**逐号裁决尺(机制连续 vs 机制替换;T-37 登记行同文收录,本稿 0a4 依此尺走补录支)**:

| 步 | 问 | 判据 | 出路 |
|---|---|---|---|
| 一·号籍 | 该号在役还是退役? | 代码现役时态自指(「本文件 X 分支」类)∨ git 考古示现行在役 → 疑现行号表漏收,先核对现行号表;确证退役 → 进第二步 | 现行漏收 → 补现行号表;退役 → 进二 |
| 二·语义 | 号所指机制连续还是被替换? | 机制连续 = 纯记法退役,所指机制/口径/锚现役原样在(0a4:分发臂与入口锚同铭、1.5s 校准在役);机制替换 = 所指机制已退役或现役语义另有载体(0q:误读兜底已退役,计数现役载体 = `cw_loop.py::CwLoop.OP_FAIL_REDISPATCH_LIMIT`;bookcard 稿 0i =「现行代码已无对应分支」的在册宣告) | 替换 → 清/换现役符号锚(补录救不了:表只能指向已死/已义变对象);连续 → 进三 |
| 三·形态 | 家族互指还是单点指认? | 家族互指 = 跨文件互为对号条目、充作家族紧凑标识(0a4:3 文件 5 处互指「位面详情口径」);单点指认 = 一句现役事实指针、现役符号锚严格更优(姊妹稿 0k「弹窗由外循环 0k 分发」→ 换「阶段一身份分发,分发判定单一源 = flow/outer_loop.md §2.2」) | 家族互指 → 补录对号保留(本稿 0a4 支);单点 → 换锚清号 |

尺的自检(防「有表即留」倒挂):姊妹稿对 0k/0i/0q 的清/换处置与本尺不冲突——彼等引用或属机制替换面(0q/0i),或属单点指认面(0k),清/换是彼形态下的正解;本尺只对「机制连续 + 家族互指」开补录支,不豁免语义已漂移的引用。

**取舍**:

- 备选 A:本屏注释删「0a4」改纯语义(「与外循环分发判定同源同参」)——放弃(单点症状修法):姊妹 4 处「同 0a4 位面详情口径 / 0a4/0t 家族口径」以本文件为对号条目,单点清后家族引用失去语义锚(互指悬空),可解析性恶化;家族面单点动手违本迭代登记纪律(先例 = T-26 对 ADR-0584 家族的处置口径)。
- 备选 B:家族 5 处注释全清(T-37 一次)——放弃:按 §2.2 逐号裁决尺,0a4 落「机制连续 + 家族互指 → 补录保留」支;清/换是同效他选但代价不成比例(3 文件注释 churn + 家族紧凑互指标识损失,收益仅少一层对号间接)。此结论不靠「有表即留」单面判据支撑——姊妹稿清 0k/0i/0q 与本尺不冲突(彼等属机制替换面或单点指认面,清/换是彼形态正解);M39 对比保留为辅证而非判据:M39 无对号表且无家族互指故必须清,0a4 有表可补且家族互指故补录更优。
- 备选 C:0a4 入 §2.3 尾句「文档残留旧号」清单(与 0r/0h/0i/0d/0e2 同列)——放弃:尾句辖「现行代码已无对应分支、仅文档残留」的号;0a4 在代码注释在册,需正式对号语义(辖域 + 现役载体),挂「残留」标签与事实不符。

**登记行(T-37 原样收编)**:

> **退役号表完备性(T-27 登记)**:`flow/outer_loop.md` §2.3 已退役号段以全树注释号制引用对账清点一次;逐号裁决尺(同本稿 §2.2,登记行自足收录):①号籍先判——现役时态自指/git 考古示在役 → 先核对现行号表漏收,确证退役 → 进②;②语义问——号所指机制被替换(语义漂移)→ 清/换现役符号锚,机制连续(纯记法退役)→ 进③;③形态问——家族互指条目(跨文件互为对号)→ 补录对号保留,单点指认 → 换锚清号。禁无尺逐号拍板。已知库外号:**0a4** = 3 文件 5 处(本稿补录清偿,尺判「连续 + 互指」);**0k** = 5 文件 8 处(grep `\b0k\b`,已排除 `cw_identity_obs.py` 的 r100k 子串巧合)——在册认领面 = expert_invite 稿(T-15)认领 `cw_screen_expert_invite.py` 3 处、bookcard 稿(T-14)认领 `cw_open_bookcard_action.py` 2 处,余 `cw_loop.py:1842`(「本文件 0k 分支」现役时态自指)/`cw_identity_obs.py:884`/`cw_screen_prep.py:800` 三处不在任何在册认领面,归本登记面;0k 两表(现行号/已退役号)均不在册且 `cw_loop.py` 为现役时态引用——裁决**先判「现行号漏收 vs 退役号补录/清引用」**,禁预设「随点位退役」的清引用出路。

### 2.3 F-3 修法:模块 docstring 次级指针重锚现役落点

单句替换(逐字;换行随 docstring 排版机械处理,除该句外全文件零 diff):

| # | 位置 | 现状(逐字) | 目标(逐字) |
|---|---|---|---|
| 1 | `cw_screen_plane_detail.py` 模块 docstring 卷首段末句 | 此前不在分发名单的教训(2026-08-30 残局恢复局,ADR-0269「新增画面忘进名单」结构性缺口)见外循环分支注释。 | 此前不在分发名单的教训(2026-08-30 残局恢复局,ADR-0269「新增画面忘进名单」结构性缺口):结构性收口的现役落点 = `kernel/cw_overlay_registry.py` 模块头(单一枚举点);分发侧操作纪律 = `flow/outer_loop.md` §2.2(新增画面 = 建档 + 处理器臂 + `CW_DISPATCH_SCREENS` 登记,三处缺一不可)。 |

依据(就地):①失效实证 = `cw_loop.py` 位面详情分支注释现文「验消失迁入 op(单尝试);关不掉 → on_fail_retry 映射 round_retry」(:1279),全文件 grep 无 ADR-0269/「忘进名单」;②新指针目标实存 = `kernel/cw_overlay_registry.py` 模块头「消灭『新增画面要手工同步多处』的结构性缺口(ADR-0269 病灶…)」+ `flow/outer_loop.md` §2.2「新增画面 = 建档(语料两向验证)+ 处理器臂 + `CW_DISPATCH_SCREENS` 登记,三处缺一不可」(代码侧同文 = `cw_loop.py::_dispatch_identity_screen` docstring);③规范 = 仓库根 AGENTS.md §8(读者只看代码与注释要能重建推导;出处 = 持久索引或纯语义描述)。

落点边界声明(防按图索骥再扑空):`kernel/cw_overlay_registry.py` 指的是其**模块头对病灶与收口的申报**,非「本屏已入注册表」——位面详情非注册表 OverlaySpec 成员(该文件 grep「位面详情」零命中);本屏分发成员资格的现役操作纪律 = §2.2「三处缺一不可」,两指针各司其职(结构收口申报 / 操作纪律)。

**取舍**:

- 备选 A:删指针留语义(句止于「结构性缺口」,仅保留 ADR-0269 引用)——放弃:ADR 档案已按用户令退役(`decisions/` 目录不在正本树,T-26 F-1 glob 实查),ADR-0269 形式合规但全文未必可按图索骥;次级指针是事故教训唯一「实际走得通」的导航,删除后指针面比现状更薄。
- 备选 B:第三落点补 `kernel/cw_obs_core.py`(审查报告点名的另一现役落点)——放弃:cw_obs_core 的 ADR-0269 应用面 = `UPPER_SCREENS` 帧态门(钩子防误触),与「分发名单成员纪律」不是同一应用面;给分发臂 op 的读者指帧态门 = 指错门,两个落点已覆盖本句教训的重建路径。
- 备选 C:同句顺带清「处理迁移」措辞(变更史语感)——放弃:审查 F-3 只点名失效次级指针,「处理迁移」与「2026-08-30 残局恢复局」经审查 §3 项 16 对应面核合规;在册一致项禁借清理之名扩面(§1 末边界)。

### 2.4 落地文件面总表(单文档方案的修正面汇总)

| 文件 | 修法点位 | 性质 |
|---|---|---|
| `screens/README.md` | §5.1 标题聚类补名 + 新增位面详情行(F-1,§2.1 #1/#2) | 正本登记面补行 |
| `flow/outer_loop.md` | §2.3 已退役号段补录 0a4(F-2,§2.2 #1) | 正本补录 |
| `operations/cw_screen/cw_screen_plane_detail.py` | 模块 docstring 次级指针单句替换(F-3,§2.3 #1) | 注释(零逻辑) |
| (登记,零落地面)本迭代汇总 T-37 | §5 覆盖政策登记行(§2.1)+ 退役号表完备性登记行(§2.2) | 登记 |

统一验收(机械可判):

1. F-1:`screens/README.md` §5.1 grep「位面详情」≥2 命中(标题 + 矩阵行);行内符号锚/area 名实存(`cw_screen_plane_detail.py::CwScreenPlaneDetail`、`货币战争-位面详情.按钮-关闭位面详情` 在档);行语义与 `plane_detail.md` §4 对照表及 §6 兜底行互查一致。
2. F-2:`flow/outer_loop.md` §2.3 grep「0a4」≥1 命中(已退役号段);`src/sr_od/application/currency_war/` 注释 grep「0a4」命中 = 3 文件 5 处且全部可经 §2.3 对号(`cw_plaza_posts.py` post_id 十六进制串的子串命中系巧合,排除);`git diff` 确认 F-2 辖域 src 零改动。
3. F-3:`cw_screen_plane_detail.py` grep「外循环分支注释」零命中;`git diff` 确认该文件仅模块 docstring 该句一处改动;`uv run ruff check` 该文件通过。
4. 全局:零行为变化(src 树无逻辑 diff);`sr-od-test/` 零触碰;全部修法与审查报告 §3 一致面零交叠。

协调注(同文件在飞面):`screens/README.md` §5.1 = briefing.md 稿(T-20)F-2 修简报行注册表指针——同节不同行不同列,两稿无行冲突,同批落地时一次成文;`flow/outer_loop.md` 与 `cw_screen_plane_detail.py` 无其他在飞面登记。
