# T-35 锁定返回 修法设计(prep_locked_return)

## 0. 元信息

- 迭代目标:货币战争全画面规范符合性审查(2026-09-22-screen-review)·只设计不落码,待用户裁决
- 状态:对抗收敛待用户裁决(对抗轨迹:r1 未收敛 2 条低→修订→r2 未收敛 2 条低→修订→r3 定点复核收敛 0 条)
- 审查输入:`.debug/progress/2026-09-22-cw-screen-review/reports/T-35-r1.md`(发现 F-1 一条;总判定 = 有问题,高 0 中 0 低 1)
- 真值基线:本稿全部「现状」陈述以审查时点工作树代码为准,落地时若代码已再变,以落地时点代码重新对账后再动笔。符号锚 = `文件::符号名`,行号仅作本稿定位辅助。文档路径根 = `docs/develop/sr_od/application/currency_war/`,代码路径根 = `src/sr_od/application/currency_war/`(下文反引号短路径均相对此两根)。
- 修法性质:F-1 = 屏文档 `screens/prep_locked_return.md` §2/§4/§5 as-built 补记,纯文档语义更新,**零行为变化**(无任何逻辑/签名/常量改动;`src/`、`sr-od-test/`、`screens/op-layer.md` 零触碰)。
- 准则参照:层合同逐条核对与「正本登记 vs 画面篇申报」判据沿 T-23 稿([wait_one_one.md](wait_one_one.md))§2.1 方法;补记形态与防扩面纪律(明确不解决清单/审查一致项禁触碰边界)沿 T-28 稿([consumable_overlay.md](consumable_overlay.md))§1/§2。

## 1. 问题与动机

### 1.1 F-1 屏文档出口面只申报 area 锚形态一种,免锚构造变体与失败出口未入正本(低)

- **现状症状**(审查报告 F-1):屏文档 §2 画面形态声明与 §5 终结交回只写 area 锚形态一种出口(「推进已发 → `round_wait` 重入;重入 = 锚 miss = 已离开 → `success` 交回」),而代码 `operations/cw_screen/cw_screen_prep_locked_return.py::CwScreenPrepLockedReturn` 存在两个未登记出口面:①**免锚形态**——模块头明文「本类 = 默认 area 锚形态的参数化变体:空 area 构造即免锚形态(无「已离开」观察信号,发出即 success,原无验效出口;有界性归外循环重派/分发)」(模块头 L22-24;`entry_ok` 空锚恒过 L57-58;`act` 免锚早退 L95-96「推进已发(免锚)」),内联形态锁在册(`sr-od-test/test/sr_od/application/currency_war/test_cw_screen_progression_inline.py::test_anchor_free_advance_succeeds_immediately`,空锚构造断言免锚出口 + 重入裁决不可达);②**推进未落地 `round_fail` 出口**(`act` L88-89「推进动作未落地」)。**测试锁覆盖申报**:fail 出口与重入裁决出口的直接锁不在本屏——`test_reentry_arbitration_flag_combination` 的 `_ADJUDICATION_ROWS` 仅 `plane_detail`(默认 area 锚形状)/`next_button`(自有 OCR 形状)两代表行(L221-236,表注释 L219「裁决代表屏」),本屏 fail/重入出口行为仅由代表屏间接覆盖,无本屏专锁;本屏在该测试文件的直接登记面 = `_OBS_ROWS` 观察装载锁(L187-192)与 `test_anchor_free_advance_succeeds_immediately` 免锚出口锁两处。生产分发(`operations/cw_loop.py` L1188-1202)恒传具体锚(`按钮-返回投资策略选择`/`按钮-返回遭遇选择`,L1190-1191;构造点 L1200),免锚分支现役零调用点——无行为违背,属正本与代码现状的覆盖差。
- **根因归层**(根源两问):①根在哪层 = **表示层(正本文档覆盖缺口)**——屏文档按生产现役形态单面撰写,构造参数化的变体面(代码模块头已声明、测试已锁)与失败出口未回填 §2/§5,命中仓库根 AGENTS.md §9「正本区……永远与代码现状一致」义务缺口。非语义层(代码行为正确,模块头/测试锁/分发三面自洽)、非约定层(op-layer §1.1/§3 合同与免锚形态逐条核对无字面相抵,核对明细 = §2.1 取舍 1——对照 T-23 件的约定层缺口,本件无执法误判源)、非流程层(单篇覆盖遗漏,无结构性缺口)。②修的是根还是症状 = 治本:补记后屏文档出口面 = 代码出口面逐位覆盖,AGENTS §9 缺口在本屏清零;「正本与代码漂移」的流程级防线 = as-built 一致性审查本身(本迭代即其执行面),本稿不另立流程件(跨件半问:T-28 与本件同为文档覆盖差,但根各在各自篇目的撰写面,逐篇补记即治本,无共同更高架构根)。
- **解决到哪**:`screens/prep_locked_return.md` 三点位——§2 形态声明补「构造两形态」与免锚行为面(#1)、§4 对照表「触发返回外循环」列连带对齐全出口(#2)、§5 改逐出口「条件/级别/交回落点」表(#3)(逐字目标 = §2.1)。
- **明确不解决**:①任何行为变化——免锚分支保留/退役为候裁动议(建议与退役清单 = §2.1 取舍 2),本稿零代码;②`screens/op-layer.md` 零改动(取舍 1 结论:层合同无缺口可补);③屏文档其余各节(§1/§3/§6/§7/§8/§9/开放设计注)——审查报告 §3 已核一致面,零 diff;④审查附注两面——§4 `progress_once` 摘要签名省略首参 `last_screenshot`(审查已判「非条款违背」)、内联形态锁桩参 area 名「按钮-返回策略选择」非建档实名(审查已判「无行为影响」),均不触;⑤第三档「货币战争-备战-补给锁定」不在分发对集——屏文档开放设计注在册候裁,不触;⑥实机行为面(`success_wait=1.5` 转场覆盖度/过渡帧下入口锚 miss 频率/免锚形态实机是否启用)——审查 §4 无法核对面,静态不可判;⑦`cw_loop.py` `_shop_card_detail_anchor_hit`/`_role_detail_anchor_hit` 零调用候选面——审查 §3 已划归商店卡牌详情/角色详情两屏审查对象,非本屏家族;⑧本屏 fail/重入出口专锁补位——属行为面动议(涉 `sr-od-test/`),覆盖现状如实申报于 §1.1 测试锁覆盖申报句,补锁与否归用户裁决,不改变本稿零触碰承诺。
- **在册核对一致项边界**:审查报告 §3 全部一致面(分型归属/决策控制铁律/观察上报形态/完备锁同格/注释持久索引面等)为落地禁触碰边界——本稿唯一修法域 = 屏文档三节补记,落地批禁借补记之名改动上述在码语义与其余节文字。

## 2. 方案

### 2.1 F-1 修法:屏文档补免锚形态申报与逐出口交回表

单文件三点位(逐字;行号仅定位辅助)。

**点位 #1:§2 画面形态声明——整段替换**

现状(逐字,§2 全段):

```
**空决策形态**(纯推进)。两 node 直继承 `SrOperation`(合同 = [op-layer.md](op-layer.md) §1.1;画面档/入口锚经构造参数覆写):观察 node = 入口锚门(构造传入的返回按钮,与分发判定同源同参;miss = round_fail 交回外循环重判)+ obs{on_screen} 挂实例属性;决策动作 node = 顶部重入裁决(推进已发 → 锚不在 = 已离开本画面 → success 交回)→ 点返回按钮单次推进 → `round_wait` 循环推进(无防御上限;`node_max_retry_times=2` 现役值仅框架异常路径消费)。空决策形态无 report 接口。
```

目标(逐字,§2 全段替换):

```
**空决策形态**(纯推进)。两 node 直继承 `SrOperation`(合同 = [op-layer.md](op-layer.md) §1.1;画面档/入口锚经构造参数覆写,**构造两形态**:area 锚形态 = 默认形态,生产现役——分发恒传具体锚(`cw_loop.py` 策略锁定/遭遇锁定身份分支);空 area 构造 = 免锚形态,休眠变体、现役零调用点):观察 node = 入口锚门(构造传入的返回按钮,与分发判定同源同参;miss = round_fail 交回外循环重判;免锚形态恒过——缺省缺位语义)+ obs{on_screen} 挂实例属性;决策动作 node = 顶部重入裁决(推进已发 → 锚不在 = 已离开本画面 → success 交回;免锚形态无「已离开」观察信号,裁决不可达)→ 点返回按钮单次推进 → 锚形态 `round_wait` 循环推进(无防御上限;`node_max_retry_times=2` 现役值仅框架异常路径消费),免锚形态发出即 `round_success` 终结(原无验效出口,有界性归外循环重派/分发)。空决策形态无 report 接口。
```

依据(就地):

- 免锚行为面三句逐位对码:「免锚形态恒过——缺省缺位语义」= `entry_ok` docstring L57-58(「空锚构造(免锚形态)无观察信号,恒过(缺省缺位语义,与骨架缺省 area 键原语逐位同)」);「无『已离开』观察信号,裁决不可达」+「发出即 `round_success` 终结(原无验效出口,有界性归外循环重派/分发)」= `act` L91-96 免锚早退分支 + 模块头 L22-24 变体声明句——目标文本即代码申报句的同义正文化,不引入新语义;
- 「生产现役——分发恒传具体锚」= `cw_loop.py` L1188-1200(两身份分支各传建档实名锚);接线状态 as-built 申报住正本的先例族 = `screens/README.md` §5.3/§7「商店锁……**生产链路未建模**」「SwapDeploy……生产执行器未接线——能力面按词表完备性保留」——接线/调用点状态属 as-built 申报,非 AGENTS §9 禁入的进度标记/状态位;
- 句式 = 同族空决策形态屏既定三分句(门 miss/重入裁决/循环推进,`screens/armory_box.md` §2 同构),仅插形态分支括注,零结构创新。

**点位 #2:§4 对照表「触发返回外循环」列——该列单元格替换(连带对齐,同病灶出口面)**

现状(逐字,唯一行的末列):

```
是(重入裁决交回):点返回 → `round_wait` 重入,构造传入的返回按钮锚不在 = 已离开本画面 → `round_success` 交回外循环(见 §5)
```

目标(逐字):

```
是(锚形态 = 重入裁决交回:点返回 → `round_wait` 重入,构造传入的返回按钮锚不在 = 已离开本画面 → `round_success` 交回;推进未落地 = `round_fail` 交回;免锚形态 = 发出即 `round_success` 交回,休眠变体)(见 §5)
```

依据(就地):

- 同病灶:F-1 病灶 = 屏文档出口面只写锚形态一种出口,§4 该列是出口面的组成部分——现状 cell 同样只写重入裁决交回一条,未落地 fail 与免锚出口同样缺席;
- 模板义务:`screens/README.md` §2 模板 §5 行「逐出口列『条件/级别/交回落点』表,**行与 §4 对照表的『触发返回外循环』列一一对应**」——只改 §2/§5 不改 §4 会新造 §4↔§5 失对应(修法引入的新不一致,重于原缺口),故 #2 为 #3 的连带必改点;
- 姊妹屏先例:同推进族四屏(`screens/item_detail_popup.md`/`refresh_odds_popup.md`/`role_detail_overlay.md`/`shop_card_detail.md`)§4 该列均枚举「重入裁决 success + 推进未落地 `round_fail`」两成分,本屏为唯一单出口 cell;
- 入口锚门 miss(观察 node 出口)不进 §4 cell:对照表 = 动作清单,门 miss 先于动作,四姊妹屏同此口径——该出口只在 §5 申报。

**点位 #3:§5 终结与交回——整节替换(prose 改逐出口表)**

现状(逐字,§5 全节正文):

```
推进已发 → `round_wait` 重入;重入 = 锚 miss = 已离开 → `success` 交回(无防御上限)。落点 = 对应 overlay(策略选择/遭遇节点分支接管)。
```

目标(逐字,§5 全节正文替换):

```
| 出口条件 | 级别 | 交回落点 |
|---|---|---|
| 入口锚门 miss(首发锚 miss = 误分发/过渡帧) | 显式失败 | `round_fail` 交回外循环重判(「下一帧重判」是外循环职责) |
| 推进未落地(`progress_once` False:返回按钮查找/点击未成功) | 显式失败 | `round_fail` 交回外循环(如实交回,不静默续推) |
| 锚形态:推进已发 → 重入锚 miss = 已离开本画面 | 画面终结 | `round_success` 交回外循环(重入裁决出口) |
| 免锚形态:推进已发即交回(休眠变体,现役零调用点) | 画面终结 | `round_success` 即刻交回(无「已离开」观察信号,发出即终结;有界性归外循环重派/分发) |

落点 = 对应 overlay(策略选择/遭遇节点分支接管)。锚形态循环无防御上限。
```

依据(就地):

- 模板义务:`screens/README.md` §2 模板 §5 行明文「逐出口列『条件/级别/交回落点』表」——prose 是单出口时代的残留写法,出口面扩为四出口后表格化即模板回归(同族表格先例 = `screens/battle_wait.md` §5 未知帧/bail 行、`screens/invest_env.md` §5 失败出口行);
- 级别词表 = 在册用法:「画面终结」= `screens/README.md` §6 推进型画面行的终结级别;「显式失败」= `screens/invest_env.md`/`invest_strategy.md` §5 表在册值(失败出口非防御上限,合同 = op-layer §1.1「循环无上限规范」「失败类出口……不属于迭代上限」);
- 四行 = 代码四出口逐一对照:`observe` 门 miss fail(`::CwScreenPrepLockedReturn.observe` L72-74)/ `act` 推进未落地 fail(L88-89)/ 重入裁决 success(L82-87)/ 免锚 success(L95-96)——出口面逐位覆盖即 AGENTS §9 缺口清零。

**取舍 1:仅补画面篇(选)vs 连 op-layer 变体登记(备选)——选仅补画面篇**

判据沿 T-23 稿 §2.1:「正本登记」的必要条件 = 层合同字面绝对条款与本屏实态相抵(执法误判源在 op-layer 字面)。本件对 op-layer 合同逐条核对,免锚形态无字面相抵:

- §1.1 两 node 形态/读屏纪律:免锚形态下两 node 结构、零 `screenshot()`、obs 只挂实例属性全部不变;观察门恒过 = 构造参数缺省缺位语义(`entry_ok` docstring 明文「与骨架缺省 area 键原语逐位同」),非门缺失,合同无「门必须非平凡」条款;
- §1.1 出口三语义:免锚出口「发出即 `round_success` 交回」= 出口①终结语义族(「执行即本访问结束交回外循环」)的构造参数特化,与「派发即终结」在册形态同族(op-layer §1.1 两 node 职责 bullet:投资两屏/补给/遭遇/策划/装备;先例引用 = `screens/encounter.md` §2「三出口均终结访问(派发即终结……合同 = op-layer.md §1.1 出口①)」);发出即按成功处理 = 验证废除语义(op-layer §1.2「执行即按成功处理」);重入裁决 success = 出口②原样;两 fail 出口 = 出口③失败治理族(op-layer §1.1 决策控制分层铁律「失败治理与停机(失败出口非决策)」);
- §3 推进型空决策判据「观察 node = 门判定;决策动作 node = 单步推进 + 重入裁决」:判据描述 node 结构形态——免锚形态下重入裁决代码仍在(`act` 顶部 L82),仅空锚参数下不可达;层正本登记粒度 = 类(op-layer §3 屏清单列类名),构造参数分支属类内实现细节,该粒度的申报宿主 = 屏文档(screens/README 头部「每个画面 op 一篇,写『这个画面做什么观察、能发哪些动作 op、哪些动作终结交回外循环』」)。

→ 层合同无缺口可补,op-layer 登记属「无缺口处立规」:把休眠构造分支写进执法合同 = 登记粒度错位 + 口径第二源(免锚变体口径的唯一源应住屏文档,与代码模块头、测试锁互为对账面,三面已在)。T-23 的 §2 自引句变体标记本件亦不需要——本屏 §2 自引「合同 = op-layer §1.1」无字面偏离,标记无处可挂。

**取舍 2:零调用点免锚分支留/删——建议保留;挂账宿主 = §2 补记的「休眠变体、现役零调用点」申报句;退役候清理批**

留的依据:①非意外死码——模块头显式声明(L22-24)+ 内联形态锁在册(`test_anchor_free_advance_succeeds_immediately` 断言免锚出口与重入不可达双轮),变体行为持续有锁护栏,无静默腐化面;②参数化契约的自然退化形态——类构造 `(screen_name, area_name)`,空 area = 缺省缺位语义,删除 = 为零生产需求新增守卫断言并连删模块头声明句与测试锁,零行为收益的纯 churn;③第三档「补给锁定」扩对候裁(屏文档开放设计注在册)保持构造参数化路径,变体面随参数化契约共存。挂账形态沿 `screens/README.md` §7 能力面先例(「商店锁:生产链路未建模——能力面登记为存在但未接线」/「SwapDeploy:生产执行器未接线——能力面按词表完备性保留」):接线状态 as-built 申报住正本申报句自身,不设独立挂账文件、不进本地 TODO(CW 迭代待办归 `.debug/progress/` 进度树)。退役动议(候裁,不在本稿范围):若后续清理批时免锚形态仍零调用点,免锚语义全部申报面一次成文清点后整族退役,逐点列名(共 11 点;漏删任何一点 = 本迭代 F-1 同型「声明面与代码不符」残留):
- 代码/锁面 4 点:① 模块头免锚声明句(L22-24);② `entry_ok` 空锚恒过分支(L59-60);③ `act` 免锚早退分支(L95-96);④ `test_anchor_free_advance_succeeds_immediately` 整支(L293-311)。
- 声明面 4 点:⑤ `entry_ok` docstring 免锚句(L57-58「空锚构造(免锚形态)无观察信号,恒过……」);⑥ `observe` docstring 免锚句(L70 末「免锚形态(空锚)门恒过。」);⑦ `act` 免锚注释半段(L91-92;L93-94「否则循环推进」半段独立保留,注释须重构);⑧ 测试模块 docstring 两 bullet(L17-18 组合锁 bullet 括注「+ 免锚参数化屏」;L21-22 免锚出口锁 bullet)。
- 屏文档面 3 点(= 本稿点位 #1/#2/#3 目标文本写入的免锚成分,退役批执行时必在树):⑨ `screens/prep_locked_return.md` §2 形态句免锚括注群(「构造两形态」句免锚半侧,退役时还原锚形态单面);⑩ 同文件 §4 出口列第三成分「免锚形态 = 发出即 `round_success` 交回,休眠变体」;⑪ 同文件 §5 表第四行「免锚形态:推进已发即交回(休眠变体,现役零调用点)」。
十一点同批执行,禁只删分支留声明面/留屏文档申报(残留 = F-1 同型病)/留锁(锁红无因)。

**验证门(落地批完成判据,机械可判)**:

1. `screens/prep_locked_return.md` §2:「构造两形态」「现役零调用点」各 ≥1 命中,「免锚形态」≥2 命中;
2. §5 表恰 4 行,与代码出口逐一对照(`observe` 门 miss fail / `act` 推进未落地 fail / 重入裁决 success / 免锚 success,锚 = `cw_screen_prep_locked_return.py::CwScreenPrepLockedReturn` 对应行);§5 内 `round_fail` 恰 2 命中;
3. §4「触发返回外循环」列含三成分(重入裁决交回/推进未落地 `round_fail`/免锚发出即交回),与 §5 对应行一致(screens/README §2 模板「一一对应」);
4. 三方口径互查一致:屏文档 §2/§5 ↔ `cw_screen_prep_locked_return.py` 模块头(「参数化变体……免锚形态」句)↔ `test_cw_screen_progression_inline.py`(免锚出口锁 = 本屏专锁;fail/重入出口 = 代表屏 ①/③ 分支间接覆盖——重入裁决出口 = ①,推进未落地 fail = ③——非本屏专锁,覆盖申报见 §1.1)无新分叉;
5. 零行为变化:`git diff` 于 `src/`、`sr-od-test/` 为空;`screens/op-layer.md` 零 diff;改动文件仅 `screens/prep_locked_return.md` 一份(§2/§4/§5 三处)。

### 2.2 落地文件面总表与统一验收

| 文件 | 修法点位 | 性质 |
|---|---|---|
| `screens/prep_locked_return.md` | §2 整段(#1)、§4 对照表出口列(#2)、§5 整节(#3)——同文件一次成文 | 屏文档 as-built 补记(零行为) |
| `src/`、`sr-od-test/`、`screens/op-layer.md` | 零触碰 | — |

统一验收:验证门 1-5 全过;修后口径互查——屏文档 §2/§5 ↔ op 模块头变体声明句 ↔ 内联形态锁 ↔ `cw_loop.py` 分发段(恒传具体锚)四方一致;全部修法零行为变化。
