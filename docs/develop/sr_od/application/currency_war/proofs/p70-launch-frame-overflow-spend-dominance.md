# P70:发射帧溢出段花费弱支配留存(L1-a,两段式判据的溢出段)

> 状态:**已证(结构命题,构造性直证,零自由参数;辖域=溢出段 g>g* 且发射帧∧战斗类节点;带内段 g≤g* 不在本命题辖域,挂 L1' 双门 fail-closed;2026-09-06 正式化)**
> 立项材料指针:`.debug/temp/currency_war/launch_gold_tradeoff/DESIGN.md` v1(命题 L1 两段式设计;sim71 判读二 35/109 帧重叠面 + sink_baseline 三面 p<0.0001 成对恶化 = 立项事实面,非证明输入)
> 代码锚:`kernel/cw_economy.py`(`interest` / `loss_exact` P47 命题 2 双轨迹递推单一源 / `saturation_line` g* 派生 / `in_must_spend_zone` 辖域缺口在案)、`kernel/cw_launch_admission.py:readiness_launch_decision`(发射判据核 armed 单一源,ADR-0557)、`sim/engine_p1.py:1130`(`range(0 if _round_launch else 8)` 发射帧短路决策段=本命题辖域缺口的代码形态)
> 数值自检脚本:`tools/cw/proofs/p70_check.py`(入库可重跑:`$env:PYTHONPATH='src'; uv run python tools/cw/proofs/p70_check.py`,全部断言通过)
> 编号定谳:math_proofs 索引顺延至 P70(索引末位 P69,无 P70 占位冲突,对账批规则下本号成立)。

## 命题(形式化)

**发射帧**(辖域前提,三合取,判据单一源 = `readiness_launch_decision`):

```
armed(= form_progress(comp, state) ≥ 1.0)∧ node_type ∈ {battle, encounter, boss}
```

且处于**本回合备战阶段的最后一帧**(发射即开战:点开战后战斗立即结算;商店每节点自动刷新 1 次、金与经验均在战斗结算后变动——`docs/game/currency_war/data/gameplay.md` 商店/结算两条机制直读)。设:

- `cap = cap_resolved_of_session(session)`,g* = `saturation_line(cap)` = 10×cap(默认局 50);
- **溢出段** D_ovf = {(g, spend) : g > g*, 1 ≤ spend ≤ g − g*},spend 为**线内 offer/升级类即时消费**(消费点对象复用 shop 出口③同族既有评估栈——R1/L2/L3/EV 降排序,不新造第二套评估语义);
- 轨迹 B(留存):不花;轨迹 A(花 spend 后守线)。EV(·) = 花费决策后的未来期望总收益(纯金流口径,血不入账,P51 公理栈)。

**命题 L1-a**:对任意 (g, spend) ∈ D_ovf,任一使战力边际 ΔV ≥ 0 的合法花费满足

```
EV(spend 轨迹) ≥ EV(留存轨迹)        (弱支配)
```

且花费侧是**零息损构造**:息账项 L(g, spend, R, Ī) ≡ 0 在全域 D_ovf 上恒成立,零新自由参数。

**带内段声明(不在辖域)**:g − spend < g* 时 L > 0 可能,判据式 `spend ⇔ S(n)·Δp_win ≥ L(g−spend, R_rest, Ī) + 摩擦` 挂 **L1'** 独立命题,证不出 = 带内维持现状不花(fail-closed)。本命题与 P68(boss 前置帧 r=0 特例)文号独立、触发面独立、禁并键;与 P69(备战等待零收益支配)互补:P69 证「出战支配等待」,本命题证该帧上的**金出口**方向。

## 证明

### 侧 1:溢出段息损恒零(零息死仓构造)

由 P47 命题 2 双轨迹递推(代码单一源 = `loss_exact`):

```
B₀ = min(g, g*) = g*                        (g > g* ⇒ 守线截断)
A₀ = max(g − spend, 0) ≥ g*                 (spend ≤ g − g* ⇒ 花后仍 ≥ 线)
  ⇒ min(A₀, g*) = g* = B₀
```

两轨迹初始金位相等,且其后每轮同收入演化、同守线递推 ⇒ 逐轮金位恒等 ⇒ 每轮息差 `interest(B_k) − interest(A_k) = 0` ⇒

```
L(g, spend, R, Ī) = 0    ∀ R ≥ 0, ∀ Ī        (对 R、Ī 全域恒零,连参数都不需要)
```

即:**溢出段的金是零息死仓**——花掉它的机会成本(息侧)精确为 0,不是近似。∎(脚本对账:`p70_check.py` 全域网格直调 `loss_exact` 断言 L=0;对照断言:带内同参数格 L>0,划出辖域边界。)

### 侧 2:留存收益通道在发射帧恒零

留存的金在发射帧之后、下一次经济事件之前的窗口内没有任何收益通道:

1. **息侧已被侧 1 封死**:两轨迹都钉在息帽 g* 上,息流恒 `cap`/轮,花不花都一样——留存的边际息收益 = 0;
2. **备战决策数 = 0**:发射帧是本回合备战最后一帧(发射即开战,见辖域前提),窗内不存在第二次买/刷/升决策——金留在手里没有下一个决策点可消费(`in_must_spend_zone` 辖域 = 有动作决策点帧,发射帧决策段数 = 0,即 35/109 帧金 ≥ g* 从未被任何判据看见的辖域缺口);与 P52 窗口局部 ε-最优形态同构:窗内剩余备战决策数 = 0 ⇒ 留存收益项恒 0;
3. **战斗结算时点隔离**:金与经验均在战斗结算后变动、商店按节点自动刷新(gameplay.md 机制直读,P69 同源前提)——留存金在「本帧」不产生任何中间收益。

### 侧 3:消费侧战力边际非负

发射帧消费即时转化为**本场战斗**战力(买牌 → 上板 → 开战)。对线内 offer/升级类消费,更强板面 ⇒ 胜率不降且条件败局伤害不升(掉血锚,注册表 `streak_floor_loss_damage`:encounter 截距 24.32 / boss 常数 26.71;boss P15v2 冻结语料锚 26.79 [24.25, 29.23]——均 ≥ 0)。故战力边际 ΔV ≥ 0。∎

### 合成

```
EV(spend) − EV(留存) = ΔV − L(g, spend, R, Ī) − 摩擦
                     = ΔV − 0 − 0 ≥ 0         (侧 3 − 侧 1 − 摩擦侧)
```

**摩擦项 = 0 的辖域依据**:本命题辖「买来上本场板」的即时战力消费——买入件即上场、不在当帧卖出,卖出退金折损(P47 命题三 fee:cost≥2 合成件卖回 −1)不进入本帧账;消费选择次优性由消费点复用既有评估栈(只产 ΔV ≥ 0 候选,且零花是退化成员)排除。因此合成式右端 ≥ 0:**花费弱支配留存**。∎

**严格性注记**:弱支配,非严格——ΔV = 0 的格(消费与不消费战力等价)两轨迹恒等;格 ΔV > 0 时严格优。

## 边界(什么情况失效,如实入册)

1. **出溢出段即失效**:spend > g − g*(花穿息线)或 g ≤ g* 时 A₀ < g*,侧 1 不再恒零——带内判据挂 L1',fail-closed 维持现状不花;
2. **消费对象出辖失效**:辖域消费 = 线内 offer/升级类即时战力消费。线外囤件、3★ 深度凑件等非即时战力消费不在本命题背书(其 ΔV 未必 ≥ 0);**计划卖回型消费也不在辖域**(其净账含卖回摩擦与时点敞口,归 P47 命题三动态修正口径——ΔV=0 的卖回计划件可因 fee ≤ −1 金严格劣于留存);
3. **cap_resolved ≤ 0(买断制息关)**:g* = 0,息账整体消解(L ≡ 0 平凡成立),命题数学上仍真但 g* 语义退化——谓词层沿用 `in_must_spend_zone` 同款「cap ≤ 0 出辖」的工程保守选择,非命题失效(显式声明,防并键);
4. **发射帧定义失效**:若机制变化使点开战后仍有备战决策(或商店刷新/金经验结算时点改变),侧 2 的「窗内决策数 = 0」前提破,留存收益通道重现 → 本命题须重验;
5. **掉血锚过期**:侧 3 的 ΔV ≥ 0 只依赖「更强板面不掉血变多」的单调性,锚值(24.32/26.71/26.79)只作辖域声明不入推导——锚过期不破命题,但 S(n) 数值面(带内段 L1' 判据输入)须随注册表重锚(P10 教训:禁字面量第二份)。

## 数字(脚本复算锚)

| 项 | 值 | 出处 |
|---|---|---|
| g*(默认局) | 50 = 10×cap_resolved(cap=5) | `cw_economy.saturation_line` |
| 溢出段息损 L | ≡ 0(全网格:cap∈{5,9,10} × 溢出量 1-120 × spend 全量枚举 × R 0-12 × Ī 4-9) | `p70_check.py` 断言 1 |
| 带内对照格 L(50,10,1,Ī)| = 1 > 0(A₀=40 掉一档)——出辖即失零 | `p70_check.py` 断言 2 |
| 掉血锚 S(n) | battle 复用 vd_p1_loss 单一源 / encounter (24.32, −4.53·rung) / boss (26.71, 0) ;boss P15v2 锚 26.79 [24.25,29.23] | `cw_registry.streak_floor_loss_damage` + p15 冻结语料 |
| 立项事实面(非证明输入) | sim71:「armed∧战斗节点∧金>g*」重叠 35/109 帧;sink_baseline:短路臂金出口/花费/空转三面 p<0.0001,残金杠杆 134.75→50.5(≈84 金/局死仓) | DESIGN v1(立项材料指针) |

## 结论强度

| 项 | 强度 | 最弱环节 |
|---|---|---|
| 侧 1 恒零构造 | **强**(P47 递推直代,对 R/Ī 全域恒零,零自由参数) | 无(脚本全域复算) |
| 侧 2 留存零通道 | **强**(机制直读 + 发射帧语义,两处机制文本互证) | 发射帧定义随机制版本变(边界 4) |
| 侧 3 ΔV ≥ 0 | **强**(单调性论证,锚值不入推导) | 消费点评估栈只产 ΔV≥0 候选是实现契约(边界 2) |
| 弱支配合成 | **强**(三侧直加,辖域内零例外) | 辖域声明即命题边界(五条全入册) |

## 关联

- **P47**(息账 L 递推单一源——侧 1 的全部数学)、**P52**(窗口局部 ε-最优形态——发射帧是其推广点)、**P68**(boss 前置帧 r=0 特例,同构不同号,禁并键)、**P69**(备战等待零收益支配,同源机制前提,互补不重叠)、**P11**(溢余金机会成本——shop 决策帧同族,本命题辖发射帧新辖域);
- **ADR-0557**(发射帧短路——本命题的辖域缺口即短路保留的决策段禁区;修复处置 = 短路前插消费仲裁段只跑本命题溢出段判据,带内 fail-closed);
- 设计正文:`.debug/temp/currency_war/launch_gold_tradeoff/DESIGN.md` v1 §1.1/§1.4(命题 L1 两段式与统一判据式;带内段 L1' 立项挂账)。

*(本件为 v1.0;未改任何现有证明文件;数值自检脚本入库 `tools/cw/proofs/p70_check.py` 可重跑。)*
