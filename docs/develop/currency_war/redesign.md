# 货币战争策略系统重设计(CW Strategy Redesign)

> **状态**:方案定稿(2026-08-21;rev9 吸收八轮评审共 100 条:
> 22+17+20+13+9+9+5+5;第八轮判"接近收敛可进入实现评审",
> rev9 收掉最后 3 条边角+2 条实现期注释项;
> rev9 重点=已访问线集合入累积态清单(遥测重建)/retest 局
> 证据只进撤销判据不入池/撤销归因判据(目标受阻才断连续)/
> 兜底线遇险行为声明;
> **r201 用户裁定补**:星级三档模型——辅助「能升就升」
> (opportunistic 档:不为辅助花刷新费,但刷出就买——
> 压缩牌库时阵容优先→CARRY 优先→辅助顺手升,是玩家策略的
> 自然结果;应急降档语义同步对齐 target→opportunistic)
> **r202 用户裁定补**:牌库操纵层融入经济模式(§4.2)——
> 1费净0压缩(零成本抽噪声)/备战席压缩件槽位管理/
> 卖出规则入线库(引擎件清理时序+兼容性)——对齐
> user_playstyle [1][11][15][17] 与 economy.md §3
> **r203 融合总表(§4.6)**:逐条对账 user_playstyle 26 条 +
> plaza_methodology M1-M16 + economy §1-8——落点分
> 结构/规则/常量/已含四类;挖出三处方案修正:
> 应急保留重生基数([18] 死亡螺旋)/bench_windows 买而不上
> ([21])/连胜维护分支([19] 三变量抉择)
> **r204 代码级审计(§6 重写)**:对账 13 个策略代码文件——
> 发现 CwStrategy 插件骨架与 redesign 正交(新策略=新具现,
> 不推翻框架);DP(cw_horizon)退役为兜底姿态(证据等级裁定);
> 可复用资产清单(shop_odds 超几何/investments 台账/
> roll_affordable 金计价门);感知质量门继承(last_hp 时效/
> level 单调守卫/streak 结算源);跨局层(run_allocator)
> 补入未覆盖清单
> **基础**:784 篇官方投稿全量深读(十类终局阵容分类 r167-r187)+
> 战力基线表 r191+用户系列裁定(r149-r192)。
> **范围**:策略层的数据模型与决策架构,不涉及代码细节。
> **数值约定**:文中所有数值均为示例(便于表述),落地以代码常量为准;
> 常量索引表见附录 A。

## 1. 背景与动机

现状的策略层存在四个被深读证据推翻的假设:

1. **终局阵容写死**:现有阵容库枚举每个阵容的固定成员与羁绊档位。
   深读结论:核心角色固定,副羁绊按来牌灵活替换(作者原话级证据)。
2. **过渡与终局割裂**:现有过渡体系与终局阵容库是两套独立数据,
   依赖某时刻"切换"。深读结论:阵容是连续演化的,不存在硬切换点;
   开局引擎选择由来牌决定,不属于任何一条线。
3. **没有阶段概念**:现有策略不知道"当前阵容能不能打过接下来的
   节点",导致买牌/升人口/存利息的时机决策缺乏依据。
4. **装备与站位静态**:装备跟角色绑定、领航员绑定是阵容属性。
   深读结论:装备公式跟核心角色驱动型走且有显式时序,领航员绑定
   是时间函数(前期保命绑生存位,后期输出换绑)。

## 2. 核心洞察

**阵容能否通过接下来的节点,是一切策略决策的根基。**

攻略作者们脑内有"这个阵容能打过哪个节点"的经验查表(「3仙舟足以
碾压整个第一位面」「四人组保持健康血量打到boss关」)。有了这个
判断能力,"战力期还是经济期"的阶段划分、买卖时机、锁线时机全部
退化为查询。784 篇投稿正是这张表的标注数据:每篇的
Early/Middle/Final 阵容+通关事实=该形态在该位面"能过"的下限证据。

已提取的位面级战力基线(幸存者证据,单向):

| 位面 | 人口中位 | 主流验证形态 |
|---|---|---|
| P1 | 3-5 | 仙舟3+DOT2(81篇)/列车4(21)/仙舟3+列车2(16) |
| P2 | 6-7 | 列车4+护盾3(40篇)/圣杯任务形态(13) |
| P3 | 9-10 | 列车6系(18+12篇) |

三连贯通形态唯一:列车4+护盾2(11篇,P1P2P3 全位面可用)。

**先验表的统计缺陷声明**:
- 篇数=流行度≠通过率(没人写失败的投稿)
- 跨版本漂移(入表按版本过滤,只收 4.4)
- 作者练度系统性高于 bot——全表水位偏乐观,**阈值乘保守系数**
  (常量 CONSERVATIVE_FACTOR,粗标定后遥测精标;详见 §7 顺序)

**贯通单点风险声明**:三连贯通形态唯一意味着克制列车系的对局
无贯通级备选——线库换线部分缓解,接受残余风险。

## 3. 总体架构

**每个决策节点 = 查战力表决定「战力 or 经济」+ 查线库决定
「为谁积累」+ 查规则集执行具体动作。**

```
┌────────────────────────────────────────────────────┐
│ 决策循环(每节点执行一次,顺序即仲裁优先序)                │
│  1. 感知:己方(羁绊/人口/等级/金币/商店/手牌/事件/HP)      │
│     + 可得的敌方/词条情报                                │
│  2. 锁线检查(先于模式判定:锁线改变动作象限,               │
│     本节点内锁线则本节点即按新象限裁决)                    │
│  3. 应急检查:危急?→ 应急模式(最高覆盖态)                  │
│  4. 查战力表:下个战斗目标能过吗(三级回退)                  │
│     + 滞回计数(战斗节点为计数单位)                         │
│     → 经济/战力模式                                       │
│  5. 象限仲裁表取动作 → 执行(买/卖/合装/站位/换线)          │
│  覆盖态优先序(严格):应急 > 追赶期修饰 > 模式(经济/战力)     │
└────────────────────────────────────────────────────┘
       │              │             │
  战力表           线库           规则集
       └──── 遥测实跑持续校准(探索局打标隔离) ────┘
```

三个数据层的职责:
- **战力表**回答"现在够不够强"(地基,决定模式)
- **线库**回答"往哪个方向积累"(方向,决定资源倾斜)
- **规则集**回答"这一步具体做什么"(动作)

## 4. 数据层设计

### 4.1 战力表

```
内容:形态 × 战斗目标区间 → 验证篇数(敢用白名单)
形态键:羁绊组合+人口+核心角色在场标记+核心星级
目标区间:P1 前/中/后、P2 三段、P3 两段(投稿只有位面级;
  节点区间初版用位面级+遥测逐段细化)
查询(三级回退,返回带证据等级):
  a) 精确命中且篇数≥阈值 → 过(强证据)
  b) 降维命中(先降星级维,再降区间维回位面级)且篇数≥降维阈值
     → 过(粗证据)  c) 仍无 → 不过(miss)
**miss 与滞回的统一语义(rev8 补复位粒度)**:
  miss 计入滞回计数器(与"不过"同权);
  粗证据的计数语义:**粗证据=过;粗证据连击达 COARSE_TOLERANCE
  → 自此持续改判 miss,直到出现强证据(精确命中)或
  **核心维变化**(核心角色在场标记或核心星级变化)才复位连击**
  ——形态变化的复位粒度=**核心维**(羁绊组合/人口/骨架件的
  变化**不**复位——否则战力模式自己的购买动作会反复冲掉
  改判,死区经由该路径复发);持续粗证据形态稳定累积 miss →
  正常进入战力模式补强
边界声明:
  - 维度交叉后稀疏,回退是主路径;各级阈值独立标定
  - 词条维未入;counter 词条由线库吃怕表在锁线层规避
  - 幸存者偏差单向;作者-试用差由保守系数吸收
  - 版本过滤;遥测聚箱顺序=回退顺序
**双证据源合成(rev6 补输入域与版本边界)**:
```
遥测证据池口径:**含探索局与正常局的全部 bot 实跑**——
  负证据的采集本身依赖探索(正常局策略绕开 miss 形态,不会
  自然积累负样本);探索局的采样有偏性由 NEG_EVIDENCE_MIN
  的更高门槛吸收(否决要求更多次尝试+更高失败率)。
  正证据(判"过")同样全池统计,EXPLORE_REPEAT_MIN 足额即可。
遥测源判定三分态:过(复现数≥EXPLORE_REPEAT_MIN)/弃权(无数据)/
  否决(负证据:尝试≥NEG_EVIDENCE_MIN 且失败率≥阈值)
合成规则:
  投稿过 × 遥测{过|弃权} → 过
  投稿过 × 遥测否决 → 不过(bot 实测证伪优先)
  投稿无 × 遥测过 → 过(遥测解锁,标 source=telemetry)
  其余 → 按投稿源单独判定
保守系数只作用于投稿源阈值(bot 遥测无需作者折扣);
生效折扣=投稿侧 CONSERVATIVE_FACTOR,与 Step0 标定对象一致
**版本生命周期(rev8 补否决撤销与重测引导)**:版本切换(如 4.5)
  时,**遥测源条目全部降级为弃权,但保留「曾有否决」标记**——
  带标记的条目跨版本后仍从正常局白名单中排除,并排入探索局
  重测队列。
  **否决撤销判据(rev8 修死锁)**:重测队列中连续
  VETO_REVIVE_PASS 次探索局通过 → **撤销否决标记,回到弃权态**
  (允许正常局采样;正常局复现满 EXPLORE_REPEAT_MIN 后恢复
  完全判定)——撤销路径闭环:否决(正常局排除)→探索局重测
  (连续通过)→弃权(正常局可采样)→正常局复现→过。
  否决不是永久封禁。
  **重测引导策略(rev8 定义;rev9 补证据口径)**:
  探索局按队列头部的重测目标运行引导:目标形态的核心件在商店
  出现时优先购买(覆盖锁线信号判据——探索局的目标优先);
  引导局自身的锁线/D 牌失败/降级逻辑照常(重测目标 D 卡失败
  计数照攒,失败→该形态的正证据不能确立,等价于一次重测失败);
  引导局打标 exploration=retest(与普通探索局区分,
  过程指标单独口径)。
  **retest 局的证据归因(rev9)**:retest 局的胜负证据
  **只进否决撤销判据,不进通用证据池**(引导加成=对目标形态
  系统性有利偏置,与"探索局打标隔离"纪律对齐;局末形态
  偏离目标时无归属争议——不入池就没有归因对象问题)。
  **撤销连续性的归因判据(rev9)**:清零「连续通过」仅当
  **目标形态的关键战斗节点实际受阻**(该形态的 miss/掉血
  导致失败);其他原因的失败(锁到别的线/其他位面血崩/
  bot 异常)不中断连续计数——防与目标无关的噪声无限推迟撤销。
  source=telemetry 的解锁条目随遥测降级一并失效,同样入重测队列。
  投稿源按版本重提取。
**正证据的探索局偏差(rev7 声明)**:判"过"的正证据若全部
  来自探索局,其代表性存疑——EXPLORE_REPEAT_MIN 之上再加
  要求:**至少一次复现来自正常局**(全探索来源的"过"降级为
  弃权级,不单独解锁形态)
```
```

### 4.2 引擎池(全局层)

```
成员(按 P1 榜验证):DOT2/仙舟3/列车2+;战技点2 待核
  **待核项处理(rev4)**:核验任务=逐篇读战技点2 主导的 P1 篇目
  (约 31 篇),判据=独立成引擎还是挂件;核验在 Step 2 之前完成,
  不过则不入池
开局逻辑:看前几家商店能凑哪个引擎 → 用它,与线的选择解耦
锁线后:按引擎×线兼容性决定引擎件留或卖
**经济期的通用积累方向(未锁线时)**:通用保值件优先级
  (折价可承受的次优,非零浪费——占用利息/备战席,诚实声明):
    1. 跨线通用骨架件(瓦尔特/星期日/花火/符玄)
    2. 双羁绊引擎件升星  3. 经验/人口
  金币纪律:保值件购买受利息地板约束

**牌库操纵层(经济模式的第四个动作;r202 融合已沉淀玩法)**:
```
机制底(已核实,economy.md §1/§3):
  1费各星卖出净 0(无手续费) → 零成本压缩牌库;
  cost≥2 的 2星合成再卖亏 1 手续费(需算账);
  池结构:1/2费 27 张/种,3-5费 9 张/种;备战席空槽是前提

融合方式——不是新模块,是经济模式动作表的扩展:
  经济模式动作优先序(未锁线):
    1. 线内/保值件购买(原有)
    2. ★牌库压缩(1费净0买,see 压缩判据)
    3. 人口/经验
  压缩判据(全部满足才执行):
    a. 当前金币 > 利息地板(购买不破息)
    b. 备战席有空槽(占格非瞬时)
    c. 目标卡是 1费(零成本);2费仅在「该卡同时是
       线内 opportunistic 档目标」时买(不然卖回亏1)
  压缩的目标选择:
    锁线后:目标线的同费卡优先(提高目标卡浓度);
    未锁线:任意 1费(全局噪声抽离——user_playstyle [15]:
      压缩是全局供给策略,买的 1/2 星既是 3合1 素材
      也抽走噪声)
  卖出规则(锁线后清理):
    非线内 1费压缩件 → 需要备战席空间时卖(净0);
    cost≥2 过渡件 → 线成型后按兼容性表处理(留/卖),
      卖出损耗=1手续费已在「折价可承受」声明内
  与 user_playstyle 的对齐:
    [1] 保息前提下多买 1/2星=压缩牌库 ✓(判据a)
    [11] 金<20(1息档)买过渡件不损息 ✓(地板按息档计)
    [17] 50金息律:>50 的每一分都该花——压缩是「该买牌」
        的合法去向之一 ✓
```
```

### 4.3 线库

每条线包含:

```
身份:line_id/核心角色/驱动型(大招流|受击流|行动流)/信号+层级
形态:P2/P3 目标形态(含人口与羁绊档位)/副羁绊可替换组
星级:成员星级目标三档(见下「星级三档模型」)
装备:驱动型装备公式(含时序)/A·B 双套/领航员后期换绑点
运营:D 牌锚点/备战席规则(含压缩件槽位管理)/多余人口方案/
      降级路径(必填)/换线触发(counter 词条/怪 → 换;
      D 卡失败 N 次 → 走降级路径)/
      卖出规则(引擎件与过渡件的清理时序+兼容性)
门槛:自动战斗友好度/局外依赖排除/词条吃怕表
```

**星级三档模型(r201 用户裁定:辅助能升就升)**:
```
target_star(专程D档):CARRY 与机制位辅助——值得为它花刷新费
  (姬子/万敌/白厄;机制位=星级改变团队机制的辅助:
   符玄「生存位硬=全队硬」/不死途「按星级放大巡海增益」/
   爻光「速度=阿哈触发率」/小黑塔「备战席供星级」)
opportunistic_star(顺手升档):一般辅助——不为它刷新,但
  刷出来就买(利息地板内):「压缩牌库时阵容优先,CARRY 优先,
  辅助顺手升」是玩家策略的自然结果(刷 CARRY 时辅助卡
  大量带出,买它们几乎零决策成本);证据:「钱多追银狼和
  爻光星级」(银狼#5)/「闪闪运气好可以上3星」(圣杯#118)/
  「远坂凛星级越高越好」(圣杯#68)/「钱多升星」(姬子#225)
floor_star(保底档):达不到时列入战力模式补强(1→2 差距大,
  2→3 是富余优化——攻略的「其余2星足矣」是保底口径,
  「能升就升」是富余行为,两者不矛盾:「足矣」=不用专程花钱)
购买判据:看到线内卡 → 当前星级 < 所在档上限 → 买
  (target 档允许为它主动 D;opportunistic 档只买刷出的)
```

三条驱动型是线库的脊柱:
- **大招流**(姬子/黄泉/大黑塔/绯英/圣杯Saber):充能件+风暴潮
- **受击流**(万敌/白厄/盾反姬子):反甲+吸伤害位互斥+词条依赖
- **行动流**(希儿/银狼/圣杯Archer):速度件+拉条链

**同层信号冲突仲裁(rev4 补)**:同层多信号命中不同线时,
按「先到先锁」(累计器/出现顺序),且同层冲突时要求信号判据
的完整度更高才锁(保守纪律的具体化:冲突=不确定,不确定不锁,
降级到下一层判据或兜底)。

### 4.4 信号与锁线

```
信号四层:0 策略驱动/1 类专属策略/2 核心卡/3 资源判据/∞ 兜底
锁线时机随层级:0 层开局即锁;2 层看到即锁;∞ 层 P2 按质量锁
感知约束:
  0/1 层:策略名有限枚举,模板匹配;0 层双帧确认+图标交叉验证
  2 层:单帧 OCR,置信下限,未识别不动作
  3 层:跨节点累计器;口径=进店帧可见次数(refresh 波不可见,
    已知遥测边界;**「万敌7只」判据=累计见到万敌 7 次**,
    按可见口径标定);换线时累计器清零
锁线保守纪律:漏锁可接受(退化兜底线),误锁不可接受
```

### 4.5 领航员绑定(两层)

```
全局层(前期):绑当前引擎的生存位/装备载体(三月七 P1 通用最优)
线库层(锁线后):线的后期换绑点;锁线时检查兼容性,不兼容才换
```

### 4.6 已沉淀玩法的融合总表(r203;逐条对账 user_playstyle 26 条 +
plaza_methodology M1-M16 + economy §1-8)

> 本节是**融合审计表**:左列=已沉淀的知识,右列=在本方案中的落点。
> 落点分为四类:【结构】=进入数据模型骨架 /【规则】=进入决策规则 /
> 【常量】=进入参数标定 /【已含】=前文已有对应设计。

**user_playstyle(口述最高权威)**:

| 条目 | 融合落点 |
|---|---|
| [1][15] 压缩牌库=全局供给策略 | 【已含】r202 牌库操纵层 |
| [2] 前期卖血+少刷新+优先凑息+买过渡 | 【规则】经济模式动作序;利息地板 |
| [3] 看核心在几级刷新概率大,没到就少D多买经验 | 【已含】线库 roll_anchor(D 牌锚点=核心费用档定等级,M5)|
| [4] 辅助2星够1星无所谓能3星更好可囤 | 【已含】r201 星级三档(floor/opportunistic/target)|
| [5] 4/5费核心必须有低费输出过渡 | 【规则】线库校验项:carry 为 4/5 费的线必须声明**过渡输出件**(绯英→银狼线/姬子→圣杯线);无声明不入选 |
| [6] 中期偶尔掉到40/30息,前提商店全是想要的 | 【规则】利息地板软化档:连续 N 节点商店命中线内件时地板降至 40/30(「偶尔」=条件触发非默认) |
| [7] 不无脑停留概率峰值级,费用段带内权衡 | 【规则】roll_anchor 不取单点等级,取**费用段带**(cw_shop_odds 概率表查询段) |
| [8] 后期必须多人羁绊,散装不可能通关 | 【已含】战力表形态键=羁绊组合(散装天然 miss→战力模式) |
| [9] 装备优先输出→辅助核心→盈余他人 | 【已含】装备公式优先序(carry_formula 最先) |
| [10] 选当前版本最容易通关的阵容 | 【已含】线库门槛(局外/自动友好/词条)+降级路径 |
| [11] 金<20 买过渡件不损息 | 【已含】r202(地板按息档计) |
| [12] 没凑到50不急升级 | 【规则】人口升级前置条件:息引擎已立(金≥50 或已锁线) |
| [13] 过渡 lv5-6+羁绊凑够+核心2星≈过 P1,停手攒息 | 【已含】战力表 P1 榜命中→经济模式的实例化 |
| [14] 星期日=通用辅助填充件 | 【已含】通用保值件优先级 1(骨架件) |
| [16] 必胜节点不D牌/升级零战损/倾向买经验 | 【规则】节点类型感知:奖励节点→刷新动作全关,买经验优先 |
| [17] 50金息律,满息即花 | 【已含】经济模式动作序(满溢即花,r202) |
| [18] hp低=运营质量报警,非ALL IN触发;死亡螺旋警惕 | 【结构】应急模式的定位被此条约束:应急≠花光——行为是「最小必要支出止损+息引擎尽量保住」(§5.4 修正:「放弃利息」改为「**利息让位但保留重生基数**(留 20 金级)**,只有效果让位于血线且无法止损时才升级为花光」——位面末最后一战除外) |
| [19] 连胜vs保息是有意识抉择(三变量) | 【规则】连胜资产字段:session 追踪连胜状态;经济模式新增**连胜维护分支**——已连胜+下节点高难(1-7/1-9 型)→允许破 50 保连(变量:连胜在手/血量边际/来牌顺) |
| [20] 过渡是有配方的成型阵容,不是乱混 | 【已含】引擎池(配方=DOT2/仙舟3/列车2 的结构化组合) |
| [21] final件买而不上,等窗口(核心等级窗/羁绊齐窗/变阵点窗) | 【结构】线库新增字段 **bench_windows**:每条线声明 final 核心的上场窗口(姬子=7级/白厄=4击破齐/万敌=1-9);备战席囤件≠上场,窗口到才上——修正了原方案「锁线即向目标形态推进」的隐含假设(推进的是购买,不是上场) |
| [22] 升级为阵容服务(框架单位等着上场→升人口) | 【已含】[21] 的对偶面:人口升级触发含「bench 有等待上场的框架件」(与追赶期升人口并行的第三触发源) |
| [23] 终局线由贯穿件锁定,不是分数涌现 | 【已含】信号四层(2 层核心卡=贯穿件;分数涌现换线被否——换线只走 counter/降级两条显式路) |
| [24] 装备转移是常态(过渡穿→核心到→迁移) | 【已含】装备公式的时序语义(「前期给X后期转移」)+新增:**迁移成本=一次性拆卸**,过渡件装备优先序考虑未来迁移(装备公式字段注明「可迁移件」) |

**plaza_methodology(M 系)**:

| M | 融合落点 |
|---|---|
| M1 入口是资源非阵容(env→策略→comp→细节) | 【已含】决策顺序=感知词条→信号锁线→线内推进 |
| M2 核心×弹性填充 | 【已含】线库(carry+flex_bonds) |
| M3 枢纽分级(早期核心/终局枢纽/纯过渡) | 【规则】保值件优先级内部排序依据:早期核心(千冶·刃0.96)>枢纽(瓦尔特/符玄)>纯过渡(只买不囤);纯过渡件的卖出规则用 M3 存活率 |
| M4 过渡=骨架拼装,并行推进 | 【已含】引擎池+锁线时机;骨架与终局并行=未锁线时保值件照买 |
| M5 人口=carry 费用档函数 | 【已含】roll_anchor(1费→5级/3费→7级/5费→速9) |
| M6 升星次序经济学(先贵后贱) | 【规则】target 档内多成员的 D 序:**先高价后低价**(升星改变费用,先锁贵件的当前费率)——线库 roll_anchor 声明序 |
| M7 装备角色特定+优先级序列+聚焦不分散 | 【已含】装备公式(carry_formula+显式时序);「攒大件不匀合」=合成优先序字段 |
| M8 无损窗口/定向梭哈窗口 | 【已含+规则】无损窗口=r202;升级节点定向梭哈=「升级前一刻金币可全额投入」规则(与[12]的息前置并存:息已立才有梭哈资格) |
| M9 保血硬约束/连胜资产/万敌独前排 | 【已含】应急模式+M9 站位;万敌独前排=受击流站位规则 |
| M10 灵活变阵/切阵容=装备驱动/成型后掉血补装备不换阵 | 【规则】**掉血归因三分法**进决策:成型后掉血→战力模式走「补装备/星级」路径,**不触发换线**(换线仅 counter/降级);切主C 需装备门槛(试用凛三件套证词)——线库字段 carry_equip_gate |
| M11 试用/本体/局外可行性 | 【已含】局外排除(r176)+试用 carry 装备门槛(M10) |
| M12 站位三层(档位/comp特例/动态换位) | 【已含】站位规则(受击流独前排等)+动态换位=阵容演进的伴随动作(线库形态含站位) |
| M13 遭遇难度选择=经济权衡 | 【规则】遭遇节点难度选择:预期战力富余→选低难多吃息;紧张→选高难避免掉血(战力表查询的次级用途) |
| M14 巨星/伙伴选择=comp引擎×乘区 | 【规则】线库新增字段 **idol_plan**:盛会之星给谁/巨星绑定(「给花火」「绑三月七」——每线声明) |
| M15 词缀双向选择(克制/有利) | 【已含】词条吃怕表(eat_kw 也在开局即用:吃词条的线优先级升) |
| M16 狼狩装备-经验耦合 | 【常量】线内含狼狩/混狼狩时,装备分配的边际收益+XP;纪律=物品栏积压报警才处理(用户修正版) |

**economy(§ 系)**:§1-3 已含(r202);§4 电表倒转不采纳(ADR-0211 维持);§5 关键回合多刷=战力模式刷新上限动态化(已含滞回+应急,多刷的具体上限=常量);§6-7 保血vs保经济/阶段经济=应急+模式切换的结构本身;§8 微经济(概念股送装备件)=开局环境选择的次级信号(idol_plan 同族)。

### 4.7 融合产生的三处方案修正(r203,上表挖出的)

1. **应急≠花光([18] 死亡螺旋警惕)**:§5.4 的「放弃利息」修正为
   「利息让位但保留重生基数(留20金级);无法止损才升级花光;
   位面末最后一战除外」——对齐[18]「断息之后经济可能再也无法
   支撑完成整局」
2. **bench_windows 字段([21] 买而不上)**:线库新增 final 核心
   上场窗口(核心等级窗/羁绊齐窗/变阵点窗)——原方案隐含
   「锁线即推进上场」是错的,推进的是购买,上场等窗口;
   与[22]「升级为阵容服务」构成双向约束(窗口到→检查人口够不够)
3. **连胜维护分支([19])**:经济模式新增三变量抉择
   (连胜在手/血量边际/来牌顺)——连胜是资产,已连胜+高难节点
   允许破息保连;未连胜不为单场破息

## 5. 决策循环

### 5.1 主流程(顺序即优先序,rev4 重排)

```
每节点:
  1. 感知
  2. 锁线检查(先于模式——锁线当节点即生效,消歧)
  3. 应急检查(最高覆盖态)
  4. 战力检查(三级回退)+滞回计数 → 经济/战力模式
  5. 象限仲裁表取动作 → 执行
```

**时序消歧(rev4)**:锁线提前到模式判定之前,锁线发生当节点
即按新象限(已锁)裁决——不存在"刚锁线但按未锁线动作"的窗口。

### 5.2 战力模式补强(已锁线×战力象限)

```
优先级 1:目标形态缺失羁绊件  2:通用骨架件
3:引擎补件  4:装备合成
**利息纪律(rev4 明确)**:战力模式保有利息地板(与经济模式同);
  只有应急模式放弃利息——战力≠panic,是"计划内补强"
```

### 5.3 敌方与词条(边界声明)

```
可得:开局词条选项/节点类型/boss 名(部分)
不可得:对手阵容细节/敌方词条实时变化
后果:counter 检测只用可识别信号;P3 决赛圈对手针对不在范围
```

### 5.4 应急模式

```
触发(存量语义):剩余可承受败局数 = HP / 单次战斗典型掉血
  ≤ 危险档(DANGER_ROUNDS)→ 触发
  典型掉血初值与标定:初值来自投稿量化证词+按位面分档保守估计;
  遥测按形态分层标定;**表外形态(miss 形态)的掉血**用
  「低置信形态池」的合并分布(遥测探索局积累),不用全池均值
行为(优先级高于经济/战力模式;**[18] 死亡螺旋修正 r203**:
  应急≠花光——利息让位但**保留重生基数**(留 20 金级),
  无法止损(战力模式+降档仍连续 miss)才升级为全额投入;
  位面末最后一战除外(赢了带板进下位面,无后续经济问题)):
  利息让位(保留重生基数)/不升人口买即战力/生存装插队/
  **星级目标临时降档**(target 档成员降 opportunistic 档——
  CARRY 从追三星降为刷到就买,与星级三档模型对齐)
  降档语义:应急的星级降档是临时目标调整(target→opportunistic),
  不是走正式降级路径;
  应急退出后回到线的原星级档(已买的降档件保留,按锚点自然处理)
退出(双条件,均以战斗节点计;**游戏机制前提声明**:
  本玩法 HP 非单调递减——节点评价回血(SS 评价补 HP,
  姬子类#46 证词「最终 boss 会 SS 补回 40 点血」)、
  治疗羁绊/装备/事件均可回升,应急退出可达):
  HP 恢复安全档(HEAL_HYSTERESIS,存量档位)持续满足
  **EMERGENCY_EXIT_HOLD 个连续战斗节点**(独立计数器,
  专用于应急退出防抖——应急进出不受"模式滞回"约束,
  但有自己的退出保持计数;常量入附录)
人口追赶期(应急退出后进入):
  - 作为模式修饰态叠加;追赶期内允许**临时下浮利息地板**
    (追赶地板=常规地板×CATCHUP_FLOOR_RELIEF,部分破地板——
    **下浮只覆盖还债动作**(升人口/线内件/引擎补件),
    保值件不享受;追赶退出恢复全地板)
  - 叠加经济:升人口置顶;叠加战力:线内件与升人口按金币分流
  - **追赶期 miss 旁路(rev7 修应急漏斗)**:追赶期滞回计数暂停,
    但**miss 不静默**——追赶期内每次战斗节点采样 miss 时,
    直接执行「追赶×战力」的动作分流(不受当前模式限制:
    滞回暂停的只是模式切换,不是战力响应);消除位面切换后
    「追赶(经济)→miss 无法切战力→掉血→应急」的漏斗
  - 追赶期滞回计数暂停;**追赶退出时强制重采样、streak 从
    当前形态重数**(追赶期形态实质演化,旧 streak 属旧形态——
    与锁线/应急/换线的级联重裁决同规格)
  - 追赶退出判据=人口≥位面基线减容差(CATCHUP_TOLERANCE,
    允许-1)且容差带回滞(防基线±1 摆动)
  - **位面切换的常态化触发(rev6 声明)**:进 P2/P3 时人口
    通常低于新位面基线 → 结构性进入追赶——**这是设计意图**
    (位面切换本来就该升人口),追赶期在遥测中将大量出现
    属正常,非异常信号;判读指标据此口径化
  - 追赶期内应急再触发:追赶态清除;应急再退出后重新评估:
    人口仍低于基线→重进追赶(幂等);已达标→不进
  - 重启恢复:首节点检查人口低于基线→重进追赶(幂等)
优先序(全文唯一真值,rev5):应急 > 追赶修饰 > 模式(经济/战力)
```

### 5.5 模式仲裁与滞回

```
**四象限动作表**:
  未锁×经济:保值件(利息地板内)+人口+引擎维护
  未锁×战力:引擎最优补件(档位提升>骨架>保值)——无目标形态
  已锁×经济:线内件+人口(按线的节奏锚点)
  已锁×战力:§5.2 分层补强
  同节点多来源冲突:按象限表唯一取用

**滞回(rev5 补采样-执行一致性)**:
  - 计数单位=**战斗节点**(战斗前查表采样;商店/refresh 帧不计数)
  - **模式生效时机**:采样结果当节点商店期即生效(战斗前采样→
    紧随的商店期按新模式执行,无滞后——与锁线消歧同规格);
    应急/锁线/追赶退出/换线事件发生时当节点重采样重裁决
    (级联生效)
  - **级联事件对冷却的处置(rev8 声明)**:
    级联重裁决是「事件路径」,**不受 MODE_EXIT_COOLDOWN 约束**
    (该冷却只管滞回路径的模式切换——事件触发的模式重定
    是重采样结果,非切换);级联事件**清零 MODE_EXIT_COOLDOWN
    计数**(旧冷却针对旧象限,象限已变无意义);PIVOT_BAN_K
    与 PIVOT_COOLDOWN **在级联中不清零**(换线保护的目的
    恰是跨事件持续——只有自身的 K 衰减/到期/豁免能解除)
  - 经济→战力:连续 M 个战斗节点 miss(含折算 miss)
  - 战力→经济:连续 M 个战斗节点过阈+冷却期满(**模式退出
    冷却=独立常量 MODE_EXIT_COOLDOWN**,与 PIVOT_COOLDOWN
    正交——rev7 消除悬空引用)
  - 量纲:全程验证篇数
  - **换线保护(rev6 重组)**:
    禁回购:换线后旧线核心件禁购 K 个战斗节点;
    豁免一:降级目标=旧线低配;
    豁免二:应急态内买即战力命中旧线核心件(生存优先,退出即失效)
    定位:**动作级禁令,叠加于真值表任何行之上**
    K 衰减依据=位面固定节点图(游戏常数,入静态配置)
  - **换线自身守卫(rev8 补失败计数边界与防环)**:
    换线事件**两侧 streak 全清+当节点重采样重裁决**(与锁线/
    应急/追赶退出的级联规格完全一致——形态剧变事件一律全清
    重数,消除"只清 miss"的不对称);换线后模式按重采样结果定
    **换线冷却**:PIVOT_COOLDOWN 个战斗节点内不得再次换线;
    **D 卡失败计数与冷却是两个独立计数器,失败计数在降级/
    换线事件触发时清零**(只增=运行期间不衰减;事件边界清零
    ——防「计数≥N 持续满足→冷却期满即连环降级滑到底」);
    **降级路径执行=换线事件**(触发禁令 K、冷却、streak 全清,
    与主动换线同规格——降级就是换线的一种);
    **降级链防环**:每局维护「本局已访问线」集合,降级目标
    已在集合中 → 改走全局兜底线(DOT,无降级路径的终结线);
    **兜底线自身遇险(rev9 声明)**:兜底线的 D 卡失败计数
    照常累积但**无降级出口**——接受(计数饱和无副作用;
    遇险由应急/战力模式应对;兜底线本身选的是最低门槛形态,
    miss 概率最低)
  - 状态真值表(替代散文优先序——全文唯一裁决源;
    换线保护为动作级禁令**叠加于每行之上**,不单列行):
    | 状态组合 | 动作来源 |
    |---|---|
    | 应急(任何) | §5.4 应急行为(追赶清除;旧线件禁令豁免二生效) |
    | 追赶×未锁×经济 | 升人口置顶+保值件(下浮地板仅覆盖升人口) |
    | 追赶×未锁×战力 | 引擎补件+升人口分流(下浮地板覆盖两者) |
    | 追赶×已锁×经济 | 线内件+升人口置顶(下浮地板覆盖两者) |
    | 追赶×已锁×战力 | §5.2+升人口并列(下浮地板覆盖两者) |
    | 非追赶四象限 | §5.5 四象限表 |
    (rev6:下浮地板只给还债动作——升人口/线内件/引擎补件;
    保值件不享受下浮,防人口债未清又积压非定向资产)

**跨节点累积态清单(rev9 补防环集合)**:
  **锁线状态/当前 line_id/引擎选择/装备时序进度/领航员绑定/
  D 卡失败计数/已访问线集合**(session 核心态)+
  滞回计数/换线保护 K 计数/换线冷却计数/粗证据连击计数/
  应急退出保持计数/追赶期状态/信号累计器——全部内存态,
  重启丢失语义统一声明:
  **锁线状态丢失**:重启后按当前 board 重新信号匹配(已成型
    的线可通过已购核心件识别——board 上的姬子3星=姬子线证据;
    无法识别则退化为未锁线态,风险=重复买已有核心件,
    由"已持有检测"拦截,损失有限可接受);
  **引擎选择丢失**:同上按 board 重建;装备时序丢失:按公式
    重新对照已装备状态推演到当前步(时序是确定性的,
    从已完成步骤续跑);领航员绑定丢失:按两层规则重绑
    (全局层规则可即时恢复);
  滞回计数清零=容忍一次额外模式切换(可接受);
  K 计数清零=换线保护提前解除,**重启后首个节点若检测到
  旧线核心件可回购,先检查降级豁免条件再放行**——旧线身份
  按「换线事件前的 line_id」恢复(锁线态丢失时从遥测的
  换线事件记录读取,无记录则禁令作废——最坏情况=容忍一次
  抖动回购,可接受);
  换线冷却清零=冷却提前结束(容忍,低频事件);
  D 卡失败计数清零=降级触发延迟(容忍,重攒即可);
  **已访问线集合丢失**:从遥测换线事件记录重建(线序列可回放);
    无记录则集合作废=接受一次环风险(与 K 计数同规格——
    连败局高发重启的场景,环风险由兜底线的低 miss 概率兜住);
  粗证据连击清零=重数(影响小);
  应急退出保持计数清零=按 HP 当前值幂等归位(§5.4);
  追赶期丢失=人口低于基线→重进追赶(幂等);
  信号累计器丢失=失真 → 该信号判据降级为不可用
```

## 6. 与现有实现的替换关系(r204 代码级审计后重写:粒度=模块级)

**关键发现(代码审计)**:现有 `CwStrategy` 插件骨架(ABC 12 钩子+
StrategySession+Manager+prep 执行环)与 redesign 正交——
**redesign 就是一个新的 CwStrategy 具现**(如 `LineStrategy`),
不推翻框架。钩子映射:锁线→update_target;决策循环→decide_prep;
装备公式→decide_supply;M13 遭遇权衡→decide_encounter(已有三分支
雏形);idol_plan→decide_megastar(现 idx=0 桩,填实);
投资优先序→decide_invest(已有);累积态→StrategySession 扩展字段
(对齐 §5.5 清单)。

| 现有模块 | 处置(r204) |
|---|---|
| CwStrategy ABC / StrategySession / Manager | **保留**(插件骨架;session 扩展累积态字段) |
| prep_director / prep_actions(执行环) | **保留**(接入前过 review——带「未验证」头) |
| cw_shop_odds(概率/超几何期望)/cw_investments(策略台账)/cw_state(机制常量) | **保留复用**(roll_affordable 金计价门=D 决策数学核,target 档主动 D 复用) |
| cw_economy / cw_plan / cw_evaluate | **替换**(经济分/硬门贪心/评估函数→战力表+模式分支;fallback 保留一版本周期) |
| cw_comps / cw_transition | **替换**(→线库+引擎池) |
| cw_horizon(DP)/cw_first_passage | **退役为兜底姿态**(r204 裁定:DP 手造板强/掉血先验 vs 战力表 784 篇实证——证据等级碾压;DP 涌现验证未达成[文件头自认];单姿态源原则。降级保留=无信号兜底局的退化路径,与位面级粗表对齐) |
| cw_run_allocator(跨局 Thompson) | **保留**(跨局层,本方案未覆盖——见 §10 补充) |
| cw_telemetry | 扩展 schema(§6 原有清单) |

**感知质量门继承(r204,代码已验证的防线,新实现不得丢)**:
last_hp 时效门(结算 HP 仅紧邻节点可覆盖,防陈值毒化应急触发)/
level 单调守卫(OCR 误读防护)/结算源 streak 方向(备战读无方向)。
**执行层细节**(deploy_fail_counts 拖拽失败记忆等)属 prep 框架,
保留不在策略层范围。
| **遥测 schema(rev4)** | 同步扩展字段清单:模式与象限/回退级别(精确/降档/miss/
  **遥测解锁**)/锁线信号层级/换线事件/K 状态/应急进出/追赶期/
  探索标记;查询端视图同步演进(项目纪律:schema 变更查询同步) |

## 7. 落地顺序(含灰度与标定时序)

```
Step 0 保守系数粗标定:数据前提=旧遥测需含形态键字段(羁绊/人口
        已有;**星级与核心在场标记若缺,先做旧遥测回填提取**——
        从 run 记录的角色列表重建,缺失项标 unknown 降维对照)
        归因分项加入「阈值过严/过松」
Step 1 战力表硬化(形态键+三级回退+双证据源合成)
Step 2 引擎池+信号匹配器(含战技点2 核验:逐篇判据,先核后入)
Step 3 线库 v1:姬子/万敌/DOT+白厄(换线验证;探索局覆盖)
Step 4 灰度接入:配置开关切换;异常回退旧路径;AB 对照
        探索局手段声明(rev4):**遇词条即打标采集**(非人为
        重开筛选——与「作者重开是偏差源」的立场一致);
        换线收益指标的功效不足已接受(低频事件,长期积累)
Step 5 实跑校准:遥测 hp 对拍;探索局打标隔离(校准管道与
        过程指标管道**双重隔离**,rev4);分形态掉血标定;
        保守系数精标;节点粒度细化
```

## 8. 验收标准

```
门槛:
  - 主指标:A8 通关率 ≥ 旧策略基线(**非劣检验语义**:预先定义
    允许劣化的边界,非简单点估计比较)
  - 辅助指标(样本效率更高,主判据):**分节点段 hp 损失曲线**
    对拍(新 vs 旧,逐位面段比较)——通关率 40 局仅作粗筛
    (低基率事件功效不足,声明在案)
  - 护栏:局均时长容差(预定义百分比常量)
  - 窗口:AB 各 ≥20 局(粗筛);不达标→回退开关+归因分项
    (miss 率/锁线错误率/换线失败率/**阈值过严过松**)
过程监控指标(排除 exploration=true 的局):
  查询命中率与回退分布/锁线时机分布/换线触发与后效/
  应急触发频率(常态频发=危险线标定错)
```

## 9. 验证来源声明

| 决策 | 证据 |
|---|---|
| 战力查询粒度 | r191 战力表(784篇) |
| 引擎池成员 | P1 榜+三引擎调研(r149) |
| 位面人口基线 | 人口中位数(策略参数,待遥测校准) |
| 信号四层 | 十类开局策略统计+作者证词 |
| 降级路径必填 | 十类深读结论 |
| 装备三型 | CARRY 驱动型分析 |
| 领航员两层绑定 | 姬子类换绑证词+三月七通用性 |
| 换线规则 | 类间切换预案 |
| 自动友好筛选 | 50 篇挂机标记统计 |
| 局外排除 | 用户裁定 r176 |
| 应急下限证据 | 希儿#8 等作者证词 |
| 通用骨架件 | Final 出现率统计 |
| 应急掉血初值 | 投稿量化证词(灼热轰炸14000 类) |

## 10. 风险与边界

1. 战力表粒度:位面级起步,节点区间长期稀疏(接受);回退主路径,
   各级阈值独立标定
2. 规则即代码:线内判据是代码层,调优动代码(接受,as-built)
3. 换线平滑度:兼容性表+白厄线验证;换线收益指标功效不足(接受,
   低频事件长期积累)
4. 版本漂移:4.5 重提取(生成器可重跑)
5. 统计功效:校准需百局量级;辅助指标(hp 曲线)补偿样本效率
6. 贯通单点:接受(§2 声明)
7. 未覆盖(显式排除):装备配方级选择/事件选项语义/P3 决赛圈针对/
   投资策略通用决策(初版用 0 层信号固定优先序)/**跨局分配**
   (cw_run_allocator Thompson 采样+必死局回收——现有跨局层保留,
   与本方案正交,后续单独对齐)

## 附录 A:常量索引(rev4;只列常量名与语义,值在代码)

| 常量 | 语义 | 出处 |
|---|---|---|
| CONSERVATIVE_FACTOR | 作者-试用练度折扣(仅投稿源阈值) | §2/Step0 |
| MISS_STREAK_M / PASS_STREAK_M | 滞回进入/退出连续战斗节点数 | §5.5 |
| COARSE_TOLERANCE | 粗证据连击折算 miss 的容忍数 | §4.1 |
| PIVOT_BAN_K | 换线后禁回购战斗节点数(衰减依静态节点图) | §5.5 |
| PIVOT_COOLDOWN | 再换线冷却战斗节点数(与 D 卡失败计数独立) | §5.5 |
| MODE_EXIT_COOLDOWN | 战力→经济模式退出冷却(与换线冷却正交) | §5.5 |
| DANGER_ROUNDS | 应急危险档(可承受败局数) | §5.4 |
| HEAL_HYSTERESIS | 应急退出安全档(存量档位) | §5.4 |
| EMERGENCY_EXIT_HOLD | 应急退出保持计数(战斗节点) | §5.4 |
| CATCHUP_FLOOR_RELIEF | 追赶期利息地板下浮系数 | §5.4 |
| CATCHUP_TOLERANCE | 追赶退出人口容差(带回滞) | §5.4 |
| INTEREST_FLOOR | 利息地板(金币下限) | §4.2 与 §5.2 |
| AB_MIN_GAMES | AB 对照最小局数 | §8 |
| DURATION_TOLERANCE | 局均时长容差 | §8 |
| EXPLORE_REPEAT_MIN | 遥测正证据复现下限(判"过",需含一次正常局) | §4.1/§7 |
| NEG_EVIDENCE_MIN | 遥测否决的最小尝试数 | §4.1 |
| VETO_REVIVE_PASS | 否决撤销:重测队列连续通过次数 | §4.1 |
| 信号阈值组 | 各层信号置信下限/累计判据 | §4.4 |
| 回退阈值组 | 三级回退各级篇数阈值 | §4.1 |
