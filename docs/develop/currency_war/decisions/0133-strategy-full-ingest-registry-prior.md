# 0133 投资策略全量 ingest(315)+ decide_event 注册表先验

## Status

accepted(2026-08-15;继 ADR-0131/0132 —— 用户问数量盘点与图鉴采集必要性)

## Context

数量盘点:INVESTMENT_ENVS **84 条**(≈图鉴 83 全量,环境侧已齐);INVESTMENT_STRATEGIES 此前仅 **19 条 T0**,docs 米游社 doc **315 条**(棱彩114+金125+银76),数据银行图鉴口径 **334 总/212 解锁**(doc 比图鉴少 ~19 条版本漂移)。策略注册表覆盖 19/334≈6%,decide_invest 对白名单外选项全 0 分 = 变相随机选。

## Considered Options

- **先图鉴轮 vs 先离线吃 doc**:图鉴轮需屏建档+滚动采集 op+占用游戏 15-20min;doc 315 条离线即可全量吃进(零游戏时间)→ **先吃 doc 拿 95% 价值,图鉴轮降级为补漂移的局间空窗任务**(采 19 条 drift + 抽样对拍米游社转写 + 环境 73 解锁效果对拍;live invest_cards.jsonl 渐进覆盖同向)。
- economy 抽取:运行时正则 vs 建库时一次性抽取 —— 一次性(可人工抽查,61 条抽到);条件性效果(本金充裕+/存款回报 的条件免费刷)**不数值化**(误标会假账,效果原文为准)。
- doc 删除时机:代码全量建模后删 —— **暂不删**(315≠334 未到图鉴全量,doc 是漂移对拍基线;图鉴轮完成后删)。

## Decision

1. cw_investments 追加 `_STRATEGIES_INGESTED` 296 条(生成块;curated 19 优先 setdefault 合并)= 全量 315;61 条自动抽 EconomyEffect(搜打撤 per_node 修正;条件性免费刷 2 条不数值化)。
2. decide_event 加注册表先验:棱彩 50 / 金 30 / 银 10 + EconomyEffect +20(上限 70 < 白名单地板 ~82,T0 白名单仍优先);解决白名单外选项互相比较。投资环境名不在策略注册表 → 行为不变。

## 后续

- 图鉴轮(局间空窗):数据银行 → 投资策略 212 解锁条目采集 → 补 19 漂移 + 对拍 → 删 doc(单一源)。
- decide_event 先验值(50/30/10/20)阶段 6 实玩校准;OCR 名与注册表精确匹配的容错(fuzzy)待 live 误配样本。

验证:注册表 315 断言 + 先验 3 断言新增;CW 全套 388 passed。
