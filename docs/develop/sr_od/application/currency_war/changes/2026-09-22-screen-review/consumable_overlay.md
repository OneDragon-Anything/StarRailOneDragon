# T-28 消耗品详情浮层 修法设计(consumable_overlay)

## 0. 元信息

- 迭代目标:货币战争全画面规范符合性审查(2026-09-22-screen-review)·只设计不落码,待用户裁决
- 状态:对抗收敛待用户裁决(对抗轨迹:r1 收敛·0 条实质;残留 3 条低[N-1 outer_loop §2.2 引文挂点/N-2 AGENTS §8 直引改导出义务/N-3 验证门 3 判域排除同批点位]一句话级,随定稿清偿;见 reviews/T-28-attack-r1.md)
- 审查输入:`.debug/progress/2026-09-22-cw-screen-review/reports/T-28-r1.md`(发现 F-1 一条;总判定 = 有问题,高 0 中 0 低 1)
- 真值基线:本稿全部「现状」陈述以审查时点工作树代码为准,落地时若代码已再变,以落地时点代码重新对账后再动笔。符号锚 = `文件::符号名`,行号仅作本稿定位辅助。文档路径根 = `docs/develop/sr_od/application/currency_war/`,代码路径根 = `src/sr_od/application/currency_war/`(下文反引号短路径均相对此两根)。
- 修法性质:F-1 = `operations/cw_loop.py` 0f′ 分支头注释一句改写,**零行为变化**(无任何逻辑/签名/常量改动;`sr-od-test/` 零触碰)。
- 准则参照:注释清理一致性参照 T-3 稿([battle_wait.md](battle_wait.md))§2.6 五条准则(禁形一·会话局部标识符 / 禁形二·变更史叙述 / 改写方向 = 结论→出处→边界 / 保留豁免 / 跨修法交叠一次成文)。本件命中面**不属两禁形**——「ESC 关」非会话标识符亦非变更史叙事,而是对现役行为的假陈述,适用条款 = 仓库根 AGENTS.md §8「注释对当前行为的陈述须与实现一致」义务(审查 F-1 规范条款同一出处);本稿实际消费的准则面 = 准则 3(改写只写现役行为,禁写成「原 ESC 分支已删除」类迁移叙事)与准则 4(审查 §3 一致项不借清理扩面)。

## 1. 问题与动机

### 1.1 F-1 0f′ 分支头注释「→ ESC 关」与实现相反(低)

- **现状症状**(审查报告 F-1):`operations/cw_loop.py` 0f′ 分支头注释(L1720)写「消耗品详情浮层 → ESC 关」;同分支实现(L1723-1727)= 派发 `cw_screen/cw_screen_consumable_overlay.py::CwScreenConsumableOverlay`,关闭动作在 op 内 `progress_once` 完成——`area_center` 读同族建档「货币战争-道具详情弹窗.按钮-关闭」center → `mouse_move`+`click` 点 ×,全程无 ESC。注释与行为相反。加重因素:该注释块是 `flow/outer_loop.md` §2.3 记法节点名的冲突仲裁权威(「权威源 = cw_loop.py 分发段注释块,本表与代码注释冲突时以代码为准」)——仲裁源自身含与实现相反的关闭方式陈述。
- **三方对照(假陈述的唯一性)**:本屏关闭方式的全部正本记载均与实现一致,唯此一处注释相反——①`flow/outer_loop.md` §2.2 阶段三行与 §2.3 现行号表 0f′ 行均记「关浮层」(无 ESC 语义);②`screens/consumable_overlay.md` §4 记「点 × 关闭……刻意不用 ESC」;③op 模块头记「点 modal 右上 × 关」并申报替代 ESC 的理由(× 是弹窗内坐标永远安全,ESC 在 modal 已自关时落备战会误弹「中断挑战」,bug#2)。同段对照面:0e3 分支注释「点 × 关」与实现一致;星徽详情臂注释「点X 关迁入 op(「不用 ESC」理由随迁 op)」(L1512)同式合规——对照下本条残留更易误读(审查 F-1 差距说明同判)。
- **根因归层**(根源两问):①根在哪层 = **表示层(注释残留)**——关闭方式从「cw_loop 内按 ESC」迁为「分发画面 op 点同族 ×」的迁移批清偿了行为面与 `kernel/cw_overlay_registry.py` 条目挂账(消耗品条目注在案:「cw_loop 0f ESC 分支已删除——现关层行为 = 分发 CwScreenConsumableOverlay 点同族『道具详情弹窗/按钮-关闭』×……建档批按该实作改声明并清 esc 值」),但 cw_loop 分发段注释头的关闭方式短语漏扫,挂账点名的是 registry 自身 esc 双声明字段,cw_loop 注释残留未被点名清偿(审查 F-1 差距说明同判)。非语义层(行为正确、三正本一致)、非流程层(单句陈述,无结构性缺口)。②修的是根还是症状 = 家族清点证明注释族内本条即全部成员(cw_loop.py 全文件 grep `ESC|esc` 恰 2 命中:L1512 为「不用 ESC」落选理由的合规陈述,与本条;清点明细 = §2.1),单点改写后本域清零 = 治本屏症状的全部;『迁移批收尾缺旧口径引用面清查门』的流程级根已由 battle_wait 稿 §2.1 跨批登记面立案(归宿 = harness landing 模板固定判据方向),本稿不重复立项(跨件半问:同根,共用同一登记,禁第三件症状补丁)。
- **解决到哪**:L1720 注释首行关闭方式短语改写为现役行为陈述(逐字目标 = §2.1);改写后 cw_loop 注释域「ESC 关」残留清零。
- **明确不解决**:①`kernel/cw_overlay_registry.py` 消耗品条目 esc 双声明(`close_action=CLOSE_ACTION_ESC` + `recovery_exit=RECOVERY_ESC`,L354/L356)——在册挂账已给清偿路径「建档批按该实作改声明并清 esc 值」,条目 `active=False` 不参与任何派生(模块头申报 + 一致性测试豁免其建档断言,零运行时效应);提前清偿 = 越建档批裁决且需同时定 close 载荷归属(建档后同族「按钮-关闭」area 在场,close_action 预期升级 `close_area`,现在改 `point` = 建档批二次返工)。挂账注陈述的是行为面,本修法后其「现关层行为 = 分发……点同族 ×」仍真。②registry 盛会之星条目 `recovery_exit=RECOVERY_ESC` 与实际恢复出口不符——同文件另一 overlay 的在册挂账(待 D 面统一切换时一并重推),非本屏家族。③`operations/tools/harvest_invest_codex.py` / `operations/cw_op/cw_op_sell_off_target.py` / `operations/cw_entry/cw_entry_enter.py` 的 ESC 用法——图鉴 overlay 退场(自带「ESC 正形化挂账」)/卖上阵面板关闭/进局弹窗各属独立语义域、各有独立申报,非「本浮层关闭方式」家族。④op 模块头「ADR-0584,A4」悬空引用——T-26 已登记的家族在册欠账,归宿随 T-37 出路裁决(审查 §2 ⑤ 与 §3 同证),本稿不触。⑤「× 坐标待实机核」与装备类详情 modal 长尾——文档开放设计注与 op 模块头双向在册申报,候实机批。⑥「0f′」分支号本身——`flow/outer_loop.md` §2.3 现行号表在册(可解析),不属残留。
- **在册核对一致项边界**:审查报告 §3 全部一致面(两 node 形态与出口语义/决策控制铁律/观察上报形态/文档 as-built 九节/完备锁形态归属/AGENTS §3 转移验证等价性/坐标单一真相源/同族同参/注释持久索引面等)为落地禁触碰边界——本稿唯一修法域 = 一句注释,落地批禁借清理之名改动上述在码语义。

## 2. 方案

### 2.1 F-1 修法:0f′ 分支头注释关闭方式改写现役行为

单点位(逐字;以「0f′ 分支头注释首行」定位,行号仅辅助):

| # | 位置 | 现状(逐字) | 目标(逐字) |
|---|---|---|---|
| 1 | `operations/cw_loop.py` 0f′ 分支头注释首行(L1720) | `# 0f'. 消耗品详情浮层 → ESC 关。获消耗品奖励后游戏自动弹介绍 modal,遮挡底层屏。` | `# 0f'. 消耗品详情浮层 → 点同族「道具详情弹窗/按钮-关闭」× 关(「不用 ESC」理由随 op)。获消耗品奖励后游戏自动弹介绍 modal,遮挡底层屏。` |

同注释第 2-3 行(「签名「消耗品」(类型 label) AND 「拖动到」(拖动使用说明——只出现在消耗品详情 modal,备战底部消耗品栏无)→ 双条件精确不误匹配。」)**逐字不动**——现役语义、无 §2.6 两禁形命中、审查未列发现(禁借清理扩面)。

依据(就地):

- 行为真值 = `cw_screen_consumable_overlay.py::CwScreenConsumableOverlay.progress_once`(读「货币战争-道具详情弹窗.按钮-关闭」center → `mouse_move`+`click`)+ 同文件模块头(「点 modal 右上 × 关……× 复用同族建档『货币战争-道具详情弹窗/按钮-关闭』」「替代 ESC 的理由:× 是弹窗内坐标永远安全,ESC 在 modal 已自关时落备战会误弹『中断挑战』(bug#2)」);
- 正本口径 = `screens/consumable_overlay.md` §4(「刻意不用 ESC:× 永远安全……」)+ `flow/outer_loop.md` §2.2 阶段三行 / §2.3 现行号表 0f′ 行(「关浮层」);
- 目标文本句式 = 同文件 L1512 星徽详情臂家族句(「点X 关迁入 op(「不用 ESC」理由随迁 op)」)——同族 modal 关闭方式收编进画面 op 后,分发面注释的既定句式 = 现役关闭方式 + 「不用 ESC」理由指针;「不用 ESC」理由的单一源住 op 模块头,分发面注释只留指针不复制理由(理由复制即第二源漂移面,正是本发现的病灶形态)。措辞差异申报:家族句的「迁入/随迁」带迁移语感,目标文本用非历史变体「理由随 op」(AGENTS §8 禁形二卡点;L1512 既有措辞不在本修法域,不触);
- 改写纪律 = AGENTS.md §8:目标文本为纯现在时行为陈述,零变更史成分(「原 ESC 分支已删除/已改点 ×」类叙事禁入——现状对错归 git 历史,注释只留当前行为)。

**ESC 语义残留家族清点结论(任务问句回应:cw_loop 内 ESC 关注释族无其他成员需一并登记)**:`operations/cw_loop.py` 全文件 grep `ESC|esc` 恰 2 命中——L1512(星徽详情臂,「点X 关迁入 op(「不用 ESC」理由随迁 op)」= 落选理由的合规陈述,与实现一致)与本条 L1720(唯一假陈述)。家族其余成员全在 cw_loop 域外且各有归宿:registry 消耗品条目 esc 双声明(建档批在册挂账,§1 明确不解决 ①)、registry 盛会之星条目 recovery_exit(自身在册挂账,②)、他域文件三处(③)。清点后本修法**无需新增任何 T-37 登记行**:域内零未决成员,流程级根共用 battle_wait 稿 §2.1 在册登记,既有挂账各自已有清偿批次。

**取舍(路线选择:取 T-22 指针现役化路线,不取 T-27 补对号表路线)**:

- 两先例路线定义:T-27(plane_detail 稿 §2.2)处理的是**含义为真、仅不可解析**的退役号引用——修法 = 给 `flow/outer_loop.md` §2.3 对号表(读旧档的「密码本」)补录一行,引用面零改动;T-22(plane_transition 稿 §2.2)处理的是**以过期指称冒充现役机制**的注释——修法 = 指称改写为现役符号锚(「指针现役化」)。本件取 T-22 路线。判据 = 引用面真伪:「ESC 关」不是待对号的历史指称,而是**对现役行为的假陈述**(连语义都反:行为 = 点 ×,非按 ESC;比 T-22 的 0q 更重——0q 至少语义实质与实现一致,只是指称过期)。「密码本补录」路线的前提(引用面整体为真、仅缺解析表)在本件不成立:把「ESC 关」补录进任何历史语义登记位 = 读者按表解析出「ESC 关」并当现役契约引用,病灶加固而非治愈;给「ESC 关」外加更正注(T-22 取舍已弃的同型备选)= 读者仍先读到假陈述,病灶未除。唯一治愈 = 注释改写为现役行为。
- 备选 A:目标文本省略同族借道、写「→ 点 × 关(「不用 ESC」理由随 op)」(0e3 同款)——放弃:0f′ 的 × 系借道同族「道具详情弹窗」档,本屏无独立画面档(分发走阶段三特殊规则正是因「无独立画面档」);省略借道会让只读 cw_loop 的读者误以为本屏建档有独立 × area。0e3 可省因其 op 自持该档,0f′ 不可。
- 备选 B:顺手清偿 registry 消耗品条目 esc 双声明(`close_action=CLOSE_ACTION_POINT` + `close_point=(1862,65)`)——放弃:清偿路径在册 = 建档批,建档后 close_action 预期升级 `close_area`(同族「按钮-关闭」area 在场即载体升级),现在改 point = 建档批二次返工;条目 `active=False` 零派生,不改无运行时风险。本修法与该挂账零交叠(挂账述行为,行为不变)。
- 备选 C:本残留补点名进 registry 挂账注(挂账文本补「含 cw_loop L1720 注释残留」)——放弃:挂账职责 = 记未清偿债;本修法落地即清偿,补点名随即作废;若本修法被裁决搁浅,审查报告 F-1 即汇总面(T-37)的呈报依据,无需第二登记位。

**验证门(本修法完成判据,机械可判)**:

1. `grep -n "ESC 关" src/sr_od/application/currency_war/operations/cw_loop.py` 零命中;
2. 正向核对:修后 0f′ 注释首行含「道具详情弹窗/按钮-关闭」「× 关」「不用 ESC」三成分(现役行为陈述 + 理由指针在场);
3. `git diff` 确认该文件仅该一行注释改动(零逻辑 diff);`uv run ruff check src/sr_od/application/currency_war/operations/cw_loop.py` 通过;
4. 四方口径互查一致:修后注释 ≡ `screens/consumable_overlay.md` §4 ≡ `cw_screen_consumable_overlay.py` 模块头 ≡ `flow/outer_loop.md` §2.3 现行号表 0f′ 行(本屏关闭方式口径唯一,无第二源分叉)。

### 2.2 落地文件面总表与统一验收

| 文件 | 修法点位 | 性质 |
|---|---|---|
| `operations/cw_loop.py` | 0f′ 分支头注释首行关闭方式短语改写(§2.1 #1) | 注释(零逻辑) |

统一验收:§2.1 验证门 1-4 全过;零行为变化(src 树无逻辑 diff);`sr-od-test/` 零触碰;与审查报告 §3 全部一致面零交叠。

协调注(同文件在飞面):`operations/cw_loop.py` 另有在册注释卫生欠账(`OP_FAIL_REDISPATCH_LIMIT` 常量注的会话号残留,plane_transition 稿 §1.2「明确不解决」登记,归 T-3 稿 §2.6 全仓注释卫生批)——本修法点位(0f′ 分支头,阶段三分发段)与该点位(常量定义段)不同段、无交叠;同批落地时两修法各按本稿与卫生批准则一次成文,禁互借名义扩面。
