# P4 成本带聚焦买光 · 五门逐件验证报告

> 验证对象:`docs/game/currency_war/research/proofs/p04-cost-band-buyout.md`(只读,未改)+ 索引行 `math_proofs.md` L16(P4 行)
> 验证人:逐件证明验证批(P4);日期:2026-09-01
> 方法:证明原文通读 → 注册表直调(`cw_shop_odds` 全 dump)→ 独立复算(内联脚本重推两个数值锚点,不信原脚本——原脚本本就佚失)→ 口述原文/玩法文档对照 → 现役代码行为核对

## 0. 对象声明(编号撞车排查)

**索引行 P4 = 「成本带聚焦买光(压库增益)」,方向证伪;文件 = proofs/p04-cost-band-buyout.md** —— 与任务预期(P30 批错位澄清指出的「费用档聚焦买光」)**一致**,开工判定无撞车。注意三套编号并存易混:

- **[30] = 用户口述编号**(user_playstyle.md L92-99,「买光商店的 1-2 费」原理泛化)——P4 的证明对象;
- **P4 = 本证明**(对 [30] 压库增益方向的证伪);
- **P30 = proofs/p30-merge-completing-purchase-ev.md**(合成补购 EV,另一命题,与本件无关)。

## 1. 阅读门覆盖声明

已读:P4 全文、math_proofs.md 索引 P4 行+纪律节、user_playstyle [30]/[3]/[7]/[32]/[34] 费用档相关条、economy.md §1(牌池结构/卖回还池不变量/种类数勘误)/§2(刷新概率与 D 牌精确模型)/§2.1(商店槽位)/§3(免费牌池操纵)、P3 全文(被引支柱)、P6 全文(下游消费方+已内嵌修正)、06_input_model.md §3(注册表地图,cw_shop_odds 行)、cw_shop_odds.py 全文(注册表直调)。公理栈(P51 头部声明的:零调参/参数溯源/超额收益隔离/纯金流)已对齐检验。characters/final_comps 全集与本件(牌池压缩概率)无建模交集,未逐篇通读,特此声明。

## 2. 五门判定

### ① 内部推导逐步核 —— **过(带口径注)**

P4 两个数值锚点独立复算(注册表直调值,v={1:20, 2:15}, a=27/27, p 表 lv4-7;模型 = P4/economy §2 同构的「二项×超几何」):

**锚点一「全买光 ≈15 张只抬命中率 ≤1.0pp」**——买走 15 张同费非目标牌后,P(一次刷新 5 格 ≥1 目标)的提升:

| 场景 | 目标 1 种 | 目标 3 种 | 目标 5-6 种 |
|---|---|---|---|
| 1费 lv4-6 | +0.20~0.41pp | +0.53~0.92pp | +0.88~1.15pp |
| 2费 lv5-7 | +0.35~0.46pp | +0.90~1.09pp | +1.25~1.43pp |

- 单目标(D 牌语境,`expected_refreshes` 消费面)全部 ≤0.5pp,与「≤1.0pp」吻合;economy §3「买 30 张约 1 个百分点」亦可复算对上(30 张单目标 ≈0.85pp)——两处文本互洽。
- **口径注**:目标 5-6 种且 15 张全为非目标的角格可达 +1.43pp,技术上越过「≤1.0pp」。但:①现实买光含自己的目标牌(目标牌离池分子分母同缩,压缩效果打折),有效 c < 15;②证伪倚仗的是「远够不到 +5pp」——即使取最角格 1.43pp 仍 ≪ 5pp,**方向判决不受影响**。判「≤1.0pp」为 D 牌单目标口径下的成立陈述,建议补一行目标集口径注(见修正建议 1)。

**锚点二「要 +5pp 需 40+ 张」**——全网格扫描(lv4-7 × 1/2费 × 目标 1/3/5-6 种),+5pp 所需买走张数 = **49~227 张,网格最小值 49**:「40+」保守表述正确(下界成立)。✅

结论行「净期望≈0 无正收益」与 P3/P6 的引用关系核过:P4 引 P3 仅取**时机边界**(e0→e1 边际只在 r3+ 兑现,r1/r2 无战斗期战力收益为零)——这是结构性论断(前两轮无战斗节点),不依赖 P3 的 hp_to_gold=0.5 系数(该系数已被 P12 判词标废),**P4 的引用面幸存**。P6 ③ 直接消费「P4 已证 ≤1.0pp」(p06 L30),数值承接无误;P4 L18 的历史修正句(2026-08-25)与 P6 支柱改写一致,回炉痕迹完整。

### ② 锚点直调注册表复核 —— **过(带时点漂移注)**

P4 本文不直接引用注册表数值(证明短小,数字来自已佚脚本),但其全部模型参数可由注册表直调复核:

| 参数 | 注册表直调 | 判定 |
|---|---|---|
| a(副本数) | `POOL_COPIES_PER_CARD = {1:27, 2:27, 3:9, 4:9, 5:9}`(V3.7 必修二+用户确认,溯源完备) | ✅ |
| v(种类数) | `DISTINCT_CARDS_PER_COST = {1:20, 2:15, 3:14, 4:14, 5:9}`(CHARACTERS 派生) | ✅ 当前值 |
| p(level,cost) | `REFRESH_PROB` lv1-10 全表(实机 OCR D-91) | ✅ |
| 商店 5 格 | `SHOP_SLOTS=5` | ✅ |
| 「15 张」量纲 | 5 格×3 次刷新全买,r1-r2 节点切换自动刷新 2-3 店的量级 | ✅ 结构推导 |

**时点漂移注**:P4 立证于 2026-08-23,economy.md 2026-08-31 勘误种类数(74→72 派生口径)。v 微调对压缩幅度是二阶效应(复算已用当前 v,方向与量级均不变),不构成推翻;但 P4 属「脚本已佚、以本篇为准」类,数值锚点无法逐位复原,只能按当前注册表重推——**重推结果与文本陈述相容**,判过。

### ③ 建模对象 vs 玩法文档原文 —— **过(带一支未证注)**

- 命题转写忠实:[30] 原文「买光商店的 1-2 费」= 过渡羁绊件集中 1-2 费带 → 带内买光 = 定向压库,P4 建模的「买同费非目标牌 → 池压缩 → 目标命中率↑」正是 [30] 的机制主张;「成本带×阶段」判据层保留也与 [30] 的矩阵化判据演进吻合。
- 机制模型与 economy §2「D 牌期望精确模型」(二项+超几何)同构,且 §1 卖回还池不变量(压库按持续持有副本数计)与之自洽——建模对象对得上游戏机制文档。
- **一支未证注**:[30] 原文的第二支「零和博弈防对手截胡(池共享)」**未被 P4 建模也未被证伪**。注册表 `acquirability_factor` 明注「忽略 NPC 消耗(未知,保守)」;economy.md 亦只说「单人 vs AI、池子小」,未确认 AI 是否消耗共享池。若池真共享,买入存在防御性通道,P4 的「净期望≈0」不含此支;若不共享,该支为空、证伪更稳。**P4 证伪的是压库命中率增益方向,辖域应明示不含截胡支**(见修正建议 2)。
- [30] 自身的 ⚠️(「同样适用于中期/后期」是 agent 推理泛化未经验证)与 P4 对泛化方向的证伪互补,文本层无冲突。

### ④ 实证 vs 账本(买光行为预测可达性)—— **有条件过(原始实证不可复核,已声明)**

- 原始数据源「压测批③ H1 干预双臂」**脚本已佚、无持久落点**(与 P31/P3 验证报告对压测批②③的同类发现一致),现存 `tools/cw/proofs/` 与 AB 目录均无 H1 买光臂——P4 在头部自行声明「结论以本篇为准」,诚实分级,但实证面**不可独立复核**,降档为「理论复算+现役行为旁证」。
- 现役行为旁证(可达性):代码中**不存在全时段带内买光实现**;压库以**有界通道**存在——`tier_push.py` 压库豁免(费用档 ≤2 硬上界+每帧 ≤2 张)、`spend_gate.py` 血紧带压库禁、`w300_dup_ruling/` 目标外同名副本压库(每轮 ≤1 笔)、`cw_system_cards.py` 草案级压库模型。这与 P4 判词「执行形态 = r3+ 按期望买,非全时段买光」**方向一致**:若压库增益如 [30] 主张(买光正收益),现役应有全买通道而非三处封顶;封顶设计与证伪结论互洽。
- P6 检验点(r1/r2 零息损放行/无强制压库分支)是 P4 结论的下游落点,P6 验证另批处理,本件不重复核。

### ⑤ 参数溯源门 —— **过**

- 本件**零拟合常数**:无任何调参量;1.0pp/5pp/15张/40+张 全部为注册表值+结构推导(5 格×3 刷)的派生量或决策量级标尺,非拟合参数。
- 溯源链完备:a=27(用户最高权威收案,公开独立文本源未找到已如实留档)、p 表(实机 OCR D-91)、v(注册表派生)。
- 公理判定:**零调参 ✅;参数溯源 ✅;纯金流 ✅**(本件证伪面只涉及命中率概率与金时机账,无 hp→gold 换算;借 P3 的时机边界是结构论断);**超额收益隔离 ✅**(证伪方向不涉连胜期权入账)。

## 3. 最终判决:**站得住(方向证伪成立,辖域注两条)**

五门无推翻项:数值锚点独立复算相容(单目标口径 ≤1.0pp 成立、+5pp 需 ≥49 张 ≥「40+」)、注册表锚点全对、建模对象与 [30]/economy 机制吻合、零拟合常数、证伪方向被现役「有界压库、无全买」设计旁证。P4 作为「口述证伪流程范本」的判例价值(证伪留档、口述保留权威、判据回炉)在索引与 user_playstyle 中承接完好,P6 对其数值的消费无误。

弱项均不翻方向:原始 H1 双臂不可复核(已声明降档)、「≤1.0pp」在多目标角格可被 1.1-1.43pp 越过、[30] 截胡支未入辖域。

## 4. 修正建议(不阻塞判决)

1. **[小改] p04 锚点一补口径注**:「≤1.0pp」标注口径 = D 牌单目标(或 ≤3 目标)场景;目标 5-6 种全非目标角格可达 ~1.4pp,仍远低于 +5pp 决策量级,方向不变——一句话注明防后续逐字引用被抓反例。
2. **[小改] p04 辖域声明**:证伪辖域 = 压库命中率增益 + 前两轮战力时机;[30] 的「防对手截胡(池共享)」支**未建模未证**(NPC 池消耗在注册表侧标注为未知)——建议在 P4 或 user_playstyle [30] 加一行「截胡支待证」,防「P4 证伪=[30] 全错」的过度引申。
3. **[在册推进] 原始实证补重跑**(低优):压测批③ H1 买光干预双臂已灭;若 sim 基建允许,可按本报告复算口径(单/多目标×15/40 张)重立一臂,把「以本篇为准」升级为可重跑证据——非必需,理论复算已足以撑方向判决。
