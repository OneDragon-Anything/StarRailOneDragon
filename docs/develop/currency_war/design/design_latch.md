# cap_sup_latched 锁存族机制现行态规格(design_latch)

> 本文=现行态规格;证明与历史单一源=IMPL_FIX_LEMMAS.md 对照表+IMPL_HISTORY.md;判定项引用格式 [现行=RN-x]。登记位辖域分工:历史批(R3-R94)证明与登记单一源=IMPL_FIX_LEMMAS.md 对照表;R95 起新批登记=design_telemetry.md 文末修复批对照表节,逐批追加(R96 批 A3)。

---

## L1 锁存载体

`cap_sup_latched: bool`(局内不可逆布尔锁存,once-seen 语义:本局任一帧观测到升帽策略候选 ⇒ 置真并保持局终,单调不减、局内不翻转)与候选名持久化(append 首见禁覆写)的**载体=StrategySession(match.session 字段,`kernel/cw_strategy_session.py`)**;写路径=operations 层共享 op `CwScreenInvestStrategy`(`operations/cw_screen/cw_screen_invest_strategy.py`)→ session 字段;判据层经 derived 读 session,**只读禁回写**。【R188 修注:载体随流程侧重构迁移——原锚 op 名 `HandleInvestStrategy`+路径 `operations/handlers/handle_invest_strategy.py` 现全树 grep 零命中(批时快照=2026-09-03 R188 亲跑);现载体=类 `CwScreenInvestStrategy`,OCR 三卡名→decide_invest→`match.session.active_strategies.append(chosen)`(批时快照=L184;L182-184 去重防重 append、L197 未注册名告警亲读);载体引用一律「类名+路径」内容锚,行号仅批时限定(R165 纪律);IMPL_ADV_R188 症1;打标不删史】锁存登记入口与依赖清单同 `audit/derived.py` 与 cap_resolved 同源段(读通道登记面,非载体)。

**GameState 不得承载锁存字段(逐帧销毁)**:`kernel/cw_state.py` 的 GameState 是逐帧快照(`obs/cw_observation.py` 每备战帧 `read_game_state` 新建 state、跨帧字段靠每帧从 session 回填,cw_state.py L157 自述「一回合决策时的局面快照」【R187 锚同步标:原锚 L150 系 ADR-0465 后插行致漂移(引文现位 L157,L150 现=snapshot_copy docstring 尾行「占帧预算 <0.1%。隔离锁=test_cw_w633_migration_b3(迁移哨兵)」),IMPL_ADV_R187 症1;批时快照=亲读;打标不删史】)——锁存落其上会每备战帧重置为 False,once-seen 保持局终语义构造性不成立。改判理由(现行结论):锁存需要局级生命周期,StrategySession 自述「一局货币战争的跨步状态(框架每局新建,局终销毁)」,且 handler 现行写入先例 `match.session.active_strategies.append` 即在此宿主上;跨臂同记录(session 三臂共享)自然成立,cw3/legacy 局写入零消费=行为零影响。op 层写路径单向性保持:op 层 → session 字段,状态函数不回调 op 层;handler 局外独立跑分支(`match is None`)显式定义=不置位、不写。[现行=R76-1][现行=R75-6][现行=R74-2]

锁存帧可见性时序:实机=overlay handler 置位生效后、同备战期判据经 derived 读锁存即生效(现行 op 链结构保证 handler 置位先于判据消费:cw_loop.py(现载体,重构后;原文件名锚 battle_loop.py 系 R78 批时旧名,重构后 src 全树 grep 零命中,R184 复扫)0e 投资策略 overlay 分支前置于备战分支+【R184 退役同步标:原「PREP_SETTLE_S 稳定门」锚已失效——该门经代码侧退役(cw_loop.py L123 明文「PREP_SETTLE_S 备战稳定门已退役(W971 §2.6/03-prep §1)」,R184 亲读);半开帧/延迟 overlay 防护替身=①逐动作回流程层确认画面②触发计算式追加等待(L123-125 亲读);行为主张(0e 投资策略 overlay 前置于备战双锚)经 R184 审查轮亲读 cw_loop.py L620-631(0e 分支,L630「货币战争-投资策略」id_mark 检测)vs L868-875(备战双锚分支)幸存——锚改指现码行号(批时快照=2026-09-03 R184 修复批亲读);IMPL_ADV_R184 症1;打标不删史】);sim=注入点「收入结算后、决策前」同帧生效(engine_p1.py L758-766)。两序若错开一帧=结构性分叉,归入时序维分叉申报(与 offered-vs-injected 构成维分立),`m6_caplatch_window` 计数差含时序结构分量,判读禁误归入构成差。[现行=R78-3][现行=R77-2]

## L2 置位谓词

置位谓词钉死=**属性匹配 + normalize 归一 + miss 不置位**,观测载体=overlay handler OCR→注册表按名取效果属性:

1. `CwScreenInvestStrategy`(`operations/cw_screen/cw_screen_invest_strategy.py`)【R188 修注:载体随流程重构迁移,旧名 `HandleInvestStrategy`/`handle_invest_strategy.py` 全树零命中,IMPL_ADV_R188 症1;打标不删史】在 `货币战争-投资策略` overlay 屏 OCR 读三卡名,候选名经 `normalize_invest_name` 归一后做**精确 dict 查询**(与 `cw_investments.py` `get_strategy` 现行入口同款,dict.get 无模糊);查询 miss(含注册表 economy=None 的战力类条目取 override 前先判 None)**一律不置位**。置位语境禁用相似/模糊匹配(其归属=遥测/采集面读侧名字解析既有纪律,与谓词分立)。[现行=R76-5]
2. 按名取效果属性 `interest_cap_override ∈ {9,10}`(注册表实名=`开源节流`(9)/`利息上调`(10),`cw_investments.py`;`买断制` override=0 系降帽须排除)命中即置位锁存,并把候选名 append 首见持久化到 session(禁覆写;置位依据帧三卡名快照单独存证)。匹配键一律取注册表实名,口述概念名禁进实现规格。[现行=R74-1][现行=R75-6]
3. OCR 漏匹配帧(候选在店但读缺/被过滤)按未观测处理,并入漏读向残余账(见 L6①);误读撞实名 ⇒ 假置位,归误读向残余(见 L6②),观测载体=置位时落候选名快照可事后核。[现行=R75-3]
4. 承重前提申报:「升帽策略 ⊆ 注册表」(注册表=T0 子集+长尾渐进补全)。长尾第 4 升帽策略 ⇒ 两侧对称漏置位且 A/B 零检出力——属知识缺口非观测噪声,治理通道见 L5。[现行=R76-6]

## L3 sim 侧置位等价物

sim 引擎**投资注入处**(`cw_sim_invest.py` 日程逐 (位面,轮) 直注单名,注入点=`engine_p1.py` L770-771 `sess.active_strategies.append(_pk)` 同位【R185 锚勘误标:原锚 L762-766 系失准——append 唯一现位=L770-771(批时快照=R185 亲读,grep 全目录唯一命中;时序语义注释「收入结算后、决策前」现位 L764-766);mtime 溯源证 R184 批时点即失准,系 R184-症1 同类未清的行号轴变体;时序主张经 R185 现码复核仍真(注入位于收入结算 L756-763 之后、决策消费之前);IMPL_ADV_R185 症2;打标不删史】)对注入名过**同一属性匹配谓词**(谓词单一源禁两份实现)命中即置位同款锁存。分叉申报:实机=offered 集合置位(handler 读三卡名时置位,含 offered 未购也锁存)⇄ sim=注入即置位 ⇒ 分布覆盖分歧显式申报;sim A/B 对「升帽可达局按 cap_sup 组装」定性=存在性覆盖非分布覆盖;sim 无 OCR 漏读/误读噪声(候选名=引擎生成真值)。`m6_caplatch_window` sim 批计数对象=sim 注入点谓词命中事件,非恒 0。[现行=R76-2][现行=R75-1]

## L4 扣减式触发器与解释区间二值判定(升帽可达性条件式消费位与扣减式触发的现行口径;标题括号去 r2 单名化=R96 批 A2,辖全消费位族)

**升帽可达性条件式(消费锁存的组装口径)**:E_rev/E_line 右端、spend_unified 右端、r2 溢余段自由带阈值、batch_form 破息门、s_line.S 饱和线分量、r1_start 门左端 L 分量(canonical 行 10 ⑦,R109 批补登,IMPL_ADV_R109 N1)等 interest.L 视界族消费位(条件式消费位枚举**单位申报(三读各自单义,R159 批)**:混合计=九处(六族位+D 双位点两处+判定端一处——系 R119 批立/R120 复核的原始枚举单位,族位与位点并计);序数位点计=十处(行 10 ①-⑦+行 12×2+行 13×1);族位计=八处(行 10 六族+行 12 一族+行 13 一族);三数互指经本句换算,主口径=以 economy §E4.2 canonical 表现行为准(批时快照:**序数位点载行集显式枚举={行 10(序数①-⑦)、行 12(位点×2)、行 13(判定端×1)}**;行 9 r2 位系行 10③ 族的阈值面登记载体——金位阈值族,economy 行 9 口径列「r2 位=条件式(…锁存联动=行 10 族)」亲读,族归并入 ③ 不另计序数[R171 批申报];行 11(cap_resolved 现值维持·承重覆盖定谳)/行 14/15(cap_sup=值域上确界上界陈述形态,非锁存条件式)不入集[申报;R171 批])【R171 快照辖域勘误标:原「行 10-15 在册」系区间快照,含非条件式位(行 11/14/15)、缺行 9 r2 位(条件式位)的载行申报,且与同句三读枚举实指行集{10,12,13}不一致——R159 修复批引入且批内三元组只核验行 10/12/13(design_telemetry R159 节亲读),系在所修句内新造不可单义快照(R131/R159 计数病类复发);三读计数九/十/八经本批推导复核维持不变(行 9 r2 位按 economy 行 9 口径列「锁存联动=行 10 族」族归并入 ③,R119/R159 既定计数不翻案);行为面零影响;IMPL_ADV_R171 症1;打标不删史】【R159 单位勘误标:原「九位齐」未申报混合单位,序数读=10/族位读=8 均不可复算,与同段「两套计数各自单义」纪律冲突(R131 型计数病类的合计单位维变体);行为面零影响;IMPL_ADV_R159 症1;打标不删史】合一处辖域分立枚举,R109 批立/R119 批计数同步——行为面辖 economy 判据族消费位,判定端辖治理通道触发器判定侧,两辖域按行 13 fail 方向列「不进行为面」分立,禁跨辖域并计**;计数换算声明,R119 批,兑现 R110-① 建议[原文=「建议后续批补『行 10 ⑤⑥ 同属 s_line.S 一族,故七位=六族』式注解」,design_telemetry R110 节 L720① 亲读][R147 括形申报标:内层引号『』系对源文(design_telemetry L720 现文用「」)的嵌套排版转换,非字形篡改——本标前无申报故违直引逐字性(R142/R143 先例=转换须随申报);除括形外逐字,三元组=(design_latch L30 原文串『』形态 Contains=True;去括形串「建议后续批补「行 10 ⑤⑥ 同属 s_line.S 一族,故七位=六族」式注解」对 telemetry L720 Contains=True;『』形态在现行态判据正文位唯一命中=latch 本处(全目录含登记节共 8 命中,批时快照=R148 审查轮亲跑、R148 报告自增位另计;排除 R147 批自增位后 telemetry L1099/L1121 等 4 处系 R119/R121 登记节先在史档——R148 批辖域限定),IMPL_ADV_R147 症2][R148 辖域勘误标:原「全目录唯一命中」臂系伪核验(grep 实况 8 命中);R147 症2 对 L1099/L1121「均「」形态」描述失真(两行「」/『』双形态并存);检验式不可复现=伪核验族实例,IMPL_ADV_R148 症A;打标不删史]:canonical 行 10 序数①-⑦(视界族七位,序数计)与本节六族位(族位计)换算=①-④⑦各一序数一族位、⑤⑥=s_line.S 一分量两序数合一族位——七序数=六族位,两套计数各自单义(序数计/族位计),互指经本句换算),与 ev_buy_veto D 判据第一/第二族双位点(canonical 枚举表行 12,cap_D 条件式,R95-B1 新增消费者,R96 批 A2 补登本枚举)统一按条件式组装——本局升帽可达(=锁存 `cap_sup_latched` 置真)⇒ 按 **cap_sup**;不可达 ⇒ 按 cap_resolved 现值;买断制局 0。r2_budget 溢余段自由带阈值随升帽可达性条件式(可达局自由带收窄=付费刷新更难=保守端;公式=economy §E3 r2_budget 行+canonical 行 9/10)【S1 塌缩标:原阈值条件式转录已塌缩为指针+方向,原句全文归档=design_telemetry.md S1 节对照表】。锁存单调 ⇒ 组装口径帧间无翻转;假置位方向=cap_sup 组装=右端/阈值上偏=放行面保守端。残余账自有量级界:「全程零观测的晚发牌构造」每帧 ≤(cap_sup−cap_resolved)×R_剩、单笔 ≤(cap_sup−cap_resolved)×R_全局(R_全局 ≤27=注册表 `TOTAL_NODES` 结构上界,默认局 ≤135 金,非 α 级)。(量纲互指:本节残余账按帧计量;ev_buy_veto D 位残余账在 canonical 行 12 按/件计量——同一 D 消费者两笔残余以构造+量纲双分立,禁并账,R97 批 O1)[现行=R72-1][现行=R73-3][现行=R71-1]

**扣减式触发器与解释区间二值判定**:判定证据=「实际利息(结算屏利息行,`cw_settlement_obs.py::parse_settlement_gold_detail` 通道,无值域守卫直读) − Σ在场加算型 `interest_flat_per_node` > cap_resolved(或锁存 cap_sup)组装值 ∧ 无 override 命中」(本判定侧 cap 消费位登记=canonical 行 13,R111 批)。判定端解释区间=「cap 组装值 + Σflat」,**未在册升帽显式划出解释区间**;区间内 ⇒ 判定端无事(不申报不触发);超区间 ⇔ 即 R79-3 触发证据,一律进注册表补全批核查(滤噪职能由「触发后补全批核查承接」,超区间频度由触发帧计数承载、不另设申报账)。S 位另存第三态残余:「升帽可达但整局未获取」(锁存置真 ∧ active_strategies 无升帽实名),量级=存量口径:S 目标线虚高 50 金+该态计数与受影响帧数分布【R137 量纲标(IMPL_ADV_R137 症4,打标不删史):原「量级换算=该态计数×50 金/受影响备战期帧」系存量当流量的金·帧读法,已改存量式,门槛挂计数×分布;单一源=design_telemetry 键节 R137 量纲标(源复核=LEMMAS R92-3(c) 亲读,半源——仅辖延迟事件计数,不辖金值单位)】,观察键=`s_line_cap_sup_unrealized`(局终快照落盘)。[现行=R86-1][现行=R85-1][现行=R83-2][现行=R90-4][现行=R92-3]

## L5 治理通道(核查批判定者节律/两观察键,现行态)

治理通道检视词表(处置「升帽策略 ⊆ 注册表」前提的失真面)双通道:

- **召回通道(一级)=宽口径预筛**:未注册名语料 effect 含「利息」子串即入人工分诊集(对一切含「利息」措辞族召回=1,误报成本=人工多看一条原文);
- **确认通道(二级)=精确正则族「最大利息|利息.{0,6}提升至」**:注册表内半边 effect 原文全扫 + 分诊集升帽确认。
- 辖域分工:本通道辖「文案含『利息』字样」的词形族**内**变体;词形**族外**变体由扣减式正交触发器承接(见 L4,两半边互补无空隙)。

两观察键(design_telemetry.md 键节 `interest_overcap_trigger`/`interest_overcap_check_closed`;【R95-3 指针勘误(IMPL_ADV_R95 A3):原指针 `docs/develop/currency_war/redesign/reports/IMPL_DESIGN.md` §5.2 系重组前介质锚,该节重组后已是指针节,键行实体在 design_telemetry,打标不删史】):

1. **`interest_overcap_trigger`**(观察级):扣减式判定证据成立帧/批计数;超标判读=非零即触发核查批。区间外帧归因分键(按核查结论,禁混计):①`r79_3_zero_obs_upcap` 零观测升帽局(核查预期关闭)/②`r79_3_ocr_misread_big` OCR 误读大值(关闭)/③`r79_3_flat_miss_misreport` 误读名 miss 漏减 Σflat 误报(关闭)/④真超息=漏登(不关闭,进注册表补全批)。另登记第五通道(漏报向)`r79_3_fake_append_flathigh`:OCR 假名 append 使 Σflat 虚高、真漏登帧差值压回区间内不触发,配 append 二次读数校验。
2. **`interest_overcap_check_closed`**(观察级):核查批判定「非升帽」而关闭的核查件数/批。**核查批判定者与节律钉死**:判定者=落码后治理通道的批判读角色;节律=事件触发——`interest_overcap_trigger` 计数>0 的批判读时点开一次核查批,无触发不开批;件=未注册策略身份(归一后同名合并一件,禁按超息值去重,超息值仅披露维);判读者缺席 ⇒ 核查义务挂账至下一个设批判读时点(顺延不悬置)。[现行=R88-2][现行=R90-7][现行=R78-4][现行=R77-1][现行=R79-3]

## L6 误读噪声三残余方向分立(现行登记三条,分立不并句)

1. **漏读向(组装面残余,R72-1 账)**:OCR 漏匹配 ⇒ 全程零观测的晚发牌构造,升帽在场但锁存不置位 ⇒ 判据按 cap_resolved 现值组装,暴露=首次属性命中帧前的备战期帧;量级界每帧 ≤(cap_sup−cap_resolved)×R_剩、默认局 ≤135 金。触发器侧同构造型的结构性触发(升帽在场但观测漏 ⇒ 真实利息按 cap_true 结算 ⇒ 区间外)归 `r79_3_zero_obs_upcap` 分键,核查预期关闭——该账辖经济判据组装面、不辖触发器面。[现行=R72-1][现行=R88-5]
2. **误读撞实名假置位(置位面残余,R75-3/R76-5 账)**:OCR 误读产出归一后逐字等于某实名 ⇒ 属性匹配命中 ⇒ 假置位(不可达局被按 cap_sup 组装);方向=假可达=保守端,量级继承 ≤135 金自有界,频度=OCR 逐字误撞率×候选命中率(精确查询口径),观测=置位时落候选名快照事后核。[现行=R75-3][现行=R76-5]
3. **误读名 miss 漏减 Σflat(扣减漏源残余,R85-2③ 账)**:OCR 误读使已持有的加算型策略名读 miss ⇒ Σflat 漏减 ⇒ 扣减式判定差值偏大 ⇒ 误报向(触发器③分键 `r79_3_flat_miss_misreport`,核查关闭)。另同族假阴性向第五通道 `r79_3_fake_append_flathigh`(假名 append 虚增 Σflat 压低触发)与取款翻转过扣键 `interest_flat_overdeduct` 各自分立登记。[现行=R85-2][现行=R90-3]

---

## 执笔备注(整理过程裁量与定谳记录)

多轮修订链逐层核对中拿不准、经查 IMPL_FIX_LEMMAS.md 对照表行定谳的实例:

1. **锁存载体终态**:R75-4 对照表行(LEMMAS L3078)裁 GameState,R76-1 对照表行(L3099)以生命周期证据改判 StrategySession 并显式禁 GameState 承载——终态=R76-1,查行=R76 对照表。
2. **sim 侧置位语义终态**:R75-1 对照表行(L3075)声明「三选一事件处对候选集调谓词」,R76-2 对照表行(L3100)以 sim 无三选一建模改写为「注入处对单名过谓词」——终态=R76-2,查行=R76 对照表。
3. **置位查询规格终态**:R75-3 对照表行(L3077)同时含「精确 dict 查询」与「str_utils 相似匹配纪律」两句(两读互斥),R76-5 对照表行(L3103)消解=精确查询、str_utils 句移出谓词语境——终态=R76-5,查行=R76 对照表。
4. **帧序条款终态**:R77-2 对照表行(L3131)含「违条款须改实现」与「入分叉申报」双处方,R78-3 对照表行(L3174)单义化=案②错帧为申报分叉、现帧序=现行结构性质——终态=R78-3,查行=R78 对照表。

---

## 附:R189 换核重切·锁存族零变更声明(2026-09-03)

> 【R201 指针标(IMPL_ADV_R201 症3,打标不删史;原文零删改):本附注的**登记单一源=design_telemetry.md R189 节**(「R189 · 批0 重切设计批」),本附注为条件项就近提示、非第二登记源;条件项(批1 范围是否含 r2 三层门/升帽可达性判据)的状态以 telemetry R189 节为准,批1 改锁存写端时只须核读单一源。IMPL_ADV_R201 症3立案:本附注原系带日期/批号的进度性声明落入现行态规格文档,与本文头注登记位辖域分工冲突的轻量双源,本标=指针化收口。】

换核重切迁移序步 5(IMPL_DESIGN §6.4-R R189-6)为**条件项**:仅当批1 范围含 r2 三层门/升帽可达性判据才新建锁存写端(CwScreenInvestStrategy 写端扩展+session.cap_sup_latched 字段)。§L1-§L6 现行规格零变更;写路径裁决链(R74-2 op 层写路径→R75-1 sim 置位等价物→R76-1 载体=StrategySession)与 R188 修注的现名现锚保持;sim 侧置位等价物断言(R75-1)为该步验收判据之一。
