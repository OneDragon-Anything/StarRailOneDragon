# 货币战争-装备图鉴

> 性质:画面档(screen_onboarding 产物;只写画面本身事实)。建档:2026-08-27(**离线降级**:存量帧建档,交互验证缺,标「待现场补」)。
> source_image:`sr-od-test/screens/货币战争-装备图鉴/default.webp`。
> automation 用途注记:图鉴 = canonical 数据采集入口(`harvest_equip_codex` op 在此页作业;对局内经备战屏「数据银行」进入,非破坏性 overlay)。本档锚定画面识别与作业区域。

## 何时出现 / 状态流转

- **入口**:数据银行主页点「装备图鉴」入口卡(harvest_equip_codex 假设前置已到位;本帧链路未重验);
- **出口**:ESC(历史实采记录:ESC×2 回备战屏 —— 中间过数据银行主页;待现场补验);
- **下级**:无独立子屏;右侧详情面板为同屏区域切换(点左网格某件 → 详情面板换内容)。

## 画面结构

- **标题**:左上「装备图鉴」+ 扩充倒计时(96天10小时,动态值);
- **顶部页签行**(y120-175):分类图标横排(合集/简易/进阶/特权/消耗品/星徽等;图形按钮,OCR 读不出——历史实采用坐标点击,如合集 (699,170));
- **左侧网格**(y230-1040,x50-1330):装备卡 7 列阵列,图标卡身 + 名字在下(y≈352/585/816 行距 ≈230,滚动推进);每行首有分类计数条(如「其他装备[23/46]」,动态);
- **右侧详情面板**(x1420-1920):选中装备的名称/类别/前后台强度/效果全文/适配角色;
- **右上**:返回箭头(图形按钮,行为待现场补验,推测=退数据银行)。

## 识别特征(id_mark)

- **标题-装备图鉴**(80-210, 22-78,lcs 0.9),conf 0.999 回验命中;
- 防撞:数据银行帧的同名文本在入口卡区 (1388,435),不在本 pc_rect → 不撞数据银行;角色/羁绊/策略/环境图鉴标题文本不同位亦不同文 → 不撞。全量 id_mark 测试通过(sr-od-test `test_get_match_screen_name`,119 passed 含本屏 fixture)。

## 可交互元素与行为

| 元素 | 坐标(pc_rect) | 点后行为 | 实测 |
|---|---|---|---|
| 分类页签行各图标 | 区域-分类页签行 (60-900,120-175) 内 | 切换左侧网格分类(历史实采走此路径) | 历史批 ✓ 本批未重验 |
| 左网格装备卡 | 区域-左侧网格名带内 | 右侧详情面板切到该装备(op 实测过点击选中流程) | 历史 ✓ 本批未重验 |
| ESC | — | 逐层退(最终回备战屏) | 历史 ✓ 本批未重验 |
| 右上返回箭头 | (~1840-1905,40-105) | 未验 | 待现场补 |

⚠️ **离线降级声明**:以上交互行为均来自历史实采记录与 op 代码,不是本批交互实测;下次进数据银行时按 od-dev-screen-onboarding §4 补现场验证。

## automation 要读信息

- 标题锚(id_mark,画面判定);
- 分类计数条「XX[N/M]」(采集进度判据:新一屏去重 0 新增 = 采全 的旁证);
- 左网格名带(OCR 行 Y 聚类,定位装备卡中心 x 列 + 推行);
- 右侧详情面板(OCR 名+效果+适配角色,数据权威源采集)。

## 关联

- 「货币战争-数据银行」(上级);消费方:`harvest_equip_codex`(tools/,当前 tab 点击仍硬编码坐标,后续可改读 screen_info);
- 模板资产:`.debug/temp/cw_shots/`(2026-08-17 图鉴整屏采集批,模板裁剪源用原 PNG 非 webp 归档版)。
