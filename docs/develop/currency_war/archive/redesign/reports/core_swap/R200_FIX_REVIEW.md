# R200_FIX_REVIEW —— R200 修复批(对抗轮七症+复审 F1/F3+观察项)独立审查

> 审查者=独立 review agent,无前提纪律执行:修复批报告的自评一律不采信,
> 「全绿/对拍一致」处亲跑才算数;独立发现与报告结论矛盾者优先报。
> 开工令已执行:先读 `sr-od-currency-war-dev` skill(strategy-work「锁的存在性
> 纪律/验证/零调参」节),通读 `design/IMPL_DESIGN.md` §6.4-R 与上游症状
> 单一源 `redesign/IMPL_ADV_R200.md`、`core_swap/REWORK_REVIEW_20260903.md`
> 后动工。零生产代码改动、零 git。
>
> 本报告验证脚本:`core_swap/review_r200_independent_checks.py`(14/14 PASS,
> 亲跑;与修复方的 200fix_r200_verify.py 断言零复用,全部从注册表/公式
> 第一性重算)。

## ① 总判决

**本批 12 个立案面(症1/2/3/5①②/5③/5④/6/F1/F3/OBS-3/OBS-4)全部判
「真解决」或「合理收口」——但修复批的覆盖面申报与实际不符:标题宣称
「对抗轮七症」,实际只修了 5 症(症4、症7 量具面两症无修复、无缓期声明、
无归属申报),此为本次审查的最高价值发现(见 ②-A)。**

亲跑复现记录(全部本机执行,非采信报告):

| 项 | 报告声明 | 亲跑结果 | 判 |
|---|---|---|---|
| 200fix_r200_verify.py | 21/21 PASS | **21/21 PASS** | 符 |
| 测试四件 | 183 passed | **187 passed**(30.8s) | 数目差 4,见 ②-E |
| CW L1 全量 | 2422P/2S/1XP/1F(预存红 w614) | **2422 passed/2 skipped/1 xpassed/1 failed**(2:14),唯一红= test_cw_w614_sim_fidelity 零漂移 digest | 符;红项属旧核 decision_v2/sim 面,本批文件面(cw4)对其可达性弱,「预存红」归属可信(无 git 态下不可终证,留痕) |
| ruff(10 文件) | All checks passed | **All checks passed** | 符 |
| review 独立脚本 | —(本审查新建) | **14/14 PASS** | — |

## ② 独立发现(与报告结论矛盾/报告未报项,按价值排序)

### A(中高)覆盖面申报失真:症4、症7 无处置、无声明

- **论断**:R200_FIX_REPORT 标题与首行宣称「IMPL_ADV_R200 **七症**(代码面)
  修复批」,但逐症修法节仅含症1/2/3/5①②/5③/5④/6——**症4(量具面:零刷新
  事故形态不拦,正式 A/B 前置守卫组双绿)与症7(量具面:sim A/B 只经
  decide_shop_screen,prep 线零覆盖,门组结论辖域大于测量域)整件缺席**:
  无修复、无「移交并行批」声明、无缓期登记。修复批清洁门申报的禁碰面
  (kernel/、sim/runner.py、decision_v2/、telemetry/)不含 ab_core_swap.py,
  即症4 的修复对象(守卫组门)并非本批禁碰面——缺席不是禁碰所致。
- **证据**(亲读+亲查):`sim/ab_core_swap.py` 现树无「formal AB ⟹ V_GAP
  注入态(或显式豁免批文)」的 v6 行(rg 'formal/V_GAP 注入态' 仅命中既有
  exempt 字典与 require_v6_green_for_formal_ab 旧函数名);门组 docstring
  亦无测量域限定句(症7 对象)。IMPL_ADV_R200 症4/症7 现状未变。
- **推理链**:报告自查表 8 行无一行辖症4/症7;若它们被派给并行量具批,
  修复批有义务在文件面/清洁门申报「本批不含症4/症7,归属=X」——现状是
  标题写「七症」、正文修 5 症,读者按标题对账即漏两症。多批零 commit
  叠加态下,这种覆盖面落差正是漂移源头。
- **判**:申报缺陷(非代码缺陷)。处置建议:要么补修(症4 的 v6 判据行/
  arm_diff_probe 携 exempt 态是量具批小件),要么在报告头部显式改口
  「本批=代码面 5 症+复审 2+观察 2;症4/症7 归量具批」并留持久索引。

### B(低)p_shop 二阶声明的升级阈在样本格上已越线

- 症2 泛化步申报「q≤~0.05 时二阶差量 ~1e-3 量级;接线日差量 >1e-3 相对
  ⇒ 升级精确式」。我的独立对拍(独立式 vs 注册表 `_refresh_dist` 无放回
  精确式):L6c1j5tc7 相对差 **1.08e-3**、L7c2j3tc20 **1.45e-3**——
  **样本格已越过其自设的 1e-3 升级阈**。方向(独立式低估 P_shop)与
  量级(~1e-3)与申报相符,修复本身无错;但按自查表第 2 行自己的可推翻
  条件,**接线日的精确式升级评估已经是必答题而非备选题**。对拍通道
  `1−_refresh_dist(p,v,a,taken_c,1,j)[0]` 我亲验可用(3 格全通)。
- **判**:真解决+申报如实,但升级阈已被实测数据自身触发——登记为接线日
  硬前置,防「~1e-3 量级」措辞被读成「无需升级」。

### C(低)buy.py T_SEARCH_A None 期的遥测归因模糊

- shop 侧 M6 在 T_SEARCH_A None / V_MS 缺读两种「不可用」下均计
  `m6_overflow_strand`;而 buy.py ev_buy_candidates 在 U_X 有值、T_SEARCH_A
  None(或 V_MS 缺读⇒窗口空)时返回 `ckey=''` + 空候选,shop.py 计入
  **`shop_ev_no_candidate`**(「无候选」)而非任何 unavailable 分键——
  「窗口不可用」与「店内无合格件」两种成因在遥测上不可辨。与 D-P2idle
  分键初衷(「无候选 vs 全拒」可辨)同型病灶的半边复发。行为面无害
  (fail-closed 少买),观测面模糊。

### D(低)症5④ free==1 放行缺专属行为锁

- 报告称「行为零变(现有 stockpile 域锁覆盖行为)」——亲查:stockpile
  消费面在测试仓仅 `test_stockpile_face_m6_strand`(strand 计数)一条,
  **free==1 末席放行行为(案③ 1★ 限定)无任何直测锁**。本批是注释面
  改动无回归风险,但 IMPL_DESIGN §2.5 案③ 明载「并入 §6.4 fail-closed
  测试面」+「放行件滞留计数与风险上界进遥测」两半边至今无载体——
  报告「现有锁覆盖」的措辞强于事实。

### E(信息)测试四件计数 187 vs 报告 183

- 报告时点 183 passed,现树 187 passed。差 4 条,无 git 态下不能终证归属;
  判为并行批在同文件追加(多批叠加态正常形态),非本批问题——但留痕:
  若后续对账发现这 4 条其实是本批未申报的测试面扩围,再升级定性。

### F(信息)OBS-4 登记形态:入口指向不实现公式的函数

- `audit/derived.py` 的 `difficulty_interim` 条目 `entry` 仍指向
  `statefn.lambda_death.difficulty_band`(真值优先半边,不产 interim)。
  「登记改未落码」的选择本身已显式申报且理由成立(观察面未载三分量),
  但 `entry_points()` 审计钩子消费方拿到的仍是名实不符的可调用体——
  公式落码前,任何按登记表「数值随注册表重算」放行的审计在此条上会
  静默放行一个不存在的公式。建议:条目加 `implemented=False` 类字段或
  entry 置空+注释,别让注释独自承载「未落码」。

## ③ 逐症独立判决(治本三问独立答)

### 症1 p_complete off-by-one —— **真解决**
①根=数学层求和域错,修在公式本体(`range(m)`+`m>refreshes` 显式 0),
正确层。②泛化:亲核同文件 `p_bar_exact`(满池分母声明在案)/
`_streak_v_table`(无二项尾)/registry `_refresh_dist`(超几何,支撑域
处理正确)——无同病。③新问题:无;`m=0` 输入退化为 tail=1(Σ 空和),
语义无害。**独立数值验证**(自选参数,不复用修复方例):4 组手算对拍
+ m=n 边界(P=q^n)+ m=n+1⇒0,全部 1e-12 内一致(脚本 A 组)。

### 症2 slot_q 分母漏扣 j —— **真解决(带二阶声明)**
①根=包装层与注册表权威模型背离,修为同式 `va−j−taken_c`(注册表
`cw_shop_odds.py` L125-127 亲读确认「拥有的 j 张已离开牌库」原文)。②泛化:
p_shop 槽间独立假设已在 docstring 声明为二阶量,方向(低估 P_shop)我
数值证实(脚本 C 组 3 格独立式均 ≤ 精确式)。③新问题:无;但见 ②-B
(升级阈已越线)。**独立网格验证**:6 级×3 费×3 持牌×3 拿走全网格与
手推式逐位一致(脚本 B 组)。

### 症3 挂后台效果三通道 —— **真解决**
①根=声明-实现缺口(0/3 接真语境),修=共享装配函数 `bench_effect_context`
+ 三通道统一消费(shop.py L451/L711/L761、mandate.py L279、entry.py L442/L533
传 state 全部亲读确认)。②泛化:同型 V_slot 净门缺口已另案(症5④)。③新问题:
`_herta_supply_present` 的三观测源(augment=proof.DIRECT_LINE_SIGNAL_STRATEGIES
单一源、板面 deployed、线内 K)我亲验:DIRECT_LINE_SIGNAL_STRATEGIES=
('黑塔纪元',) ✓、CHARACTERS['黑塔'].bench_effect='星级供强' cost=1 ✓、
'大黑塔'在册 ✓、RUST_AFFIX_NAME='库藏生锈' ✓。state=None 缺省保护端
(herta=True∧rust=False)语义自洽且已申报。通道级行为锁 3 条(fuel/funding/
sell_for_interest 语境分叉)亲跑通过。一个边角留观:line_switch_sell 的
保守子集不消费 bench_effect 语境——按 predicates 三消费位声明本就不含
塌缩出口,非本批引入,全式接线日顺带复核即可。

### 症5①② 搜索窗口查表 —— **真解决**
①根=注入态硬编码 {1,2,3}(无出处常数),修=「读法×等级×带端」运行时
查表,零新自由参数。②泛化:grep 全 cw4 硬编码窗口清零(脚本 E 组)。③新问题:
两消费位读法分立正确(CALIB_REPORT_V2 §2.3 原文核对:档级=甲/单卡=乙;
buy.py 用乙、shop M6 用甲,与规格逐字对上)。**两道门都过**:公式对
(独立手算)+对得上设计源——我以第三方身份直读 REFRESH_PROB 重算两读法
全表 L1-10,与 calib_v2_analysis.json 及代码实现三方一致(脚本 D组);
对拍链非自证循环(json 由标定批独立生成,报告方对拍+我的第三方重算
双通道)。旧锁重推合规:docstring 记录重推依据(CALIB §2.3 定谳常数
不可推导),非机械跟绿。fail-closed(V_MS None⇒空集)锁在。见 ②-C
(遥测归因模糊)。

### 症5③ 塌缩保守子集 —— **真解决**
①根=「全堵→全卖」注入悬崖,修=保守子集(仅 1★ 全额可退)。②依据复核:
p41 燃料类支配性论证在**旧线成员**域的适用性——全额退金 ⇒ 卖出净成本=0
(金域),残余损失仅再遇期权(这正是未标定量),2★+/部分退件保留的
切分线与「不可逆损失方向」一致,子集边界可辩护。③新问题:注入态锁
(1★ 放行/2★ 保留)+ None 期 blocked 锁亲跑通过。

### 症5④ V_slot 净门空语句 —— **登记+方向勘误,合理收口(择「登记」合法)**
净门数值输入系 u/V_ms 标定派生量、U_X 豁免在案(亲核 CALIB_REPORT_V2
§2.2/2.4 豁免表)⇒ 落码即新自由参数,违零调参骨架——取「显式登记」
符合 strategy-work §1 两类合法数字的禁令侧。**方向勘误独立复核属实**:
旧注释「保守代入放行」确系反号(放行门代入 0 门槛=放行偏=fail-open 向,
可逆性由 1★ 全额退承载而非「保守」措辞)——修正确。缺口见 ②-D。

### 症6 刷新费字面量 —— **真解决**
mandate.py `_s_reserve` 与 shop.py 三处消费位全部 import
`REFRESH_COST_BASE` 符号(亲读);全 cw4 `or 2)` 消费位 grep 清零
(脚本 E 组)。符号替换,无新问题。

### F1 静态守卫 AST 化 —— **真解决(+第三盲区发现核实)**
- 第三盲区(旧守卫 `_src_root` 解析 `src/src` 不存在 ⇒ rglob 恒空 ⇒ 恒绿
  no-op):推理链亲验——`proof.__file` 起 parents[5]=src 根,旧式
  `parents[5]/'src'` 确指向不存在路径,rglob 恒空成立;即**旧守卫从未
  真实扫描过任何文件**,本修复=守卫首次生效。真实 src 树零 offender
  (亲跑,18 调用点全落白名单三文件的申报与零 offender 结果一致)。
- 两盲区负测试(裸名直调红/basename 逃逸红)+全径正例绿,亲跑通过。
  AST 解析逻辑亲读:import 绑定名→尾径映射、包级前缀通配、点链解析
  均成立。已知边界(局部重绑定遮蔽/getattr 动态调用不在检测面)已诚实
  申报,且 ensure_contract 运行时核验仍是主防线——静态守卫定位为纵深
  第二层,合理。

### F3 stop_buy 辖域注释 —— **收口属实**
shop.py L426-428 弃权侧注释亲读:辖域限支配买/溢余面,M2 义务不走停手门
([41]),与 mandate.py 侧 M2 无 stop_flag 门的实现两读自洽。注释面,零行为。

### OBS-3 arm1 None 兜底死分支 —— **收口属实**
predicates.py L95-99 docstring 亲读:生产不可达性经 ensure_contract 前提
申明,保留作函数局部完备性。与 contracts 先例③(deploy_cap=None 违例)
口径一致。

### OBS-4 difficulty_interim —— **对齐(择「登记改未落码」),登记形态留 ②-F**

## ④ 自查表 8 行可推翻条件逐条执行记录

| # | 可推翻条件 | 执行 | 结果 |
|---|---|---|---|
| 1 | 数值锁红即回归;m>n 边界改判需引 P38 | 数值锁亲跑+独立例+m=n/m=n+1 边界 | 未触发 |
| 2 | 注册表改式则同改;对拍差量 >1e-3 相对 ⇒ 升级精确式 | 注册表式亲读同式;**差量 1.08-1.45e-3 已越 1e-3**(②-B) | **部分触发:升级评估到期** |
| 3 | state=None 保守端若实机显示黑塔误保护滞留可翻缺省端 | 行为锁+装配单测亲跑;实机判读面本审查无数据 | 未触发(留观) |
| 4 | V_MS 换值窗口随 V̄ 漂移(设计内);json 重生成不一致 ⇒ 查表式错 | 第三方独立重算全表一致 | 未触发 |
| 5 | vopt 全式接线日取代子集语义;sim 显示注入态 2★ 滞留成本显著则重推边界 | 锁亲跑;sim 面本审查未跑(潜伏面申报与 IMPL_ADV_R200 一致) | 未触发(接线日执行) |
| 6 | 无(符号替换) | grep 清零亲跑 | 未触发 |
| 7 | 局部 def 同名遮蔽不在检测面(已知边界) | 边界申报如实;静态守卫非主防线定位合理 | 如实 |
| 8 | 对应文档/规格演时同步 | F3/OBS-3/OBS-4 注释面亲读对齐 | 如实 |

## ⑤ 清洁门(本审查)

- 文件面:本报告 + `review_r200_independent_checks.py`(core_swap/,review_
  前缀)两件;零生产代码改动、零 git、零测试仓改动。
- 全部亲跑项:200fix_r200_verify.py(21/21)、测试四件(187P)、CW L1 全量
  (2422P/1F 预存)、ruff 10 文件、review 独立脚本(14/14)、注册表直调
  复核 4 组、第三方窗口重算、grep 清零 2 组。
- 结论强度声明:代码面各项证据强(数值/网格/负例);「预存红归属」与
  「187 vs 183 计数差」两项受无 git 态限制,证据强度=中(归属推断合理,
  不可终证),已留痕。
