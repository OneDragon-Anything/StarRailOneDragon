# P19 逐件证明验证报告(unit_bond_tags 计数语义)

> 验证日期:2026-08-31(逐件证明验证批);验证人=数学验证工程师 subagent。
> 探针脚本:`p19_val_check.py`(本目录,可重跑:`$env:PYTHONPATH='src'; $env:PYTHONIOENCODING='utf-8'; uv run python .debug/temp/currency_war/redesign/p19_val_check.py`)。
> 全程只读生产文件,未改任何 proofs/代码/文档。

## 0. 验证对象声明(编号撞车防呆)

以 `docs/game/currency_war/research/math_proofs.md` 索引行定位:**P19 = unit_bond_tags 的羁绊计数语义**(星徽 add-if-absent 幂等 / 卡带双计 / 计数上界),文件 `research/proofs/p19-unit-bond-tags-counting.md`。三节子命题:①计数上界(已证)/ ②星徽幂等性(记载为"部分证伪:序依赖缺口")/ ③卡带双计边界(已证)。**非**任何其他 P19 撞车对象(历史上编号撞车已十起,本行经索引直读确认)。

## 1. 阅读门与公理栈

- 已读:`math_proofs.md` P19 索引行、`proofs/p19` 全文、`equipment_mechanics.md` §6(口述语义权威+出处声明)、`docs/develop/currency_war/strategy/06_input_model.md` §3(注册表地图:装备全量=`cw_equipment_data.EQUIPMENTS`,plaza API 溯源)、`sim-wiring.md`「羁绊口径分层」(三侧单一源声明)、w50 测试锁(`test_cw_early_kernel.py:1351-1527`)、实现 `kernel/cw_bond_equips.py` 全文。
- 无交集跳过声明:P19 为**计数语义命题**,不含金流/EV/收益比较——economy/final_comps/transitions/plaza_methodology 等纯攻略与经济账文档与本命题辖域无交集,跳过;公理栈中「超额收益隔离」「纯金流」两条对非金流命题不可适用,同理跳过;「零调参」「参数溯源」两条适用,见门⑤。

## 2. 五门逐门判定

### 门① 内部推导(逐步核)——**通过**

- **①上界** `s_x ≤ c_x ≤ s_x + β_x + t_x`:证明用归纳于装备处理步数。逐步核:下界=Self(u) 全进 tags 且两分支只增不减 ✓;上界=星徽分支对每羁绊全程至多追加一次(seen 单调)+卡带每件恰追加一次 ✓。注意:该归纳论证对**修后两遍法**同样成立(星徽判定改为 `x∉seen∪T̄` 只会收紧上界,不破坏)——探针实测 89173 个(单位×组合×羁绊)点位 0 违例,含 `|tags| ≤ |Self|+|B∪supp(T)|+|T|` 总量界。
- **②证伪部分**(星徽先于卡带计 2):机制推导正确——单遍实现里星徽先处理时 x∉seen → 计 1 并入 seen,卡带随后**无条件**追加 → 计 2;根因定位(「成员资格封闭性只落了半边」)准确。该证伪针对的是**提出时点的旧实现**(见门④:修复已于同日落地,现行代码反序=1,证伪对象已成为历史态)。
- **③边界表**:5 行组合语义逐行核,与两遍法实现一致,探针逐行复现(§2 门③实测全 ✓,另补证明表未列的"两不同名卡带同羁绊(非成员)=2"亦 ✓)。

### 门② 锚点直调(逐项回注册表;可重跑)——**通过**

- 注册表 `EQUIPMENTS`:星徽 22 张(**=证明/测试口径 22**),逐张解析出羁绊贡献(失败清单为空);骇客类 8 件中 4 件卡带(欢愉卡带/Max、星核猎手卡带/Max)解析为卡带授予,另 4 件(扑满病毒系/管理员手套)无羁绊贡献——与 `_TAPE_BOND_GRANTS` 分表一致。
- 机制原文直调:欢愉卡带 effect=「装备者加入「欢愉」羁绊,若装备者已是…成员,则…计数+1」;星核猎手卡带=「羁绊计数+1」;星徽 22 张统一句式「装备者加入【X】羁绊」——与 `_RX_TAPE/_RX_TAPE_COUNT/_RX_BADGE` 三条正则的锚定句式逐一对应。
- 角色锚点:符玄自报=(仙舟,治疗,量子同频)——**不含欢愉**(证明用"非成员+欢愉装备"案例成立);银狼LV.999 flows 含欢愉(成员案例成立);姬子·启行自报列车同行(成员穿星徽案例成立)。
- 探针可重跑状态:脚本落本目录,输出含全部锚点行与判定(见 §0 命令)。

### 门③ 建模对象 vs 玩法文档(机制原文)——**通过,附一处文档滞后**

- 建模对象(星徽=成员资格授予 add-if-absent / 卡带=无条件计数+1 可双计 / 组合计 1)与 `equipment_mechanics.md` §6【口述·权威,2026-08-28】逐条一致;§6 的出处声明(口述原文不在仓库,依据代码注释+W362 审查引述,证据等级[口述·权威])在 P19 依据栏如实引用 ✓。
- 现行两遍法实现语义上**优于** §6 组合检验点行的描述——该行写的实现是"卡带分支 `seen.add` 后星徽查 `seen`"(单遍旧法),与现行代码不符(见 §4 勘误 2)。
- 边界备注:欢愉卡带对非成员"加入即+1"这一读法,纯文本有歧义(「加入」是否自带计数),判据来自口述权威+§6 已声明证据等级,处理合规。

### 门④ 实证 vs 账本(预测可达性)——**通过;索引行"反序现 failing"已过期**

- 证明记载的证伪探针复跑:`符玄 equips=['欢愉星徽','欢愉卡带'] → 欢愉×1`(证明记载修前=2)——**两遍法修复已落地**(commit `6edc6cbf` 2026-08-28「羁绊计数两遍法——修 C1 修复的顺序依赖缺陷(P19 证伪星徽幂等性)」),证明§②的"处置挂账(代码批)"已被该批兑现,含其要求补的反序对称锁。
- 测试锁全绿:`test_cw_early_kernel.py` w50 面 8 passed(含 `tags_combo` 正序锁、`tags_combo_rev` **反序锁=1**、成员卡带双计=3、三侧 fullset);三侧消费面 `test_cw_deploy_ops.py`+`test_cw_sim_mutex_tx.py` 85 passed 1 skipped。检验点 4(三侧传导)实证过。
- 检验点 2(序不变性全排列性质测试):测试仓**无**该性质测试;本批探针补做——26 件羁绊装备 × 6 角色 × 1/2/3 件全排列 = 17706 组合,逐羁绊计数违例 **0**(修后实现满足序不变性)。

### 门⑤ 参数溯源(各常量;公理判定)——**通过**

- 本命题**零自由参数**:无阈值、无权重、无调参量(零调参 ✓)。仅有的"常量"是 3 条正则(锚定注册表 desc 句式,溯源 `cw_equipment_data.py` 星徽 22 张/plaza API,注释带行号指针 ✓)、类别字面量('星徽'/'骇客',注册表 category 字段)、开拓者按排归一口径(`cw_chars.trailblazer_form`,与 board_from_tracked/W21 #13 同口径,注释声明 ✓)。
- 公理判定:参数溯源 ✓(全部常量可追到注册表/机制原文);超额收益隔离、纯金流两条对计数语义命题不适用(§1 已声明)。

## 3. 最终判决

**通过(数学本体成立;证明文档三处滞后于已落地的修复,需勘误)。**

- 命题①③:现行实现下严格成立(探针 89173 点位 0 违例+边界表逐行复现)。
- 命题②:其"证伪"在提出时点属实、根因分析正确、修法(两遍法)正确且已由 `6edc6cbf` 落地并带反序锁与三侧回归——**命题链已闭环**,但证明文件与索引行仍停留在"部分证伪/反序现 failing"的修复前状态。

## 4. 修正建议(均为文档勘误,非数学错误;本批禁改,移交文档批)

1. **`math_proofs.md` P19 索引行 + `proofs/p19` 状态行**:②应改为"已闭环:两遍法修复落地(`6edc6cbf`,2026-08-28 同日),反序对称锁过;'反序 failing 检验点'表述过期"。检验点 1 的"现行 failing"同改。
2. **`proofs/p19` §记号与实现模型(伪代码,L21-30)**:描述的是修复前单遍算法,与现行 `cw_bond_equips.py:134-144`(两遍法)不符——应更新为两遍伪代码或明确标注"历史态,现行实现见修法段"。
3. **`equipment_mechanics.md` §6 L96**:「实现:`cw_bond_equips.unit_bond_tags`(卡带分支 `seen.add` 后星徽查 `seen`)」为旧实现描述,应改为两遍法描述(先收齐卡带授予集 T̄,星徽判 `x∉seen∪T̄`)。
4. (增强,非勘误)检验点 2 的序不变性性质测试(≤3 件全排列)与检验点 3 的"两张同羁绊星徽计 1"用例,测试仓现仍缺——本批探针已各自验证通过(0 违例/计 1),建议修复批顺手固化为性质测试。

## 5. 证据清单

- 探针:`p19_val_check.py` + 本报告 §2 各实测行(可重跑);
- 测试:`test_cw_early_kernel.py`(w50 面 8P)、`test_cw_deploy_ops.py`+`test_cw_sim_mutex_tx.py`(85P 1S);
- commit:`6edc6cbf`(两遍法修复+反序锁);
- 注册表锚点:`EQUIPMENTS` 22 星徽全解析、4 卡带解析、机制原文直调行(§2 门②)。
