# T-22 位面过渡 修法设计(plane_transition)

## 0. 元信息

- 迭代目标:货币战争全画面规范符合性审查(2026-09-22-screen-review)·只设计不落码,待用户裁决
- 状态:对抗收敛待用户裁决(对抗轨迹:r1 未收敛 3 条低→修订→r2 收敛 0 条)
- 审查输入:`.debug/progress/2026-09-22-cw-screen-review/reports/T-22-r1.md`(发现 F-1..F-4;总判定 = 有问题;逐发现严重度 F-1 中 / F-2、F-3、F-4 低)
- 真值基线:本稿全部「现状」陈述以审查时点工作树代码为准,落地时若代码已再变,以落地时点代码重新对账后再动笔。符号锚 = `文件::符号名`,行号仅作本稿定位辅助。文档路径根 = `docs/develop/sr_od/application/currency_war/`,代码路径根 = `src/sr_od/application/currency_war/`(下文反引号短路径均相对此两根)。
- 修法性质:F-1..F-4 全部为文档语义更新与注释清理,**零行为变化**(无任何逻辑/签名/常量改动)。
- 清理准则:F-2/F-3 注释清理按 T-3 稿([battle_wait.md](battle_wait.md))§2.6 五条准则机械执行(禁形一·会话局部标识符 / 禁形二·变更史叙述 / 改写方向 = 结论→出处→边界 / 保留豁免 / 跨修法交叠一次成文),本屏特有豁免面见 1.3 与 §2.3 验证门。

## 1. 问题与动机

### 1.1 F-1 屏文档 §2 位序句与代码两 node 行为不符(中)

- **现状症状**(审查发现 F-1):`screens/plane_transition.md` §2 末句「裁决位序 = pending 先行、miss fail 后置(与未达上限弹窗的 miss 分支内序不同,各屏分支序语义各异,禁跨屏统一)」以现役口径陈述;代码实况 = 提示门 miss 在观察 node `round_fail`(先行)、`_click_pending` 重入裁决住决策动作 node 顶部(后行,且仅门 hit 后可达)——该句陈述的恰是代码模块头明文宣告已消解的单 node 形态位序,文档与代码行为相反。其比对对象(未达上限弹窗的 miss 分支内序)同属该屏代码头宣告消解的旧形态,`deploy_not_full.md` §2 亦无此句(非文档集统一公式,无跨屏对齐义务)。
- **根因归层**(根源两问):①根在哪层 = **约定层**——本屏单 node → 两 node 重构时,代码模块头同步改写了、画面篇 §2 的旧形态尾句漏扫(重构批收尾缺「旧口径引用面清查」这道门);②修的是根还是症状 = 删句治本——§2 前文对观察/决策动作两 node 的描述已完整承载现役位序,尾句是纯残留、零信息损失;『重构/迁移批收尾缺旧口径清查门』的流程级根已由 T-3 稿 §2.1 跨批登记面立案(归宿 = harness landing 模板固定判据方向,迭代内引用合法),本稿不重复立机制(跨件半问:与 T-3 F-1/F-3 同根,禁第三件症状补丁)。
- **解决到哪**:删 §2 末句整句(§2.1);§2 其余句与 §5「首发 miss → fail」经审查核一致,不动。
- **明确不解决**:任何代码行为与测试(零改动);`node_max_retry_times=8` 写值收敛为纯常量名(各屏文档统一公式,文档集整体事项,审查报告 §3.5 备注在案,不单屏动);未达上限弹窗屏自身的旧形态清理(归其屏稿)。

### 1.2 F-2 注释以现役口径引用已退役分支号 0q(低)

- **现状症状**(审查发现 F-2):`operations/cw_screen/cw_screen_plane_transition.py` 模块头 act node 描述括注「外循环 0q 的误分发计数只认 fail,完成路径必须 success」——现行机制 = 外环通用连续 fail 预算 `operations/cw_loop.py::CwLoop.OP_FAIL_REDISPATCH_LIMIT`(原 0q 位面过渡误分发守卫 =3 已退役并入本网,`cw_loop.py` 该常量注在案);旧号未随退役更新,新读者无法按现行代码解析「0q」。语义实质(计数只认 fail、完成路径必须 success)与实现仍一致,故低。
- **根因归层**:**表示层**——号制退役的清理面止于 flow 侧对号表(`flow/outer_loop.md` §2.3 已退役号表,0q 仅供读旧档对号),画面 op 注释的现役口径引用漏更;修法 = 指称换现役符号锚,语义零变化,非流程/语义层问题。
- **解决到哪**:该括注指称改写(§2.2)。
- **明确不解决**:`cw_loop.py` `OP_FAIL_REDISPATCH_LIMIT` 注释自身的会话号残留(「T-266」「W971」「2026-09-15 事故」等,命中 T-3 §2.6 禁形一)——`cw_loop.py` 不在本批文件面,归 T-3 稿 §2.6 已登记的全仓注释卫生批;「0q」在屏文档 §1/§8 的名称保留(见 1.4)。

### 1.3 F-3 变更史/退役叙事进注释(低)

- **现状症状**(审查发现 F-3,三处):①`cw_screen_plane_transition.py` 模块头「原单 node 形态『pending 先行、miss fail 后置』位序随两 node 拆分自然消解(门在观察 node 先行,裁决住决策 node 顶部)」(从单 node 改两 node 的变更叙事,与 F-1 同源文本);②同文件 `_build_transition_chain` docstring「(旧 kernel 过渡腿『三源全缺禁猜』同款语义,腿已随动作上报化切换批退役)」(退役史 + 无日期无索引的批次名);③`kernel/cw_screen_report/plane_transition.py::CwScreenPlaneTransitionObs` docstring「(行读数与 TokenCell 构造归观察侧,**迁移批接线**;None = 行空/读缺,report 不写,诚实缺位)」(「迁移批接线」无日期无索引,现读歧义——计划态措辞,实际已接线并有 `test_cw_transition_node_chain.py` 锁)。
- **根因归层**:**约定层**——AGENTS.md §8「变更史不进注释」为后立规范,存量注释未回溯清理;与 T-3 稿 F-6 同根,全仓存量卫生已由其 §2.6 登记的全仓注释卫生批立案,本稿只清审查对象两文件(跨件半问:同根,共用同一登记,不另立第二套机制)。
- **解决到哪**:三处各收敛为当前语义 + 持久指针(§2.3);句中确有的当前语义成分——「门在观察 node 先行,裁决住决策 node 顶部」「行读数与 TokenCell 构造归观察侧」——保留语义而非逐字:前句「决策 node」称谓随 §2.3 #1 对齐为「决策动作 node」(对齐依据见 §2.3 #1 目标列自注),后句逐字保留。
- **明确不解决**:同文件经审查逐行核对通过的其余措辞——「验证废除」标注 ×4(现役政策标记:验证已废除、禁重加验证+重试结构,审查报告 §3 未列命中)、「防御逐位平移」「随写点同迁」(可读作 op-layer.md §2.1「防御口径与原写点一致」契约的现役陈述,且与屏文档 §6 同款措辞同文)——本稿一律不动,禁借清理之名改写;若用户裁决按严格禁形二连带清扫,归全仓注释卫生批统一口径(含屏文档 §6 用语对齐),不混入本批。全仓其余存量注释卫生(同上,卫生批)。

### 1.4 F-4 屏文档 §1 正文含裁定日期(低)

- **现状症状**(审查发现 F-4):`screens/plane_transition.md` §1 第二条「原 0q 误读兜底(提示 rect 派发)已退役(**2026-09-16 裁定**:未建档实证的故障形态不作兜底理由,见 [../flow/outer_loop.md](../flow/outer_loop.md) §2.3 退役记录)——…」——退役现状 + 持久指针合法;括注内的日期 + 裁定叙事属变更史入正文,命中 screens/README.md §2 纪律行「as-built 无状态(事故史/裁定日期不进正文)」。
- **根因归层**:**约定层**——与 F-1 同一批文档清扫缺口的另一面(裁定叙事嵌在退役句内,退役句与指针本身合法故漏扫);修法 = 收敛为现状 + 指针,零语义损失(裁定语义由 outer_loop §2.3 退役记录单一承载,指针持久可解析)。
- **解决到哪**:§1 第二条括注收敛(§2.4)。
- **明确不解决**:§1/§8「0q」名称本身保留(经审查核一致——退役号表对号可解析,审查报告 §3.5 明示「0q 退役指针 outer_loop §2.3 在场」);其余画面篇的旧号残留(`screens/README.md` §5.5 等,`flow/outer_loop.md` §2.3 已在册申报「属文档残留旧号」)= 文档集整体事项,不单屏动。

- **在册核对一致项**:审查报告 §3 已确认一致的面(①分型申报三方一致、②零策略器问询、③链观察申报与 BOSS 排他接管、④时序 #2/#29 口径、⑤§1/§3-§9 各节 as-built 与坐标格式、⑥其余注释纪律面——含「bug#1」仓级缺陷键与「fixture plane_1to2」在档资产、§3 第 8 条在册欠账)本稿不立修法,仅作为各修法不得触碰的现状边界——落地时禁借清理之名改动这些在码/在册语义。

## 2. 方案

### 2.1 F-1 修法:§2 末句(旧单 node 位序)整句删除

| # | 位置 | 现状(病句) | 目标 |
|---|---|---|---|
| 1 | `screens/plane_transition.md` §2 末句 | 「裁决位序 = pending 先行、miss fail 后置(与未达上限弹窗的 miss 分支内序不同,各屏分支序语义各异,禁跨屏统一)。」 | **整句删除**;§2 止于「……`node_max_retry_times=8` 现役值仅框架异常路径消费)。」其余逐字不动 |

依据:代码现役位序 = `cw_screen_plane_transition.py::CwScreenPlaneTransition.observe`(门 miss → 同 node 内 `round_fail`,先行)+ `::act`(顶部 `self._click_pending` 重入裁决,后行且仅门 hit 后可达)——位序由两 node 结构必然承载(op-layer.md §1.1:门置于观察 node;重入裁决「留在决策动作 node 顶部」verbatim 在册);§2 前文两 node 描述已与代码逐项一致,末句零信息增量;screens/README.md §2 模板九节无独立「位序」条目,`deploy_not_full.md` §2 无此句 = 非文档集统一公式。

**验证门(本修法完成判据)**:`grep -nE "pending 先行|裁决位序" screens/plane_transition.md` 零命中;正向核对 §2 观察段含「miss = round_fail 交回外循环重判」、决策段含「顶部重入裁决」(现役位序描述在场)。

**取舍**:备选 = 改写为现役位序句(「门 fail 先行(观察 node)、重入裁决后置(决策动作 node 顶部)」)——放弃:两 node 结构下位序无独立陈述需求,新增独立位序句 = 冗余第二源;本发现的病灶正是「独立位序句随形态重构漂移」,删句治根,改句留同型复发面。

### 2.2 F-2 修法:模块头 0q 指称改现役 fail 预算符号锚

| # | 位置 | 现状 | 目标语义 |
|---|---|---|---|
| 1 | `cw_screen_plane_transition.py` 模块头 act node 描述括注 | 「success 交回(外循环 0q 的误分发计数只认 fail,完成路径必须 success)」 | 「success 交回(外环通用连续 fail 预算 `cw_loop.py::CwLoop.OP_FAIL_REDISPATCH_LIMIT` 只认 fail,完成路径必须 success)」——括注其余逐字不动 |

依据:现役机制 = `operations/cw_loop.py:754` `OP_FAIL_REDISPATCH_LIMIT: ClassVar[int] = 5` + 环级执行段(同文件 1081-1097 行:仅 `not ok` 计数、达限 `round_fail` 显式停交上层;ok 路径重置,1060-1065 行)——「只认 fail、完成路径必须 success」语义逐字成立;原 0q 位面过渡误分发守卫(=3)退役并入本网 = 同文件 750-752 行注;`flow/outer_loop.md` §2.3 已退役号表(0q 仅供读旧档对号,非现役指称)。

**取舍**:备选 = 保留「0q」外加「已退役」注——放弃:退役对号是 outer_loop §2.3 的职责,画面 op 注释指认现役机制必须用现役符号锚;「现役口径引用 + 考古加注」双层并存,读者仍会先按旧号找机制,病灶未除。

### 2.3 F-3 修法:三处变更史注释收敛为当前语义

| # | 位置 | 现状(病句) | 目标语义 |
|---|---|---|---|
| 1 | `cw_screen_plane_transition.py` 模块头(「……无防御上限)。」与「本屏 sim 腿 =」之间) | 「原单 node 形态「pending 先行、miss fail 后置」位序随两 node 拆分自然消解(门在观察 node 先行,裁决住决策 node 顶部)。」 | 「门在观察 node 先行,裁决住决策动作 node 顶部。」(清变更史主句;括注当前语义独立成句,「决策动作 node」称谓与 §2/模块头既有用语对齐) |
| 2 | `cw_screen_plane_transition.py::_build_transition_chain` docstring「行归属位面」句 | 「开局判别 = 容器节点镜像与 hist 双缺(旧 kernel 过渡腿「三源全缺禁猜」同款语义,腿已随动作上报化切换批退役)= P1 入口;……」 | 删括注「(旧 kernel 过渡腿「三源全缺禁猜」同款语义,腿已随动作上报化切换批退役)」,余句逐字保留——「双缺禁猜」现役语义已由后句「镜像缺失返回 None(行归属不可知,禁猜;report 内同门兜底)」完整承载 |
| 3 | `kernel/cw_screen_report/plane_transition.py::CwScreenPlaneTransitionObs` docstring ``chain`` 句 | 「(行读数与 TokenCell 构造归观察侧,迁移批接线;None = 行空/读缺,report 不写,诚实缺位)」 | 删「,迁移批接线」→「(行读数与 TokenCell 构造归观察侧;None = 行空/读缺,report 不写,诚实缺位)」——接线现状由同文件模块头「report 摄入点 = …::observe(观察 node)」申报与测试锁承载 |

依据(共用):AGENTS.md §8(「变更史不进注释……注释只留当前值成立的理由 + 指针」;出处须持久索引或纯语义描述)+ T-3 稿 §2.6 准则 2/3(变更史清出、当前语义保留;「动作上报化切换批」「迁移批」均无日期无索引,不构成持久索引)。接线事实的持久指针 = kernel 屏文件模块头摄入点申报 + `sr-od-test/test/sr_od/application/currency_war/test_cw_transition_node_chain.py`。

**交叠注(准则 5)**:#1 与 §2.2 #1 同在模块头,同段一次成文,禁分两批触碰。

**验证门**:`grep -nE "0q|原单 node|拆分|退役|迁移批|切换批" cw_screen_plane_transition.py kernel/cw_screen_report/plane_transition.py` 零命中(「平移|同迁」不纳入模式——两措辞经审查逐行核对通过,见 1.3 不解决;模式与保留语义冲突时实现者停手回本稿提请修订,禁自行豁免)。

**取舍**:备选 = #3 的「迁移批接线」改为「已接线」——放弃:「已接线」仍是接线事件的历史时态,且与指针职责重叠(模块头申报 + 测试锁已承载接线现状);删词后句子纯现在时、零损失。备选 = #1 整句删除——放弃:句中括注「门在观察 node 先行,裁决住决策 node 顶部」是审查明示应保留的当前语义成分(报告 F-3 差距说明),收敛为独立当前语义句即合规最小改动。

### 2.4 F-4 修法:§1 裁定日期与裁定叙事收敛为现状 + 指针

| # | 位置 | 现状 | 目标语义 |
|---|---|---|---|
| 1 | `screens/plane_transition.md` §1 第二条 | 「原 0q 误读兜底(提示 rect 派发)已退役(2026-09-16 裁定:未建档实证的故障形态不作兜底理由,见 [../flow/outer_loop.md](../flow/outer_loop.md) §2.3 退役记录)——节点锚 miss 的帧走未知兜底停机留证,证据入库后再议锚加固(节点盘模板化)。」 | 「原 0q 误读兜底(提示 rect 派发)已退役(退役记录 = [../flow/outer_loop.md](../flow/outer_loop.md) §2.3)——节点锚 miss 的帧走未知兜底停机留证,证据入库后再议锚加固(节点盘模板化)。」——日期与裁定叙事清出,主干与指针保留 |

依据:screens/README.md §2 纪律行(「as-built 无状态(事故史/裁定日期不进正文)」——本发现命中条款原文);指针持久性实证 = `flow/outer_loop.md` §2.3 已退役号表「0q(位面过渡误读兜底,同批退役,见上退役记录)」在场,裁定语义(未建档实证的故障形态不作兜底理由)由退役记录单一承载。

**验证门(本修法完成判据)**:`grep -nE "2026-09-16|裁定" screens/plane_transition.md` 零命中(现文两模式各仅命中 §1 第二条括注一处,§2.4 目标文本「退役记录 = …」修后两模式归零,门可达);正向核对 §1 第二条仍含 `[../flow/outer_loop.md](../flow/outer_loop.md) §2.3` 相对链接(指针保留)。

**取舍**:备选 = 裁定语义保留为现状句——放弃:该句是对已裁决定的归档复述,规范性内容由退役记录单一承载,屏文档复述即第二源漂移面(与 F-1 同根);指针为持久索引,读者一键可达,删除零语义损失。

### 2.5 落地文件面总表与统一验收

| 文件 | 修法点位 | 性质 |
|---|---|---|
| `screens/plane_transition.md` | §2 末句删除(F-1);§1 第二条括注收敛(F-4) | 正本 as-built 同步 |
| `operations/cw_screen/cw_screen_plane_transition.py` | 模块头 0q→现役符号锚(F-2)+ 旧形态句收敛(F-3 #1,同段一次成文);`_build_transition_chain` docstring 删括注(F-3 #2) | 注释 |
| `kernel/cw_screen_report/plane_transition.py` | `CwScreenPlaneTransitionObs` docstring 删「迁移批接线」(F-3 #3) | 注释 |

统一验收:§2.1/§2.3/§2.4 三验证门零命中;F-2 正向核对 = 修后 `cw_screen_plane_transition.py` 模块头含 `OP_FAIL_REDISPATCH_LIMIT` 符号锚(§2.3 门对 F-2 由纯负向补为正负双向);两 py 文件 `uv run ruff check` 通过;`git diff` 确认零逻辑改动(注释/文档单面);修后 `screens/plane_transition.md` §2 与代码两 node 行为、§1 与 `flow/outer_loop.md` §2.3 口径互查一致。
