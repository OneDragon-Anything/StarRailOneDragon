# W482 合并预注册 A/B(判据锁定,批后不得改)

> 状态:预注册文书(代码+单测已交付,跑批待编排者放行)。
> 对象:off(全部现状语义,git 基线)= vs on(A 经济循环总模型 + B 配对完成度信号,本批工作区)。
> 同池(pool='snapshot')+同 seed(seed_base 配对)同进程口径分离(两臂独立进程,各 import 各自 src)。

## 0. 开工前置(放行后第一步,W481 B-4 加固)

1. **基线复算**:从 W477 新池基线(off 臂现状即基线源)复算 ①r6 成型率基线(口径=「r6 结束时 e≥2 或 form_ok 兜底门真」局占比)②死时带金方差——据此报告**实际功效覆盖**(预注册 n=451/臂按基线 30-35%+Δ10pp 估;若基线偏离 30-35% 区间,重算 n 并在报告中如实披露,判据门本身不改)。
2. 池指纹双臂一致校验(manifest 记 `pool_fingerprint`,不一致=不可比,整批作废重跑)。

## 1. 判据(预注册锁定)

| 层 | 判据 | 定义 | 门 |
|---|---|---|---|
| **主 A-1** | 死时带金 | 死亡/出局帧 gold 均值与 >50 占比,配对差 | 显著下降(CI 不跨 0) |
| **主 A-2** | 平均闲置金 | 逐帧 (g−R*)+ 时间积分均值,配对差 | 显著下降 |
| **主·共同** | 单位金战力转化率 | 出口板深/power 口径增量 ÷ 累计金收入 | 不劣(非劣界 ±5%;W481 A-1 升格) |
| **主 B-1** | 成型率(r6 口径) | r6 末 e≥2 ∨ form_ok 兜底真 的局占比,配对差 | 显著 ↑ +10pp(McNemar+bootstrap) |
| **主 B-2** | fire→兑现率(S1/S2,q=1 检验) | 买后同轮/次轮 e 跨档的 fire 占比,按形态分列(B-3:S3 不进门只披露) | S1/S2 ≥50%,低于=精度主张证伪 |
| 守卫 1 | 息基零漂移(I-1 锚) | g≤R* 常态帧行为逐位一致(off/on 决策序列差=0) | 破=守卫破 |
| 守卫 2 | 出口 hp 非劣 | P1/P2 出口 hp 配对差 | 非劣 |
| 披露 | fire 率(B-2)/P2 容量分层 | fire 帧占比、义务帧兑现率分位面(P1/P2)、义务帧花销去向分项(跨档买/期权买/升级/刷,W481 A-1③) | 无门 |
| 活性 | 漂移广度 | ON-OFF 决策序列差异 | 漂移=0 = 机制惰性,判设计失败不进统计判定 |

- 统计口径:配对 bootstrap ≥20,000 次 + McNemar(二值主判据);分位面分层(P1/P2、fire 率分桶);逐局台账落盘可复算(第三方独立重算,W474 手法)。
- **判定**:全主显著 + 守卫过 → 两机制转正;任一主显著负 或 守卫破 → 对应机制退回;混合 → 分层归因交编排者。
- n=451/臂(W474 功效教训:37.7% 功效不可再犯);如实声明:真实效应 <7pp 时本 n 判不出,按「效应真实但小于可检域」报告,不得用不显著冒充证伪(W474 措辞纪律)。

## 2. 执行(编排者放行后)

```
uv run python .debug/temp/currency_war/w482_merged_ab/run_ab.py --n 451
```

- 脚本自动:①git worktree 建基线臂(本批 base commit)②双臂各跑 `cw_sim batch --n 451 --pool snapshot --seed-base <同一>`(worktree 臂 PYTHONPATH 指 worktree src)③双臂池指纹一致校验④台账落 `w482_merged_ab/{off,on}/`。
- smoke:先 `--n 20` 验证双臂可跑+指纹一致+判据字段可得,再放全量。
