---
screen_name: 大世界
screen_id: normal_world
appears_in: [探索, 导航中枢]
last_updated: 2026-07-28
source_image: screens/大世界/普通-朝露公馆.webp(实拍归档;主 fixture 普通.webp 保持不动,被 enter_game / CW 进入流程测试引用)
pc_alt: true
---

# 大世界(normal_world)

游戏探索主画面。角色在地图上自由移动、与 NPC/物体交互、追踪任务、从这出发进各玩法(战斗 / 菜单 / 传送 / 玩法入口)。**锁光标画面**(`pc_alt=true`,`click_game` 点 UI 需传 `pc_alt=True` 先按 Alt 解锁光标)。

## 何时出现 + 状态流转

- **入口**(从哪来):登录进游戏 / 战斗结束返回 / 菜单(背包/手机/地图)关闭 / 传送落地 → 落到大世界。
- **出口**(动作 → 下一态):
  - 走到 NPC/物体交互范围 → 出现 `> 名字 <` 提示(`移动交互-单行/多行`)→ 按 F 交互 → 进对话/玩法。
  - 遇敌 / 秘技攻击 → 战斗画面 `battle`。
  - 开地图(M)→ 大地图 `large_map`;开手机 → `phone_menu`。
  - `列车补给`弹窗(每日)→ 领取 → 关闭回大世界。
  - 传送(指南/地图)→ 加载 → 落目标地图大世界。

## 识别特征(稳定锚点)

- `pc_alt=true`:大世界锁光标(强判据)。
- **右上角色图标**(template `ui_icon_01`,pc_rect 1800,0-1900,90):稳定模板锚点(analyze 命中 conf 0.89)。
- **右侧队伍栏**:4 角色头像+名称竖排(1770-1873 / 275-655)+ 秘技点数 + `状态效果`(TECH_STATUS text)。
- **左下 UID**(26,1040-160,1063):经 `fill_uid_black` 灰色遮挡(打码),截图看不到数字。
- 易变(勿当特征):角色名/等级、任务文字、地图区名(如"鸽川区")、网络延迟("I 49ms")。

## 可交互元素

交互行为均经现场 click 实锤(2026-08-29,当前帧=朝露公馆·货币战争入口旁;点后已返回大世界)。

| 元素 | 位置(1080p) | 交互方式 | 点后行为 |
|---|---|---|---|
| 手机菜单图标 | 左上 ≈(41,70),红点=新内容提示 | click | 打开手机菜单(`菜单`) |
| 灯泡提示图标 | 左上 ≈(237,80) | click | 打开教学目录(图鉴/教学页,模板库 canonical 采集源) |
| 对话气泡图标 | 左上 ≈(244,238),红点=未读短信 | click | 打开短信「新消息通知」面板 |
| Enter 键位提示 | 左下 ≈(115,975) | **键盘 Enter**(click 无效) | 打开聊天对话浮层 |
| 活动礼记图标 | 右上 ≈(1340),红点=可领活动 | click | 旅情事记·活动礼记(巡星之礼签到等活动列表) |
| 无名勋礼图标 | 右上 ≈(1428),红点=可领奖励 | click | 无名勋礼·奖励页(已有画面档) |
| 跃迁图标 | 右上 ≈(1512) | click | 跃迁(抽卡)页;**含消耗类按钮,bot 勿点** |
| 指南图标 | 右上 ≈(1595),红点=每日实训可领 | click | 星际和平指南·每日实训(已有画面档) |
| 背包图标 | 右上 ≈(1678) | click | 背包(已有画面档) |
| 编队图标 | 右上 ≈(1760) | click | 队伍编队页(已有画面档) |
| 角色头像图标 | 右上 ≈(1840) | click | 角色详情 |
| 移动交互提示 | `移动交互-单行` / `移动交互-多行` | OCR+F | NPC/物体交互提示(含 `>名字<`)→ F 交互(如 F 货币战争=进 CW,不可逆) |
| 追踪任务图标 | `追踪任务图标` | — | 左侧任务追踪 |
| 列车补给弹窗 | `列车补给1/2` + `点击领取今日补贴` + `点击空白处关闭` | OCR(text) | 每日补贴弹窗 |
| 秘技 | `秘技-快捷键` + `秘技-点数-1/2` + `TECH_STATUS`(状态效果) | — | 秘技释放 + 点数/状态显示 |
| 队伍 | `队伍-角色头像-1..4` + `队伍-角色名称-1..4` | — | 右侧队伍栏(只读) |

**红点/角标约定**:手机菜单、活动礼记、无名勋礼、指南图标的红点(`!`)=有可领/新内容;活动类图标另有「新」角标。红点是 app 判断「有无事可做」的潜在输入,尚未建模 area。

## 识别快照(analyze_screen 实测,2026-07-28)

- 匹配画面:`大世界-普通` `is_precise=true`(注:当前帧命中 `大世界-普通`,与 `normal_world_basic` 关系见备注)。
- 命中 area:右上图标-角色详情(template conf 0.89)、右上图标-旅情事记(0.64)、右上图标-背包(0.85)。
- OCR(节选):鸽川区(地图区)、请前往匹诺康尼-飞翔时针号(任务指引)、谁动了我的经费/骇入「信息安全科」(任务)、银狼LV.999/爻光/Momojie/丹恒·腾荒(队伍角色名)、查看圣杯/召唤/Enter(UI)、状态效果。

## 备注 / 待查

- **视觉判读已补**(2026-08-29,`read_image` 原生视觉):本模型可直接判读截图,原「VLM 受限」备注作废。
- **子态(大世界多场景识别,同一画面不同状态用不同 screen_id)**:
  - `normal_world`(大世界):完整探索态,含队伍栏(4 角色头像+名称)、秘技点数、列车补给弹窗、移动交互提示等(无 id_mark,模糊 top_n 匹配)。
  - `normal_world_basic`(大世界-普通):核心导航识别态。id_mark=右上 角色详情 + 右上图标-背包(均为 template)。注:原「右上图标-旅情事记」(1300-1470)实测点开**活动礼记**页,已改名「按钮-活动礼记」并收窄 rect 消除与无名勋礼位的重叠;⚠️ 该图标位内容随活动轮换(同 slot 先后出现过不同活动图标,模板命中 conf 0.50~0.71 波动甚至不中),**不可作 id_mark**(id_mark=false);2026-08-29 补建 按钮-无名勋礼/跃迁/指南/教学目录/短信通知(均为纯定位 no_method area,交互行为见可交互元素表)。
  - `normal_world_battle_fail`(大世界-战斗失败):战斗失败态,标题"战斗失败" + 点击空白区域继续(`pc_alt=false`)。
- **fixture 归档**:`screens/大世界/普通.webp`(enter_game / CW 进入流程测试引用,勿覆盖)、`普通-朝露公馆.webp`(2026-08-29 建档帧,红点+F 货币战争交互可见)、`朝露公馆入口.webp`。id_mark 准确性测试过(2026-08-29,真阳性+无碰撞)。
- **待建档落盘**:红点状态标记按用户裁决暂不建模(有 app 需求再建)。
- **`> 名字 <` 交互判据**:NPC/物体进交互范围时名字左右出三角标记,OCR 常漏 `><` 符号 → 判交互范围以"出现名字 + app 假设可交互 → 试 F"为准(见 screen-onboarding skill)。
