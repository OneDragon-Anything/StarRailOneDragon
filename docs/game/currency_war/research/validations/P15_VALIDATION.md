# P15 逐件证明验证:条件败局伤害回归(11.32 − 0.37·rung)

> 验证对象:`docs/game/currency_war/research/proofs/p15-p1-vd-benefit-calibration.md`(只读)
> 验证人:数学验证子代理(五门协议);日期:2026-09-01
> 公理栈:零调参(本件=回归拟合件,须以显式模型形态成立:带 CI+决策区间稳健)/纯金流记账(输出只许作 λ_death 的机制叙述与概率估计输入,禁作价值换算)/参数溯源门

## 阅读门(第 0 门)覆盖声明

已读:research 全部关键篇(README/math_proofs/combat/economy/user_playstyle 重点条目 [27]/[39]/[40] 及战斗掉血节/h3_tier_core_crosstab/transitions/transition_combos/stage_transitions/board_structure + proofs/p15、p12 交叉引读)+ data 全部未建模篇(gameplay/bosses(节选)/data README;competitors/plaza_meta/advantage_layouts 经 README 索引面核对)+ final_comps(README 全文,单类篇经索引面)+ 06_input_model §3 注册表地图。characters/ 72 档经全文 grep 扫描(掉血/扣血/败局/数值锚零命中——角色档不含 HP 侧常量,与本件无关,如实声明为扫描级而非逐字精读)。注册表直调:`cw_battle_tables.py` 全文、`cw_registry.py` L330-380、`cw_coarse_battle.py` 全文、`scoring.py`/`ev.py`/`discipline.py`/`allocator.py` 消费点。

## 门① 内部推导逐步核 —— **过(两命题推导成立),附一处量化口径缺陷**

**①确定性命题(剩余战斗数)**:对 economy §10.2 P1 众数模板(reward/reward/battle/battle/supply/battle/encounter/reward/boss)逐轮复算 `battles_left = #{i≥r: token∉{奖励,补给}}`:r3=5/r4=4/r5=3/r6=3/r7=2/r8=1/r9=1,**与证明文逐位一致**✓。计数含 encounter/boss(有伤害要求的节点,[27] 掉血辖,代码 `NON_BATTLE_NODE_TOKENS` 注释同口径)✓;未知 token 按战斗计=保守缺省,推理自洽;推导消费开局帧实读表非先验模板,环境变异论证成立。

**②口径命题(条件败局伤害)**:恒等式推导逐步复核:`|L_u| = (1−p)·L_c − 2p < L_c`(p>0)代数正确✓;前提「胜恒 +2」与 combat.md §3(胜利恒+2,跨节点类型实测)及代码 `WIN_CAP=2` 双源一致✓;「每个被转化的战斗已经是败局」的因子分解语义正确。**方向命题成立**。

**量化复核(③效果表)**:0.362×10.58×0.5×{5,3,1} = 9.58/5.75/1.92,旧式 0.362×10×0.5×5=9.05,Δ=+0.53/−3.31/−7.14 **逐位复算全对**✓(Δwin=0.778−0.416=0.362 与 P3 阶梯/P12 引用一致)。

**⚠ 量化口径缺陷(证明内部)**:收益式把 **battle 档条件伤害(11.32−0.37r)乘到含 encounter/boss 的战斗计数上**——按本证明族自己的分档(registry `streak_floor_loss_damage`:encounter=24.32−4.53r、boss=26.71),同帧三类型损失差 1.5-2.5 倍,r5 帧(剩 battle/encounter/boss 各 1)的正确战斗项应 ≈ 0.362×0.5×(10.58+15.3+26.71) ≈ 9.5 而非 5.75。**系统性低估收益项**;与「末窗收紧」同向叠加,不翻转证明方向,但幅度失真,应按节点类型分段求和修正。

## 门② 数值锚点注册表直调 —— **不过(锚点可对,但缺 CI + 源语料已删)**

- `cw_registry.vd_p1_loss_intercept=11.32 / vd_p1_loss_slope_rung=-0.37`(单一源)✓;`cw_coarse_battle._LOSS_FIT['battle'][1]=(11.32, -0.37, 11.07)` 同值✓;胜率表 battle 各 rung n 合计 106+116+47+9=**278** = 证明文「n=278」✓。
- 直方池均值复算 = **11.0686 ≈ 代码 pooled 11.07** ✓(内部一致)。
- 消费面数值锚:`sim/checks/segments._P1_LEVELUP_STOP_HP=11 = ceil(1×(11.32−0.37×2))` 复算正确✓。
- **对账 `cw_battle_tables` 掉血常量(任务指定项)**:11.32/−0.37 **不在** `cw_battle_tables`(其 P1 掉血常量是回退层另一套模型:`LOSS_BASE=7.0 / LOSS_PER_ROUND=4.0 / BOSS_BY_DIR_ROUND`,按**轮次**递增;r7≈−23)与粗模型(按**成型档**,battle 败局均值≈10.6-11.3)**双源并存且量级不同**——语义不同(回退层含位置递增 B,粗模型是 B+P 混合均值),非矛盾,但注册表层存在两套掉血常数无互指声明,消费混用风险应登记。
- **框架预判证实:上游无 CI**。证明文与 registry 只给斜率 SE=1.25(注释文本,非机器可读);**截距 SE/CI 全缺**;拟合语料目录 `.debug/temp/currency_war/w324_coarse_battle/`(含 fit_results.json/corpus/拟合脚本)已被临时文件清理**删除**(.debug 不入 git)——当前拟合**不可独立复现**,这是拟合件按公理「显式模型形态成立」的硬伤。

## 门③ 建模对象 vs 玩法文档原文 —— **过(机制方向一致),附混杂警告**

- combat.md §3:**HP 只在战斗失败时扣**✓(条件化于败局正确);「节点位置基础扣 B 随节点越后递增 + 未完成进度伤害 P」——user_playstyle [27](最高权威):**扣血 = B(位面,节点序,难度) + P ∝ 敌方剩余血量**。
- 斜率为负与机制自洽:成型越高→输出越高→敌剩余越少→P 越小✓。
- **混杂警告(机制层)**:rung(成型档)与轮次正相关,而 B 随节点位置**递增**——斜率 −0.37 实际是「B↑(正)×P↓(负)」两股对消后的净值,|t|=0.37/1.25≈0.30,**符号本来就不该显著**;这不证伪命题(条件期望口径正确),但说明「按 rung 的线性均值」不是机制分解,[27] 指定的真值采集点=结算屏 OCR 三项拆解(挑战进度/基础伤害/未完成进度伤害)——B/P 分解直采才是治本标定。
- **消费边界(公理栈)**:[39] 收口明文「[27] 掉血公式服务判读归因,不进决策」;本任务公理栈进一步裁定本件输出**只许作 λ_death 的机制叙述与概率估计输入,禁作价值换算**。而现行落地(scoring.vd_refresh_score/engine_jump_gold/discipline/allocator)以 `hp_to_gold(0.5) × L_c` 做**血→金价值换算**——P12 时代以「计价非触发」自辩,在纯金流记账公理(P51 裁定后)下属**违规消费形态**。命题本身无罪,落地消费面需整改(P51 正式版 W_floor 不含此项,方向已定)。

## 门④ 实证 vs 三轮账本 —— **不过(循环论证 + 账本退化,不构成独立实证)**

复算脚本 `.debug/temp/currency_war/redesign/p15_recheck.py`(聚类稳健 OLS,按局聚类)。三批账本 = `AB_{cw3,legacy_v2}_n40_{s0,r2,r3}`(manifest 声明 coarse_calib_version=2):

| 臂/批 | battle(P1) | 败局 n(率) | 条件均值 | vs round 斜率(95%CI) |
|---|---|---|---|---|
| cw3/s0 | 116 | 94 (81%) | 10.91 | +0.19 [−0.52, +0.90] |
| cw3/r2 | 116 | 92 (79%) | 11.62 | +0.56 [−1.15, +2.28] |
| cw3/r3 | 116 | 85 (73%) | 11.93 | +0.60 [−0.95, +2.16] |
| legacy_v2(s0=r2=r3) | 116 | 78 (67%) | 11.87 | −0.20 [−0.80, +0.41] |

**三条致命判定**:

1. **循环**:账本由 `cw_coarse_battle` 生成,其败态采样就是 `_LOSS_FIT battle (11.32, −0.37, 11.07)` + 直方均值匹配——**在账本上复估系数是同义反复**,只能验实现一致性(条件均值 10.91-11.93 落在模型带 [10.58, 11.32]±噪声内、残差 SD 4.7-8.8 < 10.07 因 hp≥1 地板删失压缩——一致性过,实证意义零)。原 W324 遥测语料已删(门②),「掉血实测分布对照」当前**无独立数据可做**。
2. **账本退化**:legacy_v2 三批 outcomes 的战斗 delta 序列**逐字节相同**(同池指纹+同种子+legacy 臂代码未变)——名义 6 批实际独立样本只有 **3×cw3 + 1×legacy**;任务书「r1-r3」中的 r1 不存在(在档=s0/r2/r3)。任何跨批合并统计须按独立局聚类,否则 legacy 被三重计权。
3. 斜率方向在 round 代理下不显著(全 CI 含 0),与门③混杂警告一致——rung 梯度未被任何现有独立数据确立。

## 门⑤ 参数溯源(估计参数的 CI 与下游稳健性)—— **不过(CI 缺失 + 关键消费在 CI 内翻转)**

- **斜率** SE=1.25 → 95%CI ≈ **[−2.82, +2.08],含 0 且含大幅正值**——「成型越高败局伤害越低」的梯度**未确立**(与门③ B/P 对消机制自洽,如实降级为「水平项可用、梯度项不可辨」)。
- **截距** CI 不可得(SE 未披露、语料已删);粗估 n=278、残差 std 10.07 → SE(截距)≈0.6,loss(r) 水平 CI 约 ±1.2-2.5 hp。
- **下游消费逐个判**:
  - `vd_refresh_score`/`engine_jump_gold` 授权带(③ 的 ±0.5/−3~−7 金):loss CI ±2 hp × hp_to_gold 0.5 × 5 场 ≈ **±3-5 金/帧**——**r2 帧的 +0.53 放宽量在 CI 内翻转**(收紧/放宽方向不稳健);r5/r8 的收紧量级 3-7 金与 CI 同量级,**幅度不稳健但收紧方向由 battles_left 阶梯(确定性)主导,方向稳健**。
  - `discipline` L_c 整数倍闸/`_P1_LEVELUP_STOP_HP=11=ceil(10.58)`:CI 带 [~9, ~12] 内 ceil 值翻转 ±1-2 hp,**不稳健**(闸类消费需按 CI 端点重推或扩样)。
  - **P51 λ_death 合成(任务指定项)**:P51 需要 P15 的①**槽序剩余战斗数**(确定性推导,精度需求=精确计数——满足✓)与②桶×节点类型胜率表(语料 n=106/116/47/9,battle rung3 n=9、boss rung3 n=3 **薄桶**,λ_death=∏(1−p_win) 对 p_win 误差按剩余场数放大,薄桶 p 的 CI 是 P51 的真瓶颈)——**P51 不需要伤害幅度的精确值**(血只在机制叙述层),故 11.32/−0.37 的 CI 缺失不阻塞 P51,阻塞的是胜率薄桶;伤害侧仅需维持「条件败局均值 9-13 hp」量级叙述。

## 五门判定汇总与最终判决

| 门 | 判定 | 一句话依据 |
|---|---|---|
| ① 内部推导 | **过**(附缺陷) | 两命题代数/计数逐位复核全对;③ 数值全对 |
| ② 数值锚点 | **不过** | 锚点可对但截距 CI 全缺、斜率 CI 仅存注释、源语料已删不可复现;cw_battle_tables 另一套掉血常数双源无互指 |
| ③ 建模对象 | **过**(附边界) | 与 combat §3/[27] 机制方向一致;但现行 hp_to_gold×L_c 消费违金流公理,须改道 λ_death |
| ④ 实证 | **不过** | 账本=同系数粗模型生成(循环)+ legacy 三批重复 + 独立实测语料已删 |
| ⑤ 参数溯源 | **不过** | 斜率 CI 含 0;授权带放宽量与整数倍闸在 CI 内翻转;battles_left 阶梯与 P51 消费方向稳健 |

**最终判决:需修后站得住。** 口径命题(条件败局伤害 ≠ 无条件均值,双计论证)与确定性命题(槽序计数)**数学上成立且长期有效**;但作为回归拟合件,它当前**不满足公理要求的「显式模型形态」**:无截距 CI、斜率梯度未确立、拟合源语料已删不可复现、实证面只剩循环数据。修复优先级=先保复现与披露(CI+语料入库),再谈数值授权。

## 修正建议(含扩样清单)

1. **【复现性,最高优先】语料与拟合入库**:W324 语料已删——从生产 replay(`replay/outcomes.jsonl` 语料池,现 n=191/99 局,尚薄)重建冻结语料 + 拟合脚本 + `fit_results.json`(含逐 rung cell n/均值/CI)入 `tools/cw/proofs/p15/`(git 载体,.debug 不可作拟合件存档位);registry 注释的 SE 升格为机器可读字段(先例:`streak_floor_loss_damage` 的版本披露惯例)。
2. **【标定治本】改用 [27] 指定的结算屏三项拆解直采 B 与 P**(基础伤害/未完成进度伤害分列),拟合 B(位面,节点序)与 P(敌剩余)两支而非 B+P 混合均值——同时根治门③混杂(斜率=B↑×P↓对消)与「线性于 rung」的代理假设。
3. **【口径修正】收益式按节点类型分段**:`Σ_类型 L_c(类型)×该类型剩余数`(encounter/boss 档已在 registry),替换「battle 档 × 全战斗计数」的低估——属证明文量化层修正,不触命题。
4. **【消费面整改,随 P51 落地】**`hp_to_gold × L_c` 价值换算通道按金流公理退役:伤害只进 λ_death 的概率侧(桶×节点胜率×槽序计数合成)与机制叙述;授权带类消费改经 W_floor。P15 单篇补「消费边界」节注明。
5. **【扩样清单】**:①battle rung2/r3 败局样本(现 n=47/9,battle rung3 不可辨)→ ≥30/格;②boss/encounter 高 rung 胜率薄桶(n=3/2)→ P51 λ_death 的真瓶颈,≥20/格;③P2 段语料(现 plane2 全靠别名,已知偏差 boss 均损 +4.57/钳制率 −38.8pp);④结算屏三项拆解字段进遥测(为建议 2 供数);⑤台账生成器修 legacy 同臂三批重复问题(或合并统计时披露去重)。
6. **【注册表卫生】**`cw_battle_tables`(回退层,按轮次)与 `cw_coarse_battle._LOSS_FIT`(两态,按成型档)两套掉血常数加互指注释,防消费混用。
