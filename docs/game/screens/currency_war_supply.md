---
screen_name: 货币战争-补给
appears_in: [currency_war]
last_updated: 2026-08-13
source_image: screens/货币战争-补给/未选择.webp
---

# 货币战争-补给阶段(N 选 1 角色+装备)

补给节点的选择屏:从 N 张(实测 5)纵向卡里选 1 张(每张 = 角色 + 其自带装备)。选完「确认」回备战。

## 何时出现 + 状态流转

- **入口**:补给节点的备战屏右上「返回补给阶段」按钮(备战的非遮挡子态按钮,商店/购买经验/出战仍可用)→ 点它进本屏。
  - 补给节点是否自动进本屏、还是须手动点「返回补给阶段」:**待实机确认**(本轮观察 = bot 停在补给节点备战,补给未选,经按钮进本屏)。
- **出口**:① 点卡身选中(已选择 +1)→「确认」→ 回备战(消耗 1 次补给);② 「返回备战界面」(右上)放弃、回备战(不消耗)。

## 识别特征(稳定锚点)

- **id_mark**:「补给阶段」标题(屏上部居中,screen_info `标识-补给阶段` pc_rect [893,120,1027,230];2026-08-13 设 id_mark=true → `is_precise=true`)。
- 独有文字:「已选择 X/Y」+「剩余次数:N」+ N 张卡(角色名 + 装备名 + 阵营标识)+「确认」+「返回备战界面」。

## 可交互元素

- **N 张补给卡**(纵向,横排,实测 5):每张 = 角色立绘 + 阵营标识 + 角色名 + 装备图标 + 装备名;个别卡带「试用」角标。**点卡身选中**(已选择 +1,「确认」由灰转实)。卡内容每局随机(角色/装备/阵营随牌池)。部分卡附近见数字(如「9」「8」,含义待核:费用?星级?)。
- **「已选择 X/Y」**:(1642,912)右下,选中计数。
- **「剩余次数：N」**:(1347,969)右下,本节点剩余可选次数。
- **「确认」**:(1681,966)右下;未选(0/1)时灰不可点,选满转实,点 = 确认选择回备战(消耗次数,不可逆出口)。
- **「返回备战界面」**:(1717,44)右上,放弃选择回备战(不消耗)。
- **「角色详情」**:(102,970)左下 + Q 键;开角色详情面板(子态待建)。

## automation 要读信息

- **N 张卡的(角色, 装备)**:`read_supply_options` OCR 装备名(定义列)→ 角色按最近 x 配对(`get_char` roster 校验滤噪)→ `decide_supply` 按 `target_comp.key_equips` 契合 + 装备通用价值选最优列;钻(红/蓝=基本赢)视觉 `has_diamond` 待补(现恒 False)。
- **已选择 / 剩余次数**:判可选/选满(确认是否可点)。当前未读(待接线)。
- supply **无刷新按钮**(`decide_supply` 传 `refresh_used=True` 跳过刷新找钻)。

## 识别快照

### 1. 未选择(0/1)
- 匹配画面:`货币战争-补给` `is_precise=true`(id_mark `标识-补给阶段` 命中)。
- 命中 area:`标识-补给阶段`(text 补给阶段 conf 0.996)/ `按钮-确认` / `按钮-返回备战界面`。
- 关键 OCR:「补给阶段」(909,137)、5 角色(飞霄/千冶·刃/缇宝/艾丝妲/白厄,x=130/481/835/1188/1543,y~548)+ 5 装备(折叠小刀/生命之花/光能电池/以太钻头/幸运星,x~195/550/904/1260/1613,y~680)+「已选择0/1」(1642,912)+「剩余次数：1」(1347,969)+「确认」(1682,966)+「返回备战界面」(1717,44)。
- source_image: `screens/货币战争-补给/未选择.webp`

### 2. 已选择(1/1,选中飞霄)
- 同屏;「已选择0/1」→「已选择1/1」,「确认」由灰转实(可点)。选中视觉标记 OCR 不可见(边框/高亮待视觉核)。
- source_image: `screens/货币战争-补给/已选择.webp`

## 备注

- screen_info:`标识-补给阶段`(id_mark=true)+ `按钮-确认` + `按钮-返回备战界面`(2026-08-13 建档)。卡身点击点由 `read_supply_options` OCR 动态返回(非固定 area);`RunSupplyNode.CARD_BODY=(900,550)` 硬编码兜底(坐标待进 screen_info)。
- **备战屏「返回补给阶段」按钮**(补给节点备战子态,非遮挡)待加入 `货币战争-备战` screen_info —— 标识"这是补给节点备战",也是 `BuyShopCards` 假阳 bail(把按钮文本「补给阶段」误判 overlay)的根因,待修。
- **决策点**:`decide_supply` 代码已接(`RunSupplyNode` 调),但本屏建档前 id_mark 缺 → 检测不可靠(`is_precise=false` 撞挑战副本/模拟宇宙)→ 决策实际未稳定触发;建档后待实机验证决策真生效。
